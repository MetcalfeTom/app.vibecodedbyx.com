# Two Keepers — a cooperative lighthouse night

## log
- 2026-08-19: v1.0 — built per chat ("cooperative" + "local-only" = couch co-op by design, stated in the sub). 7×7 sea, harbor bottom-center, ships spawn top and drift home. ASYMMETRIC FOG (the co-op engine): Keeper A sees storms only in the west (x≤3), B only east (x≥3), watches alternate — you literally need each other's eyes. SHARED LANTERN: one oil pool (beam a column −10/watch, rest +8), lit column makes squalls safe. SAFE-ROUTE TRADING: per-keeper buoy hands (3 each), one placement per watch ONLY on your visible half, pass-a-buoy buttons ("trust, in rope form") so resources flow to where knowledge is. Squalls shift ±1 each watch; unlit unbuoyed squall = ship founders; harbor = "somewhere below, a kettle goes on". Win 5 saved (split a very late sandwich); 3 lost = KIND loss ("the keepers keep the lantern lit anyway — spring sailors will need it"). Persistence mid-game local-only. A11y: every sea cell a labeled button (contents narrated incl. fog), beam row aria-pressed, turnbar + status aria-live, all buttons ≥2.5rem. Zero external assets. Probe 20/20 first run — fog refusals, trade flows, oil arithmetic, all four safety cases, both endings.

## issues
- none.

## todos
- 3rd keeper mode (north watch), storm forecast trading (info as a resource), if the harbor asks.
