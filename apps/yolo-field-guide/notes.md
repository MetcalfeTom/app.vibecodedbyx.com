# The Dataset Field Guide — YOLO from photos to Raspberry Pi

## log
- 2026-08-20: v1.0 — SIX chat requests landed during the build and all shipped together. Seven chapters: (1) COLLECT (deployment-realistic collection, Ultralytics rules of thumb ≥1500 img / ≥10k inst per class, 10% background images, interactive 8-condition coverage check), (2) CONSENT (ask-first standard, incidental-people blurring, children = just don't, EXIF/GPS stripping, license honesty, "not legal advice" disclaimer, 5-scenario consent quiz with kind corrections), (3) LABEL (tight boxes, label-every-instance, occlusion conventions, YOLO txt format, THE IOU SANDBOX — draw/keyboard a box on a procedural scene, judged by real IoU exactly like eval; hand-verified iou() incl. the classic (0,0,10,10)/(5,5,10,10)=25/175 case), (4) BALANCE (max:min ratios, augmentation-isn't-information, split hygiene, 4-slider auditor with per-subject class names), (5) TRAIN & TRACK (backbone→neck→head SVG diagram, n/s/m/l/x size table, args table epochs/imgsz/batch/patience/lr0/seed, W&B/TensorBoard/MLflow by name, overfit signature warning), (6) EVALUATE (P/R/mAP50/mAP50-95, threshold explorer with live PR curve from a 24-detection toy model — evalAt() probe-verified against hand counts — plus 3 diagnose-the-run exercises), (7) DEPLOY with RASPBERRY PI 4 AS THE PRIMARY ROAD per chat: 7-step beginner walkthrough (train elsewhere → val → export NCNN@320 → 64-bit OS → saved-images-first → camera → shrink ladder), deployment-flow SVG, export formats one-liner list, quantization ladder SVG (FP32/FP16/INT8 + calibration warning), THREE HONEST HARDWARE CARDS (Pi 4 4GB primary "the main road"; Grove Vision AI V2 with Ethos-U55 "the specialist"; ESP32-S3 "the ascetic" — full YOLO does not fit, says so plainly), and a SAFE OFFLINE DEMO: simulated inference (no camera, no network, clearly labeled simulation) that draws jittered predicted boxes + ballpark latency per device, and shows a model-too-large panel for ESP32. THEME per chat: bright blue-purple gradient (135deg #3d6bff→#8a3dff), Arial (explicitly requested — overrides the house no-Arial default), white cards, 4 rich aria-labeled SVG diagrams. SELECTABLE SUBJECT: ducks/cats/mugs/rovers re-theme the sandbox creatures, balance class names, and demo labels. Latency figures labeled ballparks throughout. Probe 16/16 (two probe-side arithmetic fixes: my IoU hand case used w:15 for a 5-15 box, and 0.88<0.9 in the kept-count).

## issues
- probe hand-arithmetic wrong twice (box w vs extent; threshold boundary) — the app was right both times. Verify the VERIFIER against paper before blaming code.

## todos
- a "label 5 in a row" streak mode for the sandbox, per-class AP toy table, Coral USB accelerator card, if chat asks.
