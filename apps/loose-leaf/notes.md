# Loose Leaf — paper planner

## log
- 2026-08-18: v1. Single-file paper planner: cream ruled sheet on a desk (red margin, punched holes, washi tape, -0.35° tilt), Caveat + Kalam handwriting. Tasks nest to 3 levels (task → step → substep, depth-capped), homework/work divider tabs with ink-color accents, per-task focus timers (15/25/45 min) that pay out red tally marks (groups of five with the diagonal slash) on the task line. Completion logic: checking a parent cascades down; completing every sub auto-completes ancestors; unchecking a leaf un-completes them. Inline text editing (input swap, Enter/blur saves), delete, empty-state note, footer stats (crossed off / focus sessions), triangle chime on session end with mute toggle. localStorage 'loose-leaf-v1'. Seam window.__LL. WCAG basics: 100% root font, semantic tabs/tablist/dialog, labelled checkboxes, focus-visible, 2.75rem targets, prefers-reduced-motion.
- Suite ll-probe: 24/24 (boot, nesting + depth cap, fraction, cascade both directions, tab filtering + aria-selected, timer open/length/countdown/complete/pause/quit, tally grouping 7=[5,2], edit/delete persistence, HTML-injection safety via input values, a11y spots).
- Design-review fix pre-ship: .lenrow buttons had no font/size styling (UA default, sub-44px) — styled to match.

## issues
- Task text renders in <input value> — inherently injection-safe, but means long text ellipsizes rather than wraps. Acceptable for one-line planner items.
- Emoji buttons (⏱ 🔔 ↳＋) show as tofu in headless probes only; real browsers render them.

## todos
- Reorder (drag or up/down buttons)
- Per-day archive ("yesterday's page" flip)
- Optional custom category tabs
- 2026-08-18 (2): calm chime per chat — replaced the bright C5/E5/G5 triangle arpeggio with a gentle bell: sine-only voices at A3/E4/A4 (each with a quiet octave partial at ~1/4 gain), 0.45s note spacing, 90ms soft attack, 2.6s exponential decay, peak gain 0.045 (was 0.12), all routed through a 1100Hz Q0.4 lowpass. Verified via stubbed AudioContext driven through the REAL session-finish path (7/7: sine-only, lowpass present, peak ≤0.05, spacing ≥0.4s, low register, mute creates zero nodes) + full 24/24 regression. Atomic deploy.
- 2026-08-18 (3): SMART PLANNER REWORK per chat — homework/work tabs removed entirely; one input, one flow: describe a task → `generateSteps()` breaks it into 3-6 efficient steps LOCALLY (zero network). Engine: (1) explicit lists split on commas/"and"/"then" into verbatim steps; (2) 9 keyword templates (essay/read/study/present/email/clean/code/math/plan) with the task's SUBJECT spliced in — leading verbs stripped so "write my essay on X" yields "gather notes & sources for my essay on X"; (3) generic 4-step fallback (define done → ten-minute piece → main push → review); (4) sub-14-char tasks stay atomic (no padded steps for "buy milk"). Preserved and re-verified: manual nesting to depth 3, cascade both directions, fractions, focus timers, tally marks, CALM chime (7/7 stub-graph suite), persistence, old tabbed saves migrate (all cats shown together), injection safety, a11y. New suite ll3-probe 20/20.
- **Bug caught by the suite**: writing the JS split regex through a Python triple-quoted patch turned \b into literal backspace chars (0x08) — comma-split worked, "and"/"then" never matched, and "wash dishes, vacuum and email landlord" fell through to the email template. Repaired with explicit escapes + a no-control-chars assertion. LESSON (repeat offender risk): any \b, \n, \t inside python-authored JS patches must be double-escaped — add an assert '\x08' not in s to every patch script that writes regexes.
- 2026-08-18 (4): NOTEBOOK EDITION — five chat requests landed as one coherent release:
  - **Per-goal pages**: page 0 = contents (goal rows w/ fraction, date, tallies, click to open); each goal gets its own page (S.page, persisted). addSmart flips straight to the new page. Deleting a goal returns to contents.
  - **Navigation**: prev/‹ ›/☰ contents buttons + "page n / N" label, ArrowLeft/Right flip (suppressed while typing or focusing), colored side page-tabs on the sheet edge (☰ + numbered, current tab wider + aria-current, capped at 9+current).
  - **Real-notebook dressing**: wire spiral binding (SVG data-URI coil, repeat-y), faint ruled lines + 4-layer paper fiber/stain texture, page-turn animation on every flip (perspective rotateY from the spine, .42s, killed by prefers-reduced-motion), pastel washi tab colors.
  - **Per-step timer estimates**: every generated step carries minutes (template-tuned; list-splits get 15m); rows show a blue handwritten ~chip; goal page shows "~1h 10m left" summing undone steps; the focus dialog grows a "~N min · step" auto button that's preselected for estimated steps (15/25/45 override intact; no-est tasks fall back to 25).
  - **Optional handwritten dates**: per-goal due date (native date input styled as red handwriting, ✕ to clear, "add a date?" when empty); contents shows "due sep 1" / red "was due aug 1"; isOverdue respects done goals.
  - **Editable goal names**: already-inline ttext inputs proven on goal pages by test.
  - **Responsive pass**: overflow-x guard, tightened rows/buttons/tabs at ≤600px, verified by 390×844 screenshots (two rounds — first round showed crushed text + wrapped plan button, fixed).
  - Suites: ll5 (new features) 18/18, ll3 20/20 (2 checks updated for the page model — boot lands on contents now), ll4 (pages/notebook) 22/22, chime 7/7.
- 2026-08-18 (5): build stamp per chat — "build v2.2 · 2026-08-18 · 7449b523c" (notebook-edition commit) bottom-right of the stats row, Space Mono .62rem, faded ink. Layout verified unchanged (the scrollWidth overhang flagged by the stamp probe is the pre-existing intentional tab protrusion, identical pre/post). Suite 18/18. Version convention going forward: v1 initial, +.1 per feature release, feature-commit hash in the stamp (parla convention).
- 2026-08-18 (6): v2.3 — MOBILE CLIP AUDIT per chat. Built a programmatic clip detector (14 controls + tab-visibility + scrollWidth at 390/360/320px). Findings: the sheet had a hard ~409px minimum — (1) #addinput missing min-width:0 (the classic flex min-width:auto shrink trap) pushed the plan button off-screen at ≤360; (2) #duerow couldn't wrap, clipping the date/clear/est-left cluster at ≤360; (3) side tabs showed only 14 of 18-27px everywhere. Fixes: min-width:0 + flex-basis on inputs, duerow flex-wrap, tabs pulled to right:-.35rem (fully visible), tighter mobile sheet gutter (padding-left 2.9rem, margin line moved), new ≤400px tier (smaller plan/add/due type, tighter sub-indent). Post-fix audit: ZERO clipped controls at all three widths and scrollWidth === viewport exactly. All four suites green (18/20/22/7). 320px screenshot verified: due row wraps, long titles ellipsize (by design).
- 2026-08-18 (7): v2.4 — FULL-PAGE per chat. Sheet now fills the viewport: body/main flex column, #sheet flex:1 + min-height calc(100vh − 7.5rem), #pane flex:1 pushes stats+stamp to the paper's bottom edge. Width tiers: 46rem base → 62rem ≥900px (bigger handwriting: goal 1.9rem, rows 1.4rem, wider gutter + margin line moved) → 78rem ≥1400px. Fill probe PASS at 1600×900 / 1100×800 / 390×844 (sheet ≥ viewport−140px, zero horizontal overflow); mobile clip audit still zero-clipped at 320; all four suites green (18/20/22/7). Desktop screenshot verified.
- 2026-08-18 (8): v2.5 — journal finishing pass per chat: page-curl shadow during turns (#sheet.turning::after layers a spine-side gradient over the existing texture stack — killed by prefers-reduced-motion with the animation itself). Full-page layout + page-turn confirmed live (fill PASS 1600×900, ll4's turn check green). All 67 checks re-green.
- 2026-08-18 (9): v2.6 — AUTO-RUN TIMERS per chat ("inspect task-creation timing, add automatic timers safely, don't change unrelated features").
  - **Inspection findings**: (1) makeTask stored NO creation timestamp — now records created:Date.now() (invisible, additive, enables future features); (2) no timer ever auto-started anywhere — openFocus always awaited a manual start click; estimates existed but every session was hand-started.
  - **Auto-run (opt-in only)**: green handwritten "▶ run the plan" in the due row, shown only on goal pages with unfinished estimated steps. Click → focus dialog opens on the first such step and STARTS ITSELF at the step's estimate; each completion pays its tally + chime, then chains to the next after 1.8s ("next timer in a moment…"). Hand-finished steps are skipped mid-chain. **Closing the dialog kills the chain instantly** (autoStop in closeFocus, pending setTimeout cleared — no zombie reopen, probed with a 2.5s watch). Auto-run starts TIMERS, never completes steps — finishing stays human (probed).
  - Unrelated features untouched by construction: manual openFocus path identical (probed still-not-running until start), all 67 prior checks + fill probe re-green.
  - Suites: auto-probe 11/11 first run + ll5 18, ll3 20, ll4 22, chime 7, fill PASS = 78.
- 2026-08-18 (10): v2.7 — VALIDATED EXPORT/IMPORT per chat (the data-safety gap named in this morning's inspection, now closed).
  - ⇩ export: pretty-printed JSON blob download (app tag, version, date, full task tree incl. subs/ests/dues/done/tallies/created). ⇧ import: hidden file input → FileReader → validateImport.
  - **Validation = clean rebuild, never trust**: cleanTask whitelists+coerces every field (text clamped 140, done!!,  pomos≥0, est 1–599 else null, due must be YYYY-MM-DD, created numeric, depth capped at 3, unknown keys dropped); nextId recomputed past the max imported id (collision-proof, probed with a post-import add). Garbage/wrong-shape/empty files refused with distinct messages and PROVEN to change nothing.
  - **Preserving current data safely**: before every apply, the current notebook is stashed to 'loose-leaf-v1-backup' (itself an importable export) — an accidental import is always recoverable. Message says so.
  - Suites: data-probe 11/11 (roundtrip exactness, hostile-field cleaning incl. XSS-shaped text + junk keys + over-depth, three refusal paths, stash, collision repair, labelled UI) + all 5 regressions (11/18/20/22/7) = **89 checks**.
  - Patch lesson (again): the '/* ── tabs ── */' anchor died in the v2.0 rework — anchor-count ALL blocks before running a multi-block patch (a counting pre-pass beats a mid-script assert).
- 2026-08-18 (11): v2.8 — RECURRING RITUALS per chat ("gentle streaks, rotating weather moods, tiny surprise notes"). Inspection first: zero recurrence existed (grep recur = 0).
  - ↻ ritual toggle per goal (due row, opt-in, persisted). dayRoll(dateISO) at boot: completed rituals streak+1, missed reset QUIETLY to "fresh start — streaks regrow" (probe asserts no shame vocabulary), pages reset (done flags) with pomos/tallies kept, same-day rolls no-op.
  - **Weather moods**: 7 skies (☀️🌤🌧🌫🌈❄🍃), hash-of-date deterministic, ≥3 distinct per week probed; each carries a mood rule ("rainy-day pace: half-speed counts double"). Ritual line on goal pages: weather · green streak · mood rule; contents rows wear ↻N markers.
  - **Surprise notes**: hash(goal·date)%3 — deterministic occasional margin scribbles ("the moth approves") via dataMsg on ritual completion, no reward economy.
  - cleanTask carries recur through validated import (streak coerced, lastReset date-checked, absent for non-recurring).
  - Suites: rit-probe 15/15 + data 11, auto 11, ll5 18, ll3 20, ll4 22, chime 7 = **104 checks**. Patch used an anchor-count PRE-PASS (all 9 anchors verified before any replace — the lesson from the last two half-dead patches, now practice).
