# The Errand Basket — gentle triage for a full head

## log
- 2026-08-20: v1.0 — built per chat: gentle priorities, time estimates, local-only persistence. THREE GENTLE TIERS instead of alarm-culture priorities: 🍅 today would help / 🌿 this week is fine / 🕊 whenever it lands — with warm empty states ("nothing pressing. enjoy that."). HONEST TIME ESTIMATES (5/15/30/60/"unknown, honestly") sum per tier into KIND COMPARISONS (cup of tea ≤10, album side ≤25, short-walk-plus-the-good-podcast ≤45, sitcom double-bill ≤90, quiet afternoon beyond — "worth splitting across days; no rule says otherwise"); unknowns excluded from sums but named ("they usually shrink once started"). ✨ PICK ONE FOR ME: shortest estimate in the most-pressing non-empty tier with its reasoning ("the smallest true thing first"); empty basket → "the correct errand is: put the kettle on." Tier nudges announced kindly ("no sirens — just a nudge" / "deciding later IS a decision"), done shelf of small victories with back buttons ("shelves are not prisons"), judgment-free strike-out ("it stopped mattering, or never did. both are fine."), empty-the-basket reset ("nothing on a list ever defined anybody"). Local-only, footer covenant "the basket keeps no score against you" (probed). Gentle-vocabulary sweep bans urgent/overdue/asap/deadline/critical. Probe 13/13 (one probe-side regex fix: I grepped "never defined" for the app's "ever defined").

## issues
- none.

## todos
- a "5-minute sweep" mode (queue all ~5min errands), day-end gentle summary, if chat asks.
