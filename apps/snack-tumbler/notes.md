# Snack Tumbler — notes

## log
- v1.0 (2026-09-15): Built per two chat requests minutes apart ("physics-based 3D snack playground with simple original geometry, gravity, collisions, camera controls, and edible objects" + "add the rat-in-a-hat avatar as a jointed third-person controller, with grab, lift, chew, and swallow actions"). NO antecedent rat-in-a-hat avatar exists anywhere in the repo (exact-phrase sweep) — the request is the spec; the rat is an original primitive build. Fresh app (snack-block / snack-chef / butter-physics are unrelated).
  - `<script id="phys">` = pure, node-testable: seeded rigid-sphere physics (every snack has a bounding-sphere collider, visuals live inside it), gravity, table plane with a real edge (snacks that roll off fall and count as "rolled off"), a ring-wall dish on the table (inside bodies pushed inward, outside bodies pushed outward, only below rim height), pairwise impulse collisions w/ friction + positional correction, rolling quaternion from velocity, air spin, sleep, crumbs with ttl + cap, kinematic "pushers" (the rat shoves snacks), bite() shrinks 0.72× and removes at 0 bites. `makeRat(world)`: jointed third-person state machine — walk (camera-relative), grab/drop (nearest unheld snack in front within reach), lift toggle (overhead; G while lifted tosses), chew (bite + crumbs, last chew fills the mouth), swallow (only with a mouthful; belly grows), goTo (tap a snack → auto-walk + grab), rim hop, hints with expiry.
  - Rendering: three r128 from cdnjs (the one external script), own orbit camera (drag/wheel/pinch; a tap that doesn't drag = raycast fetch), gingham CanvasTexture table, dish, 9 original snack builds (donut/cheese/cracker/grape/cookie/pretzel/popcorn/marshmallow/gummy) + crumbs, jointed rat (body, head w/ jaw + cheeks + throat, ears, whiskers, top hat w/ band, 2 arms, 2 legs, 6-segment tail) posed procedurally from the controller state. WebAudio sfx (crunch/gulp/pop/thud/grab/shake), sound toggle. Bagel Fat One + Atkinson Hyperlegible.
  - Verified: phys+rat 65/65 node (determinism, settle, restitution ordering, dish containment both sides, lobbed-in, 12-snack pile overlap <0.08, energy decay, rolling spin axis, edge loss + event, bites, crumbs ttl/cap, hold/setPos, nearest/shake/impulse/gravity/clear, pusher shove, cap, full rat flow incl. refusals + hints, toss, goTo, rim hop); browser 36/36 at 1200/390/320 (real WebGL via swiftshader: meshes==bodies, buttons/keys spawn, W walks toward the dish, GRAB→CHEW×3→Space flow with HUD count, tap-to-fetch through the real projection + a real pointer tap, drag orbits without fetching, wheel zoom, gravity/shake/clear/drop-5, sound toggle, ≥44px, labels, a11y); screenshots both widths.

## issues
- LESSON (+): a `position:fixed; inset:0` CANVAS does not stretch — replaced elements keep their intrinsic 300×150 unless width/height are set. The probe passed (scene existed) while the screenshot showed a postage stamp; screenshot pass caught it.
- Camera-relative movement: the initial orbit angle must put the camera BEHIND the rat (theta 0 here) or "forward" walks away from the dish — the probe caught theta=π.
- Keyboard Space: don't gate on event.target being body/canvas (probes and some browsers target window); gate only on BUTTON focus so a focused button keeps its own Space behaviour.
- Probes that tap a projected snack must clear the table first — a nearer snack occludes the raycast; and at 320/390 a snack at (-3,-2) is off-screen.
- Headless WebGL works here (swiftshader) with the plain `--disable-gpu` flags; screenshots need `--virtual-time-budget` (rAF loop).

## todos
- v1.1 (chat, queued): unified low-poly claymation palette + lighting + animation language, clay squash on landings and bites, readable player-view framing, clearer bite feedback (bite pips, CHOMP pop), SWALLOW label, a dancing platypus NPC with an original dance library, low-poly city backdrop.
- Two-player? Snack "recipes" (eat a set for a bonus)? Rat customisation (hat colours).
