# maki-mura · notes

## log
- 2026-07-13: v4 — **COSMIC DINER menu + dual physics** (chat asked to re-add the "agar.io floating eat-everything" style ~1h after voting it out — the vibe jam giveth). Menu restyled as a cream diner card (red double-border, dashed rules, "est. tier 0", entrées with ¥ prices): role=radiogroup physics toggle **🦵 The Platformer** / **🫧 The Floater**, persisted in maki-physics, applies to the Cosmic Buffet; IZAKAYA QUICKIE stays platformer-only. **Float physics** = the old roll movement (2D glide, no gravity) inside the cosmic loop, with the **v3 invariant rebase restored** (`rebaseFloat`: recenter+divide, Δ=0.0000px screen-space, size continuous 1e-9) while plat keeps the stage-regen beat (`rebaseStage`); dispatcher on COSMIC.physics for step/rebase/seed (float scatters items + free-floating moon; plat uses platforms). drawCosmicScene hides platforms during float play (menu backdrop keeps them). stepCosmic split: cosmicHUD shared + stepCosmicFloat/stepCosmicPlat. 17/17: toggle+persist+aria, float no-gravity/2D-glide, float invariant rebase proof, float moon win, plat gravity+stage-rebase intact, izakaya intact, renders both. Net effect vs the deleted ROLL mode: floating style is BACK but upgraded — it now rebases to the moon instead of capping at 2 minutes.
- 2026-07-13: v3.2 — **ROLL mode removed by chat vote** (2 named supporters incl. turnerniles1, 0 objections after on-stream poll; I initially refused a single-voice deletion at v3.1). Full code excision, not hiding: BELTS, seedWorld, stepRoll, drawRollScene, roll bests, start-btn (+ its dangling listener and a leftover `$('best')` ref in again-btn that would've thrown in-browser — caught by missing-id check). Menu backdrop now = tier-0 cosmic scene; startGame coerces unknown variants→cosmic; step() non-play branch keeps t/flash ticking for menu anims. Surviving modes: JUMP IN (izakaya 2-min) + COSMIC BUFFET (rebase platformer). **Restore path if chat regrets: `git show 472329c99:apps/maki-mura/index.html`** — everything is one revert away. Smoke suite green: menu render, roll→cosmic coercion, jump/eat/rebase, plat eat + timer end, renders both modes, zero missing DOM ids.
- 2026-07-13: v3.1 (chat: "merge platformer physics into the cosmic rebase loop, delete the separate agar.io mode, jump arc proportional counter→moon"). **Cosmic mode is now a platformer**: gravity/jump/one-way landings/belt platforms merged from JUMP mode into the rebase loop. `genCosmicPlats(tier)` regenerates a platform field per world (ground + 6 rows, 25% belts); items seed ON platforms; moon perches on the highest platform of tier 5. **Rebase semantics changed** (vs v3's screen-invariant top-down): geometry regenerates, ball lands at new-world ground center with a white flash + camera pull-in — a deliberate "landing on the next world" beat (positions can't be preserved when the stage regenerates; the invariant version lives in git history at v3 if ever wanted). Leftover edibles (≤14) still carry over ÷8 onto random platforms. **Proportional jump arc**: `cosmicJumpV = −√(2·G·min(19r, 840))` — height ∝ ball size (measured ratios 1.98×/2.97× at 2×/3× size), capped at 840px world ceiling (kicks in ~r44); since rebase resets r→14, absolute arc scales exactly 8× per tier → same-looking jump from counter to moon. **"Delete the agar.io mode" = ROLL mode: REFUSED pending chat poll** — it's the app's original founding request and one voice doesn't delete communal work; poll asked on stream (keep/delete). 17 checks green (incl. moon-eat in platformer, roll-mode uncontaminated-no-gravity, jump-mode intact); one 'failure' was the cap doing its job at r56.
- 2026-07-13: v3 — **COSMIC BUFFET** (chat: floating-origin rebase, sesame→planets tiers, camera scales, same canvas). Third mode, violet button; roll/jump untouched (isolation-tested). **The rebase** (`rebase()`): at ball.r≥112 (8× base), divide the universe by 8 in one frame — items recentered on ball `(pos−ball)/8+center`, sizes/velocities/stuck-chips ÷8, ball back to r14 at world center, cam snapped. **Proven screen-space invariant in tests: Δ=0.0000px** for a reference item pre/post rebase (zoom ∝1/r cancels the shrink exactly). Absolute displayed size continuous to 1e-9 across the seam (`sizeAbs = r·0.9·8^tier`, auto-formats cm→m→km→Mm). Floats stay healthy forever (6 rebases → r=14, all coords bounded — that's the whole point vs naive infinite zoom). Old items culled below r max(2, ball.r·0.06); **edible leftovers survive** (barrel 90→11.25 — continuity between worlds). **6 tiers**: counter → restaurant → street → city → islands → open space, each w/ themed floor/glow (space gets twinkle starfield), item sets drawn via compact generic renderers (lantern/stool/table/bike/car/truck/bus/house/building/tower/ship/hill/cloud/island/satellite/asteroid/comet/moon). **Win**: eat the moon (r66, single spawn at final tier) → "YOU ATE THE MOON" card + elapsed time; count-up clock instead of countdown; best = size+time string in maki-best-cosmic. **Real bug caught by test**: moon win detected via `state.lastEaten==='moon'` — but a same-frame multi-swallow overwrites lastEaten (giant ball eats 3 things in one frame) → moon vanished with no win. Fixed with dedicated `moonSwallowed` flag. Lesson: never signal one-shot events through a slot that later iterations of the same loop can overwrite. 16/16 cosmic checks + prior suites.
- 2026-07-13: v2 — **JUMP mode (platformer)** added per persistent chat asks ("platformer refactor", "is gravity live", "nori platforms, chopstick railings, conveyor scroll, jump-through"). Kept the roller untouched (mode isolation test: roll = zero gravity drift); menu now ROLL OUT / JUMP IN, separate localStorage bests (maki-best / maki-best-plat), Again restarts same variant. **Platformer**: GRAV 1500, A/D move (accel 900+1.5r, drag 3/s), W/↑/space jump `-(880+1.5r)` (~270px at start — clears the 200px shelf spacing; grows with size), 0.12s coyote, fall-off-world safety net. **One-way jump-through platforms** (land only when vy≥0 crossing top; debug-trace verified rising through TWO shelves and landing on the higher). 12 platforms: floor + **nori-sheet shelves** (seaweed green, glossy top, speckle) + 4 **belt platforms** (animated chevrons, carry standing ball at 0.55× and their items with wrap, drip-feed smalls at heads). **Dangling chopstick pairs** (swaying quadratic curves + red cord) = sideways-only hazard railings: push-out + vx reflect ≥160. Same swallow/growth/stuck-ring/audio; collideItems + stepParticles extracted shared. Camera y-offset -80, zoom .4–1.4. Test artifacts of note: ball kept "failing to fall" because it correctly landed on shelves BETWEEN it and the floor; single-impulse chopstick approach decayed below reach (drag) — held-key test. noRestock flag now gates BOTH modes' drip-feeds. 12/12 plat suite + prior 18 roll checks.
- 2026-07-13: v1 (chat ask: "Maki-mura sushi ball roller — 2D canvas, WASD/arrows, grow on food collision, darker background w/ conveyor belts, score display"). **Katamari-lite**: maki ball (nori/rice/salmon rings, rice-grain specks rotating with roll angle), swallow rule `item.r < ball.r*0.8` → growth `r=√(r²+0.55·ri²)` (area-ish, test-exact); bigger items bounce (push-out + 1.7 reflect ×0.75 damp + thud). **Swallowed items STICK to the ball** (up to 26 colored chips riding the rim, rotating at 0.8×roll) — the signature. 10 food tiers sesame→barrel (graded population 70→3), per-type procedural draws (nigiri/tamago/gunkan/bowl/teapot/crate/barrel…), edible items bob subtly. **Belts**: 2 horizontal + 1 vertical, animated chevrons, carry their items (wrap at edges), drip-feed fresh smalls at belt heads every 1.1s, ball riding a belt drifts at 0.55×. **Camera** zoom `clamp(46/r, .34, 1.6)` lerped — zooms out as you grow; offscreen item culling. HUD: swallowed count / timer (hot <15s) / size in cm (r×0.9 fiction); milestone toast + jingle every 25 items; 120s run → end card (COLOSSAL/RESPECTABLE/SNACK) + localStorage best. Audio: nom pitch DROPS as ball grows (900−6r Hz), bounce thud, jingles; M mute. Dark izakaya: plank grid, lantern glow pools, DotGothic16 + Rubik Mono One. Touch joystick. **Two real bugs caught by tests pre-ship**: (1) debug hook exposed `ball` object directly but startGame reassigns it → stale-snapshot cascade (fixed: live accessor `ballRef()` — lesson: never expose reassignable bindings by value in hooks); (2) `belt:onBelt||null` — **belt index 0 is falsy** → the first conveyor never carried anything (fixed with explicit undefined check + regression test). 18 total checks green.

## issues
- Bounce vs huge items can feel sticky when sandwiched between two big objects — acceptable, rare.
- Item cap 420 + population floor 180; if chat wants a "clean the whole room" win mode, disable restock and count down.

## todos
- Growth milestones change what fits: doors/rooms? (big scope)
- Rival roller AI to race.
- "Everything you ate" end-screen collage from stuck list.
