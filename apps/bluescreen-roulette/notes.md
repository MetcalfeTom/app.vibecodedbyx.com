# Bluescreen Roulette

## log
- 2026-08-01 v1: born from chat's "💀 chip in the global _bar" request — `_bar/` turned out to be ROOT-OWNED (read-only to me, operator infrastructure), so the chip itself needs Fela (exact surgical patch left in /vibespace/notes.md message-to-fela). This app is the zero-blast-radius delivery of the same feature: land here → tumbling skull, "fate is compiling your crash…", seed shown, then location.replace to /windows-11-recall-nightmare/?bsod=<seed>-<r>-<s> after 1.6s. Every spin a fresh random crash; every resulting URL replays identically (recall-nightmare's seeded-BSOD system). noscript + try/catch fallbacks link to the app unseeded. Win-blue gradient, Segoe, reduced-motion safe. Tests green: parse, seed display, redirect URL shape, head/OG.

## issues
- If the operator applies the _bar chip patch, point the chip at THIS app (keeps bar JS at zero).
