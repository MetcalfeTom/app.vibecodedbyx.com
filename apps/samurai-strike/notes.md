# Samurai Strike — notes

## log
- 2026-07-05: v1.9 — strokes 8px body / 5.5px katana (tsuba 7 to match) + block pose blade now leans diagonally up-and-back toward the head (swordA −1.28→−1.92, hilt 18/−22) — high-cover guard silhouette. Parry geometry unaffected (block detection is state+facing based, not blade-position based).
- 2026-07-05: v1.8 — strokes to 6px (chat ask): legs/spine/arms all 6, head r 12.5, headband 4 (widened to ±11), katana 4 (kept slimmer than body on purpose — a 6px blade reads as a club). If chat wants even chunkier, scale figure size too or it blobs.
- 2026-07-05: v1.7 — thicker fighter strokes (chat ask): legs 3.4→4.2, spine 4→5, arms 3.2→4, katana 2.6→3.4, head r 10.5→11.5. Ribbon/tsuba unchanged. Bolder silhouettes vs the storm bg.
- 2026-07-05: v1.6 — remap button on MAIN menu too (chat ask ×3) + persistence hardening: saveBinds now write-verifies (setItem → getItem compare → clear+retry once; private-mode/quota safe, falls back to in-session binds), extra saveBinds flush on closeRemap. remapReturn tracks pause vs menu origin so ✓ done restores the right overlay + focus. Load-path validation roundtrip test passed in node w/ storage stub.
- 2026-07-05: v1.5 — **keybind remap screen** (chat ask). Pause menu → ⌨ rebind keys → 鍵/REBIND KEYS overlay: 2 columns (P1/P2) × 4 actions (left/right/slash/block), click a bindBtn → "press key…" listening state → next keydown captured (Esc cancels listening; Esc otherwise closes back to pause). **Conflicts swap automatically** (whoever held the key inherits your old one — nothing ever unbound). Binds persisted `localStorage[samurai-strike-keys-v1]` w/ shape validation fallback to defaults; reset-defaults button. All input paths read live `binds.p1/p2` (P1KEYS/P2KEYS aliases removed). Main-menu key hints (mkP1/mkP2) render from binds so they always match. keyLabel prettifies (arrows→◀▶▲▼, space→SPACE). Rebind capture takes priority over all other keydown handling; gameplay keys swallowed while remap open.
- 2026-07-05: v1.4 — ESC pause menu (chat ask ×2). 中断/PAUSED overlay w/ resume + quit-to-menu; Esc toggles (pause only during intro/fight/kill, resume anytime). Freeze = dt zeroed + phase/update block gated on !paused (phaseT, fighters, trails, particles all frozen) while background rain/lightning keep falling — clearly paused, still alive. keys cleared on pause; trySlash guarded. Corner ⏸ button (visible during play) for touch users; quitToMenu resets timeScale (no stuck slow-mo when quitting mid-kill). Focus moves to resume btn.
- 2026-07-05: v1.3 — WIN_SCORE 5→3 (chat ask, "first to three points wins"). Score pips, aria-label, menu footer all updated; updateScoreUi renders from WIN_SCORE so pips follow automatically.
- 2026-07-05: v1.2 — chudan-no-kamae retune from real references (wikipedia Chūdan-no-kamae, kendojogja, swordis): hands to hip/navel height (hiltY 10 → hand y≈−48, hip is −46), blade 45° (swordA −0.785, tip lands ≈−92 = eye line, the "psychological" high variant refs endorse), spine near-upright w/ slight forward set (lean 0.12 — refs say perpendicular spine; kept a hint of forward per chat), **staggered feet**: front ankle +18 bent knee, back +9 straighter w/ heel floating 2.5px (heel drops in walk/block). Windup/recover curves re-anchored again (windup lean 0.12−0.34p hiltY 10−54p swordA −0.785−1.465p; recover ends at new idle).
- 2026-07-05: v1.1 — idle stance retune (chat ask): torso lean 0→0.175 rad (10° forward), hands hiltY −18→−10 (8px lower) = chudan katana grip. Windup/recover pose curves re-anchored so transitions stay continuous (windup lean 0.175−0.395p, hiltY −10−34p; recover lean 0.33−0.155p, hiltY 2−12p — end states still match slash/idle).
- 2026-07-05: v1 — 2D samurai stickman duel (3 chat asks merged: one-hit kills / P1 WASD+F, P2 arrows+K / pixel-art storm temple bg). **Combat**: states idle|walk|block|windup(240ms telegraph)|slash(150ms, lunge 520u/s)|recover(260ms)|stagger(520ms)|dead. One hit kills — no HP. Reach window 22–102px in front during slash. Resolution order: both-slashing → clash (sparks, both knocked to recover); defender blocking & facing → **parry, attacker staggers** (the core mindgame — bait/block/punish); else kill. Facing auto-updates only in idle/walk (committed moves keep direction). `separate()` keeps 30px min gap (no ghosting through). Rounds: 構え(950ms input lock) → 斬れ！ → fight → kill (slow-mo timeScale→0.22, ink/blood burst, fall rotate, 一撃 banner) → reset; first to 5 → 勝利 overlay. **Controls**: P1 A/D+S(block)+F(slash); P2 arrows+↓+K. Modes: 2P local + ghost AI (1P; B rendered 0.88 alpha). **AI**: reacts to windup telegraph (block prob 0.34+0.045/round cap 0.75, else retreat), punishes stagger instantly, approach w/ random pauses/feints, slash rate scales w/ rounds. **BG**: pixel-art storm temple — painted 240×135 offscreen, blitted ×4 w/ imageSmoothingEnabled=false. 8 discrete twilight sky bands, twinkling pixel stars, 6 drifting cloud clusters, 3-tier pagoda w/ warm lit windows + torii gates + stone lanterns (pre-rendered once), wet stone ground w/ slab lines + horizon-glow reflection, 110 slanted rain streaks (brighten during lightning), lightning every 5–14s (200ms flash + jagged bolt + thunder sfx: lowpass noise rumble + 50→26Hz sub). Fighters are smooth vector ink w/ warm rim glow (shadowBlur 7 #ffaa5a) so silhouettes read on dark stone; katana pale steel w/ glint; red slash trail; kill splatter = crimson, sparks gold. **Audio**: swoosh/clang/kill(slice+delayed sub thud)/taiko/clack/thunder, mute btn. reduced-motion: no slow-mo, no lightning flash (rumble only), fewer raindrops, instant banners.

## issues
- Fighters block only from idle/walk and blocking is planted (no move) — intentional commitment, don't "fix".
- Input lock until 斬れ！ prevents round-start cheese.
- Touch controls only appear for coarse-pointer + AI mode (2P needs a keyboard by nature).

## todos
- Chat may want: best-of selector, more arenas, a dash/backstep move, round-win taunts, sudden-death draw handling (currently simultaneous kill impossible — clash resolves first).
