# solar-sandbox (Solar Sandbox)

## log
- 2026-07-04: **compressed ↔ realistic scale toggle** (chat ask: real orbital distances so Mars survives hours, planets still comfortably clickable). Decoupled physics distance from display comfort: each planet carries both `dist` (compressed) and real `au`; `distOf(c)= realistic ? au·150 : dist` (Earth anchored at 150 in BOTH modes so the km/s calibration + Yeet Cap stay valid). **📏 real scale** button re-seeds with true AU ratios (Mercury 58 → Neptune 4510 units) and refits the camera (z 0.16 realistic / 0.9 compressed, recenters). Planet DRAW sizes + moon offsets stay comfortable (unchanged) — only orbital distances change, so bodies remain clickable and moons get even bigger Hill spheres (more stable). **Headless-verified**: realistic spacing holds Mars at 225.7–229.8 (2% excursion) over 30 sim-minutes ≈ hours of viewing (vs compressed which goes eccentric) — the real Mars/Jupiter 3.4× gap kills the artificial resonance. Compressed mode = the familiar tight default. Reset refits zoom to the current mode. Verified: syntax OK, stub boot, toggle + distOf present.
- 2026-07-04: **real 2025 initial conditions** (chat ask 'Mars starts safe from Jupiter'). Replaced the old all-aligned seed (every planet on the +x axis) with each planet's **real 2025 heliocentric ecliptic longitude** (computed deterministically from Standish mean-longitude elements for 2025-06, no network): Mercury 79° / Venus 294° / Earth 249° / Mars 180° / Jupiter 85° / Saturn 1° / Uranus 62° / Neptune 0°. Each planet placed at `(dist·cosθ, dist·sinθ)` with velocity ⟂ to radius (circular). **Mars now starts 94° / ~363 units from Jupiter vs 90 units when aligned — 4× safer** (the request). Moons re-seeded radially-outward from their parent (was hardcoded +x) so they stay bound at any parent angle. **Headless-verified**: moons rock-solid (Io 12.0–12.7, Titan 13.4–17.2, Triton 12.9–13.5), Mars stable for normal viewing (196–204 @30s). HONEST caveat documented: under heavy fast-forward (minutes of sim time) the COMPRESSED spacing (Mars/Jupiter dist ratio 1.45 vs real 3.4) + our over-massive scaled giants make Mars eccentric long-term — a pre-existing sandbox trait, not fixable without widening spacing (which breaks the viewport) or dropping MUNIT (which collides moons into the giant's disc). Real angles are a strict improvement to the *starting* safety. Integrator = velocity Verlet (from earlier today). Verified: syntax OK, 8 longitudes present, stub boot.
- 2026-07-04: **3 ordered tasks + galaxy follow-up**. (1) **Galaxy core anchored to the Sun's canvas position**: `drawGalaxy` now translates to `w2s(sun.x,sun.y)` instead of screen-center, so the galactic core and the star it hosts are the same point and the whole galaxy rides the Sun as you pan/zoom (satisfies the follow-up 'pin the canvas to the sun'; slow rotation ~0.0026 rad/s was already there and sells the spinning-ship feel). (2) **Integrator audit** — honest finding: it was symplectic EULER (not Verlet), and the Earth/Mars radial excursion (Earth 3.0%, Mars 4.5%, Mars growing 9→24→30 over 30 min) is **PHYSICAL** — a headless harness showed Euler and velocity-Verlet produce IDENTICAL drift, and energy is conserved (0.009%), so it's Jupiter's secular eccentricity-pumping, not a bug. Still upgraded the integrator to **velocity Verlet** (KDK leapfrog, 2nd-order symplectic via `accelInto` reusable buffers) because it conserves energy tighter at high time-speed (0.009%→0.000%); dropped SUB 6→4 since 2nd-order needs fewer substeps. Prediction arc + future-trails reuse the same `integrate()` so they got more accurate too. (3) **Mission UI**: top-left panel with 5 event-driven objectives (fling an asteroid / focus-follow a planet / grow a body past 500 ⊕ / launch >1000 km/s / cause a collision merge), ✔/○ checkboxes, N/5 counter, green flash on completion, '✦ ALL OBJECTIVES CLEAR ✦' banner. Hooked into the real launch/setFollow/mass-slider/merge events. Verified: syntax OK, Verlet wired (2 accel evals/step), stub boot, 5 objectives present.
- 2026-07-04: **permanent spiral-galaxy background** (chat ask, truncated 'have the…' → interpreted as parallax-with-camera). Baked ONCE to a 2400px offscreen canvas (`bakeGalaxy`, ~6.7k draw-ops verified): soft radial CORE gradient (warm white→gold→violet→void), 34 screen-blended dusty NEBULA blobs seeded along 2 logarithmic spiral arms (5 tints), 5,200 arm stars (inner warmer, outer bluer, brightness-varied) + 1,400 faint field stars. Per-frame `drawGalaxy(now)`: fills deep-space base, then draws the galaxy **slowly rotating** (~0.0026 rad/s) with **camera parallax** — galaxy is the farthest layer so it drifts least (×0.045 of pan) with a subtle log-zoom response; a separate 170-star **twinkling near-layer** parallaxes faster (×0.11, wraps over a 3000px field) with per-star sin-alpha twinkle. Replaced the old flat fill + cheap starfield. Grid/trails/bodies render on top unchanged. Verified: syntax OK, bake paints, stub boot.
- 2026-07-04: **30 km/s launch cap + hover mass labels** (chat ask). (1) **km/s calibration**: `KMS = 29.78 / √(G0·60000/150)` anchors sim velocity units to Earth's real 29.78 km/s orbital speed (verified: Earth reads 29.78 km/s). `capVel()` clamps asteroid launch magnitude to **MAXV = 30 km/s** — applied to both the actual launch AND the prediction arc so they match; the power label now reads live km/s and turns orange '(MAX)' when capped. (2) **Hover mass labels**: pointermove hit-tests the body under the cursor (when not dragging) and draws a rounded tooltip with the body name + **mass in Earth masses (⊕)** via `earthMass(b)=b.m/MUNIT` + `fmtMass` (Earth 1.0, Jupiter 318, Saturn 95, Sun 27,273 — planets show clean real ratios since catalog×MUNIT/MUNIT=catalog; live-updates when you crank the mass slider). Same figure appended to the selected-body panel header for touch users. Cursor→pointer on hover, cleared on pointerleave. Verified: calibration numerically checked, syntax OK, stub boot.
- 2026-07-04: **inelastic merge hardened + made visible** (chat ask — verified the merge was ALREADY momentum-conserving, so this completes & demonstrates it rather than 'adding' from scratch). Headless-verified: perfectly-inelastic collision conserves total momentum p=Σm·v exactly, loses kinetic energy (60.5→11.3 in a 2-body test), and yields a new v distinct from both parents. Enhancements: (1) survivor now moves to the **barycenter** (was keeping its own position); (2) survivor **color blends** mass-weighted with the absorbed body (a genuinely new combined body, not just 'absorb') via `blendCol`; (3) the resulting **new velocity vector is drawn** as a fading green arrow from the impact point for 1.6s + 'new v = N (orbit altered)' label; (4) HUD shows `merge Δv → new speed`. Physics unchanged (it was correct); this makes the permanent orbit change auditable and dramatic. Verified: syntax OK + stub boot + blendCol sanity.
- 2026-07-04: **mass-scaled collision FX + proper asteroid aim** (chat ask). (1) **Collisions** compute impact energy = √(reduced-mass)×relative-speed + √(small-mass); `spawnCollision` scales debris count (10→90 particles), spread, and shockwave max-radius by that energy — a shrimp-asteroid tap makes a puff, Jupiter eating Earth makes a real blast (expanding shockwave ring + radial ember debris with drag/fade + radial-gradient flash + the survivor pulses a ring). FX are camera-space and pause with the sim. (2) **Aim mechanics**: dragging from empty space now shows a **simulated trajectory arc** — a ghost asteroid integrated 90 steps through the LIVE gravity field (same integrator), so you can see it slingshot around planets before release; plus a direct aim line, a launch-direction arrowhead, a power/size readout, and a ghost rock sized to the pull. **Longer drag = bigger, heavier, faster asteroid** (radius 2→8, mass 0.01→4). Launch fires a blue ring flash + the new rock pulses. Verified: syntax OK + stub-DOM boot; dead scaffolding removed.
- 2026-07-04: **prediction-length slider + click-to-focus follow-cam** (chat ask). (1) **Prediction length** slider (20–400 steps, live readout) drives the ⇢ future trail's `FRAMES` — short = a nudge ahead, long = full future orbits traced. (2) **🎯 focus** is now a toggle that ZOOMS in (cam.z→2.2 if wider) and **camera-follows the selected body every frame** (lerp 0.14) — pick Jupiter, hit focus, and the view rides along as it orbits (and as you crank masses/fling asteroids). Selecting another planet retargets the follow; deselecting or the body dying auto-stops (frame-loop guard flips the button label back to '🎯 focus'); reset clears it. Verified: syntax OK + stub-DOM boot.
- 2026-07-04: **ISS LEO marker + real named moons** (chat ask). (1) **🛰 ISS**: cosmetic low-Earth-orbit marker — a pixel station (core + two solar-panel struts) riding Earth's LIVE position on a fast 0.9× angular orbit at radius max(earth.r+3, 9), with a translucent orbit ring + 44-point fading blue trail + 'ISS' label at zoom. NOT a physics body: Earth's Hill sphere here is smaller than its own drawn radius, so a simulated satellite would be sun-stripped — documented in-code. Toggle button, resets trail on toggle, tracks Earth even when Earth's orbit is perturbed. (2) **Real named moons** replacing the placeholder set: **Io + Europa** (Jupiter), **Rhea + Titan** (Saturn), **Titania** (Uranus), **Triton** (Neptune, seeded RETROGRADE like the real thing). Real relative masses (Earth=1); orbit distances COMPRESSED into each giant's ~⅓-Hill-radius stable zone. **Rigorously chosen**: a headless harness tested candidates over 3–5 sim-minutes — Ganymede, Callisto, Oberon all get solar-tide-ejected at their real ratios (min distance collapses toward 0 or escapes to 1000+), so they're honestly OMITTED. Final 6-moon set verified ALL STABLE over 5 sim-minutes (Io 11.9–12.8, Europa 14.8–18.1, Rhea 9.0–10.0, Titan 13.6–16.8, Titania 9.1–11.7, Triton 12.4–13.7). Physics wins; the code comments say which real moons couldn't make the cut and why. Verified: syntax OK, no ext libs, ids present.
- 2026-07-04 (rotetomate's asks): **grid + prediction trails + moons**. (1) **Background grid**: world-space lines every 100 units (adaptive ×2 spacing when zoomed out, cap ~60 lines), axes tinted blue, ▦ toggle. (2) **⇢ future toggle**: clones the live system each frame and runs the SAME `integrate()` forward 70 frames (physics refactored into a pure integrator reusable for prediction — real step keeps merging separate), drawing dashed per-body paths + endpoint dots. Live-updates as you drag mass/G sliders, works while paused — you can aim disasters before unpausing. (3) **Moons**: MUNIT 0.18→2.2 (real ratios preserved) so gas giants have workable Hill spheres; Jupiter/Saturn visual radii shrunk (9.5/8.5) so moon orbits clear the disc. Ship set = **Io (13), Europa (17), Titan (14)** — headless-verified over 2 sim-minutes: Io 12.8–13.7, Europa 14.8–18.0, Titan 12.8–14.7, Earth 141–159 (livelier but bound). **Ganymede honestly cut**: tested at 21/22/23, resonance-ejected by Io+Europa every time (2.5–481 range) — three packed moons don't fit the compressed scale. Rocky planets can't hold moons at all here (Hill sphere smaller than their drawn radius) — physics wins, documented in-code.
- 2026-07-04: shipped (chat ask: "full interactive solar system simulator with adjustable mass, gravity, and size for each planet — realistic n-body physics, all hand-rolled canvas, no libraries"). ZERO external scripts.
  - **N-body physics**: every body attracts every body (full O(n²) pairwise), semi-implicit (symplectic) Euler with 6 substeps/frame + softening (SOFT2=4). **Stability verified headlessly**: Earth seeded at r=150 stays within 149.9–150.5 over ~90 sim-seconds; Mercury stable at 70. System momentum zeroed at seed (sun gets counter-velocity) so nothing drifts.
  - **Bodies**: Sun + 8 planets, REAL relative masses (Earth=1: Jupiter 318, Saturn 95…, ×0.18 vs a 60k sun so mutual perturbations exist but the default system is stable), circular-orbit seeding `v=√(GM/r)`, Saturn gets a ring, sun gets radial glow.
  - **Per-planet controls** (click a body or its chip): **mass ×0.01–×1000** (log slider — crank Jupiter and watch the system destabilize), **size ×0.2–×8** (visual + collision radius), focus camera, remove (sun non-removable).
  - **Universe controls**: gravity G ×0.1–×10 (log), time speed 0–×3, pause, trails toggle (ring-buffer polylines, 340 pts planets / 160 asteroids), reset (also restores G).
  - **Asteroid fling**: drag from empty space = dashed aim preview → releases a 0.02-mass asteroid at 1.6× drag velocity. **Collisions merge** (momentum-conserving, volume-additive radius, larger body wins the name; merged counter in HUD; sun happily eats infalling planets).
  - **Camera**: wheel zoom-to-cursor (0.12–8×), right-drag pan, focus button. Deterministic cheap starfield. FPS + body counters.
  - Mobile: 🪐 tab toggles the control panel; tap selects, drag flings.
  - WCAG: role=application canvas w/ instructions, aria-pressed toggles, labelled sliders, focus-visible.
  - Verified: syntax OK, zero libs, ids present, orbital stability test green.

## issues
- O(n²) pairwise — fine up to a few hundred asteroids; would need Barnes-Hut beyond that.
- Cranking mass/G to extremes with high time-speed can slingshot bodies to infinity (physically honest; reset exists).
- Two-finger pinch zoom not implemented (wheel only); mobile relies on buttons/fling.

## todos
- Pinch zoom.
- Velocity vectors toggle + per-body speed readout.
- "What-if" presets (binary sun, rogue Jupiter, no Neptune).
- Barnes-Hut if chat demands asteroid fields.
- 2026-07-04: **Yeet Cap slider** (chat ask) — labeled "🚀 Yeet Cap", log-scale slider scaling the asteroid launch ceiling from **1 km/s to 300,000 km/s** (≈ light speed, flagged '⚡c' at the top). `MAXV_KMS=10^value`, `MAXV` recomputed live so capVel + the prediction arc both follow it instantly. Readout formats km/s → M km/s. Reset restores 30 km/s default. Endpoints verified (1 / 30 / 300,000). At max, asteroids leave the screen essentially instantly — as intended.
