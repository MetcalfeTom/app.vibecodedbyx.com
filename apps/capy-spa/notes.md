# capy-spa

## log
- 2026-05-07: shipped — sibling rebrand of `capybara-onsen` per chat ask. Same engine + visuals, renamed to **湯 · 蛙鼠 (capy · spa)** with its own page title, OG tags, and `localStorage['capy-spa-best']` so the two apps keep independent best scores. Original `capybara-onsen` left intact so existing links still resolve.
- See `apps/capybara-onsen/notes.md` for the full implementation log (4 floating capybaras, drag-from-basket orange stacking with center-tolerance bonus, topple physics on cumulative-offset > 85% of head half-width, water-temp drift toward 70° with 🔥 +8°/❄ −6° buttons, 95–105° green sweet-spot band, calm score `4 + happy×2.5 per second × (1 + oranges×0.12)`, peach sky + bamboo + falling snow + steam wisps, web-audio synth ambient + plop + wood-crack + sparkle).
