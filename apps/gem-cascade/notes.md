# gem-cascade (Gem Cascade)

## log
- 2026-07-21: v3.7 — **LEVEL BRIEFING HANDSHAKE + lit-gate fix** (Angie: "stars landed but locked biomes and objectives didn't — tie the tutorial handshake to the actual level progression data"). Audit FIRST (full player walk: fresh → win 5 levels via real voyCheckEnd path): stars/unlocks/gates/objective-text all coherent — EXCEPT the audit exposed that v3.1's lit-gate condition ('previous realm fully starred') was UNREACHABLE: linear unlocks open a realm before its predecessor can be fully starred, so the glow state never fired for anyone. Fixed: gate lights when voyState.unlocked===b2*5 (its opening is literally the next win) — the 'one more win and it opens' copy is now true. **Briefing**: startVoyLevel → showBriefing(i) reuses the ov overlay: title '🐚 Level N · Realm', objective read LIVE from VOY_LEVELS[i] (target + moves — never hand-written copy that can drift), prior stars if any, and FIRST-EVER players (zero stars total) get tutorial tips (swipe/rocket/bomb/rainbow/moves-cost). '⛵ set sail' dismisses into play; briefing flag checked FIRST in ovBtn so endgame map-return unaffected; endGame clears it defensively. SUITE CAUGHT A CRASH pre-ship: briefing dismiss called announce() — a clothes-minded helper that doesn't exist here → ReferenceError on every 'set sail'; swapped to this app's live.textContent. 9/9 green. LESSON: helpers don't travel between apps — grep before you borrow.
- 2026-07-21: v3.6 — **BIOME GEM THEMES** (chat ×2, cut off both times at 'Shadow Realm dark enchanted sto…' — implemented the named six and themed ALL TEN realms so none feel unloved). Beyond v3.0's palette swaps, each realm now has a hand-drawn SIGNATURE overlaid per gem in gemStyleOverlay() (5-14 canvas ops each, cheap at 64 cells/frame): Coral Cove PEARL (nacre sheen arc + mother-of-pearl glint), Emerald Jungle VINE (quadratic vine wrap + leaf), Sapphire Depths BUBBLE (3 rising bubbles), Ruby Volcano + Volcano Ring LAVA (glowing crack veins + ember, shadowBlur), Treasure Isle GILDED (gold rim + 8-point sparkle), Shadow Realm DARK (violet aura + rune strokes), Crystal Caverns SHARD (inner glow core + facet lines), Neon Jungle NEON (electric outline pulse), Sky Kingdom CLOUD (soft halo + wisp). GEM_STYLE/GEM_STYLE_ACC set in applyBiomePalette (biome.gstyle) — free-sail modes get null = classic gems, verified zero extra ops. Overlay draws inside drawGem's save/restore so alpha/clearing scale apply. SUITE 4/4: all 10 styles mapped, chat's six present, every branch draws +ops without throwing, free-sail clean.
- 2026-07-21: v3.5 — cell stretch ACTUALLY fixed (chat ×2, insisting post-v3.4 and pointing at cells not container — they were right to insist). Draw-level audit first proved logical geometry square end-to-end (864×864 canvas, uniform DPR scale, 64/64 square cell rects, ovals only where designed: quartz + speculars). The REAL culprit was a LAYOUT CIRCULARITY v3.4 introduced: .game is align-items:center flex column → .board-wrap (no width) shrinks to fit content → canvas width:min(100%,432px) resolves against that same shrink-to-fit wrapper → circular; browsers resolve the loop inconsistently, some falling back toward the canvas's 864px ATTRIBUTE size (DPR 2) = stretched/oversized cells. Fix: .board-wrap now has DEFINITE width:min(100%,450px)+border-box (resolves against .game's grid track, no self-reference), canvas is width:100% + height:auto + aspect-ratio:1/1. Verified: sizing chain fully definite, cells square at 450/360/320px wrappers. LESSONS: (1) %-sized replaced elements inside shrink-to-fit containers = circular sizing, browsers diverge; give the CONTAINER the definite width. (2) When chat insists a visual bug survives your fix, believe the eyes — the draw audit passing meant the bug lived in LAYOUT, which node stubs can't see.
- 2026-07-21: v3.4 — board squeeze FIXED (chat report: "grid might have been squeezed by the map CSS"). Root cause was NOT the map CSS — it was v1.1's own mobile fix: stylesheet said canvas{max-width:100%;height:auto} but the INLINE cv.style.height='432px' (specificity: inline > stylesheet) survived, so on viewports <~432px the width shrank while height stayed 432 → gems rendered as tall ovals. Input was never wrong (evCell scales per-axis), only the render. Fix: inline sizing now width:min(100%,432px) + height:auto + aspect-ratio:1/1 — the board scales as a SQUARE or not at all. Verified: cell mapping exact at 336px and 432px; square invariant holds at any width. LESSON: when a stylesheet rule and an inline style disagree, the inline wins — check BOTH when sizing misbehaves.
- 2026-07-21: v3.3 — **CINEMATIC WORLD: 3D parallax + chapter portal** (chat ×4 converging: cinematic illustrated world, zigzag trails [already in], deep parallax layers, portal marking current chapter). (1) **Focus-pull camera**: rAF-throttled scroll handler on .vmap-card — each realm's offset from viewport centre (rel, clamped ±1) drives transform rotateX(rel·−4°) + scale(1−|rel|·0.045) + opacity(1−|rel|·0.28) under #vmapIsles perspective:900px → realms lean in 3D and fade as they leave centre, snap to full presence in the middle. (2) **Depth layers**: data-depth per layer (sky 0.12, decos 0.2/0.33/0.46, critter −0.08, landmark −0.12) → translate3d(0, rel·depth·60px, 0); negative = near layers counter-drift. will-change hints. (3) **Glow trail**: second SVG path (stroke 9, blur 2px, opacity .12) under the dashed trail. (4) **PORTAL**: the frontier node (unlocked−1) gets .portal — spinning conic-gradient ring (::before) + breathing radial halo (::after) in realm accent, aria 'your current chapter'; exactly one per map. reduceMo() skips parallax entirely; reduced-motion CSS kills portal spin/breath, path flow, marker walk, critters. SUITE 8/8 (one assertion was a stub-artifact: setAttribute number-vs-string — real DOM stringifies; noted, not an app bug): single portal at frontier, aria, glow+dash strokes, centered=full presence, distant=lean 4°+fade .72, sky drifts −7.2px, critter counter-drifts +4.8px.
- 2026-07-21: v3.2 — **VERTICAL ILLUSTRATED JOURNEY** (chat ×2: Angie's vertical world layout, scrolling path Coral Cove → Sky Kingdom, walking player marker; 2nd msg added zone decorations + camera zoom-in/drift-back). Map is now a single tall column: realms stack bottom-to-top (render order reversed — Coral Cove at the base, SKY KINGDOM moved to realm 10 as the summit, swapping places with Neon Jungle: display-order-driven biome swap, meaning levels 41-45 are now Neon and 46-50 Sky — stars/progress are per-LEVEL so nothing lost, but palettes at those indices shifted; documented). Each 15.5rem section: biome-sky gradient layer, winding dashed SVG path (shared PATH_D through 5 PATH_STOPS %-coords, vector-effect non-scaling, dash-offset flow animation, enters bottom/exits top so sections chain), circular level nodes ABSOLUTE on the path stops, 3 zone-specific decorations (per-realm emoji at fixed spots, greyscaled when locked), v3.1 critters/landmarks/gates/lit-gates all carried over. **Walking marker** ⛵: sits at frontier node (unlocked-1); when frontier advanced since last visit ('gem-cascade-marker-v1'), it WALKS node-to-node (680ms CSS left/top transitions, re-parented at realm borders) then persists. openMap scrolls the walker's section into view. **Camera**: node tap sets transform-origin to the node's %-position in the card → .zooming (scale 2.3 + fade, 420ms) → level starts; returning openMap re-adds .zooming at the same origin then releases after double-rAF = drift back out. reduceMo() bypasses both. SUITE 16/16 (summit order, section order, sky/path/decos, node positioning, marker create+walk+persist+cross-realm re-parent, zoom class + handoff, drift-back, scroll-to-walker). HARNESS LESSONS: stub getElementById must return NULL for ids the app creates lazily (auto-create swallowed the vmark branch); rAF drains must be BOUNDED (game tick re-queues itself → unbounded drain = 2min timeout).
- 2026-07-21: v3.1 — **WORLD MAP REDESIGN** (chat ×2: bold map UI, cute-fierce creatures, animated biome borders, glowing landmarks, unlockable gates that LIGHT UP when a region clears; gem tiles untouched — and they were: zero engine/board changes, map-screen only). Each realm card: (1) **animated border** — two-layer background-clip trick (padding-box card + 300%-wide accent gradient on border-box, background-position keyframed = flowing 'borderTide'; per-realm --acc CSS var); (2) **cute-fierce critter** (🦀🐆🦑🐉🦜🦇🐻‍❄️🦂🦅🐸 — one per realm, all distinct) idling with a bob that SNAPS into a 1.22× fierce lunge at 64% of the cycle, staggered delays, accent drop-shadow; (3) **glowing landmark** (⚓🗿🔱🌋👑🕯️❄️⚒️🏰🗼) pulsing accent glow top-right; (4) **region gates** on locked realms: nodes blurred behind a scrim, ⛓️🔒⛓️ + 'the 🦇 guards this realm · clear level N to raise the gate'; gate flips to **lit** state (⛓️✨⛓️, accent-coloured, pulsing chains, 'the gate glows — one more win and it opens') when the region BEFORE it is fully starred; (5) fully-starred open realms get **cleared** state: faster gold border tide, blazing landmark, ✨ in the title. Locked realms: greyscale critters/landmarks, no animations. Reduced-motion kills all three animation families. SUITE 11/11: card count, critter/landmark/accent per card, cleared-class detection, dark-vs-lit gate transition when prev realm completes, lit copy, 10 distinct critters.
- 2026-07-21: v3.0 — **FIVE NEW REALMS** (chat ×2: Shadow Realm, Crystal Caverns, Volcano Ring, Sky Kingdom, Neon Jungle w/ distinct palettes, backgrounds, 5 levels each). Voyage now 10 isles / 50 levels. Each new realm ships a full 6-gem PALETTE re-skin ([base,light,dark] per gem — applyBiomePalette() swaps GEMS colours in place from BASE_GEMS clone; SHAPES untouched so colourblind identity survives every realm) + gradient sky (cv.style.background takes the CSS gradient): Shadow 🌑 desaturated dusk purples, Crystal 💎 icy pastels, Volcano 🔥 lava, Sky ☁️ daylight golds+whites, Neon 🌴 full-sat neons. Original 5 isles keep default gems/flat tints. Levels 26-50: moves capped 28, targets to ~43k. Unlock cap 25→50 (the v2 Math.min would have bricked isle 6!). Endless/1000/survival always restore default palette+bg. SUITE 13/13: counts, cap maths, 10-isle map render, Shadow palette applied + gradient, Neon palette, endless restore both, unlock cap. NOTE: 2nd chat msg cut at 'plus un…' — assumed 'unlocks', already covered by star-gated progression; if it meant something else (unlockable gem skins? unique mechanics per realm), ask chat.
- 2026-07-21: v2.0 — **THE FULL MATCH-3 STACK** (chat: "world map with themed biomes, rockets, bombs, rainbow tiles, progression system, pirate-themed power-ups").
  - **Specials engine**: cells carry `special` (rocketH/rocketV/bomb/rainbow, survives collapse gravity — moved with type in collapse(), wiped on refill/clear). findMatches now returns run GEOMETRY (runs[] w/ dir/len/cells) alongside the old set/count/maxRun (all old callers untouched). computeSpawns(): h×v runs sharing a cell (L/T) → BOMB at intersection (runs consumed); unused straight 5 → RAINBOW at pivot; unused 4 → ROCKET along run axis (h-run→rocketH clears its ROW). Pivot = swapped cell if in run, else middle. Spawn cells are protected from their own clear (delete before AND after expansion). expandSpecials(set): BFS detonation chaining — rocketH→row, rocketV→col, bomb→3×3, chain-hit rainbow burns a random colour; +60 score per detonation. Rainbow SWAP activates in onSwapSettled (no match needed): partner's colour + itself; double-rainbow = whole board. hasMoves() short-circuits true if any rainbow exists (a rainbow is always a legal move). Rainbow = type 6 (RAINBOW_T), excluded from runs via type<NTYPES guard. Canvas art: rainbow orb = 6 pie-slices in the gem palette + gold ring + white core; rockets = gold double-chevron arrows; bomb = black orb + fuse + spark.
  - **Voyage (world map + biomes + progression)**: new 🏴‍☠️ Voyage mode button → #vmap overlay "The Gem Seas": 5 isles × 5 levels (Coral Cove 🐚 / Emerald Jungle 🦜 / Sapphire Depths 🐙 / Ruby Volcano 🌋 / Treasure Isle 🏴‍☠️), each isle tints the board canvas + accent. Level = move budget (16→24 by isle) + score target (~700→~16k). Legal swaps (incl. rainbow activations) spend a move via useVoyMove(); cascades and power-ups are free. End checked at idle (voyCheckEnd in onFallSettled — cascades finish first): target hit → win, stars = 1 + (≥25% moves left) + (≥50% left); out of moves → lose. Stars/unlocks persist ('gem-cascade-voyage-v1'); win unlocks next level + awards 1 random power-up. Overlay shows ★★☆ + booty; ovBtn becomes '⛵ back to the map' in voyage (resets to Play again otherwise — was a sticky-label bug risk, handled).
  - **Pirate power-ups** ('gem-cascade-booty-v1', starter stash 🧨2 ⚓1 🗺1): Cannonball = arm → tap = 3×3 blast; Anchor = arm → tap = sink the whole column; Treasure Map = instant random rainbow spawn. Armed state intercepts onPick; blasts route through beginClearSet → full detonation chaining. Buttons under board w/ live counts, disabled at 0, armed = gold.
  - **TEST SUITE 20/20** (stub harness + injected __gc hook in test copy): 4-run→rocket at pivot (pivot survives, others clear), rocket row expansion, rocket→bomb chain, L→bomb at corner, 5→rainbow at middle, rainbow-swap colour clear, voyage start/tint/win-3-stars/unlock/booty/persist/lose, cannon 3×3 + spend, map conjures rainbow, bomb rides gravity. CAUGHT 2 real bugs pre-ship: (1) voyState never voySave()d on win — progression would vanish on reload; (2) test fixture itself accidentally built an L (engine correctly bombed it — fixture fixed, engine vindicated). NOTE for future: reshuffle (fillNoMatch) wipes specials — acceptable, documented.

## log (v1 era)
- 2026-07-21: v1.1 — **true mobile support** (chat asked for a fresh 8×8 match-3 build; Gem Cascade already IS that game — 8×8, swap, match detection, cascades, ruby/emerald/sapphire + 3 more — so per depth-over-breadth it got the one thing actually missing). (1) Board was fixed 432px CSS (8×54) → overflowed phones; now `max-width:100%; height:auto`. (2) That scaling would have broken picks (cellAt assumed unscaled px) → new evCell() maps client coords through getBoundingClientRect ratio to logical space; verified CSS(105,105) on a 336px-wide board → cell (2,2) exact. (3) SWIPE gesture added (the native match-3 input): pointerdown captures start cell + pointer, pointermove past 0.4×cell-CSS threshold swaps toward the dominant axis (bounds-guarded, clears sel); pointerup without swipe = the old tap logic, so tap-tap still works. aria-label updated. Live-fired via injected __gc hook (cells/mode/sel/findMatches/cellAt exposed in test copy only): seeded match-free, swipe → mode 'swap', tap path clean, double capture OK.
- 2026-06-28: **reverted survival meter to classic green-yellow-red, kept the easing** (chat ask). Dropped the molten/lava look (flow keyframes, blurred cap, heat pulse, `.low` class). Fill colour is now level-based via `heatColor(v)`: full = green `rgb(63,164,95)` → 50% = yellow `rgb(232,200,74)` → low = red `rgb(196,71,46)` (interpolated red→yellow→green, with a darker bottom stop for depth), set per-frame from `survDisplay` with a `.3s` CSS transition so it shifts smoothly. The smoothed `survDisplay` width easing from the previous edit is unchanged (molten rise/fall feel without the molten colour). Verified: syntax OK; 0 lava/low refs; heatColor green→yellow→red confirmed.
- 2026-06-28: **lava-lamp survival meter** (chat ask). Replaced the hard `.12s linear` width snap with a smoothed `survDisplay` value that eases toward `survival` each frame (`+= (survival-survDisplay)*min(1,dt*7)`), so the bar molten-rises when a clear refills it and drains buttery-smooth. The fill is now a warm molten gradient (gold→orange→deep-red, `background-size:280%`) that slowly drifts via a `lava` keyframe (8s alternate), with a blurred radial "molten cap" glow (`i::after`) fading at the leading edge. Adds a `.low` class under 25% that layers a `heat` brightness/saturation pulse for danger. Reduced-motion disables the flow animation (ease still applies). Verified syntax OK; old linear transition removed.
- 2026-06-28: **live Hint button + capped post-loss highlight** (chat ask). New "💡 Hint" button (in a `.btnrow` with New game): during active play (`!ended && mode==='idle'`) it runs `collectMoves()[0]` and pulses a gold ring on the single BEST available swap for ~2.6s (`liveHint`/`liveHintUntil`), cleared the moment the player makes a move (`trySwap`) or on new game. Refactored both the live hint and the game-over hint to a shared `drawHintPair(mv,pulse)`. The post-loss highlight is now **capped to only the best missed move** (`hintMoves[0]` — removed the up-to-8 loop); overlay text updated to "your best one is glowing". Verified: syntax OK; hint guarded to active play; single-best cap confirmed.
- 2026-06-28: **missed-swap hints on game-over + 3 fresh achievements** (chat ask). On survival death, `collectMoves()` lists EVERY legal swap still on the board (each tested via swap→findMatches→swap-back, recorded with its resulting maxRun, sorted best-first). The Game-Over overlay switches to a translucent `.ov.hints` scrim (aligned to the bottom) so the board shows through, and `draw()` pulses gold `drawHintRing` outlines on each missed swap's two cells + a connecting line — the BEST move brightest (`#ffd86a`), the rest dimmer, capped at 8. Stats line reads "you had N swaps left — glowing on the board". Achievements grew 11→**14**: 🌪 Cascade Storm (×7 combo), 🛡 Iron Will (survive 120s), ⌛ Last Stand (run out of time with 5+ moves still available — uses new `stats.diedWithMoves`, set + `checkAch()` in endGame). Reset `hintMoves`/`.ov.hints` in newGame; win-1000 overlay stays opaque (no hints). Verified: syntax OK; collectMoves returns moves sorted by maxRun desc; 14 achievements; all wiring present.
- 2026-06-28: shipped (chat ask: "bejeweled clone, dark theme no neon, achievement system, cascade multiplier for four-in-a-row; + szymcioooo wants a 1000-gem mode and a survival mode"). ORIGINAL match-3 (genre mechanic) with own faceted-gem art — not Bejeweled assets/name.
  - **Engine**: 8×8 canvas (DPR-scaled), 6 gem types each a distinct SHAPE + muted jewel colour (ruby diamond / sapphire circle / emerald rounded-square / amethyst hex / topaz triangle / quartz oval) — shape+colour = colourblind-friendly, faceted via gradient + highlight + specular. Offset-ease state machine: idle→swap→(clear→fall cascade)|revert. `findMatches` scans rows+cols for runs≥3 (returns set + count + maxRun); illegal swaps auto-revert; gravity `collapse` + refill with fall offsets; `hasMoves` auto-reshuffle when stuck; `fillNoMatch` seeds a match-free board. Verified in node: 0 initial matches, 4-run detected, 0 holes after collapse.
  - **Cascade multiplier (four-in-a-row)**: score = `count·10·combo·runMult` where runMult = ×2 for a 4-run, ×3 for 5+. A gold "FOUR/FIVE IN A ROW · ×N" flash pops over the board. `combo` increments per cascade step (maxCombo tracked).
  - **3 modes** (mode-btn bar): **Endless** (chase the cascade), **1000 Gems** (goal line counts down from 1,000; win overlay on reach → "Millennium" achievement), **Survival** (a heat meter drains over time, ramping faster the longer you last; each clear refills it `min(40, count·3.6)`; hits 0 → Game Over overlay with survived-time/gems/score). Survival meter shown only in that mode; play timer for the 1000-gem result.
  - **Achievements (11, localStorage `gem-cascade-ach-v1`)**: First Spark, Four-in-a-Row, Five Aligned, Chain Reaction (×3 combo), Cascade Master (×5), Gem Collector (100), Vault Keeper (500), Survivor (60s survival), Jeweler (5k), Crown Jewels (25k), Millennium (finish 1000-gem). Sidebar list (dim→gold on unlock) + gold toast + chime. `surv60` is checked each survival second; others on clear / endgame.
  - **Aesthetic**: dark, NO neon — slate `#14161b`, panels, **gold `#c9a44c`** accent, Cinzel display + Cormorant italic taglines + IBM Plex Mono HUD. Subtle audio (select blip, combo-pitched clear, achievement chime).
  - **WCAG**: canvas role=application + control-summary aria-label, role=group on modes, aria-live goal/overlay/toast, focus-visible gold, mode buttons ≥2.4rem, rem units, prefers-reduced-motion eases toast only.
  - Verified: syntax OK; engine + runMult unit-tested; 11 achievements; all mode/overlay/survival wiring present.

## issues
- Survival drain ramp (`SURV_DRAIN0 + survivalTime*0.16`) is a first pass — may need tuning for difficulty feel.
- maxCombo/bestMatch reset each new game, so combo/4-run achievements must be earned within one game (intended).
- No move-limit/score-attack timer mode yet (could add).

## todos
- ~~Special gems from 4/5 matches~~ SHIPPED in v2.0 (+ bombs from L/T, + rainbow).
- Score-attack (60s) mode; daily seed.
- Per-mode high scores in localStorage.
- Tune survival curve; add a "danger" pulse when the meter is low.
