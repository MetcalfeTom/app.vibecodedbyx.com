# The Button Museum — curated by a moth and a beetle

## log
- 2026-08-19: v1.0 — built per chat. Two curators (🦋 Madame Moth: glimmer/dainty/round; 🪲 Herr Beetle: sturdy/4-holes/grand), 6 escalating sort rules (material → size → holes → compound predicates), 12 procedural buttons/round (LCG rng: 6 materials w/ shine flags, 2/4 holes, dainty/medium/grand, round/square; SVG w/ material gradients + specular on shiny). ROUND GUARANTEE: reroll until 2≤matches≤10 so every round is a real decision (probed). Backstories: deterministic per button id (WHERE×QUIRK templates — "recovered from a pie, inexplicably. It hums faintly on Tuesdays."), ❞ tale buttons feed an aria-live story line. Scoring 8/correct +10 perfect; curator reactions in 4 sass tiers each; ranks Junior Docent→Keeper of the Drawer; best persisted. A11y: every button a real <button> with aria-pressed tray state + attribute-carrying labels ("dainty round pearl button with 2 holes, in the tray"), all lines aria-live, full keyboard. Zero external assets. Probe 15/15 (incl. rule-text consistency spot checks, perfect-sort arithmetic, exactly-wrong-sort punished, tale determinism). Screenshot reviewed.

## issues
- none yet.

## todos
- a third curator (Spider, wants ALL of them), button-of-the-day cabinet, if chat asks.
