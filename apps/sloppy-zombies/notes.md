# sloppy-zombies

## log
- 2026-05-02: **Big neon bottom HUD overhaul**. New `.bottom-hud` fixed-position bar with 3 high-contrast neon cells: WEAPON (magenta `#ff5ea1`, Audiowide), HEALTH % (mint `#7df09d`, Orbitron 900), KILLS (gold `#ffd766`, Orbitron 900). Each cell has a 2px colored border, matching outer glow + inner inset glow, semi-transparent dark backdrop with `backdrop-filter: blur(2px)`, `pointer-events:none` so clicks pass through to the canvas. Audiowide + Orbitron fonts added to the Google Fonts request. HEALTH cell flips to red `#ff4844` + 0.55s `bhPulse` brightness pulse + recolored border/glow when HP <30%. KILLS cell does a brief 1.04× `bump` scale on each `killZombie` for tactile feedback. `updateHUD` now also pushes `weapon.def.name`, computed `(player.hp / player.maxHp * 100)%`, and `kills` into the new cells. Top header pills kept for wave/gold/combo/loadout context. Mobile breakpoint shrinks bottom-HUD font/padding so the bar still fits below 760px.
- 2026-05-02: **2×2 starter grid + 3 unlockable extensions**. Run now boots with FOUR interconnected zones already unlocked: BUNKER (1,1,14,10) / OUTSKIRTS (17,1,11,10) / WAREHOUSE (1,13,14,9) / DOCKYARD (17,13,11,9). Each starter zone has `starter:true` flag. Three extensions remain locked: CITADEL (east, signature instakill) / CRATER (north, signature nuke) / DEPOT (south, signature armorboost) — unlock at waves 3/6/9 via the existing milestone path. New `seedStarterMap()` resets unlock state, clears corridors+pillars+windows, builds 4 starter corridors (3 wide, deterministic xFirst orientations) connecting the 2×2 in a closed loop, places 8 perimeter windows (2 per starter on outward-facing walls), and sprinkles pillar cover in non-bunker starters. Refactored the per-unlock pillar loop into a shared `sprinklePillars(zone, {count, cx, cy})` helper used by both `seedStarterMap` and `unlockNextZone`. Boot calls `seedStarterMap()` so the title-screen render shows the full grid behind the overlay; PLAY AGAIN re-seeds for fresh procedural pillar placements. Banner gates "NEW ZONE OPENS" on `zones.some(!unlocked)` so post-CITADEL-clear waves don't lie about the milestone.
- 2026-05-02: **Loop corridors + signature zone powerups + waist-high pillars + rocket buff + speed/armor boosts**. Zone unlock cadence 5→3 waves (per chat). Each unlock now builds **two** corridors: primary connects to most-recently-unlocked zone, secondary picks the most-distant unlocked zone (real loop) — when only the bunker exists the secondary takes the same destination but with `xFirst` flipped so the two L-paths form a parallel loop instead of overlapping. `buildCorridor(a, b, opts)` now accepts `{xFirst, wide}` to make this explicit. Each unlockable zone has a **signature** powerup (`outskirts:ammo`, `warehouse:double`, `dockyard:powerarmor`, `citadel:instakill`) dropped 1 tile right of the welcome crate so the zone has a tangible reward + identity. Banner now reads e.g. "WAREHOUSE UNLOCKED · two corridors · cover · DOUBLE INSIDE". **Waist-high pillars**: new `pillars[]` array of `{wx,wy,r:0.42}` cover obstacles. `pillarBlocks(wx,wy,r)` checks circle overlap, wired into `playerCanBeAt`. Zombies push out of pillars after their per-tick movement (radial displacement to pillar edge). Bullets + beams ignore pillars (shoot-over cover). 3-4 pillars spawn per zone unlock with 8-attempt placement loop avoiding the welcome cluster (1.6u from center) + window mouth (1.4u) + stacking (1.2u min between pillars). New `drawPillar` renders chunky stub with ellipse shadow + 3-tone body + lit top rim + corner rivets. Pillars + corridors reset on `startGame`. **Rocket buff**: speed 8→14, aoe.radius 1.8→2.6, aoe.dmg 80→110. **Speed boost & armor boost** added as new powerup drops (~14% each in pool). Speed boost gives +60% movement for 14s (`player.speedBoostUntil`); armor boost stacks +12 flat damage reduction for 14s. Both reset on game start. New ⇶ cyan + 🛡 light-blue pickup glyphs.
- 2026-05-02: **Star Power touch-kill + walkable drop placement + bigger UI**. New `nearestWalkable(wx, wy)` helper spirals outward from a target point at 0.5-tile steps and 16 angles per ring up to 6 tiles, returning the first walkable position; falls back to player position if nothing reachable. All `drops.push` sites for crates + powerups (boss kill 2 sites, regular kill 2 paths) now route through `nearestWalkable` so loot can never spawn inside a wall or in the void outside an unlocked zone. **Star Power** added on top of the existing Power Armor mode: contact with any zombie while powered now triggers an instant kill (8-particle gold/cream burst + ★ floater + 4-mag shake) instead of just no-op-bite. Bosses survive but eat 200 dmg + small knockback per touch + 8-mag shake. Replaces the inner power-armor bite no-op so it's checked first in the contact branch. Pickup floater now reads "STAR POWER · TOUCH KILLS" alongside the existing armor banner. **UI font scale-up across the entire HUD**: brand 30→44px, brand sub 10→13px, pill 10→14px + pill .num 20→30px, hint 9→13px + kbd 13px (was unsized), banner 54→78px + sub 16→24px, overlay h1 clamp 44-78→64-108, overlay sub 16→22, overlay h2 54→74, upgrade name 13→18 + desc 9→13 + foot 10→14, play button 15→22, endstats 11→16 + .v 26→36. Mobile breakpoint also bumped (brand 22→30, pill 9→12, banner 54px capped) so it remains accessible at small widths.
- 2026-05-02: **Procedural corridors + zone unlocks + Power Armor + plasma rebrand**. Replaced the per-wave +1 ROOM growth with a true zone-based map. New `zones[]` (bunker/outskirts/warehouse/dockyard/citadel) all start locked except bunker. Wave-clear at every 5th wave triggers `unlockNextZone()` which marks the next zone unlocked, calls `buildCorridor(prev, next)` to procedurally route an L-shaped 2-or-3-tile-wide hallway between zone centers (random x-then-y vs y-then-x order so each unlock looks different), pushes a perimeter window into the new zone, drops a tiered crate, recomputes `ROOM` AABB, and banner-announces. New `tileWalkable(tx, ty)` checks against unlocked zones + corridors; new `playerCanBeAt(wx, wy)` validates 4 corner extents around the player so they slide along inside corners through narrow corridors. Player movement clamp swapped from `clamp(ROOM)` to axis-separated `playerCanBeAt` walkability checks. Floor render iterates ROOM bounds but masks by `tileWalkable`, with corridor tiles tinted slightly cooler (`#181a16`/`#141612`) than zone interiors so corridors read as travel routes. Wall border now auto-traces — any non-walkable cell with a walkable neighbor renders as a wall tile, so zone perimeters + corridor edges all paint correctly without hardcoded outlines. Zombies use `worldWalkable` for inside-room check; their pathing still ignores walls (intentional — keeps the horde menacing). Run-restart locks all non-bunker zones + clears corridors so each new run gets fresh procedural hallways. **Power Armor pickup** added as a rare powerup roll (~6% of powerups). On pickup: 12s of full damage immunity (zombie bites no-op, even no i-frame flicker — golden aura is the visual cue) + access to STOMP (SPACE). `tryStomp()` deals 240 dmg in a 2.6-tile radius with linear falloff to 60% at the edge, knocks zombies radially outward by 0.9 tiles, spawns concentric expanding ring fx + 24 amber/cyan particle burst, 20-mag screen shake, 1s cooldown. Player render gains a golden aura ring (radial gradient pulsing at sin(t/90)) + a bright gold chestplate with rivets while powered. Drop sprite = ⚜ amber. **Plasma rebrand**: railgun.name → 'PLASMA RAILGUN', color → magenta-violet `#d680ff`. Beam fx halo recolored 24px violet halo + 4px white-hot core + magenta shadow blur.
- 2026-05-02: **RAILGUN heavy power weapon + sniper/assault/shotgun removed**. WEAPONS dict trimmed to 6: `rifle, raygun, rocket, drumshot, tesla, railgun`. Each fills a distinct niche (precision / pierce / AOE / rapid-spread / chain / heavy-beam). New `railgun`: dmg 220, mag 4, rof 1.30, range 18, `beam:{width:0.55}` flag. New `fireBeam(def, dmg)` cast: instant-hit ray from player along aim, hits every zombie whose perpendicular distance to the ray is within `beam.width + z.rWorld` and whose along-ray projection ≤ range. Full damage every hit (no falloff) — line-pierces an entire column of undead. New 'beam' fx: 22px outer cyan halo + 4px white core + muzzle puff at start, fades over 0.30s. New `sfx.railShot` (sub thump → 2.8kHz crack → low rumble), 18-mag screen shake on fire. Wall-buys + STARTER_RACK + BOX_POOL all updated; new wall-buy silhouettes for drumshot/raygun/railgun. Bootstrap initial slot is now rifle.
- 2026-05-02: **Combo multiplier + screen shake on kills**. New `combo` state `{count, peak, lastKillT}` bumps on every `killZombie`; multiplier = `1 + floor(count/3) * 0.5` capped at 5×. Decays to 1× if no kill within `COMBO_WINDOW_MS = 2500`. New `addKillPoints(base)` helper applies combo + double on top, returns `{total, mult}` so floaters render the multiplier (e.g. `+150 ×3`). Plain `addPoints` keeps multiplier-free for box/wall/repair purchases. New cyan COMBO HUD pill (`#7df0ff` border + glow) shows `count·×mult` while active, dims when streak is dead, scale-bumps `1.18` on each kill via `.bump` class. PEAK COMBO row added to death screen. **Screen shake**: `shake.mag` decayed `4.0/frame*60`, applied as random translate `(rand-0.5)*2*mag` inside the camera-zoom transform so the world rumbles but HUD/crosshair stay still. Per-event magnitudes: regular kill `3 + min(8, combo*0.4)`, boss kill 22, rocket detonate 14, nuke 26. Combo + shake reset at startGame.
- 2026-05-02: **Smooth camera follow + zoom + wall-buys + Tesla buff + weak-weapon cull**. ORIGIN_X/Y converted from `const` to `let`; new `updateCamera(dt)` lerps toward the position that anchors the player at `(W/2, H*0.55)` with k≈4.5. `snapCameraToPlayer()` hard-resets the camera at run start. `screenToWorldZoomed` inverse for mouse aim — applied in pointermove and per-frame so the crosshair stays world-locked through camera/zoom. **Camera zoom**: `RENDER_ZOOM = 1.4`, applied as `ctx.scale` around the camera anchor inside render() — the scale wraps every world-space draw (tiles, walls, sprites, fx) so the whole world AND every pixel-art sprite grows together. Crosshair + armor badge stay outside the transform so HUD reads at native resolution. **Wall-buys**: 5 chalk-outlined wall outlets at fixed spots around the room (shotgun 1500 / rifle 2500 / assault 3500 / sniper 4500 / rocket 6500). Walk near, press E to spend points and grant the weapon to the next empty slot (or replace active). Drawn pre-render with weapon-specific silhouette icon, "PRESS E" prompt when in range, golden glow + pulse when affordable, faded "X" overlay when purchased. `purchased` flag wipes at startGame so wall-buys re-arm each run. **Tesla buff**: dmg 42→70, hops 5→8, falloff 0.78→0.88 (hop 8 still ≈33 dmg), range 7→8.5, chain.range 4.2→5.0. **Weak-weapon cull**: PISTOL and SMG removed from the WEAPONS dict entirely — starter rack is now `[shotgun, assault, rifle, rocket, drumshot]` and BOX_POOL is `[shotgun, drumshot, rifle, assault, sniper, raygun, rocket]`. The wave-start "+24 reserve if pistol" refill is gone (ammo is infinite anyway). The smg wall-buy was replaced with a rocket wall-buy at 6500.
- 2026-05-02: **Bosses + per-wave map expansion + Tesla Coil + Drum Shotgun**. Map now grows on every wave clear: `+1` width every clear, `+1` height every other clear, capped at `MAP_MAX_W=30, MAP_MAX_H=22`. Border-side windows shift out to the new perimeter; a fresh boundary window punches through every 3 waves cycling through sides. Milestone districts at waves 5/10/15/20 fire a banner + bonus crate (`OUTSKIRTS / WAREHOUSE / DOCKYARD / CITADEL`). Replaced the old one-shot `unlockDistrict2` with `expandRoomOnClear()` called from the wave-clear branch; the `if (n >= 10) unlockDistrict2()` gate in `startWave` is gone. **Boss every 5 waves**: `spawnBoss()` triggers 1.8s into wave start (banner reads "WAVE N · BOSS"). Boss = `boss:true` zombie at `rWorld 0.72`, `hp 800 + wave×220`, slower `0.45 × 1.06^wave` speed but `biteDmg:22` (vs default 8). Renders at 2.1× scale via stacked ctx transform, hsl(290) violet body, always-visible 28px violet hp bar with "BOSS" label above. Kill reward: +1000 pts, 32-particle violet/amber shockwave, guaranteed legendary crate + bonus powerup, bossRoar sfx. **Tesla Coil** (ultra-rare drop, ~6% per box via new `ULTRA_POOL`): chain-lightning gun, no projectile. `fireChain()` picks nearest zombie in 7-tile range with 60° aim-cone bias, deals 42 dmg, then arcs to nearest unstruck zombie within `chain.range:4.2` for `chain.hops:5` jumps with `chain.falloff:0.78` damage decay. Renders 'lightning' fx kind: 2-pass jittered zigzag stroke between path nodes (cyan core + soft halo via shadowBlur). New `sfx.zap` (2400Hz square crackle + sub) + `sfx.bossRoar`. **Drum Shotgun** (regular box drop): 8-pellet rapid-fire shotgun, 0.18s rof (3.3× pump-shotgun), 7 dmg/pellet, auto mode. Box pool now `[smg, shotgun, drumshot, rifle, assault, sniper, raygun, rocket]`. Run-reset shrinks ROOM back to 14×10 + strips expansion windows.
- 2026-05-02: Created as a top-down wave-survival shooter (4 boarded windows, mystery box, 6 weapons, powerups).
- 2026-05-02: **Overhauled into isometric zombie RPG.** Gameplay logic now lives in WORLD-TILE coords (wx/wy on a 14×10 tile grid); all rendering goes through `worldToScreen()` for a 2:1 iso projection (TILE_W=64, TILE_H=32). Mouse cursor is unprojected back via `screenToWorld()` for aiming and click-targets. Z-sort all entities by `wx + wy` so closer ones paint on top — proper iso depth ordering for zombies / drops / windows / box / fx / bullets. Walls render as outer dark tile rings; boards still on the inner-room edges with the ID label. **Persistent meta-progression** added via localStorage (`sloppy-zombies-meta-v1` schema `{gold, bestWave, upgrades:{vit,fire,reload,luck}}`): GOLD persists across runs, 4 permanent upgrade tracks each with their own gold cost curve (`base × grow^lvl`):
  - VITALITY (max HP +20/lvl, max 5, base 80g, grow 1.7)
  - FIREPOWER (+10% bullet dmg/lvl, max 5, base 100g, grow 1.7)
  - STEADY HANDS (-10% reload/lvl, max 4, base 90g, grow 1.8)
  - SCAVENGER (+25% loot drop chance/lvl, max 4, base 110g, grow 1.8)

  Title screen + death card both render the upgrade store as 4 cards (cost / current level / max). Buying decrements gold + bumps the level + saves to localStorage. **Loot system**: 18% × luckMult drop chance per kill spawns a rarity-tinted CRATE (4 tiers: common 60%, rare 25%, epic 11%, legend 4%); 6% × luckMult drops a powerup (max ammo / instakill / double / nuke). Crates require E to open and yield gold (`20 × rarity.mult`), then 50% roll for either a fresh weapon (mystery-box pool) or an armor piece. Equipping a worse-tier armor refunds a few gold and skips the swap. **Equipment slots**: weapon (mystery-box / loot rolls) + armor (loot only). Armor: damage-reduction passive (`8 × rarity.mult`, capped to ≥1 dmg incoming). Armor badge rendered top-left of canvas with rarity-coloured border + name. **Weapons**: 6-piece arsenal (PISTOL / SMG / SHOTGUN-pellets-8 / RIFLE / SNIPER / RAY GUN-pierce-2). All damage now scales with FIREPOWER upgrade: `dmg × (1 + 0.10 × fireLvl)`. Reload time scales with STEADY HANDS: `reload × max(0.35, 1 - 0.10 × reloadLvl)`. **Mystery box** still at 950 pts; works the same. **Wave system** unchanged in shape: 4 + ⌊wave×1.6⌋ zombies, ramping cadence, banner on start/clear, +200 wave-clear bonus, pistol +24 reserve at wave start. Zombie HP `60 + wave×28`; zombie speed `1.6 + min(2.0, wave×0.10)` tiles/sec. **Aesthetic**: dirt-tile floor with checker stagger, dark wall tile ring, sickly hsl(90–120) zombies with red dot eyes, drop-shadow ellipses on every entity, rarity-coloured glow on crates, gold/blood neon UI accents. Bungee Shade title, Special Elite body, VT323 numerals, Silkscreen meta. CRT scanline overlay + custom red crosshair (cursor:none). 14 Web Audio synth sfx including a coin pickup. Pollinations OG, 🧟 favicon.

## issues
- Iso depth-sort uses simple `wx + wy` per entity; for very close pieces this can flicker as they swap z-order tick-to-tick. A stable secondary key (e.g., entity id) would lock it.
- Crates and powerups both go in `drops[]` — fine but the kind-switch in `applyDrop`/`openCrate` is split. Refactor into a uniform pickup interface later.
- WASD → world mapping uses iso-natural diagonals (`W = (-1,-1)` world). Players who expect strict cardinal world movement will need a beat to adapt — that's the standard iso-shooter compromise.
- Armor only has one stat (damage reduction). Could add a stamina/move-speed mod or a +ammo bonus in later iterations.
- Upgrade cost curve is exponential — at L5 vit the cost is 80×1.7⁴≈668g, which takes a few good runs. Tunable.
- No mute toggle, no localStorage clear button (testing required wiping it manually).

## todos
- More weapon tiers and gear types (helm / boots / trinket).
- Item rarity-affixed weapon stats (e.g., "Epic SMG: +25% mag size").
- Pack-a-Punch-style upgrade station that consumes points + gold.
- Mobile virtual joystick.
- High-score leaderboard via Supabase.
- Interactive armor slot UI (compare on hover, manually equip from drops).
- Save current loadout between runs (right now armor + weapon reset on death).
- Mute toggle + reset-vault button.
- Boss variants per district (necromancer summons / charger / spitter).
- More ultra-rare weapons (railgun, flamethrower, freeze ray).
