# bixonimania · notes

## log
- 2026-07-30: v2 — **THE GRAND UNIFICATION** (chat: "combine the two Bixonimania apps alert and suite into a single app called bixonimania, merge all features into one page"). This app is now the canonical AHU experience: three instruments behind a chapter nav, each keeping its ORIGINAL aesthetic via body-class swap (ch-quiz parchment serif / ch-scan dark mono / ch-err magenta BSOD; masthead becomes BIXONIMANIA.SYS in Ⅲ). Ⅰ DIAGNOSTIC: the 9-question always-positive quiz + gilt certificate (v1 code, intact). Ⅱ FIELD SCAN: consent-gated 11s scan → magenta panic overlay + two-tone klaxon + telemetry stream → acknowledge silences all oscillators AND re-arms the scanner (repeatable now) + cross-references instrument Ⅰ. Ⅲ CRITICAL ERROR: :B screen, >100% counter, decorative QR, face scan (camera-free, asserted), mayo fix, transporter. Shared single AudioContext + de-clicked ding/voice helpers (was 2 separate contexts). Zero duplicate ids across the merge (checked). Standalone bixonimania-alert + bixonimania-suite apps LEFT WORKING per never-delete rule — this page is the canonical entry. SUITE 13/13 covering all three instruments + chapter switching.

## issues
- Three fonts × three aesthetics = heavier page (~6 font families). Acceptable for the flagship.

## todos
- AHU faculty page (Womple bios); instrument Ⅳ someday (marc's call).
