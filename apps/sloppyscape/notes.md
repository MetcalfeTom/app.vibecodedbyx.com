# sloppyscape (SloppyScape)

## log
- 2026-08-06 (LATER): **EMERGENCY REVERT to db23bda99 (polish pass)** — live user report: "blue box with unclickable UI". The castle-redesign (993ef0b) and equipment/cave (90d2ccf) commits are REVERTED from the live app but preserved in git history. Both passed the 81-check stub harness AND a strict-DOM harness (null for unknown ids, 3 frames run) — the failure is browser-only and unreproduced in sandbox. Leading suspects for forensics before re-landing: (1) #sailov overlay stuck `.on` (its bg is exactly the reported blue and it eats all pointer events) if setZone throws mid-transition in a real browser; (2) a real-canvas-only throw (e.g. negative-radius arc/ellipse browsers reject but the Proxy ctx swallows) inside the new tile branches or furniture renderers, leaving only the ocean fill each frame; (3) something in the ~40KB of new draw code the proxy can't falsify. DO NOT re-land by re-applying blind — reproduce in a real browser first (ask chat to test a /sloppyscape-beta/ copy, or bisect the two commits by shipping the redesign alone).
- 2026-08-06: **EQUIPMENT + THE CAVE** (chat ask: "equipment tab next to backpack with right-click Equip, visible helmet slot, and ladder/cave on tutorial island with Smithing/Melee tutors").
  - **Equipment**: side panel gained a 🎒 Pack / ⚔️ Gear tab strip. Gear view = 3×2 slot grid — helmet slot front and centre, weapon below, body+shield visible-but-locked ("soon"). `save.equip {head,weapon}` (migrated), items opt in via `ITEMS[id].equip` (stick→weapon, bronze_helm→head). Right-click Equip in the pack (auto-flips to the Gear tab to show the result), click or right-click a slot to Unequip (blocked politely on a full pack, swap-in-place supported). The slap gate + Salt's stick re-gift honour equipped weapons. **The helm renders ON the player sprite** — bronze cap band drawn over the head on all facings.
  - **The cave** (3rd zone, proving the ZONES architecture generalizes): random-walk-carved cavern under Lummy (SEED+123, ~130 floor tiles, void tile 9 = unwalkable rock, floor 10), reached via a hatch-ladder east of the castle ↔ up-ladder below, with fade transitions and a first-visit intro. Contents: 5 wall-hugging mining rocks (3 copper, 2 iron — existing Mining just works), **Torva the Smith** (red-bun smith apron; first talk gifts the Bronze helm; honest dialogue that furnace+anvil are coming) and **Sergeant Harl** (grizzled melee tutor who explains the Accuracy/Aggression/Sturdiness trio), plus a **training dummy** next to Harl: infinite HP, wobbles on hit, feeds Accuracy (+1 Aggression per hit, no kill) through the normal fight action.
  - **Keeper system refactor**: talk config (lines/metKey/greet/onFirst gift callback/zone) now lives ON the keeper object — keeperTalk(n)/requestKeeperTalk(n) single-arg, keeperAt zone-aware per keeper. Marta/Bram migrated to it; tutors are pure config.
  - **Harness: 81 checks green** — equip/unequip round-trip, equipped-stick-satisfies-slap-gate with an empty pack, ladder reachability, climb both ways, cave rock counts, tutor reachability + helm gift, dummy xp + immortality, cave draw frame. (Two mid-build regressions caught by the harness itself: tutor keepers + sheets lost to an aborted edit batch — re-applied.)
- 2026-08-06: **LUMMY REDESIGN — castle at center, roofless walkable interiors, shopkeepers** (two stacked chat asks: "top-down roofless interiors, start with the kitchen layout" + "castle at center, surrounding houses"). Lummy went full Lumbridge:
  - **Building system**: `stampRoom(x0,y0,w,h,floorTile,wallKind,door)` — floors + perimeter wall props with exactly one doorway; `furnish()` places blocked furniture props. Walls are top-down extruded blocks (timber `wall` / stone `swall`, face+top+seams, 3 variants), y-sorted so you see INTO every room. New tiles: 6 wood plank floor, 7 castle flagstone, 8 red carpet w/ gold border.
  - **The castle** (11×9 stone, south gate aligned with the dock path): courtyard with the eternal campfire, NW **castle kitchen** (wood floor, stone partition + doorway, working `range` you can Cook at exactly like a fire, shelf/barrel/counter with drawn bread loaves) staffed by **Marta the Cook**, and a NE throne nook (4-tile carpet runner + throne; sitting: "Briefly, you understand power. Then you get up.").
  - **Shopkeepers + shops**: `makeKeeperSheet(cfg)` parameterized LPC-layout sprite generator (Marta: apron + hair bun + rust dress; Bram: green tunic + trader beard, proud bald crown). Shared keeper wander/talk/walk-up machinery (`keeperUpdate/keeperTalk/requestKeeperTalk/pendingKeeper`). Right-click → **Trade** opens a buy menu (`SHOPS`/`openShop`/`buyWare`, charge only after addItem succeeds): Marta sells raw shrimp 4🪙 / bread 6🪙 / tinderbox 15🪙, Bram sells stick 2🪙 / logs 3🪙 / net 25🪙 (a coin-sink alternative to Ned's free net). New `bread` item.
  - **Surrounding houses**: Bram's Trading Post (5×4 timber, counter/shelf/barrel) east of the walls; the Quiet House (4×4, bed + table, nobody answers) SW. Cottage sprites fully retired.
  - **Harness caught a real bug**: the tree-scatter loop capped on TOTAL prop count, which ~70 wall props now exceed — zero trees would have spawned, softlocking the hull quest. Fixed with a dedicated scatter counter. 64 checks green: castle wall count, exactly-one-gate perimeter scan, BFS reachability spawn→kitchen/Bram/beach, shop buy/insufficient-funds/no-charge-on-full-pack, cooking at the range, floors/flagstones/carpet counts, plus all prior scenarios.
- 2026-08-06: **polish pass** (chat: "anchor rat's y-bob, add ground shadow, hover tooltips on skills"). Rat's bob is no longer a vertical float — it's a breathing scale anchored at the feet (paws stay planted; hit-squash also pivots at the ground now), with a darker grounded shadow that swells in sync with the breath. Skills tiles swapped the native `title` for styled hover/focus tooltips: name + level, xp + next-level threshold (or "maxed" at 50), and a mini progress bar; tiles are tabbable (tabindex 0, aria-label) so keyboard + mobile tap get the tooltip too. Harness still 39/39.
- 2026-08-06: **Combat → Accuracy 🎯 / Aggression 💢 / Sturdiness 🛡️ + ghost-rat fix** (chat asks). (1) The single ⚔️ Combat skill (`mh`) split into three: **Accuracy** drives hit chance (`0.75 + 0.01/lvl`, cap 0.95) and slap speed, **Aggression** drives damage (`+1 per 10 lvls`), **Sturdiness** is the survival stat (kills grant +6; will feed HP when the guild lands). Xp flow per slap: hit = +2 acc & +dmg×2 agg, whiff = +1 acc ("you learn from the whiff"), kill = +6 stu. Migration splits any banked `mh` xp 34/33/33 into acc/agg/stu and deletes the key — harness-verified. Guild tier gates should now read Accuracy (or trio avg) — spec note updated. (2) **Ghost rat fixed**: the 🐀 emoji rendered pale/antialiased against the crisp pixel art and read as translucent. Replaced with a hand-drawn pixel rat (grey-brown body + highlight, pink ears/tail/nose, feet, black eye) with movement-facing flip + recoil-facing toward the slapper, squish-flash kept, plus an explicit `ctx.globalAlpha=1` guard so nothing upstream can ever ghost it. Harness now 39 checks, all green.
- 2026-08-06: **LUMMY — the starting town + tutorial beach + Combat skill** (two chat asks merged: "build Lummy as a starting town with a campfire, a path to the dock, and a boat that unlocks the main island voyage" + "tutorial beach with a rat you can slap, a stick in the sand, and a combat xp counter"). Guild hut remains ON HOLD per chat.
  - **Two-zone architecture**: globals (map/W/H/blocked/props/fishSpots) become swappable via `setZone(z, entry)` over a `ZONES` registry; the main island is snapshotted untouched (same SEED, zero regen), Lummy is a new 44×44 seeded island (SEED+77, radial falloff + 16-spoke angular wobble). All zone-crossing state handled: ground items are zone-tagged (`g.z`, migrated), NPC updates/draws/click-hitboxes zone-gated, butterflies stay mainland, chat/inventory/skills/quests are global.
  - **Lummy town**: 7×7 cleared square, permanent campfire (cookable), 3 weathered thatch cottages, ~8 trees/rocks on the outskirts, dirt path (new tile 4) running south from the square to a 3-plank dock (new tile 5, walkable over water), rowboat moored at the end. Static fishing spot beside the dock (never roams — `s.static` guard in the roam loop).
  - **Old Salt** (boatman, 3rd human NPC): saltSheet = storm coat + magnificent white beard + red knit cap w/ pom. First talk gifts a Bronze axe; hull quest accepts logs in PARTIAL deliveries (`save.hullLogs` 0→5) → `save.boatFixed` → boat becomes sailable. Re-gifts axe if lost pre-quest, re-gifts the stick post-quest if none in inventory or on Lummy ground. 5 rotating sea lines.
  - **The voyage**: click boat → walk to plank → full-screen fade (#sailov) → `setZone` to the other world. Mainland got its own 2-plank dock + return boat (placed at the shore tile nearest the island centre, before fishSpots init so spots can't collide). First mainland arrival replays the classic tutorial welcome (`save.sawMain`). Broken boat: examine/inspect flavor + Salt hint; drawn holed + listing until fixed, rocking + masted after.
  - **Tutorial beach**: a Stick (🪄 "Not magic. Probably.") seeded ONCE as a real zone-tagged ground item on the sand near the dock (`save.stickSeeded`) — found/taken via the normal ground-item flow, outlines label it. **Beach Rat**: 6hp wanderer near the stick; slap requires the stick (tutorial gate message), left-click or right-click "Slap Beach Rat", 1.2s swings via the existing action system (`fight` type), 85% hit for 1-2, red splats / blue 0s, freeze-while-targeted, squish-flash on hit, hp bar + name when hurt, respawns 12-20s with a smug log line. **Combat skill** (`mh` ⚔️) added to SKILLS — 5th tile in the panel IS the combat xp counter; kills grant 10xp through the normal grantXp pipeline (floats, level-ups, jingle). Skill keyed `mh` so the on-hold Monster Hunter Guild tier-gating plugs straight into it.
  - **Migrations**: veterans (existing saves) boot ashore on main with boatFixed=true and everything intact; fresh saves start in Lummy. `save.zone/boatFixed/hullLogs/skills.mh/stickSeeded` + ground `z` tags.
  - **Verified**: syntax, id cross-check, and a 35-check stub-DOM harness — fresh boot into Lummy (world contents, walkability, stick seed), Salt quest with partial log delivery, both voyage directions land on walkable dock tiles, slap-gate → kill → xp → respawn, legacy-save migration + zone-filtered ground items, and a full draw() frame in BOTH zones.
- 2026-07-03: **System tab + channel re-routing** (chat ask). Channels are now explicit `data-ch` per line (`log(msg, colorClass, channel)`) — color classes are presentation-only. New **System** tab (2nd, after All) owns the blue login/boot/error lines (sys default). **Level-ups and drops/pickups route to Player** (level-up keeps its green styling but lives in Player). World repurposed for world events: fishing-spot moves, fires burning out, quest ready/complete callouts. Defaults otherwise: ex→Local, me→Player, tim→NPC. Tab whitelist + persistence updated (6 tabs). Verified: syntax + stub boot.
- 2026-07-03: **inisso's 3 tester bugs fixed**. (1) *Ghost fake-tabs element purged*: the header bar was still cosplaying as a tab row ('⠿ All · Game', retitled per tab — a duplicate of the real tabs). Now a plain '⠿ chat log' drag handle; retitle code deleted. (2) *Drag/resize hitbox vs tab bar*: the north resize strip + NE/NW corners overlapped the tab strip's top pixels, eating clicks. North handles now overhang OUTWARD only (top:-6/-7px, inset ≤1px, side margins widened) and `.ctabs` got z-index above handles. (3) *Height lock on tab switch*: switching filters changed visible line count and let an auto-height panel shrink/grow. `setChatTab` now freezes the panel's current pixel height before filtering — only the resize handles can change size afterwards. Verified: syntax + stub boot.
- 2026-07-03: **chat channel tabs + font bump** (chat ask — they said 'four tabs' then listed five; shipped all five). Tab strip (role=tablist) above the chat header: **All / World / Local / Player / NPC**. Channel mapping rides the existing log classes: sys→World (system/announcements, now also the default for unclassed lines), ex→Local (examines, ambient mutters, drop flavor), me→Player (your actions), tim→NPC (Tim/Ned dialogue + quest callouts). Filtering is pure CSS — `.log[data-f=…] > div:not(.class){display:none}` — so history re-filters instantly and hidden channels keep accumulating. Single-select toggle (RS-style), aria-selected, header bar retitles ('⠿ NPC · Game'), active tab persisted (`save.chatTab`). Log + tab font bumped exactly +3px via `calc(.78rem + 3px)`. Verified: syntax + stub boot (chatTab persists).
- 2026-07-03: **resizable chat panel** (chat ask; inventory intentionally stays fixed-size). 8 invisible resize handles (n/s/e/w edges + 4 corners, proper directional cursors, SE corner gets a visible grip notch) appended to `.chatbox`. Pointer-capture drag: E/S grow width/height, W/N grow while moving left/top (opposite edge pinned when clamped). Min 200×110, max 92vw×70vh, `save.chatSize` + updated `save.chatPos` persisted on release. Chatbox switched to flex column so the log stretches with the panel height. Coexists with header-drag (handles stopPropagation). Verified: syntax + stub boot.
- 2026-07-03: **draggable panels + click-to-lift inventory reordering** (inisso asks ×2: panel drag first, slot swapping layered on; follow-up added the chat panel). (1) `makeDraggable(panel, bar, saveKey)`: pointer-capture drag by the ⠿ header bars of BOTH the side/inventory panel (`save.panelPos`) and the chatbox (`save.chatPos`) — viewport-clamped, position persisted, re-clamped on resize, .dragging opacity feedback, grab/grabbing cursors. (2) **Inventory went sparse**: `save.inv` is now a fixed 28-slot array where removals null the slot instead of compacting — slot POSITIONS are stable. `addItem` fills `firstFreeSlot()`; legacy compact saves work as-is (indexes 0..n). **Click-to-lift**: left-click an item lifts it (gold outline, raised, body.lifting makes empty cells dashed/clickable targets), click an empty slot to place, another item to SWAP, same cell to put back. Examine moved fully to right-click (menu unchanged: Light fire/Examine/Drop — drop cancels lift). Lift auto-cancels if the lifted stack vanishes (e.g. consumed). aria-labels narrate lift state. Verified: syntax + stub boot w/ legacy inventory.
- 2026-07-03: **drop restrictions lifted + yellow ground-item outlines** (chat asks ×2, second specified 'in settings'). (1) Tools (axe/tinderbox/net) are now droppable like everything else. **Anti-softlock**: Tim re-gifts a lost axe/tinderbox ('You LOST it? …I keep spares.') and Ned re-gifts a lost net on talk when the tool is missing from inventory — no permanent lockout. (2) **RuneLite-style ground outlines**: yellow box + item-name label (×n included) over every ground drop; toggle lives in the side panel settings row ('🟡 item outlines: ON/OFF', aria-pressed) + `H` hotkey (input-guarded); `save.outlines` persisted, defaults ON, migrated for old saves. Verified: syntax + stub boot with legacy save (outlines→true, ground preserved).
- 2026-07-03: **world drops** (chat ask: "items land on the ground as visible sprites instead of deleting, with localStorage persistence"). New `save.ground[]` (persisted; legacy saves auto-migrated). **Drop** (inventory right-click): item leaves the pack and lands at your feet — same-item stacks merge (×n badge), different-item tile conflicts nudge to an adjacent free walkable tile (else refunded with 'No room on the ground here'), world cap 60 stacks (oldest fades). **Render**: emoji sprite + shadow flat on the tile, drawn under all standing entities, ×n count badge. **Pick up**: left-click a ground item → RS-style walk-to-tile-then-Take (`pendingPickup`, cancelled by walking elsewhere); right-click → Take/Examine/Walk here menu; backpack-full leaves it on the ground. Undroppable tools (axe/tinderbox/net) unchanged. Verified: syntax OK + stub-DOM boot with a LEGACY save (pre-ground) migrates cleanly, inv/skills preserved.
- 2026-07-03: **fishing pond + Fisherman Ned** (chat ask: "small fishing pond with a fishing spot NPC that gives you a net"). **Pond**: `carvePond()` finds a 5×5 all-grass patch 9–18 tiles from island centre and carves a 7-tile organic water blob (props on those tiles removed + unblocked); `pondWater[]` recorded. Pond gets a dedicated fishing spot (`pond:true`) that roams ONLY pond water (`relocateSpot` pool switch); pond fishing is slightly faster (2.6s vs 3.2s cast). **Fisherman Ned**: second human NPC (own `nedSheet`: blue tunic, waders, wide straw-brim hat), anchored at the pond, 2-tile lazy patrol at 48px/s. First talk grants 🥅 **Small net** (`save.gotNet`, undroppable like axe/tinderbox) + 5 rotating fishing-wisdom lines; Examine: "He smells of pond and patience." **Fishing now requires the net** ('Fisherman Ned at the pond has spares') — same tutorial loop as Tim's axe. Talk-to/Examine on left-click + right-click menu; `pendingNed` walk-up-then-talk. Welcome line added. Verified: syntax + stub-DOM boot.
- 2026-07-03: **firemaking tool gates enforced** (chat spec): axe-to-chop was already gated; added — tinderbox hard-required in `lightFire()` (not just menu-hidden), **2s strike action** (progress bar; one attempt per 2s = the cooldown, no spam), **failure chance 40% @ L1 → 5% @ L99** linear (`fmFailChance`, Cooking = firemaking proxy skill; failed strikes keep the log and auto-retry, escapable by walking), **fire duration 60s @ L1 → 120s @ L99** (`fmFireLife`, message reports actual seconds). Yew-log fires pay 14xp vs 6. Cooking burn chance aligned to the same 40→5% curve. Verified: syntax OK; curve endpoints L1=40%/60s, L50=22.5%/90s, L99=5%/120s.
- 2026-07-03: **Tim is HUMAN now + starter kit + firemaking** (chat ask: "make Tutorial Tim a human NPC who gives the player a bronze axe and tinderbox"). Sheet redrawn (`drawTimSheet`, same gobSheet canvas/layout): human skin, brown robe + belt, grey beard per facing — and the tiny red hat SURVIVES (continuity; dialogue acknowledges the career change). **First talk** grants 🪓 Bronze axe + 🔥 Tinderbox once (`save.gotStarter`) with fanfare; axe/tinderbox can't be dropped. **Chopping now requires the axe** ("Tutorial Tim hands them out to new faces") — natural tutorial loop. **Tinderbox firemaking**: right-click logs/yew logs → "Light fire" → consumes the log, spawns a temporary campfire prop at your feet (`temp:true, life:60s`, cookable like the main fire, +6 Cooking xp, auto step-off, burns out mid-cook with a message). Tim's examine/welcome/dialogue updated. Verified: syntax + stub-DOM boot.
  - Chat also asked to "clear my crow-related backlog" — audited every notes.md + app dir: **no crow-related items exist anywhere** (grep only hits crowd/crown/crowded). Reported honestly; nothing to clear.
- 2026-07-03: **v2 — GAMEPLAY FLOOD** (chat ask: "flood SloppyScape with real gameplay systems — fishing, woodcutting, mining, cooking, a quest board, and anything else that makes the island feel alive").
  - **4 skills** (Woodcut 🪓 / Mining ⛏️ / Fishing 🎣 / Cooking 🍳): xp → level via `15·L^1.8` thresholds (cap 50). Levels shorten action durations (−3%/lvl, floor 55%) and gate content (yews need WC5, iron rocks Mining5, trout Fishing5, burn chance falls with Cooking). Green xp-drop floaters + gold LEVEL UP floater + chat line + jingle.
  - **Action system**: `goDo(type,target)` walks adjacent (`pendingAction`, fish range 1.8) then `startAction` → timed progress bar over the player's head → `finishAction` rolls loot/xp and **auto-repeats** RS-style (yews 70% to continue, fishing until spot moves/backpack full, cooking until raw fish gone; normal trees/rocks deplete). Moving cancels. Trees→stumps (15s / yew 28s), rocks→rubble (20s / iron 30s), both respawn via per-prop `down/respawnT`.
  - **Inventory**: 28-slot grid in a new right-side panel (RS parchment), emoji icons + stack counts, click=examine, right-click=in-game Drop/Examine menu (contextmenu captured). Items: logs, yew logs, copper/iron ore, raw/cooked shrimp+trout, burnt fish ("You monster. It had a family."). Mobile: 🎒 tab toggles the panel.
  - **Fishing spots**: 4 roaming ripple rings on shore-adjacent water (double expanding circles + glint), relocate every 35–75s (interrupts your session: "The fish move on. So must you.").
  - **Campfire** (near spawn, animated flames + drifting smoke): Cook action, burn chance `max(5%, 40% − 4%/lvl)`.
  - **Quest board** (near spawn, wooden posts + pinned notes): 3 active quests from 6 templates (chop/yew/mine/fish/cook/talk-to-Tim), progress bars, Claim → 🪙 coins + bonus xp + fanfare, auto-replaced with a fresh quest. `questEvent()` hooks fire from every skill action + timTalk.
  - **Alive extras**: 12 drifting butterflies (2 palettes, wall-bounce), fire smoke, spot glints. Synth SFX: chop thud+crack, mining clink, splash, sizzle, level jingle, quest fanfare (AudioContext unlocked on first action).
  - **Persistence**: skills/inventory/coins/quests in `localStorage['sloppyscape-save-v1']`.
  - Verified: syntax OK, stub-DOM boot OK (quests roll + save persists in mocked localStorage).
- 2026-07-03: **Tutorial Tim + yew grove + total contextmenu capture** (chat asks). (1) **Yew grove**: densest tree cluster (max neighbors within ±4 tiles) converted to `kind:'yew'` — darker/flat-topped render, own examine lines ("worth 175 xp somewhere else"). (2) **Tutorial Tim**: goblin NPC on his own procedural LPC-layout sheet (`gobSheet`, short body via +12px offset, big ears, teeth, and the load-bearing tiny red hat w/ gold pom on ALL facings). Patrols within ±5 tiles of the grove anchor: idle 1.2–4.4s → random walkable target → BFS path at 55px/s, path cap 26. Cyan name tag. **Talk-to**: left-click Tim or right-click → 'Talk-to Tutorial Tim'; if >1.6 tiles away, pathfinds to nearest adjacent tile and delivers on arrival (`pendingTalk`, cleared if you click elsewhere). 7 rotating tutorial-parody lines (green .tim chat color) + face-the-player on talk; Examine: "A goblin. The hat is doing a lot of work." Ambient mutters every ~9–17s at 55% when player ≤5 tiles. (3) **contextmenu never escapes** (follow-up ask): preventDefault document-wide + explicitly on the #ctx container (with stopPropagation) — chatbox/HUD/menu right-clicks all swallowed. Verified: syntax + stub-DOM boot.
- 2026-07-03 (inisso feedback): **world/player scale + right-click menu**. (1) Player now drawn at 1:1 sheet scale (32×48, was ×1.5) with smaller shadow + retuned name-tag offset; trees roughly doubled (32px trunk, 23px canopy + underlayer — ~2× player height) and rocks widened to ~30px, bigger shadows. World reads big, player reads small, RS-correct. (2) **RS-style right-click context menu**: canvas contextmenu → preventDefault + beige/brown 'Choose Option' menu at cursor (viewport-clamped) with Examine <b>Tree/Rock</b> / Walk here / Cancel (Walk here only on plain tiles). Input refactored into shared walkTo()/examineProp()/tileAt(); left-click path unchanged; menu closes on outside press, Esc, blur; role=menu/menuitem. Verified: syntax + stub-DOM boot.
- 2026-07-03: shipped v1 (chat ask: "2D top-down RuneScape-like, blank green island the player spawns on, using Universal LPC Spritesheet Generator for character sprites"). **LPC caveat**: external asset downloads aren't possible from this sandbox (and apps must be self-contained), so the character sheet is **procedurally generated at boot in the LPC walkcycle LAYOUT** — 4 rows (up/left/down/right, LPC row order) × 4 frames, 32×48 per frame, drawn to an offscreen canvas (skin/hair/green tunic/pants/boots, leg+arm swing per frame, per-facing hair/eyes). Swapping in a real LPC PNG later = replace `sheet` and the row/frame constants — the renderer already speaks that format.
  - **Island**: 72×72 tiles (32px), deterministic (seeded mulberry32 + value noise + radial falloff): water/sand/grass/dark-grass. ~180 props (trees ×3 variants, rocks ×2) scattered on grass, blocking.
  - **RuneScape essentials**: click-to-walk with **yellow X marker** (red X when unreachable), BFS pathfinding over the walkable grid, path-following at 110px/s with 4-dir facing + 4-frame walk anim, camera lerp-follow, y-sorted rendering (walk behind trees), gold name tag ("Adventurer"), **examine**: clicking a tree/rock walks adjacent + prints RS-flavored examine text ("It's a rock. Geology's greatest hit.") into an RS-style parchment **chatbox** (All·Game bar, scrolling log, 40-line cap). Water shimmer + grass speckle. "You can't walk on water. Yet."
  - Mobile: tap = walk (pointerdown), no extra controls needed. Pixelify Sans UI, pixelated rendering.
  - WCAG: canvas role=application with control summary, chatbox role=log aria-live=polite.
  - Verified: JS syntax OK + boots without throwing in a stubbed-DOM vm run.

## issues
- BFS is plain queue (uniform cost) — fine at 72×72; upgrade to A* if the map grows.
- Character sheet is LPC-format but not LPC-sourced (sandbox can't download the generator's assets). If a real spritesheet ever lands in the repo, drop-in swap.
- Player POSITION isn't persisted (respawn at island centre on reload) — everything else is (skills/inv/coins/quests/ground/panels, verified against save.* fields 2026-08-06). Add `save.pos` if chat minds.

## todos (v2 candidates — ask chat)
- Name your adventurer (localStorage).
- Multiplayer presence via Supabase Realtime (see other players walk).
- Music: soft harp loop (Web Audio).
- A single quest with a yellow ! NPC.
- **Monster Hunter Guild (chat spec 2026-08-06 — ON HOLD per chat: "hold the guild hut, build Lummy first")**. Design banked from a full code read-through, ready to build: combat skills now exist as Accuracy/Aggression/Sturdiness (2026-08-06) — gate tiers on Accuracy (or trio average) instead of the old single `mh`; 4 monster tiers gated by MH level (T1 Cave Rat lvl1 / T2 Shore Crab lvl5 / T3 Gloom Boar lvl12 / T4 Marsh Drake lvl20), emoji-sprite wanderers with ned-style anchors (rats near spawn, crabs on sand, boars in dark grass, drakes by the pond), peaceful until attacked, chase ≤6.5 tiles then reset; combat rides the existing action system (`startAction('fight',m)`, swing 1.5s, hit chance ~0.7+lvl·0.01−tier·0.08, auto-repeat, monster retaliates every 2.2s); player HP `10+(mhLvl−1)·2` with slow regen, 0 HP = flee to spawn (no item loss); drops = bones + tier·3 coins; weathered guild hut (3-wide blocked footprint, moss, horned skull, carveGuild patch-finder like carvePond) + Huntmaster Grix NPC (nedSheet clone: leather, grey hair, eyepatch, gives rusty_sword, re-gifts, war-story lines) + separate hunt board (`save.hunts`, HUNT_DEFS kill_rat/crab/boar/drake/kill_any/talk_grix, only rolls unlocked tiers, dark-red claim UI, own overlay #huntov).
- **Task economy (inisso's full spec, 2026-08-06)** — build alongside/into the quest system, slayer-master style:
  - **Lowest-skill targeting**: assigned tasks always target the player's current LOWEST skill (tie-break random among tied), so the system steers you toward neglected content — deliberately complements the quest board's random rolls. Task templates can reuse `questEvent()` hooks (chop/mine/fish/cook already instrumented).
  - **Skip coins**: separate currency from 🪙 coins, earned per completed task (`save.skipCoins`). Amount can scale with task size.
  - **Refresh economy**: ONE free task reroll per real-time hour (persist `save.lastFreeRefresh` timestamp, survives reload); further rerolls in that hour cost skip coins — creates the spend-or-wait decision loop.
  - Natural home: a task-master NPC (third human after Tim/Ned) or a new tab on the quest board; persistence via `save.task` alongside `save.quests`.
