# mochi-cat

## log
- 2026-05-01: **Lo-fi synth loop + large pixelated rain window.** Three coupled chat asks landed in one pass: (1) catchy lo-fi melody that loops, (2) lo-fi window view with rainy weather, (3) a large pixelated window with rain animation as the room's centerpiece. **Lo-fi engine**: 4-bar Cmaj7 / Am7 / Fmaj7 / G7 progression at 78 BPM (one chord per bar, ~12.3s loop). Per-bar instruments: sine bass on root (one note per bar, 3.85-beat sustain at 0.16 vol), triangle pad stack of 4 chord tones staggered 18ms apart at 0.045 vol, triangle melody on every beat (4 notes per bar — bar 1: E5/G5/C6/G5, bar 2: A4/C5/E5/C5, bar 3: F4/A4/C5/A4, bar 4: G4/B4/D5/B4 — each note 0.92 of a beat with 25ms attack), filtered-noise hi-hat on every offbeat (highpass 6500Hz, 100ms exp decay at 0.045 vol), sine kick on beats 0+2 (120→38Hz exp sweep, 180ms decay at 0.22 vol). All routed through master gain → 1800Hz lowpass (Q 0.7) → split into dry (0.85) + reverb wet (0.42, via 2.4s noise-impulse convolver) → destination. Sparse vinyl crackle (4s noise buffer with ~0.6% pop density, looped, highpass 2400Hz at 0.18 gain) layered into master for tape feel. Look-ahead scheduler: `setTimeout(60ms)` tick wakes up and schedules every beat that falls within the next 0.5s of `audioContext.currentTime`, so timing stays tight even when the main thread is busy with rAF rendering. ♫ button in footer toggles. Independent of the ♪ sfx mute — players can listen to lo-fi while sfx is muted (or vice-versa). Browser autoplay policy gates the first play behind the click gesture; subsequent toggles work freely. Gentle 1.2s fade-in + 0.4s fade-out on stop. **Large pixelated window**: replaced the previous 120×80 corner window with a 260×170 centerpiece (winX=110, winY=18) — chunky 6-px pink frame, 4-pane cross grid, deep-indigo→violet sky gradient inside, soft horizontal cloud bands at 42% and 62% height, hazy moon at upper-right (16px halo + 10px crisp core), 8 veiled stars (1.5px squares, twinkling via `0.5+0.5*sin(now*0.003+i*1.7)`), and a pink sill at the bottom. **Rain animation**: 60 raindrops (each `{x, y, v}` where v=110..200 px/sec) falling inside a clipped pane region, 1×3 vertical pixel-rectangle drops in `#9bd5ff` cyan, slight wind angle (`x -= v*dt*0.18`), respawn at random x just above the top when they fall past the bottom or off the left edge. Periodic 35%-chance puddle splash blip at the bottom edge each frame (2×1 cyan rect, transparent). Picture-frame moved to lower-left (28,130) since the window now claims the back-wall center. **Pixel mode shines here** — each raindrop becomes a hard-edged ~5-display-px stripe at 96×68 framebuffer, reading exactly like Stardew/RPG-Maker rain.
- 2026-05-01: **Cursor-chase + bat mechanic replaces clicking; chunkier pixel-mode framebuffer; lime-green glowing slit eyes.** Three coupled chat asks: (1) "Null chases the cursor and bats at it" — primary input is now mouseover, not click; (2) "much more pixelated" — cranked the pixel framebuffer way down for chunky retro feel; (3) "glowing lime green eyes that pierce through the neon shadows" — pixel-mode eyes are now layered void-sclera + lime bloom + crisp lime iris + dark slit pupil. **Cursor chase**: both canvases listen for `mousemove`/`touchmove` and store the pointer position (`cat.cursor` in 0..size canvas-px coords for pet view, `room.cursor` in 0..480/0..340 world coords for room view). On `mouseleave`/`touchend` the cursor is cleared. **Pet view**: Null's body lerps toward the cursor via `cat.followX/Y` (clamped to ±60h/±26v from canvas center, 14% per-frame pull) so she leans into the cursor without ever leaving the canvas. When cursor is within 84px of her head + 380ms cooldown elapsed + bat not already animating, fires a bat: `cat.batT = 1`, `cat.batAngle = atan2(dy, dx)`, `applyPet(stageX, stageY, 'pet')`. **Room view**: cursor presence overrides the wander AI entirely — `rc.target = {x: clamp(50, W-50, cursor.x), y: 280, kind: 'cursor'}`, walk-speed bumps from 36→56 px/sec while chasing, and the station-arrival idle state-switch is suppressed (no naps mid-chase). Bat trigger fires at 60-world-unit proximity with the same 380ms cooldown. **Bat paw render**: drawn after the cat-local transform restore so the paw can extend outside the cat's body. Position = `(catX + cos(angle)*reach, catY + sin(angle)*reach)` where `reach = 56 * sin((1 - batT) * π)` — peaks at batT=0.5, returns to 0 over the decay. 3 fading motion arcs trail the paw, pink toe beans face inward toward the cat (back of paw points at the player). **Refactored petCat**: extracted position-driven `applyPet(stageX, stageY, target)` core that handles wallet credit, mega-charge logic, sfx, float reactions, save cadence; `petCat(e)` is now a thin wrapper that converts a click event to stage coords and calls applyPet. Bat triggers call applyPet directly with cursor's stage-coord position (canvas offsetLeft/Top + display-px cursor). **Click still works** as a fallback for touch devices and tap-preferring players. **Pixel framebuffer cranked**: pet view from PIX=96→64 (5px per logical pixel at 320 display, NES-tier chunky), room view from 192×134→96×68 (5px per logical pixel at 480 display). Default `state.pixel = true` for new players (existing saves preserve their preference via load()). **Lime-green glowing slit eyes** active in pixel mode: layered render in `[-13/13, eyeY]` (pet) and `[-13/13, headY+2]` (room) — (1) `#0a0014` near-black sclera so the green pops, (2) `rgba(170,255,40, 0.55+batT*0.4)` translucent glow ellipse at +2/+1 radius for the bloom-pierces-shadow effect, (3) `#aaff14` crisp lime iris core, (4) `#0a0014` thin vertical slit pupil for that classic cat predator look, (5) white pinprick sparkle at top-left. Bat tension boosts the glow alpha — eyes get brighter when she's about to strike. Smooth-mode kept the original sparkle-eye render unchanged. Brand subtitle now reads "null wants pets · ♡ ♡ ♡ ·" and the click hint says "wave near null uwu". Auto-hides after first interaction.
- 2026-05-01: **Added pixel-art filter + dedicated room view (clicking optional).** Two orthogonal display switches in a new pill row above the cat-stage: 🐾 pet (the existing click-stage), 🛏️ room (a passive bedroom scene), and 🟪 pixel (retro filter that applies to whichever view is active). Both persist to localStorage v1 (`viewMode` + `pixel` keys). **Pixel filter** drops the canvas internal framebuffer to a small physical resolution — 96×96 for pet view, 192×134 for room view — and applies CSS `image-rendering: pixelated` so the browser upscales the small buffer with nearest-neighbor sampling, giving a chunky retro look without changing any of the drawing code. Backgrounds (gradients, sparkles, fonts) stay smooth so the room reads as pixel-art sitting in a softly glowing neon space rather than the whole page going pixel. **Room view** is a 480×340 world-coord canvas (CSS display 320..560px, 1.42:1) drawn as a cozy bedroom: deep violet back wall (top 62%) with starry window + moon + tiny pink picture-frame holding a heart, pink wood floor (bottom 38%) with a striped texture, a heart-pattern oval rug centered. **Decor stations** appear only when their upgrade is owned — 🛋️ pillow (left-floor), 🧶 yarn (right-floor), 🥛 milk (center-left), 🍣 sushi (center-right), 🌸 sakura (back-right) — drawn as 32px emoji glyphs sitting on soft pink halo ellipses. **Wandering cat AI**: every 4–12s the cat picks a new behavior — 75% chance to walk to a random visible station, 25% chance to wander to a random floor point. On arrival the cat enters a station-specific idle state: pillow/sakura → `sleep` (closed-eye `~~~` + floating "z z Z" letters above the head, sustained 0.8 blush), yarn → `play` (happy `^^` eyes), milk/sushi → `eat` (open mouth + tongue). Cat walks via floor-x interpolation at 36 px/sec with a sin-based vertical bob, facing flips to direction of travel, tail wags continuously via sin, blinks every 4–9s. **Floating heart particles** spawn from the cat's head every `1.4 - log10(1+cps)*0.18` seconds (so cps=10 → ~1.2s, cps=1000 → ~0.86s, cps=1M → ~0.32s) — 4-color rotation (hot-pink/bubble/candy/gold), 1.2–1.6s lifetime with sin-sway and upward drift, hard-cap at 24 in flight. **Clicking is OPTIONAL** but rewarded — tapping the cat sprite (50px hit-radius around her current world pos) still calls `petCat` for the standard click bonus + spawns 2 extra room hearts at her position. Empty room space is unclickable so the player doesn't accidentally click "the floor" expecting a pet. **Passive cps credits identically in both views** — the global rAF tick adds `cps() * dt` to pets/lifetime regardless of which canvas is rendering, so room mode is genuinely passive: leave it open and the pets stream in. `passiveTick()` rAF loop now branches on `state.viewMode` to call `drawRoom(now)` or `drawCat(now)`. Mobile-friendly: room canvas auto-scales between 320–560px based on parent width; touch handlers route through the same `roomCatHitTest` for tap-to-pet.
- 2026-05-01: Created. **uwu cat clicker — kawaii pink-neon clicker game with a chibi pink cat that reacts to taps and evolves through 6 visual stages.** Single-file vanilla JS. **Core loop**: click the cat → gain ♡ pets (1 per click base, scaled by upgrades + multipliers) → spend ♡ on upgrades → cat evolves visually as lifetime ♡ crosses thresholds. **Cat sprite is fully procedural canvas** (no images): pink chibi body w/ radial gradient highlight, separate head circle, 2 triangular ears w/ inner pink, sparkle eyes (white sclera + pink iris + 2 white sparkles), uwu mouth (CSS bezier `w` shape), pink cheek blush, paws w/ pink toe beans, wagging tail (sin-wave), squash-bounce scale on click. **Cat reactions per click**: bounce 1→0.86 decay, blush bumps to 1, mouth opens (with tongue), happy face for 420ms (eyes squint to `^^` smile arcs), tail wags faster, blink suppressed. **Blink** every 4–8s, ~120ms keyframe with `eyeH` lerp from 12→3. **Evolution stages by lifetime ♡**: 0 = kitten, 1k = bowtie cutie (pink bow drawn on left ear w/ knot dot), 10k = sparkle puff (3 animated 4-point amber sparkles around face), 100k = crown princess (5-point amber crown w/ pink jewels stroked above head), 1M = angel neko (golden halo ellipse + small white wings flanking body), 10M = cosmic mochi (3 mint-cyan rotating orbital ellipses around the cat). **Upgrades** (10 total — chat specifically requested cozy passives): 🐾 PAW POWER (+1 click ♡/lvl, base 15), 🛋️ COZY PILLOW (+1 ♡/sec, base 40 — soft napping), 🧶 YARN BALL (+1 ♡/sec, base 90 — playful kicks), 🥛 WARM MILK (+3 ♡/sec, base 280 — cozy bowl), 🍣 SUSHI (+5 ♡/sec, base 600 — happy belly), 💋 KISSU (×1.05 click multi, base 1.5k grow 1.5), ✨ STARDUST (+25 ♡/sec, base 7k — sparkle dreams), 🌸 SAKURA NAP (+60 ♡/sec, base 24k — blossom dreams), 👑 PRINCESS (×1.15 global multi, base 60k grow 2.0), 👻 NEKO SPIRIT (+150 ♡/sec, base 180k — ghost purrs). All exponential cost scaling `base × grow^lvl`. **Click value formula**: `(1 + paw_lvl) × 1.05^kissu × 1.15^princess × (mega ? 5 : 1)`. **CPS formula**: `(pillow + yarn + 3·milk + 5·sushi + 25·stardust + 60·sakura + 150·spirit) × 1.15^princess × (mega ? 5 : 1)`. **MEGA UWU MODE**: every 50 clicks accumulates a charge (capped at 5). When you have ≥1 charge AND no active mega, the next click consumes a charge and starts an 8s burst that 5×s both click value AND cps + sets a remaining-time gold ring at the bottom of the canvas + flashes a screen-center "MEGA UWU ✨" banner in Bagel Fat One that pops in 0.6→1.15→1 with rotation. **Visual reactions** in stage coords: floating `+N ♡` text in candy white that floats up + scale-rotates + fades over 1.2s; expanding pink ripple from tap point; 25%/click chance of a bonus floating word ("nya~", "uwu", "♡", "mrrr", "purr", "✨") spawning ~80ms after the score pop. **Scene aesthetic**: deep indigo→violet→hot-pink vertical gradient bg + 2 layered fixed `body::before/::after` sparkle pseudo-elements with 7-stop radial-gradients tile-panning vertically over 50s/90s with reverse direction for parallax depth. Glassy cards w/ backdrop-filter blur(8px), 2px hot-pink border, 28px radius, pink box-shadow + inset pink glow. **Typography**: Bagel Fat One (chunky kawaii display) for title + counter + reactions, Zen Maru Gothic (rounded Japanese gothic) for body + buttons, Fredoka (rounded geometric) for taglines + meta, DotGothic16 (pixel) for stat lines + footer + mega-mode timer. **Title** "mochi cat · uwu clicker · ♡ ♡ ♡ ·" w/ pink/hot-pink layered text-shadows + 3px offset 3D, rotated -2deg with the small subtitle rotated +2deg for that hand-drawn lean. **Audio** (Web Audio synth, ♪ toggle bottom-right): pet click = 660Hz triangle pluck + 1.4× harmonic 25ms after, mega click = 4-note ascending bell arpeggio (880/1175/1568/1976), buy = 540→810Hz two-tone confirmation, mega trigger = 5-note major-pentatonic chord arp (392/494/587/698/880), error = 180Hz sine. **Persistence**: full state via `localStorage['mochi-cat-v1']` JSON — pets/lifetime/clicks/upgrade levels/mute/megaCharges. Saves on every 12 clicks + every upgrade buy + every 10s tick (so passive idle gains aren't lost on close). **Controls**: tap or click cat (mouse + touchstart), Space/Enter pet, ↻ reset wipes localStorage with confirm. **HUD**: top-right pill shows lifetime ♡ + current stage emoji + name; main panel shows huge Bagel Fat One pet counter w/ bump animation on increase + tagline "pets · ♡" + live "+N ♡/click · M/sec idle" rate line in DotGothic16. Mobile responsive: panel stacks below cat at ≤880px, canvas auto-DPR via `Math.min(2, devicePixelRatio)`, touch-action manipulation on cat for instant click feedback. Pollinations OG.

## issues
- Click value scales exponentially via Princess (×1.15/lvl) AND Kissu (×1.05/lvl). With both maxed (10 + 20) you'd hit (1.15^10 × 1.05^20) ≈ 4.05× × 2.65× ≈ 10.7× click multiplier — combined with PAW L60 (+60 base) that's 651 ♡/click. Probably a bit too multiplicative — could rebalance Kissu down.
- Mega UWU charges accumulate even while mega is already active, but the consume-on-next-click path requires `!wasMega` so they queue rather than overlap. Acceptable, players probably won't notice the queueing nuance.
- Cat sprite is drawn fresh every rAF frame — even when idle. Could skip redraws when nothing has changed (no bounce/blush/mouth/blink active) to save mobile battery. Cosmetic perf only.
- Lifetime ♡ drives evolution but spending pets doesn't lower it (intentional). However, ♡/click and ♡/sec rate counters update from current `clickValue()` / `cps()` — if Princess is bought, both jump immediately even before any new clicks. Expected behavior, just noting.
- Save format `mochi-cat-v1` doesn't preserve lastSaveT, so passive earnings while the tab was closed are NOT credited (only the open-tab passive ticker accumulates). Could add an offline-progress catch-up on load.

## todos
- Achievement toasts (first MEGA UWU, 1k lifetime, 1M lifetime, all upgrades maxed, etc).
- Prestige/rebirth mechanic: trade lifetime ♡ for "bowties" that permanently boost the global multiplier.
- Catnip mode (toggle): rare 1% chance per click to drop a "catnip leaf" pickup that floats around for 6s and 10×s the next tap.
- Multiple cats? Unlock a friend cat after a high lifetime, both auto-pet each other.
- Skin picker: gray, calico, tuxedo, ginger color variants.
- Sound: optional ambient background (slow piano + cat purr loop), separate ♪ toggle from sfx.
- Offline progress catch-up: store lastSaveT, on load credit `(now - lastSaveT) × cps × 0.5` (half rate to avoid abuse).
