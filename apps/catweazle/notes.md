# catweazle · notes

## log
- 2026-05-16: v1.2 — **1970s LWT episode framing + sepia-grain CRT atmosphere + low-poly Three.js diorama with clickable electrical appliances** per stacked chat asks (combined): "update catweazle with a 1970s film grain filter, episode titles like The Telling Bone, and a warm nostalgic atmosphere" + "use three.js to add low-poly 3D environments for the farm and the castle" + "make the UI visual and clickable with cards and a diorama layout" + "add clickable electrical appliances like toasters and switches to the 3D diorama." All shipped in one cohesive update:
  - **1970s grain + CRT overlay**: full-bleed `.grain` div uses an SVG `feTurbulence` fractalNoise data URI with `mix-blend-mode: overlay`, animated via stepped `grainShift` keyframes so it judders like 16mm at 8fps. `.scanlines` adds horizontal CRT lines, `.vignette` is a soft radial darkening, `.flicker` pulses opacity 0.96–1.04 every 2.6 s. `body { filter: sepia(0.10) saturate(1.05) contrast(1.02); }` warms the whole page towards old print.
  - **Episode plate**: small `EPISODE · No. N` + italic title strip above the scene card. `EPISODES` lookup maps every scene id → `{n, t}` with titles in the LWT show's voice — "The Strange Stables", "The Telling Bone", "The Iron Pony", "The Self-Service Shrine", "Hexwood Returneth", etc. Refreshed each `render()`.
  - **Three.js low-poly diorama**: `<canvas id="diorama">` inside a `.diorama-frame` (sky-gradient bg, ink border, screenplay-style label "EXT. HEXWOOD FARM — DAY"). Three.js r128 from jsdelivr. One scene, one camera (slow orbit `cos/sin(t·0.35) * 8.5`), HemisphereLight + DirectionalLight + AmbientLight. `stage` group is cleared + rebuilt on every scene change via `Diorama.setScene(state.scene)` called from `render()`. 8 builders: `farm` (cottage + roof cone + chimney + barn + haystacks + trees), `lane` (road + dashed centreline + hedgerows + red phone box + lamp-post), `field` (yellow rapeseed ground + hovering drone group + scattered wildflowers + distant cottage), `village` (church + spire + cross + cottages + trees), `tesco` (white supermarket + blue stripe + red dots + glass doors + car-park lines + trollies), `stones` (5 stones in partial circle + leaning monolith + capstone + moss), `portal` (two flanking stones + glowing plane + warm point-light), `wellness` (beige modern building + manicured hedges + tasteful potted plant). Camera lookAt(0,1,0). SCENE_MAP routes every game scene id (including alt branches like `s_pub_eject`, `s_tesco_alarm`) to a builder.
  - **Clickable electrical appliances** (the new mechanic the diorama earns): every `appliance(opts)` mesh is tagged `userData {isAppliance, kind, label, quote, bw}` and pushed to a per-scene `appliances[]` array (cleared in `clearStage`). A single `THREE.Raycaster` on the canvas's `click` event intersects only the appliance list. On hit: (1) golden flash on the mesh's material color (`0xffe080` for 320 ms, then restored from `_baseColor`), (2) 3-step horizontal frame shake (90 ms each), (3) `state.bw += u.bw` (clamped to BW_MAX) + `save()` + `render()` + `drama(quote, label)` letterbox interlude. Per-scene placements: **farm** = light switch by the cottage door (+3); **lane** = the speaking-bone inside the phone box (+4) and a captured-star lamp-post (+4); **field** = the watching-bug drone eye (+6 — heaviest, since drones traumatise him most); **village** = chapel light switch (+3) and parish-tannoy shouting-box (+3); **tesco** = full appliance shelf: toaster (+5), kettle (+4), microwave (+5), self-service shrine (+4), light switch (+3); **wellness** = vending machine giving crisps for hexes (+5) and tannoy intoning mindfulness (+4). Each has a hand-written drama quote in his voice ("AAARGH! The metal mouth hath leapt at me — and out flew CHARRED BREAD!"; "I fed it a hex — and it gave me a bag of CRISPS. Touchwood is appalled."). Diorama frame `role="region"` + canvas `aria-label` so screen readers know it's interactive; `cursor: pointer` on the canvas.
  - **WCAG basics maintained**: episode plate stays `aria-hidden` (decorative), but the now-interactive diorama is exposed via `role="region"` with a descriptive aria-label.

- 2026-05-16: v1.1 — **two-player adventure with Carrot the farm boy, the Amulet of Adamcos, and dramatic letterbox interludes** per chat ask (combined): "update catweazle to be a two-player adventure where you play as Carrot the farm boy, adding amulet hypnosis and dramati[c scenes]." Additions:
  - **Carrot joins the party** at `s_lane`. A new dialogue option `"Aye, lad. Walk with me. I shall need a translator."` appears on first visit; clicking adds Carrot as a third portrait + a fourth stat bar (`Common Sense` 0-50). Carrot's portrait is hand-drawn inline SVG — straw-orange hair, freckles, school-uniform collar with brown tie. His Common Sense softens bewilderment in `render()` via `effectiveBW = bw - sense × 0.15` so the wellness-retreat threshold is pushed back as he sticks around.
  - **Amulet of Adamcos**: a new scene `s_amulet` is reachable from the lane when Carrot is in the party ("Slip behind the Black Bear and rifle through the bins"). Three resolutions — take the amulet (3 hypnosis charges), let Carrot keep it (Common Sense +6), leave it (Lore +1). The amulet shows up as a gold-bordered pill `✦ amulet · 3` in the party row, with a live charge counter.
  - **Hypnosis branches**: when the amulet has charges, two existing scenes get a new choice — at `s_village` ("✦ Raise the Amulet — hypnotise the vicar") clears 14 bewilderment + 4 lore + spends 1 charge; at `s_tesco` ("✦ Raise the Amulet — hypnotise the till") spends a charge AND drops the glass rune directly (the previously RNG-gated way to get glass). Both are gated by `cond: () => state.amulet.has && state.amulet.charges > 0`.
  - **Dramatic interludes**: a full-bleed letterbox overlay (`.drama`) that pops a single italic IM Fell English line for 3.4 s on key beats — Carrot joining, the amulet acquired, each hypnosis fire, glass rune via hypnosis, and the finale's stones-singing line. Two horizontal black bars (top + bottom, 11vh each, gold rules) frame the quote; click anywhere on the overlay to dismiss early. `drama(quote, hint)` helper queues with `dramaTimer`; `dismissDrama()` clears.
  - **Farewell text at the win ending**: if Carrot joined, the `s_return` prose adds "Carrot stands at the edge of the field, hands in his pockets, trying not to cry and absolutely failing." A `seenDrama` flag prevents the home-coming drama line from re-firing.
- 2026-05-16: v1 — whimsical text RPG. Chat ask (combined): "create a whimsical RPG called Catweazle set in rural England 2026, featuring an 11th-century wizard and his toad Touchwood" + "build the Catweazle RPG, a comedic text adventure where you use magic to battle elec-trickery." Both satisfied by the same build, which is consciously a love letter to the 1970 LWT show. **Premise**: Catweazle wakes in a strange barn with Touchwood on his chest. The barn is in 2026. His goal is to gather three runes (iron, glass, voice) and return to 1066 via the ancient standing stones at the edge of the village. Along the way he must not be broken by **bewilderment** (the meter that fills every time a modern object asserts itself). **Stats**: 3 meters — Hexes (0-5, currency for casting spells), Lore (0-50, wisdom; some choices gated by it later), Bewilderment (0-100, fail at 100 → he's sent to a "wellness retreat in Surrey" with Touchwood in a plastic tub named "Kenneth"). Stats render as parchment-bar gauges with rose/gold/moss fills; Bewilderment turns amber over 60 and rose-red over 85. Plus a Runes HUD (three medieval `I / II / III` discs) that light up gold as you collect. **Scenes**: 19 hand-authored cards (`s_start`, `s_lane`, `s_phone`, `s_phone_hex`, `s_car`, `s_car_hex`, `s_field`, `s_drone`, `s_pub`, `s_pub_eject`, `s_village`, `s_vicar_confess`, `s_tesco`, `s_tesco_alarm`, `s_stones`, `s_return`, `s_end_win`, `s_end_lose`, `s_no_hex`). Each scene has a title, a `prose()` function that renders multi-paragraph HTML (with `.touchwood` italic green-highlight callouts for the toad's commentary, `.modern` small-caps for objects he's mis-naming, and a CSS `::first-letter` drop cap on the opening paragraph), and a `choices[]` array. Each choice can be gated by a `cond()` (e.g. only show "hex the iron-pony" when `hexes > 0`) and runs an `on(state)` function that mutates stats + returns the next scene id. **The three rune routes**: *iron* from hexing a watching drone in the rapeseed field, *glass* from invoking a Tesco self-service shrine (55% chance per attempt), *voice* from hexing the speaking-bone in the red phone box. Catweazle calls them "horseless carriages" / "iron-ponies", "speaking-bone" / "telephone shrine", "self-service shrine" / "great hall of cold bread" — every modern object gets a poetic mis-translation. Touchwood corrects him from the side with `<span class="touchwood">`-tagged italic asides ("you have just spent twenty-five pence calling the speaking clock"). **Hex economy**: starting 3, max 5, regenerated by sleeping at the stones or being defeated by your own incantation (the `s_no_hex` scene offers a rest action). Every hex costs 1 + adds some bewilderment, so you can't just hex everything. **End conditions**: collecting all three runes + placing them at the stones triggers `s_return` then `s_end_win` ("the cottage roof I left is still smouldering"); bewilderment ≥ 100 auto-routes to `s_end_lose` ("they have called him 'Kenneth'"). Both end cards render a single "begin again" button which clears localStorage and restarts. **Aesthetic**: full illuminated-manuscript treatment. Parchment cream `#f0e0bf` paper with two 7°/101° linen-grain repeating gradients and corner soot stains via `::before/::after` radial gradients. IM Fell English (italic) for the masthead + scene titles + drop caps; IM Fell English SC (small caps) for labels and the rune HUD letters; Crimson Pro for body prose; Cormorant Garamond italic for Touchwood's interjections. Color palette: ink brown `#2c1808`, rose-dark `#6a2810` for the scene-title bookended `☙ ❧` flourishes, gold `#8a5a18` for the drop caps, moss `#4e7a3a` for Touchwood. Choices are parchment buttons with 4-px hard offset shadow, leading roman-numeral key, optional "condition unmet" annotation when locked. **Portraits**: hand-drawn inline SVGs of Catweazle (long beard, conical hat with tiny stars, raised eyebrow tufts) and Touchwood (round green toad with comically large eyes and warts). They flank the stats panel. **Save/load**: `localStorage['catweazle-v1']` persists the full state on every choice; reloading the page resumes mid-adventure. Restart button at the foot prompts a confirm. **WCAG basics**: semantic `<section role="region">` per scene, `aria-label` on the runes HUD, focus-visible default, ≥46 px button targets, restart button has aria-label. Pollinations OG flux seed 10662.

## issues
- The scene set is hand-authored at 19 cards for a single playthrough of ~8-12 choices. A second playthrough sees the same scenes; randomized scene variants would freshen it. Worth doing if chat asks for more depth.
- The "wellness retreat" ending is gentle satire but a player who fast-paths every modern object can hit it in 3-4 choices. That's by design — the bewilderment meter is the antagonist — but a "first reveal" tutorial could soften it.
- Touchwood doesn't have agency; he's flavor text only. A "Touchwood favor" stat could open a separate dialogue tree where the toad genuinely helps you in a fight.

## todos
- Touchwood combat companion: a fight scene against a "great electrickery beast" (a malfunctioning combine harvester) where the toad picks which spell.
- A handful of branching one-shot scenes (the post office, the school crossing, the chip shop) gated on Lore so the second playthrough has new content.
- "Folklore evening" subplot: if you confess to the vicar, you're booked into a Tuesday evening where the village expects a magic show.
- Save sharing: a base64 of the state, click-to-copy, so chat can compare adventure paths.
- Soft synth lute backing via Web Audio (muted by default).
