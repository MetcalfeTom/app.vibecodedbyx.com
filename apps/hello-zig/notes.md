# hello-zig

## log
- 2026-04-27: Created. Minimal Zig hello-world demo. **Static syntax-highlighted source** (`@import "std"`, `pub fn main() !void`, `try stdout.print(...)`) with line numbers + JetBrains Mono. **3 build commands**: `zig build run` → staged stdout with "▸ run hello" → cd line → "⚡ Hello, world!" output → exit info; `zig test` → "All 0 tests passed (no tests defined — try writing some)"; `zig fmt --check` → "✓ formatting clean". **Aesthetic**: Zig amber `#f7a41d` palette over deep navy `#13141f`, Big Shoulders Display 800 brand "HELLO, ZIG⚡" with the Zig bolt, Newsreader italic footer creed: "compiles correctly or compiles not at all." Pollinations OG. Mobile @520px shrinks code to 12px.
