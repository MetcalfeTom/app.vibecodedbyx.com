# inventory · notes

## log
- 2026-07-22: v1 — chat asked for "a clean inventory: every app name and folder". 1,440 rows won't fit a stream message, so this static snapshot page lists every apps/<folder> + its <title>, filterable + sortable (name/title), each folder linking to the live app. Generated by scanning apps/*/index.html titles at build time — STATIC SNAPSHOT, regenerate by re-running the build (see git log for the generator script in the commit that touched this file). Libre Caslon + IBM Plex Mono ledger aesthetic.

## issues
- Snapshot goes stale as new apps ship; regenerate on request.

## todos
- Auto-regen via a commit hook would be nice but hooks are out of scope here.
