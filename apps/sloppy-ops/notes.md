# sloppy-ops

## log
- 2026-04-30: **Nav overhaul + live presence counter (Option B).** Sidebar collapsed from 7 pages to **5**: OVERVIEW / INFRA / LLM / STORAGE / DEPLOYMENTS. The old SYSTEM / NETWORK / RUNTIME pages are now **subtabs inside a single INFRA page** — same content, but you no longer have to sidebar-hop between three closely-related views. Subtabs use a glassy pill-tab header (`.subnav` group, `.subnav-item` buttons with active=white pill on `rgba(58,137,201,0.06)` track). Subpages share the same `data-page="infra"` attribute and a `data-sub="..."` discriminator; `.subpage` is `display:none` by default, `.subpage.active` is `display:block`. Click handler on `.subnav-item` toggles the active class. The default INFRA view opens on SYSTEM. **Live presence counter** in the topbar, right of the clock — a green pulsing-dot pill labeled `VIEWERS <count>`. On boot the dashboard now imports `/supabase-config-fixed.js`, gets an anon session, and joins a Supabase Realtime channel `sloppy-ops-presence` (broadcast/presence, **no DB writes**). Each viewer tracks `{id: shortClientId, joined_at}`. The `presence sync` event fires whenever anyone joins/leaves, and we count `Object.keys(channel.presenceState()).length` to render the live viewer count. Falls back to "1" silently if supabase is unreachable. Pill styled with cyan/green palette: `rgba(78,192,126,0.10)` background + green border + green Major Mono number + animated pulsing dot via `pulse` keyframe. Existing renderNetwork/renderRuntime/renderSystem code paths unchanged — they target the same DOM ids that are now nested inside subpages.
- 2026-04-30: **LIVE TRACE — option A (snapshot-tailed) of LLM prompts/responses.** New panel at the bottom of the LLM tab showing a scrolling terminal-aesthetic feed of recent message blocks from the active session jsonl. **Snapshot script** tails the most-recently-modified `*.jsonl` in `~/.claude/projects/-vibespace/` (the active session), reads the last ~600 KB to capture 30-40 message turns, parses each line with `json.loads`, filters to `type` ∈ {`user`, `assistant`}, and extracts the inner `message.content` block array. **Redaction at snapshot time** before any data hits the browser: text blocks → 200-char preview, **thinking blocks → completely stripped** (only `[N chars of internal reasoning — redacted]` placeholder), tool_use blocks → `name + 1 key arg` (file_path / command / pattern / query / description, repr-truncated to 80 chars), tool_result blocks → 200-char head only. Newlines flattened to spaces. Result baked into `data.json.llm.recent_messages` as `[{ts, role, blocks:[{kind, preview}]}]`. Active session id stored as `llm.active_session`. **Terminal panel** (`.trace-term` CSS class) is a `#0a0e18` dark `<pre>` with monospace lines: `HH:MM:SS  ROLE   [KIND] preview…`, color-coded — USER cyan, ASSISTANT amber, SYSTEM green, plus a kind tag pill (TEXT yellow, THINKING violet, TOOL-USE pink, TOOL-RESULT green). 11px JetBrains Mono, `max-height:420px` with overflow-y scroll. **Auto-scroll-stick**: only auto-scrolls to bottom if user was already near the bottom (within 8px) — preserves scroll position when manually browsing history. Blinking `_` cursor at the end. **Live refresh**: dashboard now `setInterval(refreshSnapshot, 5000)` re-fetches `data.json?_=<timestamp>` (cache-busted) every 5s — when the snapshot script regenerates server-side the new trace flows in without a page reload. If `data.json` is static, the 5s polling is a cheap no-op (no diff, no DOM change). Cadence is "near-live within snapshot regen interval", not true streaming. Pairs with whatever cron/inotify/manual snapshot-regen the operator wires up.
- 2026-04-30: **Deeper kernel + container metadata.** Snapshot script extended to capture another batch of real `/proc` and `/sys` values: **kernel.cmdline** (`BOOT_IMAGE=/vmlinuz-6.17.0-1008-gcp root=PARTUUID=… ro console=ttyS0,115200 panic=-1`), **kernel.version_full** (`Linux version 6.17.0-1008-gcp (buildd@lcy02-amd64-092) (gcc-13.3.0, GNU ld 2.42) #8~24.04.1-Ubuntu SMP Fri Jan 30 23:44:29 UTC 2026`), **module_count** from `/proc/modules` line count (54), **entropy_avail** (`/proc/sys/kernel/random/entropy_avail` = 256), sysctl values **swappiness** (60, default), **pid_max** (4,194,304), **threads-max** (127,391), **file-max** (very large), and **timezone** (`/etc/timezone` = Etc/UTC). **Swap detection** from `/proc/meminfo SwapTotal / SwapFree`: this box has **0 swap**. **Thermal sensors** scanned from `/sys/class/thermal/thermal_zone*` — empty on this VM (only `cooling_device*` entries, which aren't temp readouts). Schema preserved with `thermal_zones: []` so a host with real sensors would surface them. **Virtualization detection** from DMI: `vendor=Google product='Google Compute Engine'` + `os.path.exists('/.dockerenv')` → confirmed **GCE VM running inside a Docker container**. **Cgroup v2** values from `/sys/fs/cgroup/`: `memory.current=2.86 GB`, `memory.max=max` (unbounded — the container can use all 15.6 GB host RAM if needed), `cpu.max=max 100000` (unbounded with 100ms accounting period). **New KERNEL · CONTAINER · CGROUP panel** on the SYSTEM page below the existing tiles: 12 stat cards covering VENDOR / PRODUCT / CONTAINER / CGROUP-MEM (current / max) / CGROUP-CPU / TIMEZONE / MODULES / ENTROPY (turns pink if <256) / SWAPPINESS / PID MAX / THREADS MAX / THERMAL ZONES (with first temp °C if available, else "no sensors (vm)"). Plus two raw-text rows showing the full BOOT CMDLINE and VERSION STRING in monospace pills. The MEMORY panel got an extra row showing **swap** usage (currently "none configured"). Swap fill bar reads zero on swapless boxes. All snapshot-time captured; no live polling — but a dashboard refresh re-fetches `data.json` and the values update if the snapshot was regenerated.
- 2026-04-30: **LLM tab — MCP servers + active claude sessions.** Snapshot script extended again to walk `.mcp.json` + `/proc` + `~/.claude/projects/-vibespace`. **MCP config**: parses `/vibespace/.mcp.json` and serializes each declared `mcpServers.*` entry as `{name, type, host, has_url}` — host is extracted from the URL so internal URLs like `http://supabase-mcp:8000/mcp` show only the `host:port`. **Active LLM processes**: filters `/proc/[pid]/comm` for `claude`, `mcp-proxy`, and `node`, parses `cmdline` to extract `--model` and `--continue` flags so each entry shows model name + RESUMED/NEW state, computes uptime from `start_time / CLK_TCK` against `/proc/uptime`, captures RSS from `VmRSS`. **Session transcripts**: walks `/home/sloppy/.claude/projects/-vibespace/*.jsonl` capturing only `{id, bytes, mtime_sec_ago}` — **never reads the file contents** (privacy + size: 251 MB across 2 sessions on this box). Subagent count walked from `*/subagents/*.jsonl` directories. **New LLM sidebar page** displays: 6 summary tiles (MCP SERVERS / CLAUDE ACTIVE / MCP PROXIES / SESSIONS ON DISK / TRANSCRIPT TOTAL MB / SUBAGENT JSONL count), a CONFIGURED MCP SERVERS panel listing each server with its transport type + host, an ACTIVE LLM PROCESSES panel with bars proportional to peak RSS + label `MODEL · RESUMED/NEW · uptime`, and a SESSION TRANSCRIPTS panel showing each `.jsonl` file with relative size bar + age (`5m` / `1.2h` / `13d`). Sidebar nav `LLM` badge shows the live claude process count. The current snapshot reveals: **1 MCP server (`supabase` http transport @ supabase-mcp:8000), 3 active claude opus instances** (the heaviest is 658 MB / 15h, the oldest is 14 days uptime), 3 mcp-proxy bridges, 2 session jsonl files (current 45 MB / 0h, plus a 206 MB session 13 days old), and 496 subagent jsonls. Privacy: only metadata is captured at snapshot time — no transcript content, no cmdline-arg paths, no credentials.
- 2026-04-30: **Live runtime metrics — PSI, CPU breakdown, top processes, network totals.** Snapshot script extended again to capture a `runtime` block with everything the kernel exposes via `/proc` (no live agent needed; just deeper reads at snapshot time). **Pressure Stall Information (PSI)** parsed from `/proc/pressure/{cpu,memory,io}` — three resources × `some`/`full` × `avg10`/`avg60`/`avg300` percentages. On this box: **CPU pressure ~48% avg10** (high — the box is genuinely busy), MEM pressure 0% (no thrashing), IO pressure 0.02% avg10 (ample). PSI is the modern Linux equivalent of "memory pressure" — it tells you what % of time at least one task was stalled waiting on each resource. **CPU breakdown** from first row of `/proc/stat`: jiffies allocated to user/nice/system/idle/iowait/irq/softirq/steal/guest/guest_nice, normalized to %. Snapshot captures since-boot proportions: 21.3% user, 16.7% system, 61.5% idle, 0.27% steal, 0.08% iowait. **Memory detail** from `/proc/meminfo`: total/free/available/buffers/cached/active/inactive/swaptotal/swapfree (all kB). The dashboard derives "MEM USED·NET" as `total - available` (excludes page cache + buffers, the real "used" number). **Network totals** from `/proc/net/dev` (eth0): rx_bytes 2.94 GB / 1.2M packets, tx_bytes 1.85 GB / 786K packets since boot. **Top 12 processes by RSS**: walks `/proc/[pid]/stat` for each numeric pid, parses comm + start_time (field 22, in clock ticks), computes per-process uptime as `sys_uptime - start_time/CLK_TCK`, reads `VmRSS` from `/proc/[pid]/status`. Top entries on this box: claude (15h, 658 MB), claude (309h, 502 MB), claude (337h ≈ 14 days, 258 MB), mcp-proxy x3, plus assorted bashes. **New RUNTIME sidebar page** displays: 6 summary stat cards (CPU pressure% color-coded green/amber/pink at 30%/60% thresholds, MEM pressure, IO pressure, total processes, MEM CACHED, MEM USED·NET), a PSI panel with 3 rows (CPU/MEMORY/IO) each showing avg10 as a horizontal bar + avg10/60/300 numerics, a CPU TIME BREAKDOWN panel with bars for user/system/iowait/steal/irq/softirq/nice (4× width scale so even small values are visible) plus a separate idle bar in muted grey, a TOP PROCESSES panel with name·pid + bar relative to the heaviest process + RSS in MB + uptime (formatted Nd Nh / Nh Nm / Nm Ns), and a NETWORK INTERFACE TOTALS panel with iface name + RX/TX bytes and packet counts. Sidebar nav `RUNTIME` item shows the live process count as a badge.
- 2026-04-30: **Cloud-color theme + clickable git hashes.** Repainted from the dark sysadmin terminal aesthetic to a light cumulus-cloud palette: page bg is a soft sky `#f4f8fc → #d6e4f3` linear-gradient with three radial highlights (top white sun, bottom-right pale-blue glow, bottom-left white wash) over a faint 32px sky-blue grid. Panels are pure white `#ffffff → #f6f9fd` with a layered dropshadow `0 1px 0 rgba(255,255,255,0.9), 0 4px 14px rgba(58,82,128,0.08)` for that crisp paper-on-cloud feel. Borders shifted from cyan-on-dark to `rgba(58,137,201,0.18)`. Text palette flipped: `--ink: #1f2d40` (deep ocean blue) is primary, `--dim: rgba(31,45,64,0.55)` secondary. Accent colors recalibrated for white bg: green `#4ec07e`, sky `#3a89c9`, amber `#e8a93c`, blood `#e85d4d`, pink `#d957a3`. Removed the body::before scanline overlay (was a dark-theme artifact). Fonts kept (JetBrains Mono + Major Mono Display). **Clickable git hashes**: every hash in the deployments timeline (OVERVIEW summary + DEPLOYMENTS full page) now wraps in `<a class="hashlink" href="https://github.com/MetcalfeTom/app.vibecodedbyx.com/commit/<hash>" target="_blank" rel="noopener">`. Hashes get a soft sky-blue background pill (`rgba(58,137,201,0.10)`) and underline on hover. Real `git log` hashes from `data.json` go straight to the actual commit pages on GitHub. `REPO_URL` constant set near the top of the script for easy migration to a different remote.
- 2026-04-30: **Network probe page — real listeners + external reachability.** Snapshot script now also captures network state. **Listeners**: parses `/proc/net/tcp` + `/proc/net/tcp6` looking for state `0A` (LISTEN), decodes hex little-endian IP + port → `{proto, ip, port}`. On this container exactly **1** listener: `tcp4 127.0.0.11:45691` (Docker's embedded DNS resolver). Counts established connections (state `01`) — currently **5**. Captures `iface_ip` from `hostname -I`. **External probes**: `curl -s -o /dev/null -m 5 -w "%{http_code}"` against the 9 endpoints the project actually depends on (Supabase REST, Pollinations Image+Text, GitHub Web+API, Emoji CDN, jsDelivr, Cloudflare cdnjs, Google Fonts) with elapsed-time measured via `time.monotonic()`. Result: **9/9 reachable** at probe-time, latency 186–697ms (Cloudflare slowest at 697ms, GitHub fastest at 186ms). All baked into `data.json` under `network` block. **NETWORK sidebar page** displays: 5 summary cards (IFACE IP / LISTENERS count / ESTABLISHED count / REACHABLE-N/total / AVG-LATENCY ms), a LISTENING SOCKETS panel (with proto/ip/port row), and an EXTERNAL CONNECTIVITY panel where each probe gets its own row with a latency bar (width = `min(100, ms/800*100)%`, red `crit` when unreachable, amber `warn` when >600ms). Sidebar nav badge shows `<reachable>/<total>` like `9/9`. Snapshot is captured at deploy time, frozen in JSON — not live-polled (browser CORS would block any same-origin probe of these external hosts even if we tried). Note: the listeners list is what's bound INSIDE this Docker container; the externally-routed `sloppy.live:443` is served by an ingress proxy on the host, not visible here.
- 2026-04-30: **Real OS + hardware metadata pulled into the snapshot.** Extended the build-time Python script that generates `data.json` to also probe and serialize a `system` block with truly-real values from the box: hostname (`hostname`), OS pretty-name (`/etc/os-release`), kernel (`uname -r`), arch (`uname -m`), CPU model (first `model name` from `/proc/cpuinfo`), core count (`nproc`), total + available memory (`/proc/meminfo`), root-fs total/used/free GB (`os.statvfs('/')`), uptime in seconds (`/proc/uptime`), 1/5/15-min load average (`/proc/loadavg`), boot ISO (`uptime -s`), and the `/vibespace` folder size (`du -sb`). For the live snapshot: **Debian 12 (bookworm), kernel 6.17.0-1008-gcp, x86_64, Intel Xeon @ 2.20GHz, 4 cores, 15.6 GB RAM, 192 GB root disk (46 GB used / 146 GB free), 393 MB /vibespace, uptime 48d, load avg ~6.5 — running on GCP**. Added a new **SYSTEM** sidebar page that displays 12 stat cards (HOST / OS / KERNEL / ARCH / CPU model / CORES / LOAD-1/5/15 / UPTIME / BOOT / MEM TOTAL / MEM AVAIL / DISK TOTAL) plus 2 progress-bar panels: ROOT FILESYSTEM (with /vibespace shown alongside as a sub-bar so users can see how much of the disk this whole project occupies) and MEMORY (used / total). The `boot_iso` field also drives the topbar UPTIME counter, replacing the previously hardcoded "42d 03h" — it now ticks up minute-by-minute from the actual kernel boot time. The host name shows in the topbar's REGION field. All synthetic gauges still removed; this just adds more *real* data sources alongside the existing storage + git deployments.
- 2026-04-30: **Stripped all simulated data — real metrics only.** Per chat's "no fake vibes allowed" direction, removed every synthetic gauge: deleted the **METRICS** page (CPU/MEM/NET/DISK were sin-noise), the **DATABASE** page (sparkline + IDLE/WAITING/P95/QPS/CACHE/LAG were `60+sin(t*0.04)*18+noise`), the **SERVER LOGS** page (template-string feed), the **USER STATS** page (random walk on uActive/uSignup/uVotes/uMsg). Sidebar nav now has 3 entries: OVERVIEW, STORAGE, DEPLOYMENTS. Removed the synthetic 4th totals tile (REQ/S) and the DB POOL tile from the totals row, replaced with **TOTAL · STORAGE** (sum of real bytes from `du -sb`) and **SNAPSHOT AGE** (`now - generated_iso`, ticks up live). Removed the storage `mutate()` wobble — disk usage doesn't actually drift while a page is open, so values are now stable at exactly what `du -sb` reported. Removed the entire mock `SERVICES` seed (15 hardcoded services with drift/base/alloc) — `SERVICES = []` is empty by default, only populated by `applyRealData()` from `data.json`. Same for `deploys = []`. Removed mock `randomDeploy()` 14-entry timeline. Sidebar foot now reads `SOURCE · git + du / SNAPSHOT · <iso> / no live agent · no synthetic gauges`. Overview page footer adds the disclaimer "all numbers below are real and frozen at snapshot time. no live agent · no fake gauges · what you see is what was on disk when this page last shipped." If `data.json` fails to load, the dashboard is empty (rather than showing convincing-looking mock data).
- 2026-04-30: **Refactor to multi-page SPA with sidebar + categorized metric views.** Replaced the single-grid layout with a 220px-sidebar + main-content `display:grid` shell. **Sidebar** (sticky top:14px) lists 7 pages: OVERVIEW (▤), METRICS (▰), STORAGE (⛁), DATABASE (◉), DEPLOYMENTS (▷), SERVER LOGS (≡), USER STATS (◈), each with an icon glyph + uppercase label, and a side rail accent on the active item. Sidebar also surfaces real status info (cluster name, snapshot timestamp from `data.json`). Storage + Deployments items show live-updating count badges. **Page switching** is pure SPA — clicking a nav-item toggles `.active` class on a `<section data-page="...">` element. **OVERVIEW (landing)** keeps the live aggregate dashboard: 4 amber/cyan/green/pink totals (SERVICES / DB POOL / DEPLOYS-24H / REQ/S), a TOP 8 storage panel, the DB connection sparkline + 4 sub-stats, and the recent deployments list. All update on rolling intervals (totals 1.5s, db spark 1.5s, storage mutation 2.2s, deploys 3s). **METRICS** subpage: 6 stat cards (CPU / MEM / NET-IN / NET-OUT / DISK I/O / UPTIME) with live tick at 1.5s using sin+noise patterns and CPU/MEM activity bars that fill proportionally — cleaner gauge view for system-level vitals. **STORAGE** subpage: full sortable list of all 30 (mock) or all 977 (real) apps with a `filter by app name` text input that filters the list reactively; meta line shows `N apps · used / alloc`. **DATABASE** subpage: bigger 1100×120 sparkline + 6 stat cards (IDLE / WAITING / P95 LATENCY / QPS / CACHE HIT / REPLICATION LAG); the larger version pulls from the same `sparkBuf` and `dbCount` state, mirrors the small panel but with extra derived metrics (cache hit ratio, replication lag in ms). **DEPLOYMENTS** subpage: full unbounded timeline (max-height removed), pulling from the same deploys array with the same status pill + commit format. **SERVER LOGS** subpage: streaming feed of synthetic log lines (mix of nginx, auth, realtime, cron, cdn, db.write/read, storage, twitch.bot, gateway sources) at INFO/OK/WARN/ERR levels. New line every 1.1s, capped at 200 visible lines, autoscrolled when active. Format: `HH:MM:SS LEVEL src:column message:column` with column-aligned monospace. Random `#####` placeholders in templates get rolled into base36 ids on each push. **USER STATS** subpage: 6 stat cards (ACTIVE NOW with activity bar / SIGNUPS-24H / VOTES-24H / MESSAGES-24H / PEAK CONCURRENT / RETENTION-D7) ticking at 2.4s with light random-walk drift; PEAK auto-tracks all-time-high. Mobile @820px collapses sidebar to a stacked column above main. All real-data layer (`data.json` + `applyRealData`) preserved — sidebar `SNAPSHOT` field surfaces `generated_iso` so users can see how fresh the storage+deploys data is.
- 2026-04-30: Created. **Cluster ops dashboard with real filesystem + git data + live mock metrics.** 4-panel layout: top totals row (SERVICES / DB POOL / DEPLOYS-24H / REQ/S), STORAGE bars (left, 2-row span), DB CONNECTIONS sparkline + numerics (top-right), RECENT DEPLOYMENTS list (bottom-right). Each panel is a glassy slab with a subtle inner border bracket and 28px inset cyan glow. **Aesthetic**: `JetBrains Mono` everywhere, `Major Mono Display` for big numbers, faint 32px CSS background grid behind everything, 1px screen-blend scanlines. Sysadmin terminal vibe — green/cyan/amber/hot palette. Top bar shows region, uptime, and a live UTC clock that ticks every second. Footer is a `$ tail -f` console line with a blinking caret. **Storage panel**: each service row is `name | gradient bar | used / alloc unit`; bars run green→cyan→amber→hot as fill % crosses 70%/88%. Total at the top shows `used / alloc · pct%`. **DB panel**: huge `Major Mono Display` count (e.g. "78") with delta indicator (+/- vs last sample), 60-sample sparkline rendered to a canvas (path stroke + radial-gradient fill underneath + glowing dot at the head), plus 4 sub-stats (IDLE / WAITING / P95 LATENCY / QPS) refreshed every 1.5s. Sparkline uses `60 + sin(t*0.04)*18 + noise` so it gently wanders rather than spasms. **Deployments panel**: scrollable list with status pill (✓ OK / ✕ FAIL / ⚡ LIVE), app name, `<code>hash</code>` + commit subject, relative time. Live status has a pulsing amber animation. **Real data layer**: a `data.json` is generated at deploy time by a Python build script (`du -sb apps/*/` for sizes + `git log --pretty=...` parsing for the last 30 commits touching `apps/`). The dashboard `fetch('./data.json')` on boot — if it loads, **SERVICES** are replaced with the real top-30 apps by size (bytes → MB) and **deploys** are replaced with real commit hashes + ISO timestamps + commit subjects. Fallback to baked-in mock arrays if the JSON is missing or fails to parse. Storage allocation tier auto-picks from `[1, 5, 10, 25, 50, 100, 250, 500] MB` as the first tier above `usedMB * 1.4` so each bar reads at ~60-70% fill (the realistic SRE "almost full" feel). Footer flips to `snapshot · <generated_iso>` when real data is loaded so users know how fresh it is. **Mutation loop**: storage values wiggle by ±0.06 MB every 2.2s with occasional drift sign flip, the sparkline samples every 1.5s, deployments re-render every 3s (so relative times tick up), totals every 1.5s. Every 18s a mock fresh deployment may prepend to the list (only when running on mock data; real data list is static). Mobile responsive (`@media max-width:540`) collapses totals to 2 columns. Pollinations OG.

## issues
- **Real data is static at deploy time.** Browser JS can't query the server filesystem live, so the `data.json` snapshot is a fresh-at-deploy-time picture; the dashboard timestamps the snapshot in the footer. Subsequent commits won't appear until someone re-runs the snapshot script and pushes (an automated `pre-commit` or post-merge hook that regenerates `data.json` would keep it fresh — todo).
- The mutation loop wiggles `s.used` slightly on real data too, which is a tiny lie (real disk usage doesn't change while the page is open). Acceptable since the wobble is < 0.1 MB. To freeze: set `s.drift = 0` for real entries. Currently we set `drift = (Math.random() - 0.5) * 0.04`, very small.
- Sparkline data is 100% synthetic — there's no actual database connection metric to query. Keeping it as a "vibe" chart, clearly under "DB · CONNECTIONS" but acknowledged in the meta line "postgres · pgbouncer".
- Deployment list shows the commit subject as the second line; some commits have very long subjects (Moon Rover entries are 100+ chars), truncated at 80 with an ellipsis.
- Generation script would be cleaner as a Node build step, but Python is what was available; runs in <1s.
- Region is hardcoded `us-luna-1` (mock) → flips to `us-vibe-1` once real data loads (still mock — we have no actual region).

## todos
- Pre-commit/post-merge hook to regenerate `data.json` automatically.
- Click a service bar → drill-down panel showing per-file size for that app.
- Click a deployment → modal with the diff stat (additions/deletions).
- Real DB metric: query Supabase via the same anon key for an aggregated `count(*)` from a few key tables, plot the actual rate of change. Would replace the synthetic sparkline.
- Incident chip: when a service is `> 95%` for `> N seconds`, fire a banner alert.
- Time-series storage: persist the sparkline samples to localStorage so reloads show the last 24h trend instead of starting from a sin curve.
- Filter pills: filter deployment list by app name or status.
- Dark/light theme toggle (currently dark only).
- WebSocket "live deploy" feed integration if we ever stand up a CI agent.
