# neon-parkour-puzzle

## log
- 2026-04-28: Created. **3D first-person neon rooftop parkour with safe falls + gravity-flip zones** (merges 4 simultaneous chat requests: "3D neon parkour puzzle game with rooftops and safe falls" + "3D neon rooftop parkour with Three.js and puzzle elements" + "high-fidelity 3D parkour puzzle game using Three.js with vibrant neon rooftops and vertical challenges" + "3D parkour game with vibrant neon rooftops using Three.js and add gravity-flipping zones"). **Stack**: Three.js r0.160.0 via importmap (`https://unpkg.com/three@0.160.0/build/three.module.js`), single ES module script. WebGL renderer w/ pixel ratio capped at 2, FogExp2 (`#180735`, density 0.012). Camera = PerspectiveCamera 78° FOV, near 0.1, far 600. **Pointer-lock controls**: `canvas.requestPointerLock()` on click, mousemove → yaw/pitch via `e.movementX/Y * 0.0024`. Pitch clamped ±π/2-0.05. WASD + arrow keys = camera-relative XZ movement, Shift = sprint (16 vs 9 u/s), Space = jump (×2 max — double-jump enabled), R = manual respawn, Esc = release lock. **Player physics**: `pos`/`vel` Vector3s, gravity -28 u/s², terminal -45, jump impulse +11 (first) / +10 (double). Air control 0.45×, ground accel 70 u/s². Friction `pow(0.001, dt)` while grounded + no input. Coyote time 0.12s after leaving platform. Horizontal speed cap = sprint flag. Player approximated as upright capsule (radius 0.5, height 1.8). **Platform collision**: 20 BoxGeometry platforms in `PLATFORMS[]` w/ `{x,y,z,w,h,d,color,isStart,isGoal}`. `checkGround(pos, vy)` walks all platforms — if XZ-overlap and `feetY <= top + 0.05` and `vy <= 0.2` → snap feet to platform top + ground player. `collideXZ(pos)` push-out by smallest-axis when player intersects sides during traversal. **Layout**: rising spiral path from start plaza (0,8,0) → mid-rooftops with branch detour at (-26,16,-10) hiding sigils → ascending sky bridge → top spire → goal pad (0,34,-58) w/ golden portal ring + sky beam. ~26m vertical climb total. **7 sigils**: octahedron core (gold) + larger octahedron wireframe (pink) + sphere halo (additive blend) at fixed offsets above 7 platforms; collected when player center within 2.2u; `sigilSfx()` 880/1320/1760Hz triangle arpeggio + 18 gold particles burst. Banner shows `N / 7` on each pickup, `all sigils · portal awakens` on full clear. Goal portal beam fades from opacity 0.06 → 0.32 (pulsing) once all collected; reaching it triggers `triggerWin()` w/ stats overlay. **Safe-fall mechanic**: when `pos.y < -8` → `rescuePlayer()` → 30 catch particles burst at fall point + `catchSfx()` (bandpass-swept noise 400→2200Hz + sine 220→880Hz chime) + cyan radial flash overlay (`.flash.show` keyframe `catchPulse` 0.85s) + ✦ caught ✦ banner + lerp player back to `lastSafeSpot.y + 4` over 900ms easeOut cubic. `lastSafeSpot` updates every time player grounds on a platform (snapped to platform center). **Gravity-flip zones** (2): vertical antigrav columns at `(8,18,-42)` cyan and `(-6,26,-54)` violet, 4×22u and 4×18u. Visual: inner cylinder w/ 0.18 opacity additive blend + outer wireframe box + 5 stacked rising torus rings (rings advance phase per frame, opacity peaks at column center). While player center is inside a zone's AABB → vertical velocity gets +60 u/s² upward kick (cap 14 u/s) instead of normal gravity, and `jumpsUsed` resets so you keep your double-jump after the lift. Functions as a "flipped gravity" puzzle element — lets you reach platforms otherwise unreachable. **Skybox shader**: Sphere(380) BackSide ShaderMaterial w/ procedural synthwave gradient (deep purple bottom → hot pink horizon → indigo top) + horizon sun disk w/ horizontal scan-band stripes (synthwave classic) + sparse star field via hashed noise. Uniform `t` drives slow sun drift. **Distant grid floor**: 1200×1200 plane at y=-50, ShaderMaterial that draws scrolling 40×40 grid via fract+smoothstep, color-cycles pink↔cyan, fades by distance from origin. **Background towers**: 24 box towers at random angles 110-170u from origin, varying heights 30-85u, edge-glow LineSegments cycling pink/cyan/violet. Tower group rotates `t*0.00002`. **HUD**: top-left = SIGILS / HEIGHT / TIME pills (Major Mono Display labels + Audiowide values, glassy backdrop-filter:blur(6px), color-coded borders cyan/pink/gold). Top-right = "NEON · PARKOUR" Audiowide title w/ 2px chromatic-split text-shadow + "ROOFTOP · DRIFT" Syne Mono subt. Bottom-left = control hints. Bottom-right = ↻ RESET button. Crosshair = 14px circle + 2px cyan core dot, mix-blend screen. Banner overlay = Audiowide 48px cyan w/ glow + pink offset, `bannerIn` keyframe 1.6s scale+fade. Win overlay = full-viewport gold vignette w/ stats (sigils/time/peak height) + Run Again button. **Title screen**: full-viewport `.start` overlay w/ tutorial text, pulsing CTA chip (`pulseGlow` keyframe), camera idle-orbits scene at radius 24 while waiting; click → fades + requests pointer lock + starts timer. **Audio**: WebAudio synth suite — `jumpSfx(strong)` triangle 440→620Hz / 660→880Hz, `landSfx` 180Hz sine thump, `sigilSfx` 3-note triangle arp 880/1320/1760Hz w/ 60ms stagger, `catchSfx` bandpass-noise whoosh + rising sine chime, `winSfx` 5-note triangle arp 523/659/784/1047/1319Hz at 130ms intervals. Audio created on first click/keystroke (`ensureAudio()` resumes suspended ctx). **Particles**: gravity-affected (vy -= 6 dt), opacity fades by life/maxLife, mesh disposed on expiry. Used for double-jump puff (8), catch-rescue burst (30), sigil collect (18). **Mobile**: HUD pills shrink + bottom-aligned, but no virtual joystick yet (pointer lock + WASD assumes desktop) — should be added if mobile users complain. **Aesthetic**: void `#08001a` body + violet `#b86bff` / pink `#ff2bd6` / cyan `#3df0ff` / gold `#ffd96b` / bone `#f3eaff` palette. Audiowide + Major Mono Display + Syne Mono + Space Mono fonts. Pollinations OG.

- 2026-04-28: **Bugfix — start button unresponsive crash**. Chat reported start button completely dead. Root cause: `startOverlay.addEventListener('click', …)` was registered ~600 lines into the module script, AFTER all Three.js/WebGL/shader init. If anything threw during init (WebGL unavailable, importmap unsupported, shader compile fail on a particular GPU), the click listener never attached — the user saw the title screen with no way forward. **Fix**: split bootstrap into a non-module `<script>` that pre-binds the start-button click handler immediately on `DOMContentLoaded`, calling a `window.__parkour.begin()` function that the module sets when ready. Wrapped the entire module body in try/catch — on any init failure, the start overlay text swaps to a red `⚠ FAILED · TO · LOAD` message with the actual error. Also captures `window.error` events as a backup. Switched `import * as THREE from 'three'` to `await import('three')` inside the try block so importmap/network failures land in the catch instead of an uncaught module-level throw.

- 2026-04-28: **Bugfix — overlay layering**. Win overlay had `pointer-events:auto` + `inset:0` while invisible (opacity 0), sitting on top of the start overlay (later in DOM = higher stacking) and swallowing all clicks meant for the start button. Fix: `.win` defaults to `pointer-events:none; visibility:hidden`, only flips to `auto/visible` on `.show`. Added `z-index:5` on `.start` for extra defense.
- 2026-04-28: **Feature — Tyrell Corporation ziggurat + Blade Runner spinners + volumetric haze**. Massive 7-tier stepped pyramid placed at `(-110, -12, -250)`, total height ~102u (tier widths 90→14, heights 18/16/16/15/14/12/11). Each tier = `BoxGeometry` w/ `MeshBasicMaterial` mapped to a per-tier `CanvasTexture` from `ziggTexture(rows, cols)` — paints a `512×256` canvas: `#0c041a` base + 5px violet striations + a window grid where ~78% of cells get a hot-orange/red rectangle (outer `rgba(255,56,32,0.55-0.9)` bloom + inner `rgba(255,188,138,0.7-0.95)` core). Each tier also wears a thin red rim glow `LineSegments` (`#ff3a1c`, opacity 0.55). Crown beacon at top: bright orange `Sphere(2.4)` + 30u red Sprite halo (additive). Both ziggurat tiers + beacon use `fog:false` so they punch through the dense `FogExp2(0x180735, 0.012)` (which would otherwise wipe them out at distance 250 — fog factor `exp(-(0.012*250)^2)≈0.0001`). **Volumetric haze**: 7 large red-tinted Sprites (170-290u wide, 0.13-0.23 opacity, additive blend) drift slowly around the ziggurat at varying altitudes — each bobs `±drift*2.4u` on Y via `sin(now*0.0005+phase)` and slow X drift. Plus 5 low purple-magenta haze blankets at ground level (240×100u sprites, 0.22 opacity) for the iconic dystopian groundfog. **Flying cars**: 48 spinners. Each = `Group` with 2 additive Sprites (red 2.6u front + amber 2.0u rear at local offset `(-2.2, -0.4, 0)`). Group rotates yaw to align with velocity (`atan2(vx,vz)`) so the amber light trails behind in world space. Spawn in box of 340×240u centered on tyrellPos at altitude 18-100u. Each has random heading + speed 5-14u/s + 0.6-2.0u Y bob at 1.6Hz. `updateSpinners(dt, now)` advances pos, wraps when ±220x or ±180z out of zone. **Helper**: `createGlowTex(hex)` generates a 128×128 radial-gradient Sprite texture for halos/lights/haze. **Frame loop**: beacon halo pulses scale `0.86-1.0×` at 0.66Hz, haze sprites drift, spinners update — added between tower rotation and updateHUD. Deep red glow against the violet skybox = signature Blade Runner vista.

- 2026-04-28: **Feature — jump key remap**. Default still Space; persisted to `localStorage['neon-parkour-jump-key-v1']`. Two entry points: button at bottom-left of the start overlay (works pre-game, no pointer lock), and button in the bottom-right toolbar `JUMP: SPACE` (works during game after Esc to release lock). Click → `remappingJump = true`, button labels swap to "PRESS · ANY · KEY · (esc · cancel)" with cyan pulsing-glow keyframe `listenPulse`. Next keydown captures `e.code` and persists. Esc cancels. The bottom-left HUD `<kbd>` element also reflects current binding via `humanizeKey(code)` which maps `Space → SPACE`, `KeyA → A`, `Backspace → BKSP`, arrows to glyphs `↑↓←→`, `ShiftLeft → L SHIFT`, etc. `e.preventDefault()` on the jump-key keydown ensures Backspace doesn't trigger browser back-nav (chat specifically wanted Backspace as a "live dangerously" jump option for zennlogic — fully supported).

## issues
- **No mobile controls**: pointer lock + WASD = desktop-only. Mobile visitors see HUD but can't play. Should add virtual joystick + tap-to-jump for mobile (low priority — game is desktop-first by design).
- **Collision is AABB-only with no slope/edge handling**: hopping off corners feels fine, but if you scrape against a platform side while jumping, you'll stop dead. Acceptable for boxy rooftops but worth tweaking if it feels janky.
- **Player can clip into the side of a platform from below** if vertical velocity is high enough on a frame: collision resolves to the smallest XZ push-out which can teleport you sideways. Mitigated by gravity cap (-45) but not bulletproof.
- **Last-safe-spot tracking only updates on ground-contact**: if player jumps into the void from mid-air with no ground touch since spawn (e.g. they sprint off the start plaza without landing elsewhere), they respawn back at the start. Fine.
- **Gravity-flip zone is technically a "lift" not a real gravity flip**: player still oriented head-up, can't walk on undersides of platforms. Implementing real gravity flip with inverted ground-check would require flipping camera roll + bottom-plane landing logic. Acceptable simplification — visually + mechanically it reads as "gravity is reversed in the column."
- **Goal trigger uses simple cylinder-distance check (4u XZ + 4u Y)**: stand on the goal platform without crossing the ring → still wins. Could tighten to "must walk through the ring" but current behavior is forgiving = good.
- **No skybox loading state**: shader compiles instantly but on weak GPUs the sky may flash black for one frame. No mitigation.
- **Three.js is ~600KB from CDN**: first-load delay possible on slow connections; cached aggressively after.
- **No checkpointing UI**: the `lastSafeSpot` is invisible; users may not realize they're being respawned at the last platform they touched. Banner says "✦ caught ✦" which gives the gist.

## todos
- Mobile: add virtual joystick (left thumb) + jump/sprint buttons (right thumb) + look-by-touch.
- Speedrun timer leaderboard: Supabase `parkour_runs` table w/ time + sigils-collected, top-10 display.
- More platforms: branching paths with optional sigils, secret areas requiring gravity-flip combinations.
- Moving platforms: oscillating Y or XZ paths with carry-velocity for the player.
- Wall-running: detect player against vertical face while jumping, allow Shift+jump to wall-kick.
- Platform-specific gravity zones (e.g., a zone that flips gravity for the entire scene briefly).
- Color-coded sigils: collect-in-order puzzles where wrong order resets sigils.
- Time trial mode: ghost replay of best run as a translucent player figure.
- Better audio: ambient synth pad drone + distant pulsing bass to match the synthwave aesthetic.
- Photo mode: F to freeze + free-cam orbit + screenshot.
- Real gravity-flip implementation: orient player upside-down + invert ground check to land on platform undersides.
- Endless mode: procedurally generate platforms above the goal that scroll downward forever.
