# ultimate-barn · notes

## log
- 2026-07-22: v2.3 — **WALL JUMPS + WALL SLIDES** (chat: proper physics, spring off vertical surfaces). Mechanics: x-collision while AIRBORNE registers wall touch (wallDir ±1, 0.12s wall-coyote; screen edges count too). WALL SLIDE: airborne + falling + holding INTO the wall → vy capped 140 (vs 900 terminal), scuff marks drawn at the contact edge; release the key = free fall (slide is a choice). WALL JUMP: jump input during wall-coyote while airborne → vy=-JUMP·0.95 up + kick vx=RUN·1.35 AWAY, face flips, 0.16s LOCKOUT during which momentum owns the chicken (holding back toward the wall cannot cancel the kick — the classic feel); control returns after. Ground jump takes priority (else-if chain); grounded wall-jump impossible. Triangle blip on kick. Works on base platforms, hay, planks, springs (any solid), and screen edges — every trap tower chat builds is now a movement route. Build v2.3 (sentinel). SUITE 10/10: touch registration, slide cap 140, hug-release freefall, spring up -532 + kick -337, lockout immunity, control return, grounded exclusion, CHAINED kicks between two towers (kick left → catch left tower → kick right +338).
- 2026-07-22: v2.2 — persistence-path TRACE (chat: 'obstacle still saving immediately on placement') + the actual fix. TRACE VERDICT: HEAD is correct — exactly ONE supabase insert exists in the whole app, inside confirmPending() (post-proof); the placement pointerdown contains zero persistence (marks pending only). The report matches v2.0 behavior → stale browser cache (v2.1 had shipped minutes earlier; the barn had no staleness defense). FIX: fresh-build sentinel in the watchdog script — __BARN_BUILD version constant, 5-min self-refetch (no-store + ?fresh= bust), regex-compare served build, mismatch → hay-styled pill '🌾 a fresher barn (vX) was raised — you are on vY · [reload]', shows once. Tested: same-build silence, newer-build pill + reload wiring, cadence. BUMP __BARN_BUILD EVERY DEPLOY (like clothes-minded's hash stamp). Report-back for the user: hard reload once — from then on the barn announces its own updates.
- 2026-07-22: v2.1 — **VALIDATION PHASE** (chat: trap persists only after its builder beats the level with it; 200 most recent CONFIRMED survive). New contract: clear → place → trap enters PENDING ('wet paint': 0.7 alpha + marching hay-dashed outline, fully FUNCTIONAL — it can and will kill you) → beat the barn again WITH it live → confirmPending(): insert + broadcast fire ONLY now → same proof-clear immediately offers the next placement (every clear does double duty: seals your wet trap + earns a new one — continuous prove-as-you-build loop). Death during validation changes nothing (keep trying). X (or scrapPending) deletes the wet trap locally — never persisted, escape hatch for self-bricked runs. Leaving the page abandons pending naturally (local-only). HUD wet-trap pill; footer states the rule ('placed AND proven beatable by its builder'). The 200-cap already applied at load = 200 freshest CONFIRMED. SUITE 11/11: pending blocks insert+broadcast, wet trap lethal, death preserves pending, proof clear → insert exactly once + broadcast + permanence + next offer, scrap leaves no residue.
- 2026-07-22: v2.0 — FULL REBUILD on a per-app config (chat: "create the supabase-config file and push a full rebuild"). The barn no longer touches the site root: apps/ultimate-barn/supabase-config.js is its own copy (established pattern — benzingrad/piano-orbit/wire-desk carry theirs), game module imports './supabase-config.js' RELATIVE, watchdog v2 probes the SAME relative path (import() + fetch), so diagnosis and reality can't diverge. Verified: only real external import is the jsdelivr @supabase/ssr ESM (the root-path string in the file is a doc comment), exports intact (default client + supabaseSession), both scripts node --check clean, rebuilt module passes the strict-mode boot replay. If the blank screens were the root config path not serving, this kills that failure class dead; if they were CDN stalls, the watchdog now names it on screen. Self-contained per project rules, finally.
- 2026-07-22: v1.2 — watchdog v2 per chat ('unknown error' on canvas → 'need a raw exception catch and log before the module import fails'). Root insight: window.onerror is BLIND to module/resource failures (opaque events, empty message) — v1.1 could only shrug. v2 catches at the SOURCE: (1) dynamic import('/supabase-config-fixed.js').catch in the CLASSIC script = the raw exception of the exact import the game performs ('config import THREW: …'); (2) fetch probes: config (status+content-type) + jsdelivr @supabase/ssr HEAD — the failing LINK gets named; (3) error listener v2 distinguishes resource failures (names target URL) / real errors (message + stack line) / opaque events (labeled honestly); (4) unhandledrejection captured; (5) everything → window.__barnLog + console.error mirror; timeout paints up to 6 raw log lines + 'screenshot THIS and post it in chat'. SUITE 8/8: raw import exception, both fetch probe outcomes, resource-URL naming, opaque labeling, rejection capture, console mirror, paint, heartbeat silence. Next blank screen = a screenshot with the actual culprit named.
- 2026-07-22: v1.1 — blank-screen report investigated (chat). Could NOT reproduce: HTML tag-balance clean, strict-mode module boot clean (module scripts run strict — sloppy-mode stub tests can miss this class, so the replay was strict), import chain verified (/supabase-config-fixed.js exports intact). Verdict: client-side, most likely the jsdelivr ESM import stalling — module failures are SILENT (whole script dies, no console for most users, static page + dark empty canvas = 'blank screen'). Shipped the countermeasure instead of a guess: (1) classic-script WATCHDOG (survives module death) — captures window error events, and 3.5s without the module's __barnAlive heartbeat paints the actual diagnosis ONTO the canvas ('the barn failed to wake up · error: … / module never started — likely the CDN import stalled · hard reload · tell chat this message'); (2) loop() sets window.__barnAlive每 frame. Verified: dead-module → paints diagnosis; alive → silent. Any future blank is now a readable bug report. (Also fixed in passing: mesa-rex favicon URL was missing a UTF-8 byte — %F0%A6%96 → %F0%9F%A6%96.)
- 2026-07-22: v1 — chat greenlight ("Ultimate Barn party platformer: each cleared run adds one permanent obstacle to a shared Supabase level"). ORIGINAL take on the genre mechanic (accumulating-sabotage platformer) — own name/art/code, no commercial IP. **Loop**: run the single-screen barn (START pad → barn-door GOAL, 6 base platforms) → clear → offered 3 random obstacle kinds of 5 (🌾 hay solid 46², 🪵 plank solid 96×14, ⚠️ rake kill-spikes, 🐔 hen patrolling killer ±70px sine, 🦘 spring bounce 1.35× jump) → ghost-place with pointer (green/red validity ring; REFUSED on start/goal safe zones, out of bounds, or overlapping another trap) → nailed into barn_obstacles (kind,x,y; read-all/own-writes; e2e probed) → run restarts, level permanently crueler for EVERYONE. **Shared**: boot loads newest 200 (reversed to build order), broadcast channel ultimate-barn-v1 'obstacle' → traps appear live in every open tab with a toast; presence-lite via status pill. **Physics**: AABB x-then-y, gravity 1700, run 250, jump 590, coyote 0.1s + 0.12s jump buffer, fall-off-world death, kill boxes inset 3px for fairness. R = voluntary restart. Deaths/clears HUD (session). Touch: ◀ JUMP ▶ row on coarse pointers. De-clicked audio boops (4ms attacks per house rule). Rye + IBM Plex Mono, night-barn palette w/ lantern glow at the door. SUITE 15/15 effective (16 checks, 2 initial fails were: stub textContent number-vs-string [4th occurrence — real DOM stringifies] and a single-frame spring test that needed a fall loop — retested ✅ vy=-796 vs jump -590).

## issues
- Level CAN become impossible if chat bricks the door corridor — cap of 200 newest means old traps rotate out eventually, and safe zones protect spawn/door, but a determined wall of hay could gate progress. Relief valve candidates: own-trap deletion, or a demolition vote.
- builder column currently 'anon' — wire sloppyBarGetContext username later so traps carry signatures.

## todos
- Trap signatures + "planted by X" on hover
- Demolition votes for impossible-barn recovery
- Epochs: every 200 traps archive the barn and hang a plaque
