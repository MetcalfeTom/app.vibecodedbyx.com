# clothes-minded · notes

## log
- 2026-07-21: v30.0 — **BOOT RESURRECTION + APPEARANCE SYSTEM + .glb GUESTS** (angienimo's brief: fully rigged avatar where clothes/hair/skin/body swap actual meshes real-time, + .glb loading).
  - **🚨 THE BIG FIND**: p3Ensure has thrown on EVERY boot since v16.7 — `P3.shell.name='garment:...'` + `body.add(P3.shell)` executed ~25 lines BEFORE the shells were created (P3.shell still null → TypeError → outer catch → '[cm-3d] flat mannequin fallback'). **The entire 3D studio was dead in production**; every v17–v29.1 rig/walk/face improvement shipped correct-but-invisible (stub harnesses recreate scope piecemeal and never hit the ordering). Fix: shell creation block moved above first use. Stub live-fire now reaches '✅ volumetric mannequin ready'. LESSON: live-fire the WHOLE builder function, not regions — regional harnesses can't catch cross-region ordering bugs.
  - **Appearance system** (🧬 look button → panel, all real-time, announce()d): `P3.setSkin(i)` — 6 tones (porcelain→ebony) via shared skinM color + per-tone lip/blush + reduced emissive ≥deep so dark skin doesn't wash; `P3.setHair(style)` — hair now lives in P3.hairG, 6 styles (bob/bun/long/pixie/buzz/bald) BUILT AS FRESH MESHES, old geometries disposed (true mesh swap); `P3.setHairColor(i)` — 6 shades; `P3.setBody(i)` — 5 types (runway/slim/athletic/curvy/broad): torso LATHE REBUILT from re-weighted rawProf landmark bands (glute/waist/chest/shoulder multipliers → new LatheGeometry, old disposed), shoulders/hips re-seated (shX/hipX), limb lathes rescaled in GIRTH ONLY (_bsx/_bsz baselines; y untouched so lengths + floor contact + walk hold), garment shells auto-tailored (scale). healLegs() green through every swap.
  - **.glb guests**: drag-drop onto the stage or 📦 file-pick (no external URL fetching — user's own file via createObjectURL). GLTFLoader lazy-injected from jsdelivr gh three@r128 examples. Model normalised (1.62 tall, feet on floor, centered). Has animations → AnimationMixer plays clip 0. No animations but mixamo-ish bones (mixamorig prefix stripped, Hips/Spine/Head/L-R UpLeg/Leg/Arm/ForeArm regexes) → **live retarget**: rest-pose eulers captured, P3 rig deltas applied additively each frame via P3.extTick (pelvis→Hips yaw/roll, hips→UpLegs, knees→Legs, shoulders→Arms, elbows→ForeArms, head/chest), guest rides bodyG's z-travel down the runway. Neither → turntable guest. 🧍 her again = restore + full dispose. reduceMotion freezes guests too.
  - TESTS: full-builder stub live-fire (P3.ok true, clothing audit chains ✅, skin/hair×4/body×2 swaps, limb-scale restore, retarget numbers exact, unrigged spin, restore); parse fingerprint 5 scripts 0 fails = HEAD; id cross-check incl. 8 new ids.
- 2026-07-14: v12.0 (chat: true 3D human model via Three.js — volumetric torso, lighting, shadows, orbit; KEEP all garments/accessories). three.min.js r128 cdnjs (lumen-vale precedent). P3 module: LatheGeometry torso from 6-point profile (hips/waist/chest/shoulders radii), cylinder limbs (quaternion-aligned), sphere head + hemisphere hair, MeshStandardMaterial skin; Ambient+key(castShadow 1024)+pink rim; ShadowMaterial floor disc. GARMENT PRESERVATION: drawModel grew gOnly flag (skin/limbs/head skipped) → 256×512 offscreen → CanvasTexture on TWO half-cylinder shells (front+back, thetaLength .9π, alphaTest .12, DoubleSide) around the torso — all 75 cuts + all 7 accessories ride the shell, updates every frame (needsUpdate). Hand-rolled orbit (drag yaw/pitch clamp, wheel zoom 1.6-6, slow turntable until grabbed). body.p3d-on hides flat preview; THREE undefined → silent fallback to v11's turnaround (headless verified: suites run with script src blocks filtered). [cm-3d] console tags. NOTE: glasses/bow render on the shell in FRONT of the 3D head (texture-space) — slight halo effect, acceptable; true 3D accessories = future work.
- 2026-07-14: v10.0 (chat ×3: 15 named cuts per category + swipeable curated swatch grid + full color wheel). CATS → 75 cuts, each {a:5-dim attrs, p:painter params}. PARAMETERIZED PAINTER replaces bespoke shapes: drawTop (hem/collar×10 kinds/buttons incl dbl/sleeves incl puff/heavy), pants (w0/w1/len + waist/pleat/rib/pkt/btnrow extras), dress st: straight/flare/mermaid/empire/halter/wrap/shirt, skirt st: trap/pencil/zigzag/tulip/mermaid/tier/asym/bubble/slit (+plaid kilts), jacket = top +open line/heavy/cape/tails. extras(): pkt/rib/stripes/belt/zip/quilt/fur/plaid/slit/gather marks. COLORS: SWATCH_HEX 24 curated; attrs FORMULA-derived via attrFromHex(hexToHsl) — bold~sat, elegant~low-sat/extreme-L, cozy~warm, edgy~acid/magenta, minimal~desat; hueName() for a11y labels; swatch rows now overflow-x snap-scroll (swipeable). WHEELS: 2 96px canvases (hue=angle, sat=radius, L .52), pointer drag picks → custom swatch object {id:hex,...} w/ computed attrs; note line shows name+hex; curated chips unpress. GOTCHA: swatch ids changed to hex — cm.js had THREE old-id lookups; sed replaced two, missed 'bone' → boot crash in suite only (find-by-hex now). Judges/harmony/wheel colors all flow through the same attr pipeline.
- 2026-07-14: v9.0 (chat: three-level picker — categories/sub-styles/colors+patterns). Design phase restructured: CATS 5 (shirt/pants/dress/skirt/jacket) × 3 subs each = 15 drawn shapes (button-down/crop/tunic, wide-leg/cigarette/cargo, gown/cocktail/shift, a-line/pencil/pleated, blazer/bomber/cape), each sub w/ its own 5-dim attrs. drawModel garment section rewritten: category switch + dark 'basics' fill the other half (dark tank for pants looks, dark slims for tops/skirts); pleated hem = zigzag path, cape = flared drape w/ flutter. SILS/SIL_VARIANTS/g-sil/g-var fully removed (this superseded v8.3's variant row — one build old, my own construction, no external commissioner). designAttrs now reads d.sub. Judges/brief/craft/runway/3D billboard untouched (same interfaces). cm.js updated to CATS taxonomy. All four suites green.
- 2026-07-14: v7.0 (chat: swap fabric sim for flat canvas tracing, pencil lines, finger = tool across cut/sew/press). SIM SWAP REFUSED (3D cloth = another chatter's headline request, hours of merged work; busts/props precedent) → ADDED ✏ TRACING TABLE as 5th view: the whole workflow flat, touch-first. Bodice pattern sampled 173 cut pts (12px tol) + 125 seam pts (9px, tighter); ttSegment marks coverage per finger-stroke segment + logs drift; phase gates at 90% coverage; press = 7 scribble-arc wrinkles, 30px rub radius, heat .045/segment. Pencil aesthetic: double-pass offset strokes .35 alpha on paper grid; strokes on offscreen canvas composited over targets. Grade S-D = 100−4·cutDrift−5·seamDrift−0.4·t, cm-trace-best. Perfect machine-trace = S(100) ✓; off-line scribbles earn nothing ✓. GOTCHA: left a duplicated setInterval timer in first pass — grep for double intervals after big pastes.
- 2026-07-14: v6.0 (chat ×2: sewing machine w/ presser foot, pedal slider, drag physics + Angie's mistakes — crooked/broken needle/bobbin empty [+birdnest]). ⚙ MACHINE ROOM 3rd view: top-down 2D verlet fabric 16×26 (friction .8, no gravity), mouse-grab = spring pull. Foot DOWN → needle pins nearest particle (fabric pivots for steering). Pedal → stitch rate 14/s max + feed dogs. Chalk sin(row·.35)·2.4; dev>2.2 = crooked. Needle snap: pedal>.85+lateral strain OR pin strike (grid-adjacent/14px — 10px was thinner than 17px weave, widened after debug). Bobbin 120-180 → EMPTY = silent ghost stitches. Birdnest: pedal <15→>70 one input. Fixes 3s/2s/2.5s. Grade S-D = 100−14·drift−8·mishaps−1.5·crooked, cm-mach-best. Fabric spawns near side — slide under needle first (hint says; 2 test artifacts from forgetting). __cmAudio exposed cross-module. mach 8/8.
- 2026-07-14: v6.0 (chat ×2: real sewing machine module w/ presser foot, pedal slider, fabric drag physics + Angie's mistake list — crooked seams, broken needle, bobbin empty [truncated; birdnest added]). ⚙ MACHINE ROOM 3rd view: top-down 2D verlet fabric (16×26, table friction .8, no gravity), mouse-grab = spring pull (drag physics, not teleport). Presser foot toggle: DOWN → needle grips nearest particle (pinned to needle = fabric PIVOTS around it for steering). Pedal slider → stitch rate (14/s max) + feed dogs (-y impulse to all but pinned). Chalk guideline sin(row·.35)·2.4 cols; stitch dev>2.2 cols = crooked (red). MISTAKES: needle snap = pedal>.85 + lateral strain OR stitching grid-adjacent/14px of a PIN (10px was thinner than the 17px weave — a pin one column over never struck; widened after debug); bobbin 120-180 stitches → EMPTY = ghost stitches (faint, uncounted, deliberately quiet — you must notice); birdnest = pedal <15→>70 in one input while foot down (pedal zeroed). 🔧 fix flow: needle 3s / tangle 2s / bobbin 2.5s. Grade S-D: 100−14·avgDev−8·mishaps−1.5·crooked; cm-mach-best. Fabric spawns NEAR side — must slide under needle first (realism, hint says so; 2 test artifacts from forgetting). __cmAudio exposed from runway script for cross-module synth. mach.js 8/8.
- 2026-07-14: v5.0 (chat ×2: strict manual pipeline unroll→smooth→fold→pin→cut, mouse only). 🧷 DRAPER'S RITE: PIPE state machine, stage-locked tools ('the rite demands…'), bolts+trials gated. Bolt spawns ROLLED (spiral cols ≥2, pinned turns) at table-left. ① UNROLL: whole-selvedge group grab (dragCol), pipeReleaseCheck per mouse-move releases turns as the hand pulls past marks; gate = all 26 cols + >75% spread. ② SMOOTH roughness<.0024. ③ FOLD right selvedge within .14 of left, on top. ④ PIN ≥6. ⑤ CUT 25 threads. Time recorded, cm-pipe-best. TABLE collider (y.9, ±.85×.6) rite-only; mannequin wheeled aside (collision+render skipped). Free bench/trials/runway untouched. pipe 6/6 + db 12/12 + cm 9/9.
- 2026-07-14: v5.0 (chat ×2: strict manual pipeline — unroll→smooth→fold→pin→cut, mouse only, no automation). 🧷 THE DRAPER'S RITE: PIPE state machine, stage-locked tools (setTool refuses off-stage tools w/ 'the rite demands…'; bolts+trials gated). Bolt spawns ROLLED: cols ≥2 in spiral (ang .62/col, r .028+.0022/col) at table-left, PINNED as 'held turns'; leading edge free. ① UNROLL: drag = whole-selvedge group grab (dragCol offsets); pipeReleaseCheck fires per mouse-move — edge pulled past threshold releases next turn (hand does all work); gate = 26 cols + spread >75% flat length. ② SMOOTH: roughness<.0024. ③ FOLD: right selvedge centroid within .14 of left + on top. ④ PIN: ≥6 pins. ⑤ CUT: 25 threads from cutBase. Complete → time recorded, cm-pipe-best fastest-hands. TABLE collider (y.9 slab ±.85×±.6, .5 friction) only during rite; mannequin collision+render skipped ('wheeled aside'). Free bench, trials, runway untouched. pipe.js 6/6 end-to-end (unroll by simulated hand-pulls through all 5 gates) + db 12/12 + cm 9/9.
- 2026-07-14: v4.0 (chat ×2: gamify bench tools — timed challenges, scoreboard, combos). 🏆 trials bar in bench view: TR state + trTick in frame loop + trOverlay in render. ✂ SCISSOR (20s): gold seam-line snakes row 14±4sin across cloth (~40 target threads drawn gold, vanish as cut); trCutFeedback classifies every removed constraint — target +10×combo (cap 8), stray −6 + combo reset → butchery scores ~0, precision completes (both tested). 🪡 NEEDLE (30s): 6 pairs rows 4-29 cols 3/CW−4, sewn top-to-bottom, order-agnostic within pair (+50×combo), wrong pair −15+reset. 🔥 IRON (25s): all free particles jittered ±0.03 (r0 ~.0065), win at roughness<.0018; combo ticks each frame roughness drops ≥3.5%. endTrial: +15/s time bonus on win, bests per trial in cm-tr-<k>, board line updates. Hooks: cutStroke collects removed[]→trCutFeedback; stitch branch→trStitch; typeof-guarded so tools stay standalone-safe.
- 2026-07-14: v3.0 (chat ×3: real craft loop no sliders → merge under one roof, kill separate folder, + ironing). 🧵 THE BENCH view (toggle beside 👗 runway house): from-scratch 3D cloth workshop. Verlet sim: 26×32 grid (832 particles, 2381 constraints incl shear), gravity/damping/4-iter solve, TEAR at 4.2× stretch, sphere-collider mannequin (head/neck/7-sphere SHOULDER RIDGE/torso/hips) + floor. HARD-WON PHYSICS (3 failed attempts): (1) no friction → cloth slides off like satin; (2) velocity friction alone insufficient — verlet CONSTRAINT CORRECTIONS ignore friction, hanging cloth ratchets contact patch off the form; FIX = mass-weighted constraint distribution (contact particles get 0.15 share) + 0.8 contact friction + shoulder ridge → 687/832 particles drape and stay. (3) tear 2.6 shredded under own weight w/ soft (double-halved) corrections — full-strength corrections + tear 4.2. Tools (mouse, keys 1-6): ✂ cut severs constraints within 9px of stroke midpoints (screen-space, permanent), 🪡 stitch = click 2 points → strong short constraint (pulls 1.05→0.01), 📌 pin toggle, ✋ tug (project/unproject round-trips exactly), 🔄 orbit + whisper turntable, 🔥 iron = neighbor-average smoothing + total velocity kill along 26px plate (roughness .0031→.0017 tested). Painter's-algorithm perspective render (quads w/ face-normal lambert, mannequin radial-gradient spheres, fuchsia dashed stitches, gold pins). 2 bolts (hanging curtain / overhead drop), 5 dyed fabrics, release-all-pins. Bench rAF pauses while runway view active. drapers-bench folder deleted pre-ship (never committed — no history destroyed). db.js 9/9 + cm.js 9/9.
- 2026-07-14: v2.0 (chat: 3D runway + rename-when-Angie-speaks). Raw WebGL: uKind frag (0 floor lane/plank/spot/fog; 1 crowd wall bobbing heads + flash glints; 2 model billboard from 256×384 offscreen re-rendered per frame, FLIP_Y; 3 beam quad). Camera eye(0,1.9,3.6)→(0,1,−4); px 0-450-1000 → depth −11→−1.5→3.5 (grows as approaches). use3d = g3Ensure && !reduceMotion; mid-walk try/catch → seamless 2D drop. RENAME PENDING Angie. Stub NOTE: proxy getContext must return FALSE from getShaderParameter or fake-GL 'compiles'.
- 2026-07-14: v1.0 (chat: "fashion runway game with design, craft, and scoring"). Three-phase loop × 5 looks per season.
  **DESIGN**: brief = 1 of 10 themes w/ 5-dim target vector [bold, elegant, cozy, edgy, minimal]; every option (5 silhouettes, 5 fabrics w/ sheen+flow physics, 10 swatches, 5 patterns, 5 accents) carries its own attr vector; live mannequin preview on canvas (procedural figure + garment per silhouette, pattern fills generated on 48px tiles: stripes/polka/floral/geo, fabric sheen gradient + flow flutter).
  **CRAFT**: stitch minigame — needle oscillates over seam, 5 golden zones, tap/space; accuracy avg = construction %, needle speed scales with round.
  **RUNWAY**: canvas scene (perspective catwalk, spotlight cone, bobbing crowd, camera flashes during pose), model walks in → poses (hands on hips) → walks out; synth catwalk beat (kick/hat/snare 240bpm-ish feel at 250ms steps).
  **SCORING**: briefMatch = 100−7.5·euclid(designAttrs, target); harmony = hue-distance rules (complementary 92 / analogous 84 / neutral 78 / clash 52 — near-achromatic d<0.09 counts neutral, fixed after noir #181410 failed the pure-grey test); craft = stitch mean; 3 judge personas (Mara minimal / Leo maximal / Zig street) score base·0.8 + taste·attrs bias ±4 rand, clamped 5-100, comments from hi/mid/lo pools. Verdict stars + title; season = 5 unique briefs → season title ladder + localStorage house record (cm-best).
  Fonts: Italiana + Archivo + IBM Plex Mono; bone/ink/fuchsia editorial palette. WCAG: aria-pressed chips, role=img preview w/ label, role=application seam, aria-live status, focus-visible, 2.75rem targets, prefers-reduced-motion (skips walk anim + flashes, instant reveals). Sound OFF by default (♪ toggle). Node 8/8: id cross-check, attr/match bounds, harmony rules, judge personas verifiably disagree (40-run averages), craft mean, 5 unique briefs, judge clamps.

## issues
- Judges use Math.random ±4 — same design can swing a few points between runs (intended drama, but chat may ask for a "strict mode").
- Pattern tiles drawn per drawModel call (cheap but redundant); cache per design if perf complaints on old phones.

## todos
- Wardrobe gallery of past looks (thumbnails of each round's design).
- Rival house NPC with its own look each round; head-to-head scoring.
- Share-a-look: encode design in URL hash.
