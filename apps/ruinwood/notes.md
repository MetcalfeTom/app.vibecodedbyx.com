# Ruinwood — notes

## log
- 2026-07-06: v4.4 — sword auto-grab (chat ask): walking within **2 tiles** (16px, squared-dist check in frame loop) of the standing blade takes it automatically when unarmed — pickup refactored into takeSword(tx,ty) shared by the E-path (kept for the already-armed flavor line). Harness-verified with a directed test (frame() now exposed harness-side): grabs at 1.5 tiles, does NOT at 3+.
- 2026-07-06: v4.3 — balance pass (chat ask): **world 72×52 → 96×68** (~75% more tiles; only the MW/MH consts — camera/gen/scans all parametric). **Enemy density cut ~⅓**: wantN min(24, 12+2d) → min(16, 8+d) — combined w/ the bigger map the woods feel much emptier/lonelier (intended). **Bare-fist punch**: attack key now ALWAYS works — unarmed = 750ms cooldown (vs 300), 9px range (vs 12), 0.5 dmg (hp is float-safe), 520ms stagger (longer stun than the sword's 260ms — its real value), 40% knockback, stubby skin-tone jab anim (sin arc, 220ms) + low-freq whoosh via sndSwing(fist), one-time "bare knuckles" lore line replaces the old empty-hands refusal hint. **Sword-placement fallback**: random ring scan can fail in dense/wet spawns (hit it live: TEST world-1 had 0 swords in the 96×68 gen!) — exhaustive full-map pass now takes the eligible tile nearest ~9 tiles from spawn; harness asserts swords===1 per world. NOTE: MW/MH change means same-seed worlds differ from v4.2 (saves restore safely — position guard drops you at spawn if your old coords are now inside a wall).
- 2026-07-06: v4.2 — feedback FX (chat ask): **drowning vignette** — #drown DOM overlay (z4, pointer-events none) w/ bottom-up red gradient whose reach is CSS var `--climb` = 18% + swimProgress×48 + missingHearts×28 (cap 95%), opacity 0.3–0.95, driven per-frame while swimming, fades (0.55s) on land; the red literally climbs as the cold takes hearts. **Stalker lunge shake** — hurtPlayer() now RETURNS whether damage landed; a landed hit from a stalker mid-lunge sets shakeUntil=now+280ms; render() backfills dark then ctx.translate's the frame ±3.5px decaying — gated on prefers-reduced-motion. Browser-verified: dry opacity 0 → 0.51 @ 49% climb after 2.5s swim → heart drained + climb rebases; screenshot confirms the red wash (needed transition:none override in harness — same headless frame-freeze artifact as wire-desk dark mode).
- 2026-07-06: v4.1 — QoL bundle (chat ask): **save-state** — ruinwood-save-v1 {seed,depth,x,y,hp,sword,altar} written every 5s while unfrozen + at grow/portal/sword/altar; "↩ continue SEED · world N" button in menu (shown at boot + on ✦ new world via refreshContinue) replays the whole generate() chain 0..depth so deep-world biome rolls reproduce (prevBi dependency), then restores pos (wadeAt-guarded)/hp/sword/altarFound. **Pulsing E-prompt** — square canvas badge w/ live bind label (keyLabel(binds.use) ≤3 chars, VT323 7px) floats over the FACED tile when it's altar/sword/portal/well/hut, sine-pulse alpha (static 0.9 under reduced-motion). **Fading keyboard hints** — #hintbar bottom-center shows live binds on grow/continue, fades (1.4s transition) after 8s OR on first bound keypress; hidden entirely on coarse pointers. Touch controls already existed (dpad/E/⚔) — extracted showTouch() reused by continue. HARNESS NOTE: the new setInterval keeps Node alive — rwtest stubs setInterval/setTimeout now. Browser-verified: hints show→fade on input, reload→continue button labeled correctly→world+dialog restored, zero page errors.
- 2026-07-06: v4 — **square UI + sword loot + swimming w/ drain + 3 enemy types** (3 overlapping chat asks merged). **Square**: every border-radius zeroed (pills, buttons, dialog, inputs, kbd, dpad, tcE/tcA circles→squares); pixel depth pass: inset-bevel + hard-offset shadows (no blur) on all UI, :active press-in states (translate+shadow shrink), stage::after edge vignette (radial + inset ring, z4 under UI, pointer-events none). **Sword**: you start every world UNARMED — T_SWORD=16 stamped once per world on grass/deco 5–14 tiles from spawn (rng scan, 500 tries), drawn as blade-in-stone w/ pulsing glint; E to draw it (attack gated on player.hasSword, 3s-throttled hint line if empty-handed), ⚔ shown in hearts pill, portals carry it (2nd sword = flavor line), grow() resets it. **Swimming**: wadeAt() = solidAt minus water for the PLAYER only (shades still can't cross — water is now an escape route); 0.45× speed, half-submerged sprite w/ animated ripple ring, deeper/slower sine steps, cold-warning line on first entry, **drain: 1 heart per 4.5s in water** (ignores i-frames; swimAcc resets on land). **Enemy types** (all biome-tinted via foe colors): shade (baseline roam+chase), stalker (25%: 1hp, thin tall 1-eye sprite, shadows you at 0.72× then LUNGES 2.4× for 0.7s w/ 2.4s cd, eye goes white while lunging, 60px aggro), brute (15%: foe.hp+2, wide horned sprite, 0.55× speed, 7px contact range, shoves you 22px, takes only 40% knockback). Node harness: 1 sword/world, all 3 types spawn w/ correct hp, chain determinism preserved. Headless-browser smoke test: world gen, movement, swim warning fired, square radii verified computed 0px, zero page errors. NOTE: gen changed (sword consumes rng before enemies) — same-seed worlds differ from v3.
- 2026-07-06: v3 — **every portal world is a different biome** (3 chat asks merged: fresh biome + palette + tree shapes / unique sky + inscription / villages + monsters). 7-entry `BIOMES` table: green (home) / ember / pale / violet / sporeling / burnt / amber wood — each with full tile palette (grass/dot, water/glint, shore, tall-grass, tree quad), a `shape` id for `drawTree()` (0 round canopy, 1 snow-tipped pine, 2 birch w/ notched pale trunk, 3 dead snag w/ ember tick, 4 giant spotted fungus, 5 drooping willow), 2-line biome tree lore, sky pair (applied to `document.body` radial gradient = the void around the stage), and a `foe` block (name/body/core/eye colors + hp 1–3 + spd -7..+11: moth shades fast/fragile, frost wraiths & spore drifters slow/tanky, ash hounds fast). Biome pick: depth 0 always green wood; deeper = `1 + hash2(seedNum,3,11)*(N-1)`, collision-bumped so consecutive worlds never repeat (prevBi only mutates along the deterministic 0→1→2 portal chain, so replaying a seed chain reproduces every world — Node-verified). **Villages**: 1–2 per world, 7×5 landlocked clearings ≥14 tiles from ruin, ≥16 apart — 3–4 solid huts (T_HUT=14, thatch roof tinted with the biome's tree colors) + a stone well (T_WELL=15), both with examine lore. **Inscriptions**: 12-entry pool, altar line picked by `hash2(seedNum,7,13)` so world 7 is never the same ruin twice. Portal arrival line announces biome + foe ("world 3 — the violet wood. moth shades move between the trees."). Tested by evaluating the FULL script under DOM stubs in Node (harness-side hook injection, nothing shipped): clean load, depth 0–8 chain, biome/village/foe/inscription asserts, chain-determinism replay, seed divergence — all green. NOTE: sandbox had no HTTP egress this session; live URL not curl-checked.
- 2026-07-06: v2.3 — Waterfox/Firefox find-bar fix (chat report): keydown now e.preventDefault() on every BOUND key (WASD/E/attack + any remap) plus space/arrows/apostrophe/slash — stops find-as-you-type stealing movement keys when the game has focus. Guarded by an input/textarea target check FIRST so the seed field still types W/A/S/D freely. Subtle ⓘ browser-note in the menu, shown only for Firefox/Waterfox UA: "click the forest once so movement keys go to the game, not the find-bar."
- 2026-07-06: v2.2 — **CRITICAL FIX: T_PORTAL undefined crashed the whole app on load** (chat report via urughak: no world, grow button dead). The v2 patch that added portals declared `T_PORTAL=13` via a single-line string replace, but the tile-const declaration wraps across TWO lines, so the replace silently no-matched — T_PORTAL was USED (LORE table at top-level, drawTile, gen, travel) but never DECLARED. Top-level `LORE[T_PORTAL]=[...]` threw ReferenceError at load → aborted the IIFE → zero event handlers bound → menu stuck, grow inert. The `new Function` syntax check passed (valid identifier, undefined only at runtime); caught by executing the full script under DOM stubs. Fix = add T_PORTAL=13 to line 270. Verified: load throws nothing + grow() generates world, closes menu, sets seed/hearts. LESSON (noted below): syntax-check ≠ runtime-check for multi-line sed edits.
- 2026-07-06: v2.1 — **movement-freeze bug fixed** (chat report: WASD dead after seed gen) + brighter palette. Root cause: `grow()` shows the intro line via `say()`, and `frozen()` counted an open status dialog as frozen → the movement/enemy loop (gated on !frozen()) blocked WASD until the dialog was dismissed. Input binding was correct (global keydown, keys[k] flags); the input-LAYER gate was wrong. Fix: frozen() now only trips on the menu + remap overlays; the status dialog is non-blocking (use() already advances lines before its own frozen check, so no regression). Palette brightened across every tile painter (grass #33452f→#436b3c, tree greens +~50% luma, water #17434c→#1f5f6b, stone family lifted) — woods read sunlit instead of murky.
- 2026-07-06: v2 — **hack-and-slash + portal chain** (chat ask). New remappable ATTACK action (default SPACE): 300ms-cooldown swing w/ 140ms visible arc sweep (rotates ±0.85 rad around facing), hits shades ≤12px in the front half-plane — 2HP each, knockback + white hurt-flash, death poof (6 green pixels) + low thud. **Shades**: 12+2/depth (cap 24) spawned ≥9 tiles from spawn, outside ruin; roam (1–3s wander legs, 30% idle) → chase ≤46px at 26+2/depth speed; wall-bounce collision; the ruin repels them (old stones, old rules). Contact = 1 heart, 1.1s i-frames (sprite flicker), 3 hearts (DOM pill); death → respawn at spawn w/ lore line. Movement/enemies freeze while any dialog/menu open. **Portal**: T_PORTAL stamped beside the altar in EVERY ruin (animated violet swirl) — stepping on it generates world depth+1 from fnv(seed+#depth), arriving at the NEW world ruin portal (one-way, infinite chain); seed pill shows "· world N"; rising portal sweep sfx; 1.5s travel cooldown so you can step off. Touch: ⚔ button above E.
- 2026-07-06: v1 — forest biome pixel world generator (chat ask). **Gen**: seed string → FNV → mulberry32 + hash2 value-noise fbm. 72×52 tiles: elevation fbm w/ radial island falloff → water/shore; forest-density fbm → trees; deco rolls (rock/flower/shroom/tall-grass). **The one ruin**: 400 candidate scans for an 8×6 landlocked footprint → stamped: corner pillars (25% already fallen), walls w/ gaps + rubble, slab floor, south-facing doorway w/ cleared approach, center ALTAR (pulsing gold spark until found). Spawn ≥22 tiles from ruin (fallback ≥12) so it must be found. **Player**: pixel wanderer (cloak/hood/satchel-hints-facing), continuous movement w/ 4-corner AABB vs SOLID tiles, diagonal normalized, camera clamped w/ sub-tile scroll (fx/fy offsets). Canvas 240×160 internal (30×20 view of 8px tiles), CSS pixelated upscale. **E examine**: facing tile → per-tile lore pools (deterministic pick via hash2 so a tile always says the same line); ALTAR first-touch = lore sequence incl. the seed itself + deep chime + altarFound. **Remappable keys**: up/down/left/right/use, capture UI (click→press, esc cancels, conflict steal), localStorage ruinwood-keys-v1, menu hint renders live binds. Seed pill + new-world button; last seed prefilled from localStorage. Touch: dpad + E button (coarse only). Water shimmer ticks, ruin fireflies, step blips + triad chimes. Pixelify Sans + VT323. Pollinations OG.

## issues
- Tiles say the same lore line forever (deterministic by position) — intentional, feels like a real place.
- No goal beyond finding the ruin — it's a generator/toy, not a quest game.
- Verify with FULL-script execution under DOM stubs (rwtest harness pattern in v3 log), never just a `new Function` syntax check — v2.2 lesson.
- Biome/world gen must stay deterministic from `fnv(seed + '#' + depth)` — never mix `Math.random()` into gen (only menu `randSeed()` uses it), or seed replay + the altar's "will grow this exact wood again" promise breaks.

## todos
- Chat may want: day/night, more ruin variants, creatures, minimap, shareable seed via URL hash, biome sliders.
