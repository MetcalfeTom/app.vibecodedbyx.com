# Keep Momentum — small wins, reflected back

## log
- 2026-08-28: v1.0 — built per chat: smallest polished Keep Momentum with local storage, editable AI-style reflection, approve/edit/discard. DELIBERATELY SMALL: one input, one draft box, one log — nothing else. AI-STYLE REFLECTIONS, honestly labeled in-place before every draft ("written by little templates on this page — no model, no network. edit it into truth"): deterministic per text (fnv→mulberry), three-part opener+middle+closer, middle picked by a 5-way classifier (start/finish/again/hard/plain — "Finishing collects a debt the start took on — paid in full"). THE THREE VERDICTS: ✓ approve saves entry+reflection ("the wheel turns a notch"), ✎ edit focuses + selects the draft ("the draft is yours now"; probe proves the EDITED text is what saves, not the template), ✕ discard saves the entry PLAIN ("your words were already enough" — discarding the reflection never discards the deed). The sub states the covenant: "the reflection never outranks you." Log: quoted reflections, plain entries unquoted, turn counter, dates; injection-inert via textContent (probed with a live img-onerror). Clear is kind ("clearing a list never undoes a deed"). localStorage (keep-momentum-v1, 30-entry cap), flywheel spin on save (reduced-motion killed), geometry check from day one. Probe 14/14 first run.

## issues
- none.

## todos
- a weekly "wheel report" summary card, streak-adjacent gentle counter (days with turns, no guilt for gaps), if chat asks.
