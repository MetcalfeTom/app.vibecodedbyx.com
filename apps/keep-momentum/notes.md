# Keep Momentum — small wins, reflected back

## log
- 2026-08-28: v1.5 — two trios, per chat: the fill-in row became DONE TODAY (🚶 took a walk / 🍽 did the dishes / 🛋 rested on purpose — unchanged) and gained a sibling WORKING ON row of three in-progress goals (✍ still working on the essay / 🏃 training for the 5k, slowly / 📦 clearing the spare room, bit by bit — dashed borders so "not finished" reads at a glance). Same covenant as v1.4: chips fill the box with editable text, focus it, never submit; each group label says so; goal chips get their own status line ("still in motion counts, by the way"). The writer gained ONE middle so goals get an honest sentence instead of a finish line: `ongoing` — "Still in motion is still motion. Unfinished is not undone." — matched by working on / in progress / halfway / not done / bit by bit / training for / learning / slowly, checked before `again` (so "still working on" is ongoing, "went for a run again" stays again). Flow untouched by construction: all 23 v1.4 checks pass verbatim + two-trios, goal-chips-editable-flow (edited goal text + its reflection saved), goal-discard-plain, chips-reachable-44 (scroll-normalized hit test on all six). Desktop 27/27, mobile 5/5 (new chips-tappable-clamped). Rings note still says five — the sixth middle is a sentence, not a ring.

- 2026-08-28: v1.4 — two chat requests in one release. FILL-IN CHIPS: three accessible option buttons under the prompt (🚶 took a walk / 🍽 did the dishes / 🛋 rested on purpose — rest counts, on purpose) that FILL the input with editable text and never auto-submit (group aria-label states "nothing submits until you choose"; probe fills, edits to "rested on purpose, no guilt", runs the full approval flow, asserts the EDITED text saved). PRACTICAL LABELS: "↻ reflect it back" → "✍ get reflection"; chip labels "Edit it first if you like"; chip status "…then get your reflection"; probe sweeps main for zero "reflect it back". SUITE FIX with a lesson: the hit-test check started failing after chips added height — clean-page debug showed hit=INPUT#did, so the app was innocent; earlier focus() calls scroll the now-taller page and the input drifted off-viewport by check time. The check's intent is occlusion, not scroll position → scrollTo(0,0) before measuring. LESSON: elementFromPoint checks in long suites must normalize scroll first — focus() side-effects accumulate. Suites: desktop 23/23, mobile 4/4.

- 2026-08-28: v1.3 — mobile typography clamp, per chat (shipped on request after the v1.2 audit flagged it): @media (max-width:40rem) steps the triple-scale down to phone-sane sizes (h1 2.3rem, input 1.25rem, etc., incl. !important overrides for the inline-styled nolog + buildstamp); desktop ≥40rem untouched. Verified BOTH ways: full 21-check desktop suite passes verbatim at 1000px (type-tripled still asserts ~44px input), and a new 4-check mobile probe at 375px — no-horizontal-overflow (scrollWidth vs clientWidth), type-clamped, controls-reachable (elementFromPoint), buttons-tappable (≥40px). Mobile screenshot confirms the whole app fits in ~1.5 screens.

- 2026-08-28: v1.2 — prompt rewording per chat, two requests in one: "what moved forward?" → "what did you do today?" EVERYWHERE (panel heading, input aria-label, sub line, meta description — probe sweeps main for zero remaining "moved forward"), and the follow-up nudge made conversational + consistent: "anything counts — even 'got up'. so, what did you do today?" Flow untouched by construction — all 20 prior checks pass verbatim plus prompt-consistent; suite 21/21.

- 2026-08-28: v1.1 — chat art-directed live through four escalating requests, all shipped as one release: TYPOGRAPHY TRIPLED (every rem font-size ×3 via regex patch — input now ~44px computed, h1 ~79px; column widened 32→64rem; probe asserts computed sizes), FULL-SCREEN OLYMPIC RINGS (fixed viewport-filling SVG backdrop, five circles, opacity .16, aria-hidden, pointer-events none; probe fixed for the scrollbar-vs-innerWidth discrepancy — compare against documentElement.clientWidth), 30% INFINITY SIGN (fixed ∞ glyph at font-size:30vw bottom-right, probe checks |computed − 0.3×innerWidth| < 3px), DARK THEME (walnut night palette, all white surfaces → #141009, probed: body bg channels <40, ink >220 — contrast preserved because content sits on solid panels at z-index 1; elementFromPoint hit-test proves the input reachable through the decor), RESTRAINED LASER TRAIL (canvas z2 pointer-transparent, 14-point cap, amber fade, ABSENT under reduced-motion — canvas removed entirely). ANIMATION FIX (chat's final ask): the trail loop is demand-driven — rAF stops when the trail empties, restarts on pointermove; concretely verified by the harness itself: dump-dom probes hung on the always-running loop and complete cleanly after the fix, no rAF cap needed. Handoff flow untouched: all 14 v1.0 checks pass verbatim. Suite 20/20. HARNESS LESSON: screenshots of infinite-rAF pages hang virtual-time-budget mode — use real-time --screenshot (no budget) + timeout instead.

- 2026-08-28: v1.0 — built per chat: smallest polished Keep Momentum with local storage, editable AI-style reflection, approve/edit/discard. DELIBERATELY SMALL: one input, one draft box, one log — nothing else. AI-STYLE REFLECTIONS, honestly labeled in-place before every draft ("written by little templates on this page — no model, no network. edit it into truth"): deterministic per text (fnv→mulberry), three-part opener+middle+closer, middle picked by a 5-way classifier (start/finish/again/hard/plain — "Finishing collects a debt the start took on — paid in full"). THE THREE VERDICTS: ✓ approve saves entry+reflection ("the wheel turns a notch"), ✎ edit focuses + selects the draft ("the draft is yours now"; probe proves the EDITED text is what saves, not the template), ✕ discard saves the entry PLAIN ("your words were already enough" — discarding the reflection never discards the deed). The sub states the covenant: "the reflection never outranks you." Log: quoted reflections, plain entries unquoted, turn counter, dates; injection-inert via textContent (probed with a live img-onerror). Clear is kind ("clearing a list never undoes a deed"). localStorage (keep-momentum-v1, 30-entry cap), flywheel spin on save (reduced-motion killed), geometry check from day one. Probe 14/14 first run.

## issues
- none.

## todos
- a weekly "wheel report" summary card, streak-adjacent gentle counter (days with turns, no guilt for gaps), if chat asks.
