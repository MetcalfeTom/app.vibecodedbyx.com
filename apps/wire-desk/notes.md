# The Wire Desk — notes

## log
- 2026-07-06: v1 — news database app (chat ask: split layout + "store articles in sup[abase]", follow-up ask: NZ Herald + RNZ priority feeds alongside BBC / Reuters / Al Jazeera). **Layout**: masthead ("THE WIRE DESK", Bodoni Moda 900 + Spline Sans Mono, cream broadsheet palette: paper #f2ead8 / ink #211c14 / wire-red #a82414) over a split main — left 19–26rem wire feed (search input + source rail + headline list w/ HH:MM red timestamp, boxed uppercase source chip, green WIRE tag on feed rows, serif headline), right bone reading pane (red source block, full date + relative, italic byline, drop-cap first paragraph). Mobile ≤820px: single column, story pane slides over w/ back button + Esc. **Data**: `wire_desk_articles` (id bigserial PK, headline, source, content, byline, link, kind, + auto user_id/created_at/updated_at, default RLS read-all/write-own, indexes on created_at + link). kind = 'feed' | 'community'. **Feed ingestion**: browsers can't fetch the RSS feeds (none of them send CORS headers — verified with curl), so `ingest.js` (zero-dep node, runs in the sandbox / via cron, NOT in the browser) fetches 5 official feeds — NZ Herald rss.nzherald.co.nz, RNZ /rss/national.xml, BBC feeds.bbci.co.uk/news/world, Reuters via Google News RSS `source:reuters` (Reuters killed their public RSS), Al Jazeera /xml/rss/all.xml — parses items w/ regex RSS parser (CDATA-safe, see issues), strips HTML, dedupes by `link` against the newest 1000 feed rows, anon-auths via REST `/auth/v1/signup {}`, inserts ≤12/feed with **created_at = real pubDate** so the wire is truly chronological. Seeded 58 articles on first runs. **Cron**: durable job `1db4db23` every 2h at :43 runs the ingester (7-day auto-expiry — needs re-arming). **Frontend**: source rail chips ALL / NZ HERALD / RNZ (NZ pinned first per chat) / BBC / REUTERS / AL JAZEERA / COMMUNITY; feed stories show summary + "read the full story at <source> ↗" (https-checked, noopener); community compose (headline/source/byline/story, byline persisted to localStorage) inserts kind='community'; retract only on own community rows; realtime INSERT/DELETE via postgres_changes prepends w/ red flash + keeps count pill live. All user text rendered via textContent (no innerHTML) — XSS-safe. WCAG basics: rem everywhere, roles/aria-live on status + compose, aria-pressed chips, focus-visible, 44px targets, reduced-motion kills flash/slide.

- 2026-07-06: v1.1 — added **Stuff** (chat ask). stuff.co.nz/rss serves **Atom** (`<entry>`, link in `href` attr, `<published>` date) — parser now matches item|entry blocks, prefers href when `<link>` body is empty/non-URL, falls back published→updated for dates. Rail order: ALL / NZ HERALD / RNZ / STUFF / BBC / REUTERS / AL JAZEERA / COMMUNITY. DB seeded: 70 feed articles across 6 sources (12 each, NZ Herald 10).

- 2026-07-06: v1.2 — chat reported stuck "connecting…" + no articles, suspected RLS. **RLS verified healthy** (bare anon key AND fresh session JWT both read rows via REST — unlike the app_votes quirk, select USING(true) works here). Actual bug: boot awaited `supabaseSession()` BEFORE `load()`, so a hung/slow auth (e.g. auth-lock contention with the nginx top bar's own supabase client) blocked reads that need no session at all. Fix: load()+subscribe() fire immediately; session resolves in background under an 8s Promise.race timeout and only gates compose/retract; load() retries 3× w/ backoff; "warming up the presses…" placeholder while fetching.

- 2026-07-06: v1.3 — **full headless-Chromium network trace** (chat ask to trace page load). Built a real browser harness in the scratchpad: playwright-core + chromium_headless_shell, missing system libs (libXcomposite/atk/nss/dbus/drm/xkbcommon etc.) extracted from Debian-bookworm .debs into LD_LIBRARY_PATH (no root needed — pin bookworm versions, trixie builds need GLIBC_2.38), tiny node server mimicking nginx path-mapping (/wire-desk/→apps/wire-desk, root supabase-config-fixed.js). **RUN 1 (served over HTTP): perfectly healthy** — config → jsdelivr ESM graph (8 modules) → REST GET articles 200 @277ms → 70 .wire-item nodes in DOM, status "70 on the wire"; auth signup ran in parallel AFTER reads started (v1.2 decoupling confirmed working). **RUN 2 (file:// open): reproduces the report exactly** — `import '/supabase-config-fixed.js'` resolves to file:///supabase-config-fixed.js → blocked (CORS, origin null) → module never executes → status stuck "connecting…", 0 articles. Root cause of the report = opening the HTML from disk (or a blocked CDN), NOT RLS, NOT app logic. Fix shipped: inline non-module watchdog — if `window.__wireBooted` isn't set 6s after load, status turns err: "db client failed to load — open this app via sloppy.live (not a local file) and disable script blockers". Re-traced both runs after the fix: served run unchanged, file:// run now shows the explanatory error.

- 2026-07-06: v1.4 — smartass42 reported exact console error `supabase-config-fixed.js:1 Failed to load module script` = Chrome's wrong-MIME error (server answered the module request with an HTML fallback/404 page instead of JS). sloppy.live unreachable from sandbox (can't inspect nginx response for root-level files), so fixed decisively by **vendoring**: copied supabase-config-fixed.js into the app folder, import './supabase-config-fixed.js' (app-relative assets always serve correctly + matches self-containment policy). Harness-verified: module 200s from /wire-desk/supabase-config-fixed.js, 70 articles render. NOTE: other apps (card-lounge, age-of-ai, cosmic-greenhouse, dimension-ten, bridge-to-miranda) still import the root-absolute path — if similar reports come in for those, vendor the same way.

## issues
- **CDATA-before-tag-strip**: `<![CDATA[...]]>` contains no `>` until its terminator, so a naive `<[^>]*>` strip swallows the whole payload — BBC + NZ Herald wrap titles in CDATA and parsed to ZERO items until stripTags unwrapped CDATA first. Order: unwrap CDATA → strip tags → decode entities → strip again.
- Feeds have NO CORS headers — never try to fetch them from the browser; ingest.js is the only path. sloppy.live itself is unreachable from the sandbox (HTTP 000) while external domains work — don't burn time on curl-checking the live URL.
- Feed rows belong to whatever anon user the ingester created that run — nobody can retract/update them via RLS, table grows ~100 rows/day. Fine for now (app reads latest 200); if it ever matters, ask about an admin cleanup path.
- Cron auto-expires 7 days from 2026-07-06 — re-arm with CronCreate or re-run `node apps/wire-desk/ingest.js` manually.

## todos
- Chat may want: more feeds/categories (sport, tech), per-source article pages, byline linking to Twitch identity, unread markers, ticker tape of latest headlines across the top.
- Could add sub-second polish: relative timestamps auto-refresh (currently static per render).
