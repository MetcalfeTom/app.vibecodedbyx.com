# Shroom Glen

## log
- 2026-08-01 v1.3 — **cat-crow banter system** (per chat): periodic speech bubbles above both sprites trading insults. HTML chip bubbles (cream/orange for cat, black/red for crow, CSS tail) projected from 3D each frame via Vector3.project(camera) → screen px, hidden when behind camera or expired. **Exchanges**: 12 hand-written opener+retort pairs with alternating openers ("You molt." / "Says the fungus vacuum." · "We are many. You are one." / "I am one. Nine times."), fired by a 9–16s logic-layer timer when crows are present; reply lands 1.7s after the opener on the nearest crow (ref-tracked; hides gracefully if that crow despawns mid-line). Alone in the glen → 50% chance of a cat mutter instead ("…too quiet."). **Contextual barbs**: crows gloat on a landed hit ("CAW-ught you."), and a crow that flies through unhit has a 25% chance to earn the cat a dodge-brag ("Missed. Again."). Single-exchange cooldown (~5s busy window) prevents bubble spam; hit gloats preempt. aria announces both lines. 13/13 checks green: bank shape, timer paths (banter/mutter), reset window, opener→scheduled reply, cooldown, projection positioning, expiry, hit-gloat, dodgebrag, crowless fallback.
- 2026-08-01 v1.2 — **elvish translator mushroom** (per chat, asked twice). Rare violet-gold variant: taller stem, brighter purple emissive cap, gold halo; at most ONE alive in the glen, ~12% chance per respawn slot. Pulling it scores +1 like any shroom but fires an `elvish` event instead of `collect` → harp-pentatonic arpeggio, double particle burst (violet+gold), and a small non-blocking popup card sliding up bottom-center: the phrase in Elvish (Cormorant Garamond italic, glowing) over its English original, footnoted "as the glen remembers the elven tongue". 14-phrase bank mixing authentic Tolkien (Elen síla lúmenn' omentielvo / Pedo mellon a minno / crebain for the crows) with glen-lore constructions ("Nine lives, one heart" → "Nertë cuili, er órë"). Auto-hides after 7s, click dismisses, aria-live announces pair. Crows keep flying while you read — dodge first, translate second. 9/9 checks green: bank sanity, one-elvish cap, +1 + event semantics, popup fill/auto-hide/click-dismiss, full frame pipeline, normal collects unaffected.
- 2026-08-01 v1.1 — **persistent global leaderboard** (per chat). New table `shroom_glen_scores` (display_name, score, user_id + RLS: read-all ✓, insert/edit/delete own-only) — verified END-TO-END from the sandbox: anon session insert → HTTP 201 → a *second fresh session* reads it back (global read confirmed, unlike app_votes) → cross-session DELETE correctly refused by RLS. App: separate `<script type="module">` owns supabase (ESM createClient, getSession→signInAnonymously) and exposes `window.glenLB {ready, submit, refresh}`; main script stays classic and just calls the glue. Game-over overlay gains name input (sanitized [a-zA-Z0-9_- ] ≤16, persisted) + "carve it" button (disabled at score 0, double-submit guarded, offline message "the glen keeps no records tonight") + top-10 list (escaped, #1 tinted amber); start overlay shows a best-hauls top-3 line. 5/5 glue checks green (gameover→refresh, empty-name refusal, sanitization, submitted-state, double-submit guard) + live DB probe. One `claude_smoke_test · 3` row remains from the probe (its session is gone; RLS prevents cleanup by design — first real player buries it).
- 2026-08-01 v1: chat asked for "low-poly 3D forest game with a cat that follows the mouse, glowing shrooms to collect, crows to dodge — Three.js, no frameworks". Three.js r128 (cdnjs, same as lumen-vale), single file, no frameworks. **Architecture (deliberate, for testability)**: pure logic layer `L` + `tickLogic(dt)` (cat seek/arrive + heading slerp, world-radius clamp at 54, 8-shroom population invariant, crow spawn/steer with 0.4 lead toward the cat's target, hit detection with 2s invuln, 9 lives → gameover) emitting an event queue consumed by the render/audio layer — so the whole game logic is stub-testable without WebGL. **Scene**: fog-matched twilight (#1d1430), displaced flat-shaded ground disc, ~80 low-poly trees (5-sided trunks, stacked 6-sided cones, 3 leaf tints), 160 drifting fireflies (Points), directional moon + warm center light. **Cat**: orange box-cat with cream chest, green emissive eyes, cone ears, 4-segment nested-group tail that sways, trot leg-swing when moving, blink-flicker during invulnerability. **Shrooms**: emissive teal caps + pulsing transparent halo, chime + cyan particle burst on collect. **Crows**: black cone body, flapping box wings, red eye, caw telegraph on spawn (audio), bob in flight, despawn past world+30 or 14s. **Camera** soft-follows the cat; mouse→ground via manual ray-plane intersection (no Raycaster); touchmove = mouse. Difficulty: crow interval 5.5s→2.0s, speed 13→24. Wind-noise ambience (audio unlocks on start button), best score in localStorage, WebGL-missing fallback message with disabled start. Grandstander + Reddit Mono HUD. 14/14 checks green: fallback path, FULL buildScene live-fire via Proxy-THREE (R3.ok flag — per repo lesson), seek/clamp, collection invariant, caw/hit/invuln/gameover events, restart, entity render sync.

## issues
- ~~WebGL can't run in the sandbox — real visual confirmation must come from chat.~~ **CONFIRMED WORKING on real device 2026-08-01**: chat user coldpresss played it and harvested the first elvish translator mushroom (+1 awarded) — so rendering, cat follow, collection, the elvish popup, and presumably crows all function in a real browser. frame() hot path is field-verified.
- Crow meshes are keyed by array index — fine because crows array is filter-rebuilt each tick and meshes fully resync each frame; don't "optimize" that into stable ids without also changing the filter.

## todos
- Pounce (click = dash) if chat wants more agency.
- Shroom varieties (rare gold = +5, wilting = timed).
- Night/day drift; owl ally that intercepts one crow.
