# Slime Lab

## log
- 2026-08-05 v4.1 (`56693ae`): **orbital camera** per chat ("full 3D with orbital controls without losing bubble physics") — no engine swap. `TILT` now mutable (pitch 0.14–0.55) + `CAMYAW`; drag EMPTY space during mixing (outside `|x-bx|<rx·1.35`, `y>topY-95`) to orbit. Yaw = `orbitYaw(dg)` rotates all surface entities (rest parts, bubbles, ripples [now carry y], scrapes, trail) around bowl axis in normalized ellipse space — radius preserved exactly; sticker + scratches are angle-based (`cos(worldA+CAMYAW)`), hide behind rim with edge fade. Pitch = `orbitPitch(nt)`: bh scales `1.25−TILT·0.85` (low camera = taller silhouette), entities re-anchored across the surfaceY shift (`g1.surfaceY + (y−g0.surfaceY)·sry1/sry0`), ground shadow height `3+TILT·22`. Physics always runs in current screen frame → popping/landing/drips/motor identical mid-orbit. Harness: 19 checks incl. radius-preservation to 4 decimals + post-orbit popping. **True free-perspective 3D would still need a three.js rewrite — poll chat before attempting; don't hack it into this file.** Stamp v4.1 · 56693ae.
- 2026-08-05 v4.0 (`1c4dfd3`): **bowl progression system** per chat. `BOWLS` tiers keyed by `PROG.batches` (localStorage `slime-lab-progress-v1`): **plastic** (start; stirMul 0.85 friction + 900Hz gritty bed while stirring + fading `MIX.scrapes` marks + static scratch/sticker render; spawns `MIX.bubbles` during stir — pop by steering spoon within 14+r px, unpopped ones → `LAST_BUBBLES` → `decor.bubbles` rings inside the finished blob), **heated glass** (need 3; translucent w/ side layers + pulsing coil + heat shimmer; bubbles auto-pop at age>1.1s; stirMul 1.3), **electric mixer** (need 7; steel bowl + MX-9 base w/ LED + arm; stage-3 = hold-to-run motor: omega ramps to 22 rad/s, whisk `MIX.whirl`, stir += omega·dt·0.30, vortex dimple, live centrifugal label `ω²·(srx/1000)/9.8` g with ⚠ overspin >3.6g + rim-fling splashes). Bowl card on bench (locked buttons show 'mix N more'), batch counter, `finishMix` increments+saves+unlock-toasts. Snapshot `MIX.bowl` at freshBatch so switching mid-mix is safe. Smoke rewritten: 13 checks across all tiers. Stamp v4.0 · 1c4dfd3.
  - NOTE: batches only count in the SAME browser (localStorage) — chat asking "why locked" = different device/incognito.
- 2026-08-05 v3.0 (`d6540ca`): **ceramic overhaul** per chat — glossy ceramic bowl replaces the glass beaker (**refraction pass + MIXBUF removed**; glass look retired). Ceramic = glaze linear gradient + radial specular gleam + warm table bounce light + rim-thickness band (outer rx / inner riX=rx·0.90) + interior wall gradient + pigment-colored painted stripe. `drawRoom` now lamp-lit (warm radial pool at mat + vignette — applies in play mode too). Pour physics visuals: glue = wavy bezier stream + 3-ellipse viscous coil at impact; activator = discrete drip particles (`MIX.drips`, spawn every 0.13s while held) → splash ripples (`MIX.ripples`, 0.8s life; sprinkle landings spawn small ones too) + droplet spray (`MIX.splashes`); activator pools as pale surface patches until stir m≥0.85. Mix-ins now 5 chips (glitter/beads/**stars**/**foam**/charms) with 3D-ish rendering: `drawBead` (radial-gradient sphere + spec), `drawStar` (10-vertex path, spins), glitter sparkle cross-flash, `drawFoam` (3-circle matte clump; foam also ×0.985 damp = softer jiggle) — same shapes inside the blob via decor.stars/decor.foam. Spoon = wooden scoop (2-tone ellipses + handle highlight + goo shadow) with fading blend trail (`MIX.trail`). Surface radius capped to inner wall (`min(hwAt, rx·0.85)`). Stamp bumped v3.0 · d6540ca. All checks green.
- 2026-08-05 v2.2 (`5aa5815`) + stamp (`c3a99ff`): **refractive glass + version stamp**. Refraction = screen-space pass (NOT WebGL): backdrop under the bowl copied to offscreen MIXBUF each mix frame, re-drawn through the bowl clip in 36 strips with cubic edge displacement (`u³·11 − u·4.5`) + center magnification (1+0.10(1−u²)); liquid side-layers ×1.045 horizontal magnify; cyan/pink Fresnel fringes on inner walls. Stamp convention adopted (parla-style): `VER` const = 'v2.2 · 5aa5815' → etched on bowl front in drawMix + `.stamp` badge bottom-right of stage (always visible; lets users detect stale cache — doesn't bust it). **Bump VER + hash on every future feature commit.** Harness note: offscreen canvas means smoke-test stubs need getContext on createElement'd elements too.
- 2026-08-05 v2.1: **third-person tilt + bowl depth** per chat question ("or is a full 3D engine needed?" — answer: NO, fake-3D elliptical projection in plain 2D canvas). `TILT = 0.30` (ellipse ry/rx). Beaker → curved **glass bowl** (bezier silhouette via `bowlPath`): you now look INTO the mouth — back rim arc, shaded interior-wall region (rim ellipse down to surface ellipse), then a **liquid surface disc** (ellipse at `hwAt(liquidHeight)` radius; bowl profile narrows toward the base) tinted by whatever floats on top (activator until stirred, then blend). Stir streaks + pour splash-ripples now render ON the tilted surface disc; fill-target lines became dashed depth ellipses; spoon head is projected/clamped INTO the surface ellipse and drawn squashed; sprinkles land distributed across the surface ellipse (`eoff` per particle, land-y = surfaceY + eoff·sry·√(1−u²)) and orbit elliptically while stirring. Side-view layers through the glass kept (that's the "layered" readout). Mat got matching perspective: grid fans toward viewer from a center vanishing point + quadratically-compressed depth rows. Same smoke suite green (8/8) + parse/ids. If chat ever wants MORE 3D (orbiting camera, real lighting), THEN it's a three.js rewrite — current approach caps at a fixed tilt.
- 2026-08-05 v2: **hands-on layered bowl-and-spoon mixing** per chat — the automatic v1.1 mix animation is replaced by an interactive 4-stage sequence (the user does the pouring, in order): (0) **glue** — press & hold to pour, stream follows pointer x, liquid rises to a dashed fill line; (1) **activator** — hold to drizzle, forms a visibly separate pale layer floating on the glue with a boundary line; (2) **add-ins** — tap 3× to sprinkle pigment squares + glitter/beads/charm particles that fall and rest on the surface; (3) **stir** — a spoon follows the pointer, drag circles around the bowl center; angular sweep accumulates to STIR_NEED (~2.2 turns). Stirring blends the layers: glue color lerps to pigment, activator layer + boundary fade, unmixed streak swirls spin and vanish, resting sprinkles get dragged around and absorbed. finishMix → blob with birth wobble. **Enter = auto-complete current step** (keyboard fallback, also in hint text). Per-stage tools drawn (tilting glue bottle, dropper, shaker, spoon), progress label above bowl (%, sprinkle count, stir %). Pour/stir audio reuses the looped noise bed via `bedGain(v, freq)` — glue glug 300Hz, activator trickle 850Hz, stir slosh 480Hz keyed to stir velocity + half-turn squish blips. Pointer handlers gate on MIX before grab logic; smoke test rewritten to drive the whole interactive sequence (8/8 green) + parse/id checks.
- 2026-08-05 v1.1: **mixing animation** per chat ("finalize with mixing anima[tion]"). MIX button (and R key) now runs a 2.7s beaker sequence before the blob appears: glass beaker on the mat → glue pour (stream + liquid rises in the glue's liquid color from `GLUE_LIQ`) → pigment drips (liquid lerps to pigment via `lerpColor`) → activator drizzle + add-in sprinkle (glitter squares / bead dots sink and swirl, charm emoji falls) → stir stage (orbiting wooden rod, vortex highlight arcs, surface sine-swirl, slight beaker wobble — killed by prefers-reduced-motion). Stage transitions fire small squish/crunch blips; stir re-triggers sMix; `finishMix()` swaps beaker → grounded blob with birth-wobble impulses + big squish (+crunch if beads). Stage label text above beaker names each step. `MIX` state gates pointer input and bypasses physics/draw in `step()`. Room drawing extracted to `drawRoom()` shared by both paths. Verified via slime-smoke.js: full boot + tweak + all 4 stages + finishMix + post-mix physics run error-free under stubbed canvas.
- 2026-08-05 v1: initial build per chat request — slime creation lab with recipe-driven soft-body physics + synthesized sounds.
  - **Recipe model**: 4 glues (white/clear/pearl/glow → alpha, gloss, damping), 3 activators (borax=snappy/stiff/high-pitch, contact=stretchy/soft/low-pitch, starch=bouncy), dose slider 0–100% (cure), 6 pigments, 3 add-in chips (glitter, foam beads, charms). `derive()` maps recipe → P: {stiff, damp, stretchMax, slump, bounce, wobbleK, pitch, wet, ...}. All controls apply instantly (small wobble pulse); MIX button re-pours a fresh batch from the top.
  - **Physics**: center particle (gravity 1500, wall/floor bounce by activator restitution) + 30 perimeter points spring-bound to `center + dir(ang)·r·(sx,sy)` with neighbor cohesion, per-frame damping `P.damp^(dt·60)`, droop gravity `·slump` on points. Squash/stretch (sx, sy) modeled as underdamped springs (wobbleK from cure) → jello wobble on impact; grounded idle slumps toward puddle `(1+slump·0.55, 1−slump·0.38)`. Undercured = droopy sticky puddle, overcured = stiff rubber ball.
  - **Interactions**: drag inside blob = grab (gaussian-weighted pull on points near grab angle, center follows weakly); pull past `stretchMax·r` → grip SLIPS (auto-release + snap sound + wobble impulse) — stretchMax is the recipe differentiator (contact+low dose ≈ 3.3r+, borax+high dose ≈ 1.2r). Quick tap = poke (radial dent + squish). Fling on release (smoothed pointer velocity ×0.75). R = re-mix, M = mute.
  - **Sound (Web Audio, all synth)**: sSquish = bandpass noise sweep, f0 = (240+420·pitch), wet recipes add 1–3 sine bubble squelch blips; sSnap = square osc drop at 170·pitch·(1+i) + highpass tick; sCrunch (beads only) = 5–9 staggered highpass noise ticks; continuous stretch bed = looped bandpass noise, gain from drag speed; sMix = swirl sweep. Pitch derives from dose × activator → borax high dose literally snaps higher.
  - **Render**: quadratic-Bézier through point midpoints, radial gradient (light→pigment→dark) with glue alpha, glow glue = shadowBlur 34, gloss highlights scale with glue gloss, clip-path inclusions (twinkling glitter squares, foam bead dots, emoji charms) ride the (sx,sy) deformation. Mat + wall + dotted lab backdrop on canvas.
  - **A11y**: aria-pressed on all toggles, role=status readout, role=application canvas w/ control summary, focus-visible, 44px targets, rem sizing, prefers-reduced-motion kills twinkle.
  - Fonts: Slackey + Varela Round + Space Mono. Sticker/craft-bench aesthetic (cream grid bg, ink borders, hard offset shadows).

## issues
- No live curl verification at build time — sandbox had zero HTTP egress (curl 000 to sloppy.live AND localhost). Verified via node parse + $()-vs-id cross-check only. Re-verify in browser when possible.
- Meters (STRETCH/SNAP/WOBBLE/CRUNCH pips) are derived heuristics — tune the normalizations if chat says they look wrong.

## todos
- Possible: two slimes to compare side-by-side; scent add-in (visual only); stretch strings visual when grip slips on wet recipes; save/share recipe codes.
- Chat may want: more glue types (metallic, clear-red), inflate/blow bubble mechanic, slice with scissors.
