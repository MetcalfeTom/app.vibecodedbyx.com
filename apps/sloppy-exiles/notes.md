# sloppy-exiles

## log
- 2026-05-07: **TUI roguelike view + console-log dump** (two related chat asks bundled). Toggle with backtick (`` ` ``) — opens a full-screen green-on-black phosphor terminal overlay rendering the same live game state as ASCII. Game keeps ticking underneath.
  - **Map**: 40×40 grid as colored `<span>` cells in a `<pre>`, double-spaced horizontally so each tile is 2 chars wide (matches a terminal's ~2:1 cell aspect ratio for square iso tiles). Glyph table: `@` player (cyan w/ glow), `#` wall (jade), `.` floor (dim), `·` town safe-zone, `s` skeleton, `T` swamp troll, `S` toxic slime, `%` BOSS (pink w/ glow), `$` drop (gold w/ glow), `>` portal (violet w/ glow), `~` boss projectile, `+` chest (only visible while in town).
  - **Top status bar** in 0.18em-tracked uppercase: floor / hp / mp / gold / lvl / kills / `◉ AUTO` indicator. **Right rail** (22rem, capped at 42vw): class + level + xp + skill points, ASCII bars for HP/MP/XP (`██░░ 87/100` w/ low-HP red flash + full-HP cyan flash), equipped slots with stat numbers, skill cooldown readouts (`ready` / `2.4s` / `locked`), zone coords + floor-kills progress + portal status, last 8 activity-log entries.
  - **Render cadence**: rebuilt every ~85ms (≈12 Hz). 1600 spans → single `innerHTML` write per frame, ~19k DOM-string chars/sec — light enough to leave the iso renderer untouched. `buildTuiGrid()` is the shared snapshot; `renderTui` formats it as colored HTML, `buildTuiAscii` formats it as plain text for the console dump.
  - **Console dump** (key `L` while TUI is open, or the `▼ console` button in the bar): `console.log`s a clean ASCII snapshot — header line with all HUD numbers, the 40×40 map bordered with `+--+`, legend line, last 8 log entries. Best-effort `navigator.clipboard.writeText` so the user can paste straight to chat or a gist; toast confirms the copy.
  - **CRT vibe**: 0.18rem repeating-linear scanline overlay on the whole panel, `text-shadow` glow on terminal text + selected glyphs (player/boss/drop/portal). `IBM Plex Mono` weight 400/600. Color palette: phosphor green `#7af07a` body, jade `#3aa07a` rail headers, cyan-white `#bfffaa` highlight, gold `#ffd45e` drops, magenta `#ff5fbf` boss, violet `#b46bff` portal, dim `#1e3a26` for floor noise.
  - **Accessibility**: overlay is a proper `role="dialog" aria-modal="true"` with `aria-hidden` toggling, focus moves to the close button on open, ESC inheritance (the existing pause toggle still works), `aria-live="polite"` on the status bar, `prefers-reduced-motion` no-ops the glow shadows, `<pre>` map labelled, ≥2.4rem button targets. Mobile: rail shrinks to 18rem, font scales down.
- 2026-05-07: **Full HP / MP on respawn** (chat ask) — root cause: `endGame` runs after the killing blow set `player.hp = 0`, so `saveVault()` flushed `hp:0` to the vault. The next `startGame` then computed `hpRatio = 0/maxHp = 0` and set `player.hp = max(1, round(maxHp*0)) = 1` — bot rose with 1 HP. Fix: gate the vault-HP/MP-ratio restore on `vault.hp > 0` (wasAlive). When the saved snapshot is from a dead run, the class-init `player.hp = cls.maxHp` set earlier in startGame stays in place, so the bot respawns fully restored. Mid-session reloads from a live save still preserve their HP ratio (e.g., closing the tab mid-fight at 30% HP and reopening — that vault.hp > 0 case still works).
- 2026-05-07: **Enemies pathfind around walls** (chat ask) — same A* + LOS smoothing the auto bot uses, now applied to every non-boss enemy in `tickEnemies`. New `enemyFollowGoal(en, gx, gy, now)` reuses the existing `autoFindPath` / `autoSmoothPath` / `autoHasLOS` primitives (pure functions over `tileWalkable` — caller-agnostic). Each enemy carries its own lazy-initialised path scratch fields prefixed with underscore so the existing serialiser ignores them: `_path`, `_pathIdx`, `_pathGoal`, `_pathTime`, `_lastPos`, `_stuckT`. Direct LOS short-circuits A* (the common case — no fight in an open room ever pays for the search). When LOS is blocked, recompute triggers are: no path, goal drifted >1.5 tiles, staleness >1.2s, stuck >0.5s with <0.10 tiles of movement. Waypoint advance threshold 0.55 tiles. **Performance**: 30 enemies × LOS-clear common case = essentially free; in worst-case fully-blocked combat scenarios each enemy pays ~1 A* call/sec, so ~30 calls/sec total on a 1600-node grid — still sub-millisecond per call. Skeleton archers behind a pillar now route around it instead of doing the classic ARPG wall-shimmy. Boss AI (NEXRAD Cloud) was already projectile-only and doesn't use the melee loop, so it's unchanged.
- 2026-05-07: **Auto-mode persists through death + auto-respawns** (chat ask) — when the bot dies with auto on, `state.auto` no longer flips off; the death overlay shows "THE BOT FALLS" with a "↻ rising again in a moment…" subtitle, then `setTimeout(2400ms)` dismisses the overlay and calls `startGame()` automatically. Pill stays green, panel stays mounted, log stays continuous: pushes "Perished on floor N — rising again" into the log on death and "Bot rose from the dead" + "↻ RESPAWNED · auto-mode kept the wheel" toast on respawn. The respawn timer guards against the user manually clicking restart in the meantime (re-checks `state.phase === 'end' && state.auto`). On respawn, `startGame()` now also clears the AI's path scratch space (`autoPath`, `autoPathIdx`, `autoPathGoal`, `autoPathTime`, `autoLastPos`, `autoStuckT`) so the bot re-evaluates from scratch — enemies array was just wiped, any cached enemy goal would be a dangling reference. Pill `.on` class and panel `.show` class are also re-synced from `state.auto` inside `startGame` so the UI never desyncs across the death/respawn boundary.
- 2026-05-07: **A\* pathfinding for the auto bot** (chat ask) — bot now navigates around walls instead of bumping into them. Grid-graph A* over the 40×40 `TILES` array with all 8 neighbours (orthogonal cost 1, diagonal √2), corner-clip prevention (a diagonal step requires both adjacent orthogonals walkable), 600-node expansion cap, octile heuristic, 1600-node max — sub-millisecond per call so it can run aggressively.
  - **LOS short-circuit**: `autoHasLOS(sx, sy, gx, gy)` does a Bresenham-ish 4-samples-per-tile raycast through `tileWalkable`. When LOS is clear, the bot skips A* entirely and drives straight at the goal — saves work on every step in open rooms.
  - **String-pull smoothing** (`autoSmoothPath`): walks the raw waypoint chain greedy-forward picking the farthest reachable waypoint with LOS from the current position. Removes the tile-by-tile zigzag so the bot cuts diagonals across open spaces while still respecting walls.
  - **Goal snapping**: if the goal tile itself is a wall (e.g., an enemy briefly sitting on a pillar pixel), `autoNearestWalkable(tx, ty)` spirals outward up to radius 4 to find the nearest walkable tile and uses that as the A* target. Falls through to null only on truly unreachable goals; caller falls back to straight-line.
  - **Path lifecycle** (`autoFollowGoal(gx, gy)`): recomputes when (a) no path exists, (b) the goal has moved >1.5 tiles since last compute, (c) staleness >1.5s (handles moving targets), or (d) bot is stuck (position changed <0.12 tiles over 0.6+ seconds — emits "Re-routing around wall" log line). Otherwise just steers `player.targetGx/Gy` to the current waypoint, advancing through any waypoints inside 0.55 tiles each tick. Final waypoint is the actual fractional goal so the bot lands on the precise drop / portal / enemy position.
  - **Combat integration fix**: previously the existing `tickPlayer` combat logic would override the AI's pathfinding `targetGx` whenever `player.attackTarget` was set (it walks straight at the enemy ignoring walls). Now `tickAuto` only sets `attackTarget` when the enemy is BOTH within `attackRange × 1.4` AND has clear LOS — otherwise it nulls `attackTarget` and pathfinds toward the enemy first. Once the bot rounds the wall and sees the target, the engagement clip kicks in and combat proceeds normally. Signature skill is also gated on `inRange` so the bot doesn't waste it on a wall.
  - **Path overlay**: when auto is on, the canvas draws a pulsing lime dashed line from the player through the remaining waypoints with `lineDashOffset = -t * 24` for the crawling-marching-ants effect, gold dot at the final waypoint and lime dots at intermediates. Lets the audience see the bot's brain working in real time.
  - **State**: 6 new fields (`autoPath`, `autoPathIdx`, `autoPathGoal`, `autoPathTime`, `autoLastPos`, `autoStuckT`). All cleared on toggle-off so toggling back to manual leaves no stale state.
- 2026-05-07: **Progress Quest auto-mode** (chat ask) — full zero-player automation with a Progress-Quest-style status panel that narrates what the bot is doing, runs forever, and grinds pure stat growth.
  - **Toggle**: `B` key or the green **AUTO** pill in the top HUD bar (next to Floor). When on, an AI overlay drives the player and uses the existing input pipeline (sets `player.attackTarget` + `player.targetGx/Gy` + calls `useSkill`/`useSignature`/`tryDodgeRoll`) — no parallel movement system, no special-case combat. WASD held by the human still wins so you can grab the wheel back instantly.
  - **Status panel** (bottom-left, `#autoPanel`, slides in only when auto is on): pulsing-dot "Auto-Quest" header, current activity label in italic IM Fell English ("Executing a Skeleton…", "Salvaging Mythic Sceptre…", "Descending to floor 4…", "Hauling loot to the chest…", "Searching the wastes for prey…"), gradient progress bar tinted by activity kind (gold/orange = fight, cyan = travel, violet = loot, sage = haul, magenta = descend), and a 6-line scrolling log of recent completed actions ("Slew a Troll", "Looted Mythic Greaves", "Equipped Glorious Crown", "Trained Mana Affinity", "Descended to floor 3", "Bot took the wheel"). Footer stat row shows running totals: Slain · Loot · Floors · Pts spent.
  - **Activity progress** drives the bar: combat activities show `1 - enemy.hp/enemy.maxHp` (the bar fills as you wear the target down — watching it tick toward 100% is the whole Progress Quest aesthetic). Travel/loot/haul/descend activities ramp linearly over their duration. Re-tapping the same label preserves progress so the bar doesn't stutter.
  - **Auto-spend skill points**: every frame, `autoSpendSkillPoints()` scans all branches for the cheapest available node and immediately purchases it, pushing "Trained <NodeName>" to the log. Stat sheet is never sat on, prereqs cascade naturally, the bot eventually unlocks every active skill (Cleave/Frost Bolt/Life Drain) and starts using them.
  - **No idle wander**: when the field is empty, the bot marches a long line through map center to provoke spawns instead of meandering. The activity panel still says "Searching the wastes for prey…" so the user gets feedback.
  - **Priority stack** (recomputed every frame in `tickAuto(dt)`):
    1. **Emergency dodge** — if a boss projectile (`nx-bolt` < 1.4 tiles, armed `nx-hail` inside splash + 0.3 buffer, `nx-pulse` ring within 0.7-tile band) is incoming and the dash is off cooldown, the bot picks an escape vector (radial-out for hail/bolt, perpendicular sidestep for the expanding pulse ring) and fires `tryDodgeRoll()` for the i-frames.
    2. **Exit portal** — if the floor is cleared and the descend rune is up and there's no live enemy, beeline for it (auto-advances to the next floor when the existing pickup-radius check fires).
    3. **Loot grab** — closest drop within 5 tiles, taken first if no enemy is within 4 tiles. Items still auto-pick-up on contact via the existing `tickDrops` 0.5-tile check.
    4. **Combat** — closest live enemy becomes `player.attackTarget`, which feeds into the existing walk-into-range + auto-swing logic. Skill picks: **Cleave** when 2+ enemies are within 1.7 tiles, **Frost Bolt** for ranged poke at 3–9 tile distant targets, **Life Drain** when HP < 45% and an enemy is within 4 tiles, **Signature** every ~5s opportunistically when an enemy is within 8 tiles. All gated on cooldown + mana availability + skill-tree unlock.
    5. **Wander** — no enemy / no drop / no portal → drifts toward map center on a 1.4s cadence, with a small randomized angle once we're already close so the bot doesn't camp the spawn.
  - **Town behavior** — auto walks to the chest if the satchel is ≥28/32 full (auto-opens the chest panel which pauses input naturally), otherwise immediately portals back to the wastes so the bot doesn't camp. No farming the chest in a loop.
  - **HUD pill** styled in green when on (`#7ddc8a` w/ pulsing glow shadow, `aria-pressed` toggled, 2.75rem min-height for accessibility, ◉/◯ glyph swap, hover focus-visible). Pill is in the top-right HUD next to the Floor cell — `pointer-events: auto` since the rest of `.hud` is pointer-transparent.
  - **Auto turns off on death** so the next run starts manual; explicit toast feedback on every toggle ("◉ AUTO MODE · ON · the bot takes the wheel" / "◯ AUTO MODE · OFF · manual control"); WASD held by the player still wins (the WASD branch in `tickPlayer` runs after `tickAuto` and overrides the AI's target via the existing `player.targetGx = null` reset), so handing control back is instant.
- 2026-05-07: **The NEXRAD Cloud rare boss** (chat ask) — a storm-cell boss that only appears when RainViewer + Open-Meteo report real-world precipitation in the player's region. Pulls its palette directly from the neon-radar app (`['#3072c8','#5fc2a4','#9aff5e','#ffd03e','#ff7a3e','#ff2e9c','#b13eff']` — NEXRAD-III) so the boss looks like a piece of the storm map that walked off-screen.
  - **Real-world gating** (`checkNexradPrecipitation`): two-step check on app boot + every 5 min. (1) RainViewer manifest fetch confirms the radar service is actually publishing recent frames. (2) Open-Meteo (`/v1/forecast?...&current=precipitation&timezone=auto`) gives actual mm/hr at the player's coords. Decision matrix: precip > 0.05mm → DEFINITELY raining (eligible); Open-Meteo down + RainViewer up → 25% probabilistic eligibility; both down → no boss. Geolocation is requested silently on boot — denied/timeout falls back to a CONUS-center default (39.5N, 96.5W) which still hits Open-Meteo for *some* signal.
  - **Spawn** at the current zone's map center (`TOWN_GX, TOWN_GY`), gated on `state.nexradPrecip && !state.nexradBossSpawned && !state.inTown && !state.exitPortal` AND a 5-minute cooldown after the last kill (`state.nexradBossKilledAt`). Per-frame roll at 0.06% means the appearance is rare and feels like a weather event, not a button. Telegraph toast: "A STORM GATHERS · the NEXRAD cloud descends" (legendary tier).
  - **Stats**: `hpMul:12.0, dmgMul:0 (proj-only), speedMul:0.32`. Level = `max(3, player.level + 2)`. Tanky and slow — the fight is about dodging, not trading hits. No melee damage on contact.
  - **Three radar-coloured attacks** on independent cooldowns (in `tickNexradBoss`):
    1. **Lightning Bolt** (`nx-bolt`, 2.6s CD, 22 dmg) — straight-line zigzag projectile fired at the player's last known position. Speed 5.5 t/s. Drawn as a cyan-shadowed lime zigzag with a yellow core.
    2. **Hail Rain** (`nx-hail`, 4.5s CD, 14 dmg, 0.7-tile splash) — 5 orange/pink hail orbs spawn in a 0.6–2.2 tile ring around the player with `fallY: 1.6` visual offset that decays over ~0.6s. While falling, only a magenta shadow telegraphs on the ground; once `armed` (fallY ≤ 0.05), a 0.45s splash window paints an expanding ring and damages on contact within `r`. Boss attacks the position you WERE in — sidestep it.
    3. **Cell Pulse Ring** (`nx-pulse`, 6s CD, 18 dmg) — expanding ring centered on the boss, `radius: 0.4 → 5.5` at 3.5 t/s. Damage triggers once when the player crosses the ring band (within 0.42 tiles). Ring colour cycles through the full radar palette as it expands (low-blue → mid-green/yellow → high-red/pink/violet) via `nexradColor(radius/maxR)`. Damage band passes you only once per pulse (the `hitOnce` flag) — jump-cut perpendicular to the ring's expansion to escape.
  - **Player-targeted projectile pipeline**: new `targetsPlayer: true` flag on boss projectiles routes them through a separate hit-detection branch in `tickProjectiles` that checks against `player.gx/gy`, respects `player.invulnUntil`, and calls `damagePlayerFromBoss(dmg)` (which applies a 350ms invulnerability window so consecutive ticks of the same ring don't multi-hit). Boss projectiles never damage other enemies.
  - **Render** (`drawNexradCloud`): big dark storm-cloud silhouette = 5 puff circles with a near-black radial-gradient fill, neon-stroke outer rim that cycles through the palette (`haloColor` at phase t·0.18), opposite-phase inner rim, and an animated lightning fork that crackles across the body every ~1.6s. Two glowing yellow eyes (flash to magenta on attack tick). Oversized `THE NEXRAD CLOUD` name plate + 56-px HP bar.
  - **Reward**: guaranteed Mythic (legendary) drop scaled to `max(player.level, en.level)`. Boss XP: `240 + level*40`. Boss gold: `200 + level*30`. Drop TTL bumped to 60s. Special toast: "THE STORM PASSES · legendary drop · cooldown 5min". `state.nexradBossSpawned` resets so a new storm can roll back in eventually.
- 2026-05-03: robust localStorage persistence — full snapshot save (chat ask).
  - **Schema v2 save** at `sloppy-exiles-save-v2`. Captures everything needed to resume a run mid-floor: `classKey`, `level/xp/xpNext/skillPts/gold`, current `hp/maxHp/mp/maxMp` (saved as ratio so restoring after maxHp changes does the right thing), `floor/floorKills/kills/killsToClear`, `inventory[]`, `equipped{}`, `chest[]`, and `purchasedNodes[]`.
  - **Backward-compat**: if v2 missing, falls back to legacy `sloppy-exiles-vault-v1` which only had gold + inventory + equipped + chest. Safe defaults fill in the rest (level=1, floor=1, etc.).
  - **Defensive load**: every field type-checked. Numbers default safely; arrays default to `[]`; equipped defaults to all-null. A corrupt JSON parse returns null and the run starts clean rather than crashing.
  - **Skill-tree restore**: previously, restoring `purchasedNodes` set the membership but never ran each node's `apply(player)` callback — so passive bonuses like +max-HP, mana regen, melee multipliers never took effect on a reload. Fixed: after restoring the set, iterate every purchased node and re-call `apply(player)` so live stats match what the player earned.
  - **Load order fix**: `purchasedNodes.clear()` was happening AFTER `loadVault` and AFTER `cloudLoad` populated it, wiping the very state we just restored. Now cleared FIRST, then both restore paths re-fill it.
  - **HP/MP rehydration**: player.hp/mp restored as a RATIO of saved hp:maxHp (so if a passive bonus boosted maxHp on reload, hp scales proportionally instead of clamping).
  - **Auto-save triggers** (`saveVault({silent: true})` everywhere persistence matters):
    - Level up — instant flush so the new level + skill point can't be lost.
    - Loot pickup — every new item in the satchel persists immediately.
    - Skill-tree node purchase — passive bonuses cement instantly.
    - Town entry / floor advance / chest deposit / chest withdraw / equip swap (existing).
  - **Periodic flush**: `markDirty()` flag + 10s background flush in the main loop. If anything mutated state and the player's still alive, it flushes silently.
  - **Page-unload safety**: `beforeunload` + `visibilitychange` handlers flush dirty state synchronously. Closing the tab or backgrounding the app on mobile no longer loses the last few seconds.
  - **Title-screen save slot**: shows `WARRIOR · LV 7 · FLOOR 3 · 142g · 4 inv / 12 chest`. The DESCEND button retitles to **CONTINUE** when a save exists. New `⌫ DELETE SAVE` pill (with confirm prompt) wipes the vault and the legacy v1 key.
- 2026-05-03: chain lightning + AoE blast on the mage's basic attack (chat ask).
  - **Replaces the mage's plain ranged projectile.** When the mage clicks an enemy in range, instead of a single arrow-shaped bolt it casts a chain that bounces between nearby enemies and ricochets off dungeon walls.
  - **Chain rules** (constants near the implementation): up to 5 jumps, 4.5-tile arc range between enemies, 5-tile wall ricochet range, 1.4-tile AoE blast at every enemy hit. Damage falloff per jump: 100% / 85% / 70% / 55% / 45%.
  - **Targeting**: nearest unhit enemy with line-of-sight (16-step Bresenham-ish sample through `TILES`) wins the next jump. If none in range, scans 16 angles for a wall tile within ricochet range, picks the closest hit, lands the bolt just before the wall, then continues hunting from there. Wall bounce counts as one segment but does no damage — produces a small spark cluster.
  - **AoE blast**: each enemy hit also deals 40% of the bolt's damage to other enemies within 1.4 tiles of the hit point (`chainBlast`), plus a cyan AoE ring particle.
  - **Visual** (`drawJaggedBolt`): per-segment perpendicular-jitter polyline (4-14 segments depending on length), big cyan glow stroke (lineWidth 6, shadowBlur 12), bright white inner core (lineWidth 2, no glow). Wall-segment ends spawn 5 tiny yellow sparks. Whole chain fades out over 0.42s.
  - **Class targeting**: warrior/archer click-attack untouched. Only `player.classKey === 'mage'` flips into the chain path. Mage's per-shot CD is 0.55s (the chain is doing way more work than a single bolt — slight tempo throttle balances).
  - **State plumbing**: new `lightnings` array, `tickLightnings(dt)` removes expired entries, `drawLightnings()` runs after `drawEntities()` so bolts sit above sprites.
  - Mage class card + class tagline updated to advertise "chain lightning · ricochet".
- 2026-05-03: render-loop optimization — bake static map to offscreen cache (chat ask, zennlogic lag).
  - **Root cause**: `drawTiles` was iterating `GRID_W × GRID_H = 1600 tiles` per frame. Each tile painted a diamond fill + stroke + occasional crack stroke + occasional rune fill, plus 4 polygon fills + 4 strokes per pillar. ~10k Path2D ops per frame for the static map alone — that's the lag zennlogic reported after the 24×24 → 40×40 expansion.
  - **Fix**: bake the entire static iso map into an offscreen `mapCache` canvas exactly once, at the end of every `buildMap` call (i.e. once per floor). Per-frame `drawTiles` is now a single `ctx.drawImage(mapCache, ...)` blit. Estimated per-frame map cost: ~10k Path2D ops → 1 drawImage. Frame time should drop noticeably on the larger grid.
  - **Cache geometry**: bounding box computed from worldToScreen extents + 36px pillar overhead + ½-tile padding. For a 40×40 grid → ~2560×1316 offscreen canvas (~3.4 MP, ~13.5 MB). Re-allocated once per floor (no leak — same canvas element re-sized).
  - **Animated bits stay live**: the town pulse halo (sin-wave alpha) is the only frame-varying part of the static map, so it's drawn separately on the live canvas as a single `ellipse` stroke after the cache blit. Town shrine sigil, crack lines, runes, pillars all baked.
  - **Refactor split**: `drawTile` / `drawPillar` (used module `ctx`) → `paintTile(c, x, y)` / `paintPillar(c, sx, sy)` that take an explicit ctx, so they can render into either the cache OR the live canvas. drawTile/drawPillar removed.
  - Verified the procedural-map redraw was indeed the lag source and not something else (entity sort + render is O(N) where N≈30; cheap by comparison).
- 2026-05-03: bigger map + procedural rooms-and-corridors + 2 new enemy kinds (chat ask).
  - **Map size**: GRID_W/H bumped 24×24 → **40×40** (~2.8× area).
  - **Procedural rooms+corridors generator** replaces the old single-arena + 12 random pillars. Algorithm: fill with walls → carve fixed TOWN room (9×9) at world centre → place 7 random rooms (4-9 wide/tall) without overlap (1-tile padding) → connect each new room to the previous via L-shaped corridor → 2 extra random corridors for loop variety → sprinkle 0-2 pillars per non-town room → force-clear town safe zone. Corridors carved 2 tiles wide (1 + widening tile) so they don't feel claustrophobic. Each floor re-rolls the layout.
  - **TILE_SEED** is now a function that re-seeds on every buildMap call (was a one-shot IIFE pinned to the original 24×24 grid — would have indexed out of bounds at 40×40).
  - **ROOMS** array kept around so future tooling (boss room, minimap, room-clear bonuses) has somewhere to read room rectangles.
  - **Two new enemy kinds**:
    - **Swamp Troll** (green hunched ogre with tusks + moss patches): 2.6× HP, 1.7× damage, 0.55× speed, 1.4× attack-CD. Tanky brawler — punishes standing still.
    - **Toxic Slime** (jiggling green dome with sliding shine + falling toxic drips + tiny grin): 0.55× HP, 0.85× damage, 1.45× speed, 0.75× attack-CD. Glass cannon — chips the player and is hard to dodge in tight corridors.
  - **Floor-scaled enemy mix**: floor 1 is mostly skeletons; troll/slime weights ramp +4% per floor (capped at 35% troll / 40% slime). Skeleton stays at minimum 25% to keep the early game legible.
  - Each enemy now carries its `kind` + `attackCdBase`; `tickEnemies` uses the multiplier on swing CD so trolls swing slower, slimes faster.
  - `drawEnemy` branches by kind: `drawSkeleton`, `drawSwampTroll`, `drawToxicSlime`. Shadow size, label, and chill-glow shared. Level pill now includes the kind label.
- 2026-05-03: lazy supabase import — fix "blocked at the door" load failure (chat ask, zennlogic).
  - The static `import supabase, { supabaseSession } from '/supabase-config-fixed.js'` sat at the very top of the module. Any server hiccup that returned the wrong Content-Type (e.g. nginx serving a 404 page as text/html) would block the whole game from booting — what the chat called "blocked at the door".
  - Switched to a lazy dynamic `await import('/supabase-config-fixed.js')` wrapped in try/catch. Both `supabase` and `supabaseSession` are `let` bindings that start null and only get set on successful resolve. A new `supabaseReady` promise gates async callers.
  - `ensureSupaSession`, `cloudSave`, and `cloudLoad` all `await supabaseReady` and bail if the import failed. localStorage vault remains the single source of truth offline; cloud sync is now genuinely optional.
  - Verified the recent stash update did NOT break the database client: chest items live only in localStorage `saveVault`, never in the cloud `cloudSave` row (which keeps the original `inventory` / `equipped` / etc. fields). The `sloppy_exiles_saves` table schema is unchanged.
- 2026-05-03: click-to-attack + ranged buff (chat ask).
  - **Auto-attack removed.** No more nearest-enemy-in-range scan firing on cooldown. The combat tick now only acts on `player.attackTarget`, set by clicking an enemy.
  - **Click-to-attack** behaviour:
    - Click empty ground → walk only, clears any queued attack target.
    - Click an enemy in range → swings/fires once on the next tick (subject to attack CD), then waits for another click to fire again.
    - Click an enemy out of range → walks toward it; once inside attack range, fires one shot.
    - Target stays "locked" until the enemy dies, the player clicks elsewhere, or the enemy escapes beyond `attackRange × 3 + 1` tiles (drops the lock so the player isn't dragged forever).
  - **Ranged range bumped**: archer 6.0 → **9.0** tiles, mage 5.0 → **8.0** tiles. Warrior unchanged at 1.5.
  - **State plumbing**: `player.attackTarget = null` initialized + cleared on respawn, on town entry, and whenever the player clicks empty ground.
  - Updated canvas `aria-label` and on-screen control hint to reflect "click enemy to attack — no auto-attack".
- 2026-05-03: storage chest in town zone (chat ask).
  - **Chest entity** rendered at `(TOWN_GX + 2, TOWN_GY)` — 2 tiles east of the town portal. Iso 3D box with left/right/top faces (browns + gold trim), front gold lock, lifts ~1.2px with a slow sin bob. Only drawn while `state.inTown` is true.
  - **Open**: click the chest tile (auto-walks the player there if too far) OR press `V` while in the town safe zone within `CHEST_OPEN_RADIUS = 2.5` tiles. Pulsing `[ V ] open` prompt floats over the chest when in range; "vault chest" idle label otherwise.
  - **Vault overlay**: two-pane layout — SATCHEL (32-cap) on the left, CHEST (48-cap) on the right. Each pane has a live `N/cap` badge + DEPOSIT ALL / WITHDRAW ALL bulk buttons disabled when source is empty or destination is full. Click an item in either pane to swap it across. Capacity-checked on every move; full-state shows a toast.
  - **Persistence**: `player.chest` array added to player state. `saveVault` / `loadVault` (localStorage `sloppy-exiles-vault-v1`) now serialize chest items alongside inventory + equipped + gold. `cloudSave` deliberately omits chest because the existing `sloppy_exiles_saves` Supabase table doesn't have a `chest` column — listed as a TODO for cross-device chest sync.
  - **Auto-save** on every chest close + every individual deposit/withdraw, so closing the tab can't lose stash items.
  - Reset on a fresh class start: `player.chest.length = 0` then re-restored from vault.
- 2026-05-03: Created. **Isometric click-to-move ARPG** — Diablo / Path of Exile arcade-flavoured slice. Single-file canvas 2D, no build step. **Iso projection**: 64×32 tile (2:1 ratio), `worldToScreen` / `screenToWorld` helpers convert (gridX, gridY) ↔ pixel space. 24×24 tile grid, border tiles + 12 interior pillars are blockers. Camera lerps toward player at `dt × 6` rate. **Player** at grid centre — cloaked figure with hood, glowing ember-orange eyes, dagger held to facing side. Click anywhere on stone → walk there; click an enemy → walk into auto-attack range. **Combat**: auto-attack triggers when an enemy is in `attackRange = 1.4` tiles, 0.6s cooldown, damage = `attackDmg * meleeMul × (crit ? 2 : 1)`. **3 active skills** unlocked via the skill tree, fired with `1 / 2 / 3`: Cleave (8 mp, 1.6s cd, 2.4-tile AOE for 2× weapon damage), Frost Bolt (12 mp, 1.0s cd, ranged 1.6× projectile that chills targets 2s @ 45% speed), Life Drain (20 mp, 4.5s cd, 3-tile AOE 1.8× damage + heals 25% of total). **Enemies**: skeleton mobs with bone-pixel sprite (legs + ribcage + skull + jaw), per-frame flash on hit, frost halo when chilled, level label + HP pip above. Spawn cadence ramps `Math.max(0.6, 2.0 - waveT * 0.04)` — wave gets denser as time passes. **Loot**: 60% drop chance per kill, 4 item slots (weapon/helm/robe/ring), 5 rarity tiers with weighted roll (60/25/12/2.5/0.5%): common/uncommon/rare/epic/legendary, color-coded gold→green→yellow→purple→orange. Stat scales with rarity multiplier (1.0/1.4/1.9/2.6/3.5×) and player level. Auto-equip on pickup if upgrade. Equipped stats compose: weapon = +attackDmg, helm = +maxHp×2, robe = +maxMp×2, ring = +crit chance. **Skill tree** with 3 branches × 3 nodes: Combat (Sharper Strike, Cleave, Berserker — under 30% HP gives +60% dmg), Arcane (Mana Affinity, Frost Bolt, Mana Surge), Shadow (Vitality, Life Drain, Shadow Strike — +15% crit). Branches gate by prereq node; each costs 1-2 skill points. Earn 1 point per level-up. **Inventory**: 32-slot grid + 4 equipped-slot row showing what's worn. Click an item to swap it into the matching slot (current item bumps to inventory). **Atmosphere**: dark void background, per-tile cracked-stone shading with crack lines + occasional purple rune glyphs (ᚠᚱᛞᛟᛇᛉᛗ), pillars rendered with proper iso 3D faces (left/right/top) + blood-red ember dots on top, drifting orange ember particles upward, big radial vignette. **Audio**: Web Audio synth — hit (sawtooth + square), crit (square + triangle pair), cast (triangle two-tone), pickup (bright square), level-up (4-note ascending arpeggio), death (3-note descending), bone-crack (sawtooth + filtered noise burst). **HUD**: TL level, gold, skill points; bottom row HP bar + 3 skill icons + mana bar; centre-bottom XP bar. Toast banner for loot pickups, level-ups, system messages. **Aesthetic**: UnifrakturCook display title "Sloppy Exiles" with blood + ink drop shadow, Cinzel for stat labels, IM Fell English italic for body. Palette deep blood-red `#a82226`, ember `#ff8a33`, rune-purple `#a86bff`, bone `#cfb98a`, gold `#d9a13a`. Pollinations OG, ⚔️ favicon. **Accessibility**: rem units throughout, no html font-size override, ≥4.5:1 contrast (paper on near-black HUD cells, gold-on-black accents), semantic `<main>`/`<aside>`, canvas `role="application"` with full control summary aria-label, overlays `role="dialog" aria-modal="true"` toggling `inert`, focus-visible 4px gold outline, `prefers-reduced-motion` skips the dmg-pop float animation, skip link to canvas.

- 2026-05-04: chat ask — "make map edges open into new areas". Added zone system. New `state.zoneX/zoneY` (default 0,0 = home). `buildMap()` carves 3-wide × 5-deep edge gates at every cardinal midpoint via `carveEdgeGates()` and connects each gate to the nearest existing room with `carveCorridor`. New `checkEdgeCrossing(dt)` runs after `tryMove` in tickPlayer; touching `gx ≤ 0.55`, `gx ≥ GRID_W − 1.55`, etc. fires `crossEdge(dir)` which wipes enemies/projectiles/drops, increments the zone delta, calls `buildMap()` for a fresh wing, drops the player at the OPPOSITE gate, sets a 0.6s `_edgeCooldown` so the post-warp landing tile doesn't immediately re-fire, clears walk/attack target, and shows a `OUTER NORTH WASTES · (0, −1) · floor 1` toast. Zone names tier on Manhattan distance (OUTER / DEEP / FAR) and pick a compass-themed name (NORTH WASTES / IRON CRAGS / SUNKEN VAULTS / etc). Town stays anchored to (0,0): `useTownPortal()` rebuilds the home map if you weren't already there and remembers your departure zone+coords on `state.returnFrom`, so pressing P again drops you back exactly where you left across zones too. Exit portal is abandoned on cross (re-earn with kills); `floorKills` carries over so the kill quota progresses across multiple wings. Town safe-zone gold halo + chest already gated by `state.inTown` so they only show in the home zone; added an `isHomeZone()` gate on the gold floor tile in `paintTile` so wing zones at the same coords just look like normal floor.

- 2026-05-04: chat ask — clearer status HUD for active skills + cooldowns. Each of the 4 skill slots (1 cleave / 2 frost / 3 life-drain / 4 signature) now carries:
  - **Per-skill accent color** via `--skill-color` CSS var: ember `#ff8a33` for cleave, ice cyan `#5fc0e0` for frost, blood violet `#a86bff` for life-drain, gold `#d9a13a` for signature.
  - **Radial cooldown sweep** — `::before` overlay using `conic-gradient(from -90deg, ...)` masks the slot clockwise from 12-o'clock with a `--cd-pct` CSS var. Sweep auto-adapts to whatever cd value `useSkill` last set, so signature skills with class-dependent cooldowns (warrior 3.5s / archer 2.5s / mage 5.0s) all draw correctly.
  - **Numeric countdown** in the slot center (Cinzel 1.05rem white with shadow, tabular-nums) — replaces the icon visually while the slot is on cooldown; icon fades to 25% opacity.
  - **Mana cost pip** — small mono `8` / `12` / `20` / `12` in cyan at the bottom-right corner. Turns red when MP is insufficient.
  - **Ready glow** — when the skill is unlocked, off-cooldown, AND affordable, the border + box-shadow take on the skill's accent color (ember/ice/violet/gold). Locked skills stay at 35% opacity. Insufficient-mana skills get a red border instead of the glow.
  - **Off-cooldown pulse** — `skillReady` keyframe (scale 1 → 1.07, glow 0 → 0.95rem → 0) fires once on the rising edge from `cooling` → `ready`. Tracks via `_skillReadyPrev` map.
  - **Hover tooltip** — `data-name` attr holds `Cleave · 8 mp · 1.6s · AOE swing`. CSS `:hover::after` floats a Plex-Mono pill above the slot. Refreshed on `refreshSkillBar` from `SKILL_META`.
  - **Per-frame ticker** `tickSkillHud()` hooked into the main `loop()` right before `clearScene()`. Cheap DOM mutations on 4 slots only — sets `--cd-pct`, the countdown text, and toggles `cooling/ready/low-mp/ready-pulse` classes. `prefers-reduced-motion` no-ops the keyframe + transitions.
  - Skill bar is now `role="status" aria-live="polite"` so screen readers hear cooldowns when `cd-text` updates.

- 2026-05-07: chat ask — inventory items are now **physical Matter.js rigid bodies**. Loaded `matter-js@0.20` from jsdelivr in the head (regular `<script src>` so `window.Matter` is available before the module script boots). Replaced the satchel overlay's `<div class="inv" id="invGrid">` with an `<canvas id="invPhys" class="inv-phys">` (vault chest still uses the CSS grid because it needs deterministic two-pane bulk-move semantics). New `populateSatchelPhysics()` builds a fresh Matter mini-world inside the canvas: 4 static walls around the canvas edges, gravity 0.85, one chamfered 56×56 rounded-rect body per inventory item dropped in from above with a small spread + tiny angular velocity. Body density scales with rarity (legendary 0.0030 → common 0.0012) so legendaries feel chunkier when you toss them around. Restitution 0.32, friction 0.45 / 0.018 air for that satchel-cloth rest.
- **Custom canvas render**: per-frame, items are drawn as a black inner card with a rarity-colored stroke + matching shadow-blur halo (16px on legendary, 12px epic, 8px rare, none below). Item icon rendered centered as a 22px serif glyph; +stat label in IBM Plex Mono bold tucked at the bottom in the rarity color. Faint radial vignette over the canvas for depth.
- **MouseConstraint** for drag (cursor flips to `grabbing` while held); custom tap-vs-drag detection (240ms / 8px) so a quick tap on an item still equips it via the existing `equipItem(idx)` path. The full physics world is rebuilt on every equip — natural metaphor for the satchel reorganising itself after you pull something out.
- **Lifecycle**: `toggleInv()` defers `refreshInvUI()` by one frame so the overlay's CSS sizing settles before we measure the canvas. `teardownSatchelPhysics()` cancels the rAF loop, clears the engine, and removes pointer listeners on close so nothing keeps spinning during gameplay. Resize listener rebuilds the world if the canvas changes size.
- **Graceful fallback**: if Matter fails to load (CDN block, offline, etc.), `ensureMatter()` returns null and `populateSatchelPhysics` silently no-ops — the equipped row + close button still work, the vault chest is unaffected, the game continues.

## issues
- Pathfinding is direct-line-with-blocksAt only — no A*. Player walking to a clicked point on the far side of a pillar will get stuck against the pillar's edge. They can re-click around it, but a proper grid A* would feel better.
- Camera doesn't auto-pan toward enemies (e.g. on damage taken) — player can be partially off-screen if running away from a chasing mob with the lerped camera trailing.
- Enemy AI is simple "walk straight at player". They bunch up and don't try to flank.
- No pause menu — Esc just toggles a paused state with a banner; pressing T or I does open the skill tree / inventory which paused the game, but there's no traditional pause-menu UI.
- Projectile collision uses simple distance check, no path sweep — at very high speeds a frost bolt could tunnel past a slim enemy.

## todos
- A* pathfinding for click-to-move so the player navigates around pillars cleanly.
- Boss every 5 waves (giant skeleton with multi-phase mechanics).
- Spell book UI showing all unlocked skills + cooldown timers.
- Persistent profile via localStorage (saves level, gold, equipped, skill tree across reloads).
- More enemy types (zombie shambler, fire wraith with ranged attack, swarm rat).
- Procedural dungeon — multiple rooms connected by corridors instead of single arena.
- Multiplayer realtime via Supabase broadcast (mirror the ninja-tag pattern).
- Crafting / item upgrade system.
