# Lumen Vale — notes

## log
- 2026-07-05: v4 — **anatomical wingsuit pilot + overhead chase camera** (chat ask ×3; researched real overhead wingsuit form first — tri-wing: arm membranes wrist→flank→THIGH, separate leg wing between legs to ankles, arms nearly straight in swept-back delta, legs wide/toes pointed, symmetric silhouette; refs en.wikipedia.org/wiki/Wingsuit_flying + wingsuitlearn.com). Replaced the camera-attached FPV suit rig with a world-space `pilot` Group (~1.75u human, prone, local -z forward, rotation order YXZ): helmet sphere (squashed), chest+hip boxes w/ orange spine panel, per-side arm Group pivoted at shoulder (upper+forearm cylinders via `limb()` helper — cylinder between two points using quaternion setFromUnitVectors — hand box, 5-vert arm membrane fan wrist→elbow→shoulder→armpit→thigh), thigh+shin limbs + pointed foot, 4-vert leg-wing quad hips→ankles. **Physics decoupled from camera**: new `flyerPos` Vector3 is the flight body; bird mode copies it to camera (FPV unchanged), suit mode drives pilot.position/rotation and a **chase cam** — desired = flyer − forward*4.2 + up*5.4, terrain-clamped, `camera.position.lerp(1-exp(-5dt))`, lookAt flyer+forward*7−1.5y (drone-above feel). `enterBird()` now syncs flyerPos from camera. Anim: arm groups sweep ±(spd*0.22 − pitchIn*0.12), membranes shimmer rotation.x sin(flapPhase*5.3+i*2.1)*0.014*(0.3+spd). Suit toggle mid-flight = smooth cam transition (lerp); suit→bird snaps to FPV. FOV kick shared.
- 2026-07-05: v3.2 — **articulated wingsuit rebuild** (chat ask ×3, same request). Replaced v3.1 stub with a rig: `suitRoot` (camera child, identity → children live in camera space) holding torso stack (chest + harness strap + abdomen + hips + 2 legs + static leg-wing quad, all past z=-0.55 because **camera.near=0.5 clips anything closer** — key layout constraint) and per side `shoulder Group → elbow Group → wrist Object3D` chains (cylinder limbs, sphere joints, scaled-sphere hand). Pose driven per frame in `poseSuitArm`: shoulder yaw `-s*(0.16 + spd*0.38 - pitchIn*0.28)` (sweep back w/ speed, forward on flare), shoulder roll `-s*(0.05+buzz) - bankIn*0.22` (both arms same bank sign = right dips/left rises), elbow bend `clamp(0.5 + pitchIn*0.35 - spd*0.28, 0.15, 0.95)`. **Draped nylon**: 6-vert BufferGeometry per side, outer edge = live shoulder/elbow/wrist world positions → `camera.worldToLocal` (needs `camera.updateMatrixWorld(true)` first, called once per suit frame), elbow/wrist verts sagged down (0.045/0.02 + flutter), inner edge fixed down the torso side; needsUpdate + computeVertexNormals each frame (6 verts, cheap). frustumCulled=false on fabric (verts move outside static bounds). Physics still untouched. suitParts=[suitRoot] (fabric is a child, visibility cascades).
- 2026-07-05: v3.1 — **wingsuit toggle** (chat ask). New 🪂 suit button (aria-pressed = suit active, persisted `localStorage['lumen-vale-body']`). Second camera-parented body: per side a nylon fabric quad (#e8641e orange, wider chord/shorter span than bird), arm cylinder along the leading edge (rotateZ 90° then rotation.y −0.24 to follow the edge), gloved hand sphere at tip; center torso box replaces the beak. Mirroring via group scale.x=-1 — flips winding, so ALL suit materials (incl. skinMat) are DoubleSide. **Physics 100% identical by request** — only the idle anim differs: bird flaps (10Hz climb / glide-V), suit flutters (`sin(flapPhase*5.3)*0.014*(0.4+v/V_MAX)`, phase rate 4+v*0.09 — buzzes harder in dives). refreshBody() replaces setBodyVisible (visibility = flying && matching bodyType). Manifesto got a v3.1 card ("the valley doesn't care what you are, only how you fall").
- 2026-07-05: v3 — **first-person bird simulator rewrite** (chat ask, message truncated at "camera at" — interpreted as camera at the bird's eyes). App now BOOTS mid-glide in bird mode; orbit camera demoted to secondary toggle (🧭 button / F / Esc). **Arrow keys only** (WASD removed per request): ↑↓ = pitch target ±0.72, ←→ = bank ±0.85; all inputs eased exponentially. **Energy model**: `airspeed += (-sin(pitch)*55 - (airspeed-CRUISE)*0.30)*dt`, CRUISE 62 / clamp 32–210 — dive ≈187 max, climbing bleeds to stall; stall (`(40-v)/8`) forces the nose down regardless of input. Turn rate `yaw -= bank*(0.75 + 45/airspeed)*dt` (tighter when slow, like a real wing). Camera roll = −bank*0.9 via YXZ euler. **Visible body**: camera parented to scene (required for camera children!) with 2 quad wings (BufferGeometry, Lambert DoubleSide, mirrored via mesh scale.x=-1) + cone beak; wings flap sin(phase)*0.5 at 10Hz when climbing/slow, lock to shallow V (0.06±0.06 @2Hz) when gliding. Ground assist: clamp to terrainH+3.5 and ease pitch up (no faceplants); soft ceiling 1500. FOV 55→71 with speed (reduced-motion caps at +6). **Mobile**: drag-offset steering (offset from touch start /110 → bank/pitch), release = glide; throttle button + all WASD/Shift/Space code deleted. Old free-fly is GONE deliberately — don't resurrect without a chat vote. Manifesto card updated (v1.1→v3, "there was never going to be walking").
- 2026-07-05: **manifesto.html** (chat ask by rotetomate) — creator manifesto + full feature ledger page, same aesthetic (Fraunces italic/Space Mono, night gradient, amber, CSS twinkle stars). Sections: 5-refusal manifesto (no screenshots/textures/world-edge/menus/loading-bars), per-feature cards each with a "philosophy —" footer (terrain, 3 shaders, day cycle, fly mode, infinite streaming, pines, birds, the recall-nightmare embassy), and an appendix documenting **the secret baguette that doesn't exist** (spawn condition: forest noise returning exactly 0.5 — asymptotically never; intentionally NOT implemented, the joke is that it's documented-but-nonexistent; do not "fix" this by adding one unless chat explicitly votes to betray the bit). Linked via the main app's h1 title (now an <a> with pointer-events:auto inside the pointer-events:none header).
- 2026-07-05: v2 — **infinite terrain + pine forests + bird flock** (chat request, after research pass on chunked-LOD/instancing best practice). Replaced single 1000u mesh with a chunk streamer: 400u chunks, Chebyshev rings 0–5 (121 live chunks, ~2.2km view radius matching fog falloff). LOD by ring: ≤1→48² verts, ≤3→24², else 12². Each chunk carries a **skirt** (border ring dropped 10+step units, terrainMat now DoubleSide) so LOD seams never show cracks. Apron of 4 grid cells per chunk provides neighbor heights for normals + 8-tap cavity AO. Rebuilds are upgrade-only (res increase or missing veg) — high-res chunks parked in far rings just age out when disposed, no downgrade churn. Build queue sorted by ring, budget 1–2 chunks/frame (dt-adaptive); ring ≤1 built synchronously at boot behind the loading screen. terrainH lost its island falloff — pure infinite heightfield; fly-mode radial bound removed, ceiling 1200. Water (6000u quad) + sky dome now follow the camera each frame — waves/sky math are world-space/camera-relative so it's seamless. **Pines**: per-chunk InstancedMesh pairs (trunk cylinder + cone canopy w/ per-instance setColorAt greens), scattered by seeded xorshift from chunk coords, gated by height 2.5–42 / slope ny≥0.86 / fbm forest-clump mask ≥0.48, ring ≤2 only (~25 chunks × 2 draws). frustumCulled=false on instanced meshes (r128 culls by base geometry bounds — wrong for scattered instances). Trees are MeshLambertMaterial lit by a new DirectionalLight+AmbientLight synced to the palette each frame + scene FogExp2 (custom shaders keep their own fog). **Birds**: one 26-instance InstancedMesh (squashed cone silhouettes), 3 flock anchors drifting with the camera, circular soar paths + sine bob, flap via z-roll + x-scale oscillation, terrain-clearance clamp, fade out at night (visible when dayF>0.08). Research refs: ZyFou/ProceduralTerrains (skirts), Codrops fluffy-grass (chunked instancing for culling), utsubo 100-tips.
- 2026-07-05: v1.1 — **bird flight mode** (chat request). 🕊 fly button / F key / Esc toggles orbit↔fly. Fly: WASD or arrows (W includes pitch — forward follows full look direction), drag = yaw/pitch look (±1.35 rad pitch clamp), Shift = 2.1× boost, Space/E up + Q/Ctrl down (undocumented bonus). Velocity-based movement (ACC 280, exp damping 2.3/s → ~120 u/s cruise, ~255 boosted). Banking roll from strafe + smoothed look-velocity, applied via camera.rotation 'YXZ' (pitch,yaw,roll). Speed-scaled FOV kick 55→68. Collision: camera clamped to terrainH(x,z)+3 (analytic, exact), water min y=2, ceiling 800, radial bound 1400 (scale-back). Mobile: coarse-pointer shows round hold-to-fly throttle button bottom-left; drag steers. Enter-fly derives yaw/pitch from current camera direction; exit re-aims OrbitControls target 80u ahead + resets FOV. Buttons blur after click so Space doesn't re-toggle. NOTE: OrbitControls stays attached but enabled=false during flight (its handlers early-return) — don't remove that flag.
- 2026-07-05: v1 — single-file three.js (r128) landscape. CPU-generated terrain (240×240 grid, 1000u): domain-warped FBM base + broad valley noise + ridged-multifractal mountains gated by a mask, island edge falloff so the world sinks into water. Per-vertex analytic normals from height grid + cheap cavity AO attribute (8-tap ring average). Three custom ShaderMaterials:
  - **Terrain frag**: height/slope banding (sand→grass→rock→snow) with noise-jittered thresholds, rock strata, underwater tint, hemisphere ambient + lambert + blinn spec on snow, AO, exponential distance fog + animated valley height-fog.
  - **Water frag**: analytic wave normals (2 sines + 2 scrolling vnoise octaves, central differences), fresnel deep↔sky mix, sun specular + sparkle noise, same fog.
  - **Sky frag**: zenith/horizon gradient, sun disc + double glow, moon disc, hash-star field with twinkle (night only), FBM cloud layer with silver lining, all on a 5000u backside sphere whose vDir is camera-relative.
- Day/night cycle: sun orbits (160s day at 1×), JS-side palette keyframes (night/dusk/day) lerped by sun elevation and pushed as uniforms to all 3 materials. Light source hard-switches sun→moon at e=-0.02 (they're antiparallel — lerping through 0.5 would produce a zero vector / NaN, hence the hard select + y-clamp).
- UI: fading title + hint, HH:MM clock pill (phi 0 = 06:00), pause-sun + speed (1×/4×/12×) buttons. OrbitControls with damping, autoRotate until first interaction (off under prefers-reduced-motion).

## issues
- No headless browser in sandbox + no HTTP egress at build time → shaders verified by review only, not runtime compile. If black screen reported: check devtools for THREE shader compile errors first.
- Mobile mediump precision can make sin-hash noise slightly banded — acceptable, but if users report artifacts, switch hash21 to a mediump-safe hash.
- Water has no shore foam / depth blending (no depth texture) — fake it with terrain-distance noise if requested.

## todos
- Optional: fireflies/particles at night, god-ray post pass, seed input to regenerate terrain, screenshot (PNG export) button.
- Time-of-day scrub slider if users ask for direct control.
