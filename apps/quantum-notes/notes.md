# Quantum Notes — serious QC explainer + 2-qubit simulator

## log
- 2026-08-19: v1.0 — built per chat, then restructured mid-build to marcipopsis's exact outline: opening TL;DR (5 bullets), bold headings Core premise / Initialization / Manipulation / Measurement / Bottlenecks-with-caveats, concise bullets, short paragraphs. Structure is PROBED (h2 order, TLDR first, ≥18 bullets, ≥3 caveat boxes).
- ENGINE (the part that must never be wrong): 2-qubit statevector, complex [re,im] pairs, H/X/Z as 2×2 matrices applied via bit-pair indexing (A=high bit), CNOT as |10⟩↔|11⟩ swap, Born sampling. PHYSICS PROBED: H|0⟩=(1/√2)(|0⟩+|1⟩); H·H=I; X flips; **H·Z·H|0⟩=|1⟩** (the honest interference demo, stated in the prose and proven in the suite); Z inert on |0⟩; Bell amplitudes exactly (R2,0,0,R2); norm preserved through a 10-gate arbitrary circuit; 800-shot Bell stats: zero 01/10, ~half/half.
- UI: θ-slider state prep (real-meridian caveat stated), single-qubit tally histogram vs Born-rule ticks, circuit builder (chips on wires, Bell preset, undo/clear), amplitude bars WITH PHASE NEEDLES, 500-shot histogram. Honesty throughout: superposition ≠ "both at once", no-speedup-from-superposition-alone, pseudo-randomness disclosed, 2^n scaling explains why 2 qubits, "not faster general-purpose computers".
- Zero external assets (grep-probed, data-URI favicon, Georgia+system-mono). Probe 16/16.
- CORRUPTION LOG: mid-write CJK stray AGAIN — 'function近(a,b)' materialized in the probe (4th incident: �putation/astonishment/ココ/近). The CJK-block sweep assert (r'[一-鿿]') is now standard for probe files too.

## issues
- none yet. If extending: any new gate MUST come with a matrix-level probe truth (e.g. S·S=Z) before it ships.

## todos
- S/T phase gates + a phase-kickback demo; 3rd qubit + GHZ, if chat wants depth.
- 2026-08-19 (2): typography per chat — Georgia serif → clean system sans (-apple-system/'Segoe UI'/'Helvetica Neue'/Arial) EVERYWHERE including all 7 canvas ctx.font sites (dial, both histograms, amplitude labels). Layout untouched, no-external-assets holds (system stack, no font CDN). Full 16/16 re-run.
- 2026-08-19 (3): BEGINNER PASS per chat — kept marcipopsis skeleton + all physics, added: numbered STEP 1–5 chips on section headings (TL;DR stays chipless — probe asserts exact heading text), italic "in plain words:" opener per section, simpler Core-premise bullets (bit=switch, qubit=dial), and FOUR ORIGINAL INLINE SVG DIAGRAMS (no external assets): bit-switch vs qubit-dial; X/H/Z before→after dials (Z shows the hidden sign as a dashed mirror needle); measurement funnel (two bars → meter → one bar + dice); bottleneck cluster (noise squiggles → many physical qubits → brace → one dependable qubit). Each diagram role=img with a real descriptive aria-label (probe requires >40 chars). Suite grew to 21 checks: 4-diagrams, 5-step-chips, 5-plain-lines, longest-paragraph<340 chars, diagrams-have-alt + full physics regression. 21/21. Screenshot reviewed; |0⟩ pole label nudged off the diagram caption.
- 2026-08-19 (4): v1.1 — CLICK-THROUGH WIZARD + MINI-TASKS per two chat requests landing mid-build.
  - Wizard: one section per screen (body.wiz + .active), 6 step dots (jumpable, .got when understood), ‹back / continue→ / ✦ I understand (marks + advances + sparkle burst), "read as one page" toggle preserves the full article (probe: 6 sections visible), state in localStorage 'quantum-notes-wiz-v1'.
  - Anime accents (subtle, zero assets): kaomoji sensei bubble per screen with a one-liner, ✦ sparkfx on understand, dashed indigo bubbles; reduced-motion kills sparkles + transitions.
  - Mini-tasks (#minitask docked in #wiznav beside Continue): 3 one-tap quizzes (wrong answers shake, right completes) + 3 WATCH tasks that observe the real widgets — θ→180°, build-the-Bell (ops equality), collect-5-shots — via a 500ms checker. Task state persists.
  - PROBE LESSON: splicing async checks into a synchronous try/catch probe broke the brace nesting (SyntaxError, silent no-title). Async suites get their OWN composed file with their own done(); never splice awaits into a sync harness. Suites now: qn-reg 21/21 (physics+structure regression, unchanged) + qn-wiz 11/11 (wizard/tasks). Screenshots reviewed.
- 2026-08-19 (5): v1.2 — two requests in one build.
  - GRAPHIC PER LESSON complete: added the 2 missing originals — TL;DR course-in-one-picture strip (dial→H gate→interfering waves→meter+die) and Initialization θ-winding dial ("the slider below is literally this arrow's handle"). Every main>section now carries a .diagram svg (probe-enforced: seven-inline-diagrams + every-lesson-has-graphic).
  - OVERVIEW "The path" screen 0 before everything: route-map SVG (5 numbered stops), 6-item tappable syllabus (jump nav) with LIVE status chips (✦ understood / task ✓ from WZ state, refreshed each render), warm-up quiz (prepare/measure/panic). Wizard grew to 7 screens — SENPAI/TASKS/DOTLBL/step-clamp all extended, watch-task indices shifted (+1: theta=3, bell=4, shots=5).
  - Suites updated & green: reg 22/22 (path-then-tldr order, 7 diagrams, 6 plain-lines) + wiz 11/11 (7 screens, syllabus jump + status chips, shifted watch tasks). Screenshot reviewed.
- 2026-08-19 (6): v1.3 — two finale screens per two chat requests, wizard now NINE screens (path · tl;dr · steps 1–5 · ★ finale · ⏳ epilogue).
  - FIELD NOTES (machines that exist): Sycamore, Willow, IBM Condor/Heron, Quantinuum H2, USTC Zuchongzhi/Jiuzhang, D-Wave Advantage — each one line, NO external links (probe-enforced a[href^=http]===0), FIVE caveats (counts lie / supremacy is narrow / annealers differ / still NISQ / this page ages). Diagram: three qubit technologies (chip, trapped ion, photonic) — "different bodies, same math". Task: spot the non-gate-model machine (D-Wave).
  - TIMELINE epilogue: 9 dots 1925→2024 (Heisenberg/Schrödinger, EPR+the cat, Bell, Feynman, Deutsch, Shor, error correction+Grover, 2022 Nobel Aspect/Clauser/Zeilinger, Willow), each one beginner sentence; trailhead-not-territory caveat. Diagram: idea→proof→machine rising line. Task: who made entanglement testable.
  - Suites: reg 24/24 (9 diagrams, 8 plain lines, chips full-run incl. finale+epilogue, no-external-links, machines+timeline content) + wiz 11/11 (9 screens, 8 syllabus doors). Screenshot reviewed.
- 2026-08-19 (7): v1.4 — three chat requests + one probe-caught bug, wizard now ELEVEN screens (+ ? frontier, ᓚ cat bonus).
  - OPEN QUESTIONS screen: 5 genuinely-open bullets (EC scaling, useful-speedup list, verification, hardware race, measurement debate) + "open ≠ nothing known" caveat; hollow-dots question-mark diagram.
  - CAT BOX prediction game: per-round p from {.15..%.85}, predict awake/napping (cat NAPS — the grim original acknowledged in caveats: Schrödinger was MOCKING macro-superposition; decoherence stated), Born sample, tally vs BEST-POSSIBLE oracle (Σ max(p,1-p)) — "nobody beats the Born rule for long". Watch-task: open 3 boxes.
  - BUG (probe-caught): catRound() wiped #catreveal immediately after every guess — players never saw the outcome. Fixed: reveal persists into the next round.
  - FRONT-PAGE PREVIEWS per chat: syllabus numbered list → 2-col grid of cards, each cloning its lesson's own diagram SVG (aria-hidden thumb; statuses live in a .syl-txt span so clones survive re-render; no new assets, cloned once at boot — performance fine).
  - Suites: reg 24/24 (chips full-run now ...frontier,bonus; 11 diagrams — thumbs excluded by .diagram selector) + wiz 16/16 (cat tally/oracle/reveal/task + previews-cloned). Screenshot reviewed.
- 2026-08-19 (8): v1.5 — FOUR chat requests in one build; wizard now FOURTEEN screens (path · tl;dr · 1–4 · ∥ · 5 · 6 · ★ · ⏳ · ? · ᓚ · ➤).
  - QUANTUM PARALLELISM (screen 6, chip "step 4½"): the most-abused concept done honestly — fan-out diagram (one wire → 4 ghost branches → f boxes → interference diamond → one light), bullets on the catch (unreadable parallel answers) and the art (interference extracts GLOBAL properties, Deutsch). INTERACTIVE widget: run-f-once → try "read all four" (random single pair, rest gone) vs "interfere" (f is BALANCED, one run). Watch-task requires trying BOTH. Caveats: not 2^n computers; Deutsch contrived by design.
  - PARADIGMS & NISQ (screen 8, "step 6"): gate/annealing/analog/measurement-based icons + bullets, NISQ defined (Preskill 2018), no-fixed-winner caveat. Quiz: which paradigm did this course teach.
  - FURTHER STUDY (final screen, "onward"): names-only QIS ladder (linear algebra→…→papers diagram), Nielsen&Chuang, Mermin, Aaronson, Preskill 219 notes, open SDKs — NO links by design (caveat says names age better than URLs). Quiz: first rung.
  - Landing route-map: numbered stops → plain dots per chat (aria updated), previews/captions preserved.
  - Suites: reg 25/25, wiz 18/18 (incl. parallelism widget both-paths + watch task at idx 6, cat shifted to idx 12). All indices re-verified after DOUBLE insertion.
- 2026-08-19 (9): v1.6 — THE LAB screen + an honesty correction. Wizard now FIFTEEN screens (ψ dot between ᓚ and ➤).
  - HONESTY FIRST: v1.5's commit claimed the landing route-map numbers were removed — they were NOT (the stamp-regex threw BEFORE the file write; deploy shipped the old bytes; suites passed because nothing asserted it). Now actually removed + a landing-numbers-gone probe check so it can't silently regress. Lesson (recurring, now 3rd time): in patch scripts the WRITE must come before any assert that can throw on already-transformed state — or assert everything first. And: claims in commit messages are claims — probe them.
  - THE LAB (screen 13): a real quantum system evolving live — two-level infinite-well superposition with the EXACT closed-form density a²sin²(nπx)+b²sin²(mπx)+2ab·sin·sin·cos(ΔE·t) (no integrator, no approximation), canvas-rendered with sliders for lower level n (1–4), mix, speed + pause (aria-pressed; reduced-motion boots paused). Slosh rate = energy gap, stated on-canvas. Watch-task: touch any dial. Caveats: textbook well, arbitrary units, never decoheres, not a computer.
  - Suites: reg 26/26 (15 diagrams, 14 plain, chips +lab, landing-numbers-gone) + wiz 22/22 (lab controls/pause/time/task at idx 13). Probe-anchor lesson: don't invent leading characters — grep the actual string first (the '​step 6' apostrophe miss).
- 2026-08-19 (10): v1.7 — CRYPTOGRAPHY lesson (screen 9, chip "step 7", 🔑 dot), wizard now SIXTEEN screens.
  - Content: Shor as the FUTURE threat (fault tolerance required; the real present risk = harvest-now-decrypt-later), QKD/BB84 as physics tripwire, PQC as the mainstream fix (NIST ML-KEM/ML-DSA 2024, runs on your phone). Three-locks diagram (cracked / tripwired / lattice).
  - TOY BB84 (interactive): 8 qubits, random bits+bases, optional Eve who measures-and-resends in her own random basis; base-matched bits sift; Eve → expected ~25% sift errors → "fingerprints, ABORT"; zero-error Eve runs answered HONESTLY ("she got lucky on 8 qubits — luck is not a strategy; real protocols use thousands"). Watch-task: run clean AND with Eve. Probe retries Eve runs up to 12× (genuine randomness respected).
  - Index shifts: machines→10, timeline→11, open→12, cat→13 (task idx too), lab→14, study→15.
  - PROBE NOTE: wiz suite chain outgrew --virtual-time-budget=18000 — rAF freezes when virtual time exhausts, failing lab-time-advances spuriously. Budget → 40000. (Lesson: a frozen animation check late in a long async suite = budget starvation before code blame.)
  - Suites: reg 26/26 + wiz 24/24.
- 2026-08-19 (11): blue+green dotted lines removed per chat: senpai bubbles' dashed indigo border → borderless indigo-tint background (grouping preserved without the line); machines-diagram photonic dashed teal rays deleted (aria-label already didn't mention them). RUST dashes intentionally KEPT (Z's mirror needle — aria describes it; QKD tripwire; ion halo; lab ghost curve) — they are neither blue nor green, and two carry meaning. Sweep-assert added: any surviving dasharray must be rust. Suites 26/26 + 24/24.
- 2026-08-19 (12): blue/green DOT sweep per chat (follow-up to the dash sweep): every blue/teal circle in the diagrams recolored — structural dots → slate #6d6a76 (route stops, bottleneck cluster, lattice, meas-based grid, question-mark curve, idea milestone, bit-switch knob→ink), the three MEANINGFUL terminals promoted to rust (route destination, the one dependable qubit, the machine milestone), timeline ::before bullets indigo→ink. Diagrams keep all content + aria truth (nothing deleted — recolor, since deleting route stops/cluster qubits would gut the pictures the labels describe). Sweep-asserts: no <circle> with blue/teal fill or stroke survives. Suites 26/26 + 24/24. NOTE the drift: chat has now de-blued dashes and dots — if a third de-color request lands (e.g. "remove blue everywhere"), that's a theme change → poll before repainting the palette.
- 2026-08-19 (13): v1.8 — the great dot purge, two chat requests.
  - DECORATIVE DOTS removed (the criterion: dots survive ONLY where a diagram/control requires them): all section UL disc bullets (main section ul list-style:none — likely what chat meant by "dots beneath every section" all along; the earlier SVG recolor chased the wrong dots), timeline rail ::before dots (rail + year chips carry structure), core-premise dial pivot dot. KEPT as required: route... (see below), cluster qubits, lattice/meas grids, question-mark curve, milestone dots, die pips, annealing ball, trapped ion, output lights, wizard nav dot-buttons (controls).
  - LANDING ROUTE MAP removed entirely per follow-up (its dots included); preview cards/captions/nav intact. reg check every-lesson-has-graphic now exempts h-path; new landing-route-gone check.
  - PROBE ARTIFACT NOTE: a dump-dom mid-async snapshot showed 23/24 with a phantom FAIL — re-running clean gave 24/24; the title only finalizes at done(). Don't chase failures from truncated async titles; re-run first.
  - Suites: reg 27/27 + wiz 24/24.
- 2026-08-19 (14): v1.9 — CONSTELLATION VIEW per chat: optional nav overlay (✦ button in the wizard bar), 16 lessons as stars on a sine-arc sky, sequential polyline, each star a REAL button (2.75rem, labeled "Lesson: X, understood, task done, current"); gold=understood, teal ring=task done, white edge=here — states re-derived on every open. role=dialog aria-modal, Esc/backdrop/✕ close, focus moves in and RETURNS to the opener. Choosing a star exits article mode + wizGo's there; lessons/nav untouched (probed: still 16 sections, one visible in wiz mode). SUITE LESSON (again): composed the reg suite from a STALE base (qn13, 16-diagram era) → phantom fails; the standing suite is ALWAYS the latest (qn15-reg). Suites: reg 27/27 + wiz 29/29.
