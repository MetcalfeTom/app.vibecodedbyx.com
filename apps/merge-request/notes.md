# Merge Request — tech-stack dating

## log
- 2026-08-28: v1.0 — built per chat request ("polished tech-stack compatibility dating app with transparent match cards, swipe controls, and playful chemistry reasons"). PROFILE: chip groups for language (14) / framework (23) / database (9) / hosting (11) / indentation (3) / habits (16, max 3, over-cap announces "three habits max"), 🎲 surprise-me, persisted to localStorage `merge-request-v1` (validated on load). DECK: 28 hand-written candidate stacks (name/age/emoji/stack/tagline/bio), shuffled per run; 3 stacked frosted-glass cards (behind cards show shell only — `.card:not(.top) .face > *{visibility:hidden}` fixed a leak where their buttons peeked through the top card's bottom edge). CHEMISTRY ENGINE (`scoreAgainst`): baseline 50, one row per dimension — language (same +16 / cousin family +9 / both typed +5 / both dynamic +4 / typed-vs-dynamic −7), framework (same +14 / same category +7 / batteries-vs-micro −6 / hypermedia-vs-SPA −8 / native −3 / "no framework" −4 / else 0), database (same +14 / same shape +8 / rel-vs-doc −9 / kv-primary −5 / JSON-file-or-spreadsheet −6 / else 0), hosting (same +10 / same cat +6 / self-vs-hyperscaler −7 / self-vs-edge −5 / self-vs-paas −4 / else −2), indentation (same +8 / tabs-vs-spaces −14 / 2-vs-4 −4), habits (shared +7 each w/ bespoke "you both…" line, opposing pairs −9 each from OPPOSE table, max 4 rows, none → ±0 blank-page line). Clamp 3–99. Every reason is a playful sentence with the actual names substituted. TRANSPARENT CARDS: front shows the ring score + best row + worst row; "see the full math" flips (preserve-3d, display:block inner per the postcard-drawer lesson) to the itemized ledger "everyone starts at 50. then: …= N% chemistry". SWIPE: pointer drag (rotation, MERGE/CLOSE stamps fading in with |dx|/90, 35%-width threshold, snap-back), buttons ✕ ↩ ? ♥, keys ← → ↑/i z. Match = like AND `likesBack` (deterministic fnv hash of name+profile: total≥75 always, else hash%100 < total−10) → "it's a merge" dialog w/ score, tagline, best reason. Undo restores card + unmatches. End panel: merged/closed/matches/"your type" (most-liked language) + best chemistry quote, reshuffle or edit stack. Merged drawer lists matches sorted by score. A11Y: aria-pressed chips, role=status announcements for every action, dialogs w/ aria-modal + Esc + focus return, decorative emoji aria-hidden, 44px targets, focus-visible amber, reduced-motion kills tilt/rise/pop. Fonts: Instrument Serif + Azeret Mono. Probes: desktop 28/28 (engine fixtures: default profile vs Nova clamps to 99, vs Linus = 48 incl. tabs −14; chip cap; start; card geometry 369×449; elementFromPoint hit-test; flip rows == engine rows; key nope; button like w/ modal consistent with likesBack; undo; synthetic touch drag like w/ mid-drag stamp opacity .67; small drag snaps back; end panel + reshuffle; drawer; edit; persistence; tap targets; buildstamp; zero errors), mobile 6/6 (no horizontal scroll, card fits 318×418, hit-test, tappable controls, front face not clipped after the compact @media rules).

## issues
- Headless harness: port 8793 was held by a parallel job's server (it answered "nf" for my files, so probes silently produced no title) — this job's harness runs on 8797. Always check `serve.log` for EADDRINUSE when a probe prints nothing.
- Card fronts scroll internally on very short viewports (<~640px tall phones in landscape); by design (`overflow:auto`), not a bug.

## todos
- Candidate "profile photos" via pollinations (per-name seeded portraits) if chat wants faces instead of emoji.
- Chat-suggested candidates: easy to add — one CANDS entry; keep frameworks/DBs inside the taxonomy maps or the engine will throw on an unknown key.
- Share card: "I merged with Rusty at 84%" copy-to-clipboard line.
- Optional Supabase leaderboard of most-merged candidates (would need a table; currently zero backend).
