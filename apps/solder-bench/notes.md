# solder-bench · notes

## log
- 2026-08-31: v3 — **BEKEN MODULE ID card** per chat ("Beken module identification with photo-style SVGs, chip markings, matching pinouts, and reversible read-only steps first"); added here instead of a new app (depth over breadth). `BEKEN_MODULES` data: CB2S/WB2S (11-pad twins, FULL pinout per Tuya datasheets — CEN/ADC·P23/GND/TX1·P11/RX1·P10/3V3 + P24/P26/P6/P7/P8) and CB3S/WB3S (18-pad, deliberately PARTIAL: only service pads named; pad order left to datasheet + continuity check — probe asserts the partial cards stay partial). `bekenSvg()` photo-style: PCB, shield w/ laser text, antenna meander, castellated pads (circle+rect halves). `BEKEN_STEPS`: 7 read-only/reversible steps (photograph → match marking → UNPLUG MAINS → 3.3V UART wiring → read chip id → dump flash twice + compare → journal the backup), each with a badge the probe checks; flashing explicitly off-card. Selection + checklist persist in `store.beken`. Suite bk-tail 24/24 ×3 widths incl. regression on the original pinout card (D34 click — the first svg [role=button] is EN, a POWER pin, whose panel stays disabled: pick a GPIO pin for click regressions; SVGElement has no .click(), dispatch MouseEvent).

- 2026-07-11: v2 — **multi-board profiles + build journal + stock calendar** (chat ask; "keep the existing pinout diagram" honored — same SVG builder, now data-driven). (1) **BOARDS registry**, 3 profiles: `devkit-v1-30` (unchanged), `devkitc-38` (adds D0 + SD0-SD3/CMD/CLK = GPIO6-11 wired to internal flash → ERROR-level "will crash the chip" rule), `s3-devkitc` (ESP32-S3 DevKitC-1 44-pin: NO input-only pins, strap {0,3,45,46}, ADC1=GPIO1-10 / ADC2=11-20, touch T1-T14, no DAC, USB D-/D+ = 19/20 warning, octal-PSRAM caution 35-37, UART0 43/44). `capsOf`/`pinWarnings` now take a board param; SVG height/viewBox computed from pin-row count. Per-project board select in pinout header; switching boards keeps assignments — ids that don't exist on the new board render as red "orphaned" rows w/ one-click remove (no silent data loss). (2) **Build journal**: per-project `journal[{ts ISO, text}]`, textarea + log button, newest-first with YYYY-MM-DD HH:MM stamps, per-entry delete. (3) **Stock calendar**: month grid (monday-first), pure `calendarEvents(proj)` collects — BOM `eta` (new date column; ◆ amber, suppressed once received/mounted), auto date-stamps from `hist` (status flip to ordered/received/mounted writes `hist[status]=today`, first flip only), journal ✎ entries; day click → detail list, prev/next/today nav, today outlined. `migrate()` upgrades v1 stores in place (board/journal/eta/hist) — runs on load AND on JSON import. CSV export gained the eta column. **Node-verified**: 3 board integrity checks (no dup ids, every signal pin has a gpio entry), 6 per-board rules (flash pins, S3 strap/USB/PSRAM, S3-no-input-only, v1 regression), calendarEvents (eta suppression, same-day stacking), migration shape.
- 2026-07-11: v1 (chat ask: electronics project manager — BOM tracking + pin assignment tables + ESP32 pinout diagrams, browser-based). **Board model**: ESP32 DevKit V1 30-pin (DOIT layout — LEFT: EN,VP,VN,D34,D35,D32,D33,D25,D26,D27,D14,D12,GND,D13,VIN; RIGHT: D23,D22,TX0,RX0,D21,D19,D18,D5,TX2,RX2,D4,D2,D15,GND,3V3; GND2 internal id to keep keys unique, displayed as GND). GPIO fact tables: INPUT_ONLY {34,35,36,39}, ADC1/ADC2 channels, DAC 25/26, touch, strapping {0,2,5,12,15}, UART pins. **Pure rule engine** `pinWarnings(id, {signal,dir})` — errors: output/inout on input-only pin, analog on no-ADC pin, any signal on power/EN; warnings: strapping pins, UART0 loss, ADC2-dies-with-WiFi. All 6 rules node-verified. **Pinout SVG**: built from JS pin arrays (namespace createElementNS), board body + WROOM shield + USB, clickable/keyboard-focusable pin groups (tabindex+role=button+aria-label), assigned pins fill copper w/ signal label outside the row, selected ring. Click → side panel: capabilities line, signal/direction/note form, live warning box (updates on input BEFORE saving), assignment table with amber/red row edging per warnings. **BOM**: inline-editable rows (ref/part/value/pkg/qty/unit-cost/status needed→ordered→received→mounted w/ color coding), totals strip (lines/parts/€cost/%mounted by qty), CSV export (proper quote escaping, node-verified), add/delete. **Projects**: multi-project via header select (new/rename/delete w/ confirm + last-project guard), whole-project JSON export/import (shape-validated). All in localStorage `solder-bench-v1`; seed project ships with ESP32+LED+resistor BOM and D2=STATUS_LED so nothing looks empty. PCB aesthetic: soldermask green grid bg, copper/gold accents, Michroma + Fira Code. Pollinations OG (flux seed 2288). Hook `__bench {pinWarnings,capsOf,bomToCsv,csvEsc,GPIO,store,proj,selectPin}`.

## issues
- PRE-EXISTING phone overflow: the pristine app measures 388px scrollWidth at a 320px viewport (before the Beken card; probably a table or the projbar). The Beken suite asserts "no NEW overflow" against that 388 baseline. Fix opportunistically when chat mentions phones.
- Board is hardcoded DevKit V1 30-pin. If chat asks for the 38-pin or ESP32-S3, add a BOARDS registry (pin arrays + GPIO map are already data-driven — mostly a data task).
- prompt()/confirm() used for project ops — fine for v1, custom modals if chat complains.

## todos
- More boards (ESP32-S3 DevKitC, 38-pin WROOM, Pico W?).
- Wire-color column in assignment table.
- Print stylesheet for the pinout + BOM (workshop paper copy).
