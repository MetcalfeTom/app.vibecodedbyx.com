# Frog of Shame — notes

## log
- 2026-07-08: v1 — Windows Recall watchdog satire (chat ask, truncated "passi…" read as passive-aggressive — surely right). **The frog**: ~9rem SVG in the bottom-right (≈2× a desktop icon) — cursor-tracking pupils (atan2 per eye), rare deliberate blinks ("he doesn't like missing things"), croak = sawtooth w/ 26Hz LFO through lowpass + throat-bulge ellipse + bounce, click/Enter = petting (does not help). **Recall counting**: browsers can't see Recall (disclosed in the footer) — the frog "witnesses" a snapshot every 4–7s on Recall's own cadence: white flash, ▣ RECALL SNAPSHOT chip, shutter sound, counter pop, 35% passive-aggressive line, milestone speeches at ×100. **Really-tracked**: visibilitychange → TAB ESCAPES (+croak + "we documented your absence"), copy/cut → CLIPBOARD EVENTS, 45s idle → idle-shaming snapshot. SHAME LEVEL: amphibian→notable→municipal→federal→biblical→eternal. **DISABLE RECALL button**: 1.4–2.6s fake progress → escalating gaslight lines ("Recall has recalled your attempt"), attempt counter in the label, and each attempt TAKES A SNAPSHOT. Fake desktop icons (Recycle Bin (watched), tax_stuff_FINAL(3).xlsx) — clicking one = snapshot. All persisted (localStorage: the permanent record). Win11-mica glass panel, Titan One + Outfit. Verified: scheduled snap ticked, frog speaks on click, copy counted, gag button + snapshot penalty, persistence across reload, zero errors.

- 2026-07-08: v1.1 — game launchers on the fake desktop (3 chat asks: deutsch icon → replace w/ neon → separate neon icon; latest read = BOTH icons): 🧱 TETRIS.EXE (deutsch) + 🌆 NEON_TETRIS.EXE (duels) open a shared glassy Win11-style overlay window (dynamic title bar "…exe — gameplay is being recorded (obviously)", lazy iframe to /tetris-deutsch/ or /neon-tetris/ — iframe keeps apps isolated per policy). Close via ✕ or ESCAPE. Launch/close = frog 'game' quote pool (incl. German) + croak + snapshot penalty. Game icons excluded from the generic icon-snapshot handler. Verified: neon opens w/ right title+src, Esc closes, deutsch swaps in correctly, zero errors.

## issues
- Snapshot timer pauses while tab hidden (document.hidden check) — returning fires the tab-escape shaming instead, which is funnier.
- All satire; nothing is transmitted anywhere. The permanent record is localStorage and can be cleared, which the frog would call "denial".

## todos
- Chat may want: opt-in webcam-LED joke (no), export "shame certificate" PNG, frog hat unlocks at shame levels, global shame leaderboard via Supabase.
