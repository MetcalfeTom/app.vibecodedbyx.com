# Code Creatures

## log
- 2026-04-25: **Go syntax recognition + reward boost.** Parser was treating Go snippets like errors — `func(){}`, `:=`, `<-`, goroutines all dropped error blocks. Added `go` as the 4th first-class language alongside py/js/cpp: `LANG_GLYPHS.go = ['~','` + "`" + `','→','¶',':=','<-','chan','defer','go']`, `LANG_COLORS.go = { main:'#00add8', accent:'#67e2f4' }` (Go gopher cyan), `LANG_KEYS = ['py','js','cpp','go']`. `langOfText()` now scores 16 Go-specific signals: `package` (+5), `func(...)` declarations (+4), `:=` short-decl (+3), channel ops `<-` (+4), `go func` (+4), `defer`/`chan` (+4 each), `interface{}` (+3), `struct{` (+2), `fmt.X(` (+3), `import "..."` blocks (+3), `range`/`make` hints. `humanizeParseError()` upgraded to detect Go-flavored parse errors and emit `Go detected · not valid JS · drops a polyglot error block` instead of generic V8 noise. Storage migration adds `go:0` to old `c.lang` maps. **Reward boost**: Go snippets pay 1.5× hunger relief + 1.5× xp on consumption (Go syntax is denser/harder than the others) and bump `c.lang.go` by **3** instead of the standard 2 — fast-tracks the polyglot risk + sprite shift. Pop label shows `+xp · GO ×1.5`. Plant `langPick` weights bumped to include `go` (2/13 chance per regrow) so the garden's plant mix slowly diversifies into cyan glyphs.
- 2026-04-25: **Canvas shift + 137px top boundary + memorial persistence fix.** Three combined changes: (1) `#gardenCanvas` shifted down 45px inside its wrap (`top:45px; height:calc(100% - 45px)`) so the floating action toolbar has visual breathing room. `resizeGarden` now subtracts a `CANVAS_OFFSET_Y = 45` from logical canvas height. (2) New `garden.topY = 137` physics ceiling computed in `resizeGarden`. Agent bound clamps, hazard spawn y-ranges, snippet/error spawn y-ranges, food/toy ceilings, and error block bounce all respect topY → things can no longer slip behind the header. Sweep replaced every `garden.H - N` magic number with `garden.floorY`/`garden.topY` relative offsets so the soil line, agent fade y, memorial pop default, and dead-creature drop-y all stay anchored to the visual ground glyph. (3) **Memorial persistence fix**: `loadCreatures()` now strips creatures saved with `alive:false` (they were stuck on disk if the user refreshed during the 1700ms cleanup setTimeout), stashes them in module-level `__orphanDead`, and the boot script promotes any whose id isn't already in `state.memorial` via `recordMemorial(c)` before re-saving the live roster. Names like "Durf the Third" no longer vanish — they make it onto the wall.
- 2026-04-25: **DNA currency from zapping + toolbar restyle.** New `state.dna` currency persisted to `localStorage['code-creatures-dna-v1']`. New `awardDna(amount, x, y)` helper bumps the counter, persists, refreshes HUD, and floats a `+N 🧬` pop. `zapHazard` now pays `DNA_REWARD = { virus:3, worm:6 }` (plus +1–3 if the hazard had infected a creature recently — bonus polyglot harvest). HUD pill `#gardenDnaCount` lives in `.garden-meta` next to visitors/roaming, with `dnaBump` keyframe (scale 1→1.16→1 + green box-shadow flash) on every gain. Currency carries across sessions. **Toolbar restyle**: all action buttons now match the Lay Egg style — `.tama-item.food-item` font dropped 12px → 11px, padding 4px 9px → 4px 10px, gap 5px → 6px, cost size 10px → 9px so the visual rhythm aligns. **Stacked categories**: replaced the wide horizontal `.tb-foods` row with two named stacks — FOOD (Spaghetti / RAMen / Megabyte / Byte arranged in a 2×2 grid via `grid-auto-flow:column`) and BINARY (Mini / Big binary balls in a 2-row column). Each gets its own vertical `.tlabel` divider so the toolbar reads as: ACTIONS · FOOD · BINARY · GUARD. The Big Binary Ball glyph dropped its inline `font-size:17px` override to live within the shared 14px glyph rule.
- 2026-04-25: **Firewall warmth on eggs.** Firewall turrets now bake nearby eggs as a side effect of being lit. New `FW_EGG_WARMTH_RATE = 10` constant; in `tickEggs`, each alive firewall within `FW_TURRET_BURN_R + 36` (≈114u) contributes `FW_EGG_WARMTH_RATE * intensity * fall` warmth/sec to each egg, where `intensity = max(0.35, fw.hp/fw.maxHp)` (decays as the turret burns out, but never fully zero until hp=0) and `fall = 1 - sqrt(d²)/heatR` (linear distance falloff). Stacks on top of nesting-adult passive radius warmth — multiple turrets can rapidly heat an unattended egg. Visual: ~55%/sec chance per warmed egg of emitting a small `♨` heat pop near the egg. Each egg carries `_towerHeat` for future debug/UI surfaces. Defenders incidentally double as incubators: park a few firewalls near a fresh egg and it hatches in roughly half the usual time.
- 2026-04-25: **Plant grazing mutates DNA + sprite language + polyglot virus risk.** Syntax plants are now real food. Each plant is tagged with a `lang` (`py` / `js` / `cpp`) and a glyph from `LANG_GLYPHS` (Python `: λ # _ . " '`, JS `{ } => ; ( ) = .`, C++ `* & :: ++ << >> -> &&`). New `nearestPlant(ag)` + `eatPlant(c, plant)` helpers: when a creature past the cell stage has hunger > 24 and no other priorities (food/egg/error/toy), it walks to the nearest plant. On contact, the plant's glyph is appended to `c.dna` (sliding 32-char window — sprite cache key includes DNA so the sprite mutates immediately), `c.lang[plant.lang]` is incremented, `feedCreature(c, 14, 4)` triggers, and the plant is marked `eaten:true` with `regrowAt = now + 22..46s`. **`drawCreatureBody` language affinity**: `dominantLang(creature)` returns the most-eaten language (≥2 glyphs); when set, the creature's `fillChars` are overridden to that language's glyph palette and `mainColor`/`accentColor` swap to its signature color (Python blue `#5fc0e0`, JS yellow `#f5b362`, C++ violet `#c986e0`). Outline char also flips to a language token (`:` / `;` / `*`). **Plant regrowth**: every `tickGarden` checks eaten plants past their `regrowAt`; rerolls a fresh `lang` + glyph from the same anchor and emits a `✦` sparkle pop in the new color so the garden's syntax mix shifts gradually. **Intelligence boost**: `getIntel` now adds `languageBoost(creature)` = capped per-language counts × 0.35 + diversity bonus (`div ≥ 2` → +`div * 1.2`). A creature that has tasted multiple languages thinks faster — earlier self-feeding, faster error scrubbing. **Polyglot virus risk**: new `virusRiskBonus(c)` = `min(1, div * 0.18 + total * 0.025)`. Hazard contact now rolls `Math.random() > risk` AGAINST the natural-immunity check — even a healthy adult can fail immunity if it knows enough languages. Infection duration now scales `infectSec * (1 + risk * 0.8)`, and a `risk > 0.4` polyglot pops `INFECTED · POLYGLOT` instead of plain `INFECTED`. **Typed snippets inherit language**: `langOfText(src)` heuristic scores typed JS against Python/JS/C++ token signatures (`def`, `=>`, `#include`, `std::`, `template<`, etc.); `spawnSnippetsFromText` now returns `{count, lang}` and tags each spawned snippet item with `lang`. When a creature eats a typed snippet, `c.lang[snippet.lang] += 2` and a representative glyph is spliced into the DNA so the sprite shifts immediately. Status line shows `✓ valid · spawned 2 snippets · CPP flavor` so the player can see the inferred language. Pop label on consumption shows `+xp · CPP`.
- 2026-04-25: **Interactive syntax bar + edible code snippets + error blocks + janitorial cleaners.** Added `<form class="syntax-bar">` strip between toolbar and canvas: prompt `>_` + monospace input + ▶ run button + status line. On submit, `new Function(src)` validates JS syntax (NEVER executed — only constructed). Valid → `spawnSnippetsFromText(src)` drops 1–3 drifting `snippet` food items at random spots, each carrying the typed text rendered with RGB-split (pink/cyan ghosts behind green core). Snippets drift horizontally like the bg glitch (`vx:±16-38`, sinusoidal y-bob), eaten by any creature for 34 hunger-off + 16 xp. Invalid → `spawnErrorBlock(msg)` drops a red glitchy box with the parser error message (RGB-split text, pulsing border, ±jitter). New `garden.errors[]` array, 90s ttl, persists until cleaned or expires. New `isCleaner(c)` check: adolescent+ creatures with `intel ≥ 4` patrol for error blocks, walk to them, scrub for 1.6s (rate `1 + intel*0.08`), gain 12 xp + 18 hunger-off. Cyan wipe progress bar from left, scrub sparks `× / \ ·`, 8-particle cyan burst on completion. Cleaner duty triggers between food/egg priorities and toy play, gated on `hunger < 65`. Bar status flashes green on success, red on error, auto-resets after 4.2s.
- 2026-04-25: **Parental instinct + intelligence stats + autonomous self-care.** Added two new DNA-derived stats: `par` (parental, drives egg tending) sourced from vowels (×0.6) + punct (×0.5) + brackets (×0.35), and `intel` (intelligence, drives self-care decisions) from digits (×0.7) + ops (×0.6) + brackets (×0.45). Stats surfaced on the detail modal as PAR (pink `#ff9bbf`) and INT (keyword purple) tiles in the now-3×2 stat grid. Stage gates: full power at adult/elder, ~45–55% at adolescent/forager, intel only ~25% at hatchling, parental zero pre-adolescent. **Egg tending**: in `tickAgents`, after food/infection checks, parental adults scan all eggs within `140 + par*12` reach and pick the most needy (cold/low-incubation get priority via score `dist - need*1.4`), walk over at speed `46 + par*1.5`, and once within 22u stop and "huddle" — adds an active `(1.6 + par*0.25)` warmth/sec on top of passive radius warming + emits "huddle" pink pop ~45%/sec. `tickEggs` passive warming now scales by parental stat (`1 + par*0.18` boost) AND extends radius (`70 + par*6`); a `♥` heart pop spawns when an agent is actively nesting (<32u). **Self-feeding**: hunger threshold for autonomous food-seeking drops from 35 → `max(18, 35 - intel*0.9)` so smart creatures top up their reserves before crisis. Toy-play deprioritized when an egg needs tending. Egg gets `_huddlerId` field (transient, for FX). Won't tend if hunger > 70 (survival-first). Won't tend while infected (erratic mode overrides). Effect: an adult with 4 vowels + a punct mark in its DNA reads ~5 PAR and naturally drifts to any laid egg, parking on it like a hen — eggs that used to fail now hatch reliably as long as you have at least one parental-leaning adult in the garden.
- 2026-04-25: **Real egg incubation mechanic.** Replaced "Lay Egg → instantly creates creature" with a multi-stage incubation system. Eggs now sit in the garden as physical objects on `garden.eggs[]` (persisted to `localStorage['code-creatures-eggs-v1']`) with `{id, dna, name, parents, x, y, incubation, warmth, age, wobble, laidAt}`. **Warmth** decays at 4/sec from 65 starting value, +32 per click, +6/sec passive when an adult/elder is within 70u (fades with distance). **Incubation** rises +1.4/sec while warmth ≥ 22, decays -0.6/sec when cold; at 100 the egg hatches into a creature, at -25 it dies and emits "egg failed" pop. Render: warmth-colored halo (orange when warm, blue when cold), gradient egg body, cracks appearing as incubation > 0.45, gold incubation arc on top, warmth bar below, "cold" shiver text under cold eggs. Click an egg in the garden → +warmth pop. **Egg modal button** changed: `⌬ Hatch in Garden` → `🥚 Lay Egg in Garden`. `hatchEggToCreature` is now invoked by the egg-tick when fully incubated, not by user input. Lifecycle: type DNA → modal closes → egg appears in garden → must be warmed (player clicks or adult nests nearby) until 100% incubation → cracks + hatches → creature spawns at egg position. Failed eggs leave a "× × ×" debris burst.
- 2026-04-25: **Breed converted to overlay + natural immunity + autonomous breeding + worm slim.** **Breed**: removed standalone `Breed` nav tab + `view-breed` section, replaced with a fixed-position fullscreen overlay (`z-index:22`, radial-dim background, 5px backdrop-blur) triggered by a new `⊕ Breed (two adults)` button in the garden toolbar. Modal scales 0.94→1 on `.show`, has rotating ⊕ glyph, click-outside-to-close, ESC-to-close. Default state changed `view: 'hatch'` → `view: 'garden'` (hatch view was removed earlier). **Natural immunity**: healthy adults/elders (HP ≥ 80, hunger ≤ 40, not currently infected) now shrug off virus/worm contact — only take 0.2× damage capped at 4hp, pop "IMMUNE" cyan label, hazard purged (set hp=0). Constants `IMMUNITY_HP=80`, `IMMUNITY_HUNGER=40`. **Autonomous breeding**: every 1.2s `tryAutoBreed()` scans for eligible adult pairs (within 64u, hp≥78, hunger≤45, age≥1.5min, 90s per-creature cooldown via new `lastBredAt` field, garden pop cap 8). On match: crossover their DNA, spawn child creature at midpoint with hp:90/hunger:18, cost both parents +18 hunger, emit ♥♥ hearts at parents + 14-spark burst at midpoint, set both `lastBredAt=now`. Constants `AUTOBREED_*`. **Worm slim**: redesigned the worm hazard from chunky 7-segment serpent to a sleeker 6-segment data-spline using `[· ~ - = o ▸]` glyphs sized 5–11px, smaller crumbs (2 instead of 3), 8px font (vs 9), removed `bold`, smaller eye radius 1.1, lower tangent factor 0.32 — looks more like a darting data-serpent than a fat worm.

- 2026-04-24: **Garden-first layout + ⚡ Zap Virus tool + worm redesign + Hatch view removed.** Standalone Hatch tab/view deleted — the inline egg modal already handles laying. Nav now shows just Garden / Breed; Garden is the default active view. Removed `view-hatch` HTML, `.hatch-stage` / `.egg-arena` / `.hatch-side` / `#hatchInput` / `#eggCanvas` / `.bar` / `.crack-burst` CSS, and the entire HATCH VIEW JS subsystem (`hatchInput`, `hatchBar`, `hatchTyped`, `hatchTargetEl`, `hatchHint`, `hatchBtn`, `newEggBtn`, `eggCanvas`, `crackBurst`, `getCurrentHatchDna`, `updateHatchUI`, `animateEgg`). Breed flow's `hatchInput.value = ''` + `updateHatchUI()` calls dropped — it just goes `switchView('garden')` + `openEggModal()`. INIT no longer focuses `hatchInput`. **Layout**: `.garden-wrap` is now a CSS grid with named areas `meta / toolbar / canvas mem` so canvas + memorial sit side-by-side on wide screens (`grid-template-columns: minmax(0,1fr) 280px`). max-width 1240→**1860px**, height calc tightened (`100vh - 110px`), `.main` padding reduced 18/22/24 → 12/22/18. <1080px collapses back to single column with canvas `aspect-ratio:16/10` + `min-height:420px`. **⚡ Zap Virus tool**: new `.tama-item.zap-tool` button after the food row (lime-green active glow). Toolbar handler matches both `[data-item]` AND `[data-tool]`; `setPlaceMode('zap')` toggles `.zapping` (cursor:crosshair) on the canvas + writes a hint w/ live hazard count. Click in zap-mode → `hazardAtPoint(px,py)` (center distance, hit radius `cfg.size + 14` ≈ 30–32px) → `zapHazard(hz)` spawns a green `ZAPPED`/`PURGED` label pop + 8-arm ring of `⚡` micro-pops + sets `hp = 0` (existing filter sweeps it next tick). **Multi-target**: each click zaps **one** hazard (the closest within radius). Shift-click stays armed for chain-zapping; plain click stays armed only if more hazards remain on the field, otherwise auto-disarms. Cursor preview while zap-armed: animated dashed crosshair ring + `⚡` glyph + bright lime lock-on ring around whichever hazard is in range. Fixed a latent crash where `garden.placeMode === 'zap'` would have hit `ITEM_TYPES['zap'].color` (zap is not an item type) — preview now branches on zap vs item. **Worm redesign**: replaced the 5-segment `~~~≈` trail with a serpentine ASCII spine — 7 tapered glyphs `· ~ { ( ( o ▶` sized 8→14px, each rotated to follow the spine tangent, swaying along a sinewave perpendicular to velocity (`Math.sin(phase*5 - fromHead*0.55) * 5.5`), shadowBlur ramps 4→15 from tail to head so the head reads as the active end. Behind the tail: 3 corrupted-data crumbs (`1`, `0`, `·`) drift on a delayed sine. A red sensor eye `#ff3b4a` (radius 1.5±0.45·sin(phase·11)) pulses at the head, offset along velocity. Body color `#ff9b3a` amber kept. Cohesive with the rest of the app's ASCII typography aesthetic + clearly directional.
- 2026-04-24: **In-garden egg laying.** Replaced the "Lay Egg → switch to Hatch tab → switch back" detour with a self-contained modal that opens directly over the garden canvas. Modal absolutely positioned inside `.garden-canvas-wrap` (z-index 25) with backdrop blur + radial dim, animated egg glyph, dedicated `#eggInput` textarea, live `eggCount/eggTarget` ratio, gradient progress bar, and `⌬ Hatch in Garden` button (disabled until 32 chars). Submit reuses the same creature-construction pattern (`hp:100, hunger:0, alive:true, infectedUntil:0, parents: state.pendingParents`) and seeds from `state.pendingDna` if a bred egg is queued. ESC closes the modal (precedes place-mode cancel); Cmd/Ctrl+Enter hatches; click outside the card closes. Breed flow's "Hatch this child" now jumps to the garden view + opens the egg modal instead of switching to the hatch tab. Extinction "Begin Again" button likewise routes to garden + opens the modal.
- 2026-04-24: **Memorial wall: DNA + offspring count.** Each cell now shows the dead creature's full 32-char DNA string in a per-character class-tinted strip (vowels green, consonants purple, digits cyan, ops pink, brackets amber, punct soft-green) on a dark inset panel with bottom-fade overflow mask. Added a Silkscreen offspring chip — `⌬ N heirs` (cyan) or `∅ no heirs` (muted) — counted live by `countOffspring(parentId)` which scans both `state.creatures` and `state.memorial` for entries whose `parents[0|1]` matches the deceased's id. `recordMemorial` now stores `parents` so multi-generation lineage can still be reconstructed after every ancestor has died.
- 2026-04-24: **Memorial wall.** Added an "In Memoriam" panel below the garden canvas that lists every fallen creature so they aren't forgotten across sessions. Each death calls `recordMemorial(c)` which captures `{id, name, dna, cause, born, diedAt, lifespanMin, stage}` and unshifts onto `state.memorial` (capped at 240, oldest-evicted). Persisted to `localStorage['code-creatures-memorial-v1']` independently of the live `code-creatures-v1` roster, so deletions and extinctions don't wipe it. Render: auto-fill grid `minmax(180px, 1fr)`, max-height 240px scrollable. Each cell has `†` glyph in keyword purple, Fraunces creature name, lived-XmYs duration, and a color-coded cause chip (starvation = red, old age = amber, virus = green). Empty state: "no losses yet · the soil is undisturbed".
- 2026-04-24: **HOTFIX — aging math + load-time mass death.** Two compounding bugs killed every creature instantly on load: (1) old creatures saved before the aging system existed had `.born` timestamps from hours/days ago — first survival tick saw `ageMinutes(c) >> MAX_AGE_MIN(30)` and insta-killed them all of "old age". (2) The hunger/HP rates were `rate * dtMin * 60`, which collapses to per-second instead of per-minute as the comment intended — making fresh creatures starve in ~53s. Fixes: storage migration now rebases `c.born` to `Date.now() - 60s` (with `hp ≥ 80`, `hunger ≤ 30`) for any creature whose age already exceeds `MAX_AGE_MIN * 0.95`. Survival math dropped the spurious `*60` so `hungerRate`, hp drain (`18/min`), regen (`22/min`), and infection drain (`9/min`) are now actually per-minute. Hunger rates rebalanced to per-minute units: cell 5, amoeba 7, hatchling 10, forager 12, adolescent 13, adult 14, elder 9. With these: an adult fills hunger in ~7 min, starves in another ~5.5 min — needs feeding 2–3 times in its 30-min life. Also clamped survival `dt` to 2.5s max so backgrounded tabs don't age creatures by hours on resume.
- 2026-04-24: **Virus rework + infection mechanic.** Viruses no longer drawn as a chromatic-split `V` — now a **green jagged 5-segment zigzag** (double-stroke for a "⚡" feel) with a glowing green core dot, surrounded by **3 orbiting corrupted red binary strings** (5-bit pseudo-random, `1`/`0`/`░` glitch char, jdtterring per phase tick). Color flipped from red `#ff5e6c` → green `#5af07a`; binary aura is `#ff3b4a`. **Infection**: virus contact now sets `c.infectedUntil = now + 14s` (in addition to hp/hunger damage), then virus is consumed. While infected, creatures (1) move **erratically** — `±70` vx / `±50` vy random jitter while chasing food, `±90`/`±60` while wandering, lunge speed × 1.35; (2) become **ravenous food thieves** — always seek `nearestItem(ag, 'food', /*ignoreClaim*/ true)` ignoring the hunger threshold AND ignoring the `claimedBy` filter, seizing claim from prior owner; popups show `STOLEN` tag in green. (3) Visual: green dashed halo + orbiting binary `0/1` glyphs + chromatic-split ghost copy of the sprite drawn under at `lighter` blend mode. Survival tick drains `1.5 hp/min` extra while infected, clears `infectedUntil` once timestamp passes. `nearestItem` got new `ignoreClaim` flag. Virus dmg dropped 18→14 + hunger kick 8→6 since infection itself is a major drain. Storage migrates `c.infectedUntil = 0` for old saves; hatch flow inits it.
- 2026-04-24: **Garden habitat (shared)** — replaced static "Stable" inventory grid with a live, multiplayer Garden where creatures roam. Tab renamed Stable → Garden. New full-area canvas (`#gardenCanvas`) with parallax scene: faint twinkling stars, drifting code-snippet phrases (`function()`, `=> {…}`, `// hum`...), dotted soil line, 28 swaying ASCII plants (`Y ψ ✿ ❀ ✺ * ☘ ✦ ⌇`) + ground-radial green glow. Each creature becomes a wandering **agent** `{x,y,vx,vy,facing,wanderT,pauseT,bob,fade}` — re-targets every 1.4–4s with occasional graze pause, bounded box, mirrors sprite by facing direction, soft elliptical shadow. Sprites rendered to per-creature 96×96 offscreen canvases via existing `drawCreature` and cached by `dna+stage` sig (max 64, oldest evicted) with 180ms refresh cadence so animation persists without redrawing every frame. **Shared via Supabase Realtime broadcast** — channel `code-creatures-garden-v1` (broadcast + presence, **no DB writes**). Each client gets an 8-char `CLIENT_ID`, broadcasts a slim roster (`{id, dna, name, xp}`) every 5s + on hatch. Listeners spawn guest agents `garden.guestAgents` keyed by `${clientId}::${creatureId}`. Guest creatures render at 0.78 alpha with a tiny `· guest ·` label above; click shows toast "X is just visiting" instead of opening detail. Stale guest snapshots purged after 18s no-contact. Presence sync drives a `N visitors` HUD pill with live/connecting/offline dot. Hover any creature → DOM tooltip + canvas name+stage label box (cyan for yours, purple for guests). Click your own → opens detail modal (existing flow). HTML head upgraded `<script>` → `<script type="module">` to support ESM `createClient` from jsdelivr. Hatching now jumps to Garden (was Stable). Empty state shows only when 0 creatures total (mine+guests). DPR-aware canvas resize on window resize. Ground bound margin 56px, top margin 120px, bottom margin 80px keep creatures away from edges. depth-sorted by y so closer ones overlap further.
- 2026-04-24: **ASCII pixel-art renderer** — replaced canvas-blob renderer with a character-grid stencil engine. Stage drives grid size (13×13 hatchling / 15×15 adolescent / 17×17 adult). Body shape stamped in cells (oval/blob/hex/diamond/cube) from class dominance, then outline pass marks any body cell with a non-body neighbor. Per-class palettes: vowels → soft `o 0 O ° º` (string-green), consonants → bold `# % & @ §` (keyword-purple), brackets → geometric `+ * × ✦` (bracket-amber), ops → edgy `/ \\ < > ◇` (op-pink), digits → grid `[ ] = # ■` (num-cyan). Each body cell picks a fill char deterministically (`(x*13+y*7+seed) % len`) and draws via `fillText`, outline cells get a 30%-cell shadow glow. **Eyes**: 1–3 (con-driven) sclera cells with `O` char + 50%-cell glow; pupils overlaid as separate `•` chars at 70% font size, tracking time gently `(cos t*0.6, sin t*0.6) * 0.18`. Blink every ~16s when `sin(t*0.4 + seed*0.0003) > 0.94` flips eye to `-`. **Mouths**: 6 modes from `(pun + op*2 + seed) % 6` — happy `‿‿‿`, flat `——`, surprised `o`, wavy `~~`, toothy `v`, smug `uwu`. **Cheeks**: tiny accent dots either side of mouth when vowel+punct > 4. Antennae from brackets extend up from body top (with sin-driven tip sway), legs from digits extend down from body bottom (alternating `"`/`,` chars with phase), tail from operators extends right with `~ - .` pattern. Pattern dots from punct sprinkle accent `·` chars in random body cells. Adult aura: 22 orbiting `· ·  ✦` chars on a 0.55× max-edge radius ring, twinkling via `sin(t*2.2 + i*0.55)`.
- 2026-04-24: Created. Virtual pet game where DNA = your typing. **3 views**: Hatch / Stable / Breed. **Hatch flow**: pick from 4 starter egg styles (cosmic/ember/pixel/glyph), type into the textarea — every character (32 chars total) seeds the egg's DNA. Egg canvas animates: binary `0/1` glyphs orbit the shell at the start, crack lines spread as DNA fills, on full DNA a `crack-burst` keyframe pops the egg open and spawns the creature. **DNA→Stats**: `classify(ch)` maps each char to one of 6 classes — `vowel` (HP+3, charm+1), `consonant` (HP+1, atk+1), `digit` (atk+2, spd+1), `op` (atk+3, def-1), `bracket` (def+3), `punct` (spd+2, charm+1), space ignored. Final stats = base+sum, then × `levelMultiplier` per stage. **Stages**: hatchling (xp 0–29) → adolescent (30–99) → adult (100+). Stat scale 1.0 / 1.4 / 1.9. **Procedural creature renderer**: canvas-drawn, deterministic per-DNA. `dnaSig(dna)` = FNV-1a hash → mulberry32 PRNG seeds all rolls. Body shape from class dominance: vowel-heavy → blob, digit-heavy → hex, bracket-heavy → diamond, op-heavy → diamond/cube, balanced → oval. Body color = HSL pulled from dominant class palette + PRNG hue jitter. **Decorations** (all driven by DNA char-class counts): `bracketCount` → antennae stems on top, `opCount` → tail wiggle (sine-driven anim), `punctCount` → dot speckles, `digitCount` → side stripes (by adolescent+) and leg count, eye count = clamp(letter-count/8, 1, 4) with whites + tracking pupils. Adult stage adds rotating aura ring + extra accent ring. **Naming**: `generateName(dna)` picks 2 syllables from per-class pools (e.g. vowel pool: "ae","io","ua"…) seeded by hash → "Aelyn", "Voxim", etc. **Stable**: scrollable grid of all hatched creatures, each card = mini-canvas + name + stage + xp bar + 5-stat strip + ✕ release button (with confirm). Click card → opens detail/feed view. **Feed**: textarea above creature → every typed char awards XP via `xpForChar(ch)` (vowel/consonant 1, digit 2, op 3, bracket 2, punct 1) + floating CSS-class-colored "+N" sprite floats up from char position. Stage-up triggers re-render with new shape/aura + level-up flash. **Breed**: pick 2 parents from stable → preview card showing crossover DNA + child name + child stats. `crossover(a, b, mutationRate=0.05)`: zip-walks 32-char DNA arrays, picks each slot from random parent, rolls mutation per char (5%) replacing with same-class character (preserves trait shape). "Hatch this child" → sets `state.pendingDna` + switches to Hatch view with egg pre-filled. **Storage**: `localStorage['code-creatures-v1']` = JSON array of `{id, dna, name, xp, born, parents:[a,b]}`. **Aesthetic**: VS Code dark+ palette (`--bg:#0d1117 --kw:#c986e0 --str:#7ec97a --num:#f5b362 --fn:#5fc0e0 --op:#ff7e9a --comment:#6a7480`) on void backdrop with subtle radial accents + 1px grid texture. Title "Code · Creatures" in Major Mono Display 800 with `Creatures` in keyword purple gradient. Subtitle Fraunces italic. Body JetBrains Mono. Stat readouts in Silkscreen for that retro gauge feel. Egg canvas 320×320, creature canvas 360×360, both HiDPI via DPR. Class-tagged characters in the typing input get color-coded as you type (live syntax-highlight effect). Footer hint shows a small DNA preview map (32 colored squares = char classes seen so far). Tab nav top: HATCH / STABLE / BREED with active underline. Mobile @720px: nav becomes pills, creature canvas shrinks, breed picker stacks. Pollinations OG.

## features
- DNA = your typing. Every char shapes hp/atk/def/spd/charm.
- 6 char classes (vowel/consonant/digit/op/bracket/punct) → 6 different stat affinities
- 3 evolution stages with stat multipliers and visual upgrades (legs, aura, accent ring)
- Procedural creature renderer (canvas) — each DNA = distinct visible body
- 4 starter egg styles (cosmic/ember/pixel/glyph) with different binary glyph palettes
- Live syntax-highlight on the typing input — see your DNA classify in real time
- Naming generator (2-syllable, class-pool seeded)
- Genetic crossover breeding with 5% same-class mutation
- localStorage persistence (release pets to free slots)
- VS Code dark+ aesthetic with a typography-forward UI

## issues
- DNA cap is 32 chars. Longer inputs get truncated. Could expose a "lineage size" upgrade later.
- Breeding always picks the SAME parents → child outcomes vary only by mutation roll. Re-rolling preview button could let you reseed without leaving the breed view.
- Adult creatures look quite different across DNA but the aura ring is a bit subtle on dark bg — could pulse it harder.
- localStorage has no slot cap; could fill up if user breeds endlessly. Add a soft max (e.g., 30) with FIFO release prompt.
- Stat multipliers per stage are hardcoded (1.0/1.4/1.9) — not visible in UI. Could show a tooltip.
- Mobile keyboard covers the egg canvas while typing — works but UX is awkward. Could add a sticky mini-egg preview.

## todos
- Sound: typing clicks (per-class pitch), egg crack, hatch chirp, level-up chime, breed swirl
- Stat-driven battles: pet vs pet auto-battler using HP/ATK/DEF/SPD; winner gets XP
- Daily quests: "feed 50 vowels", "breed two punct-heavy pets" → reward stat-boost items
- Trait genes beyond the 6 classes — emoji DNA, hex literals, snake_case syllables
- Shareable creature codes (export DNA + lineage as a string)
- Cross-app: "creature pet" widget that shows your top creature on other Vibespace apps
- Multiplayer breeding via Supabase broadcast — pair eggs across viewers
