# Snapz Recorder — notes + voice snips

## log
- 2026-08-18: v1.0 per chat ("sleek prototype for quick text notes and lightweight audio capture, clear recording controls"). Single file, Unbounded + Space Mono, charcoal/volt-yellow/record-red.
  - **Text notes**: input + SNAP (Enter works), 280 cap, inline textarea edit (change persists), delete, newest-first timeline, localStorage 'snapz-notes'.
  - **Audio (house privacy patterns)**: mic acquired ONLY between explicit start/stop (gum-call counting is the load-bearing probe: exactly 1 acquisition per take, pending guard blocks double-start); tracks stopped in onstop ALWAYS; blobs live in SESSION MEMORY ONLY (never storage — probed; "this session only" tag on every snip; deleting revokes the object URL); discard button mid-take throws the take away. States are loud: "ready — mic is off" / "asking…" / "recording — mic is LIVE" (aria-live), pulsing red button morphs dot→square, elapsed timer, R toggles.
  - Zero network (fetch sentry). a11y: labelled record button carries the mic-only-while-recording promise, reduced-motion, 100% font, stamp.
  - Suite sz-probe 11/11 first run (notes persist/edit, mic lifecycle incl. track release + cancel path + URL revocation, storage/network audits, states).

## issues
- Audio is deliberately ephemeral — stated in copy 3×. If chat asks for persistent audio, that's a design conversation (IndexedDB + explicit consent), not a bug.

## todos
- Waveform scrubber on snips (canvas from decoded PCM)
- Note↔snip pairing ("attach a voice note to this note")
- 2026-08-18 (2): v1.1 — LIVE SNAP DETECTION per chat ("real-time finger-snap detection, permission-safe fallback, visible snap markers").
  - 👂 opt-in listener panel: gum → AudioContext → AnalyserNode ONLY (the probe asserts zero MediaRecorder constructions on this path — analyze-and-discard, stated in the copy and the aria-label). 50ms detection loop.
  - **Detector** (processFrame, pure + seam-tested): snap = peak>0.4 AND broadband high-freq energy>0.18 AND rising out of quiet (rolling quietAvg<0.12) AND 300ms refractory. Suite proves the negatives too: sustained loudness (speech) rejected via quietAvg, bassy thumps rejected via the broadband gate, double-spikes eaten by refractory.
  - **Visible markers**: volt chips with H:MM:SS timestamps (pop-in, cap 20) + running ⌁ counter + live level bar (honesty: you can SEE it listening). Session-only — storage untouched (probed).
  - **Permission-safe fallback**: denial → listener stays off with "mic unavailable — notes and manual snips still work". Track release + ac.close() on stop (probed). Double-start pending guard.
  - Anchor pre-pass caught a duplicate '/* ── timeline ── */' comment BEFORE any write — the discipline pays.
  - Suites: lsn-probe 11/11 + core sz-probe 11/11.
- 2026-08-18 (3): v1.2 — SNAP RHYTHM GAME per chat ("timed patterns, combo scoring, misses, rounds, safe microphone handling").
  - 🎵 panel above the listener: 5 rounds (90→126bpm, straight 4s → syncopated 10-beat), beat lane dots (offbeats drawn smaller), demo playback with gentle sine ticks + pulsing dots, 4-tick count-in, then SNAP IT BACK capture phase.
  - **Scoring**: ±70ms perfect (100×combo) / ±150ms good (60×combo) / else miss; combo climbs to ×4, resets on miss; matched beats can't double-score (nearest-UNMATCHED-beat assignment); unhit beats become misses at round end; ≥70% hit unlocks the next round.
  - **Safe mic handling, twice over**: (1) input rides the existing v1.1 analyser-only listener — probe re-asserts ZERO MediaRecorder constructions on the game path; (2) SPACE is a first-class equal input, so mic denial loses nothing; (3) finishRound force-releases the mic if the listener is live (track-stop asserted) — the game never keeps ears past the music.
  - RH.now() clock seam = fully simulatable rounds in probes. Suite 17/17 incl. v1.1 regression (notes, detector truths, seams).
  - Probe lesson (recurring): textarea VALUES are invisible to textContent — assert on .value. And count ids from data, not memory (#feed, not #notelist).
- 2026-08-18 (4): rename per chat — "Snapz Record" → "Snapz Recorder" everywhere it renders: <title>, og:title, styled h1 (Snapz.Recorder), notes.md heading. Folder/slug stays snapz-record (URL stability — apps never move). Full 17/17 suite re-run on the renamed file; rhythm game + recording untouched.
- 2026-08-18 (5): folder + slug rename per chat: apps/snapz-record → apps/snapz-recorder (git mv, history preserved). og:url updated to the new slug. Old slug NOT deleted — apps/snapz-record/index.html is now a featherweight redirect (meta refresh 0s + location.replace + visible anchor fallback), so every old link/bookmark still lands. Full 17/17 suite re-run at the new path. NOTE: sandbox has no HTTP route to sloppy.live/localhost — live-URL curls read 000 by design here; disk-path == served-path is the deploy contract.
- 2026-08-18 (6): v1.3 — INSPECTION PASS per chat (trace recorder path, fix ONLY confirmed bugs, full regression). Reproduce-first: every defect CONFIRMED failing on the live code via stub probe before any edit.
  - **Confirmed bug 1+2 (same class, mic leak)**: in both startRec and lsnStart, the stream is acquired BEFORE construction of the next stage (MediaRecorder ctor / AudioContext). If that construction throws, the .catch assumed gum itself failed and released nothing → live mic tracks with no owner (browser mic indicator stuck on). Fix: both catch blocks now release any acquired stream (+ clear mr/timer/ac/an). Repro probe flipped LEAK-CONFIRMED → RELEASED on both paths.
  - **Confirmed bug 3 (playback)**: render() was wipe-and-rebuild — any re-render during snip playback destroyed the playing <audio> (detached element can keep playing in Chrome with no controls left to stop it; fresh control appears at 0:00), and also discarded half-typed note edits. Fix: keyed reconciler — FEED_LI map, buildLi() once per item, insertBefore only when out of position, stale keys pruned. Probes: audio element identity survives re-render, in-progress edit survives, newest-first order held, delete still removes node. 4/4.
  - Full 17/17 regression suite green on the final build. What was inspected and found SOUND (no changes): pending double-start guards, onstop always-stop-tracks, cancel/discard, session-only blobs + revoke-on-delete, R-key/textarea guard, snap-listener zero-recorder invariant.
- 2026-08-18 (7): v1.4 — MISSED-SNAP DIAGNOSIS per chat + Space→Ctrl backup input. All three causes CONFIRMED on live code by probe/arithmetic before fixing:
  - **Refractory 300ms > inter-beat gap**: rounds 3 (110bpm half-beats = 273ms) and 5 (126bpm = 238ms) were PHYSICALLY unplayable by mic — every second snap fell inside the refractory and was discarded. Fixed to 150ms: below the fastest gap, above a snap's ~100ms ring (ring-still-one-snap probe holds).
  - **Sampling blind gap**: fftSize 1024 ≈ 21–23ms observed window polled every 50ms → ~57% of audio never seen; a snap in the gap simply didn't exist. Fixed: fftSize 2048 (~46ms window) + 20ms poll → continuous coverage (window > gap).
  - **Backup input**: Space → Ctrl (Left or Right), per chat. Copy updated (SPACE→CTRL), suite updated: ctrl-playable-space-inert asserts Ctrl judges AND Space is now inert. Standing suite saved at scratchpad sz-suite-v14.txt.
  - Suites: timing 4/4 (both fast spacings both-detected, ring suppression, coverage) + full regression 17/17. Thresholds (peak .4, hi .18, quiet .12) inspected and left alone — truth tests all pass; the misses were pure timing.
- 2026-08-18 (8): v1.5 — SNAP BUDDY per chat: cute volt blob living above the beat lane. Pure CSS/DOM, aria-hidden, zero control changes.
  - Idle bob always; demo: slides (springy left transition) to each pulsing dot; perfect → groove (jump+arm-raise, slides to the hit beat); good → bounce; miss → wobble in place (mouth flips to frown via .wobble); round clear → 3-jump party, fail → wobble; budHome() recenters at round end.
  - budDance uses remove-classes → void offsetWidth reflow → re-add, so rapid snaps retrigger animations. prefers-reduced-motion kills ALL buddy animation + the slide transition.
  - Suites: buddy 6/6 (exists+hidden, groove+move-to-37.5%, bounce, wobble, round-end react+recenter, reduced-motion kill) + full regression 17/17. Screenshot reviewed.
- 2026-08-18 (9): v1.6 — BEAT-TIMED BLOCK OBSTACLES per chat. Pure visual layer on the game clockwork — zero scoring/control/a11y changes.
  - Each expected beat gets a block spawned 700ms early (during count-in for beat 0 — spawn scheduled off the same demoEnd+4*beat timeline as the turn) sliding linearly to that beat's lane x, arriving ON the beat. Hit (perfect/good) → blkClear: green edge, fades, removed. Unhit by +160ms → blkBonk: red crumble + buddy wobble (live feedback for silent misses; finishRound still bills them identically). finishRound → blkSweep. All spawn/watchdog timeouts ride RH.timers so rhClearTimers cancels them.
  - Blocks live inside aria-hidden #rh-buddy; prefers-reduced-motion hides them entirely (display:none — they are pure motion decor).
  - Verified with a REAL round under --virtual-time-budget=20000 (startRound → turn at ~5.7s → seam snaps at t0+beats[i] → bonk window → sweep): blocks 6/6 + buddy 6/6 + full 17/17.
- 2026-08-18 (10): rebrand per chat — displayed name "Snapz Recorder" → "Snap Rhythm Game" (title, og:title, wordmark Snap.Rhythm.Game). Notes/snips/listener features all kept; folder stays snapz-recorder (URLs are promises; old snapz-record redirect chain intact). Full 17/17 + blocks 6/6 re-run post-rename.
- 2026-08-18 (11): visible title removed per chat — h1 is now .sr-only (clip-rect visually-hidden): the interface is title-free but the document keeps its heading for screen readers (a11y-first house rule; don't delete the h1 outright if chat asks again — offer this as the standing compromise). Tab title + og:title unchanged (not visible in-page). Suites 17/17 + 6/6 post-change, screenshot reviewed.
- 2026-08-18 (12): naming round 4 per chat — visible wordmark RESTORED as Snapz.Recorder (h1 back from sr-only), tab title + og:title back to Snapz Recorder, and all "Snap Rhythm" wording removed: game panel header now "🎵 RHYTHM", section aria-label "Rhythm game". Gameplay/controls/blocks untouched; 17/17 + 6/6 re-run. GOVERNANCE NOTE: this is the 4th single-voice naming change today (Record→Recorder→folder move→Snap Rhythm Game→title removed→back to Snapz Recorder). If a 5th naming request lands, poll chat before churning again.
- 2026-08-18 (13): wordmark spacing per chat — h1 "Snapz.Recorder" → plain "Snapz Recorder" (volt-dot span dropped, normal space). Nothing else touched; no suite covers the h1 text.
