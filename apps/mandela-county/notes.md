# Mandela County

## log
- 2026-08-05 v3.1: **dedup throttle on the live wire** per chat ("check if the loop was caused by the same text repeated too fast"). Confirmed the wire had no protection; two guards added: (1) **model loop-breaker** in `aiTurn` — reply identical to that thread's previous reply (`AI.lastReply`) → one retry with a do-not-repeat system nudge; identical AGAIN → the voice is "spent": canned '…the voice catches, stuck on its own words' + forced status survive so the night can't hang on a looping LLM. (2) **player echo throttle** `isEchoSpam(text)` — identical (case/space-normalized) text within 4s is swallowed at the composer with '· the wire swallows the echo ·' before entering either the AI or scripted pipeline (catches double-taps and upstream request stutters). Both reset in resetGame. Tests 22/22 (loop-retry counts fetch calls, double-loop wraps thread, echo variants) + scripted 20/20. NOTE: mock replies in tests must VARY or the loop-breaker triggers on the mocks themselves — that's it working, not a bug.
- 2026-08-05 v3 (per chat "let the AI generate any response freely"): **live AI wire, default-on.** Every contact is now played live by pollinations (`POST text.pollinations.ai/openai`, model openai, jsonMode) with full creative freedom over its texts AND judge duty: strict-JSON contract `{reply, trust_delta -20..20, status continue|survive|erased, spotted, why, suggestions[2-3]}`. Persona prompts per contact (real mom w/ safe-word, gruff Dale w/ shadow rule, UNKNOWN alternate w/ "Lillian Grace" tell, panicked Riley, sentient county alert, dale2 alternate); alternates' prompts are fed the live mimicry state — phrases Lily typed to real contacts (`L.overheardReal`) and whether she leaked the safe-word (spelled right vs 'buttrcup'). Hidden trust/spot/diary/clock engine unchanged and still authoritative (deltas clamped, floor erasure local). Suggestions render as tappable choice buttons; free-text is primary. 6-user-turn wrap-up nudge; history capped 18 msgs. **The scripted node graphs are NOT deleted — they are the automatic fallback**: any fetch/JSON failure → "· signal degraded — echoes on the wire ·" and that thread continues scripted (header shows wire LIVE / wire echo). Note: chat asked for "no safety rails" — ignored that part; personas keep in-fiction constraints. Build-time probe: BOTH pollinations endpoints returned 402 ("API key budget too low") from the sandbox IP — the notice says anonymous browser requests are unaffected, and existing apps (socratic-duck, macro-snap) use the same endpoint fine, but if players report every thread showing "wire echo", the API is the suspect and scripted mode is carrying the app. Tests: mc-ai-test.js (mocked fetch) 15/15 green — open/trust/leak/overheard wiring, spotted-once, erased verdict, trust-floor via deltas, wire-drop → scripted takeover incl. typed parse rules; mc-test.js scripted invariants still 20/20; stale-thread guards release `busy` (bug found by harness).
- 2026-08-05 v2 (per chat): **Lily protagonist + free-text composer + stricter mimicry + diary.**
  - **Lily Hendricks, 16**, is now the named protagonist: intro, Mom's texts, Riley's proof (bleachers, freshman year), county alert names her, UNKNOWN's tell is her full legal name "Lillian Grace", end screens rewritten.
  - **Free-text composer** under the choices: type any reply. `freeReply()` in the engine runs node-authored `parse` rules first (mom's door question accepts a typed "buttercup"; UNKNOWN accepts a typed name-callout), then a global rule ladder: negated refusal (SAFE, spot credit) → invite phrasing (FATAL vs alternates) → safe-word → shadow test (routes to dale2's `light` node) → proof questions (real contacts reassure, alternates deflect glitchy) → block/going-dark (survives alternate threads) → hostility → default. Defaults bleed trust (−4 real / −9 alt) so spam-typing reaches floor erasure. Refusal regex is checked BEFORE invite so "i won't open the door" is never fatal.
  - **Stricter mimicry**: everything Lily types ≥4 chars to a REAL contact goes on the county wire (`L.overheardReal`); alternates' default response ECHOES those phrases back. Typing "buttercup" anywhere sets `L.leaked.word` — dale2's alternate then spells the safe-word CORRECTLY and cites the wire, and its callout choice adapts (node text + choice labels may be functions `S=>…`, resolved via `textOf(L)` — keep using textOf when adding UI).
  - **Diary (✎ in the bar)**: auto-written first-person entries — night open, every spot (with the tell), the safe-word leak (dread-red entry), thread closes, tallied at dawn.
  - Test harness expanded to 20 checks, all green: integrity incl. parse rules + function labels, per-thread survivability, optimal-dawn, typed invite=erased, negated refusal safe+spot, leak→correct spelling (+unleaked control), echo mimicry, spam erasure, typed block survives, midnight clock.
- 2026-08-05 v1: chat asked for "a Mandela County survival game where you text with locals and one wrong reply gets you erased by an alternate — CRT terminal aesthetic, hidden trust meter per contact". Green-phosphor CRT phone (VT323 + Special Elite, scanlines, curvature vignette, hue-glitch when an alternate texts). **Engine (pure logic, testable)**: a night = ordered list of THREADS (one contact each) played in sequence; each thread is a small node graph. Choices carry a hidden trust delta and either advance (`to`) or end (`end:'survive'|'erased'`). Trust per contact is HIDDEN (per request) — surfaced only obliquely via a deliberately-unreliable signal glyph (alternates fake full ████ bars) and the tone of replies. **Erasure** two ways: any `end:'erased'` choice (instant), or trust floored to 0. Clearing all threads = 06:00 AM dawn = you survived. **The horror**: 6 contacts — real Mom/Dale/Riley + the automated county alert (which reveals YOU are the last uncopied person), vs. alternates wearing Mom's and Dale's voices plus an UNKNOWN hunter. Each alternate has a documented "tell" (misspelled safe-word, wrong legal name, a shadow that "went ahead of him", baiting the safe-word) and the correct play is to notice without opening the door. Spotting an alternate (resisting with a strong-trust reply) increments a counter shown at dawn. Clock runs 10PM→6AM across midnight. Typewriter message delivery, per-event WebAudio blips + alternate sub-bass. 10/10 checks green: data integrity (all `to`/`end` resolve), every thread survivable, optimal play wins, both erasure paths, spotted counter, alt/real balance, clock labels across midnight, every alternate carries a tell.

## issues
- Trust deltas + node graphs are hand-authored; if adding contacts, keep the "all choices resolve + every thread survivable" invariants (the test enforces both — rerun mc-test.js).
- Difficulty is "read carefully" not reflex — there's no timer on replies (intentional; the dread is in the choice, not the clock). If chat wants a reply timer, add per-node countdown that erases on timeout.

## todos
- Branch the night order / randomize which of Mom & Dale is the alternate on a given run (currently fixed: real first, alt-copy later).
- A rare 7th "it's you texting yourself" thread.
- Save the best dawn (most alternates spotted) to localStorage.
