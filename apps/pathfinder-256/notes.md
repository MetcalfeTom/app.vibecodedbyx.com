# Pathfinder 256 — A* multi-agent sandbox

## log
- 2026-08-19: v1.2 — stress-test mode, per chat. New panel: seed input + "run stress test" → fresh seeded caves, 20 PLAYERS each A*-walking to seeded randomized destinations (new goal drawn on arrival, up to 5 reachability tries), 100 MONSTERS with auto patrol routes. REPEATABLE: probe proves seed 777 twice = every player and monster on identical cells after 30 ticks (and seed 778 differs); tags don't pause in stress mode, they just count. PERFORMANCE METRICS in the readout: tick ms (rolling 60-tick avg/max via performance.now), A* calls + expansions per tick, player count — probe asserts avg tick < 250ms at 120 agents. Exit restores the pre-stress sandbox byte-identically from an exportData snapshot. CORE REFACTOR: sim.player → sim.players[] (sim.player kept as players[0] alias so nothing external broke); monsters aggro/chase the NEAREST player (LOS + radius per nearest), disengage when the nearest is beyond 2R; catches use a player-cell set; idle monsters avoid all player cells; astarReal gained an expansion-cap param (genRoute uses 6000) + per-tick metrics counters. Browser-only stated in the panel copy — nothing leaves the page. Suite now 34/34 (pf3-probe): all 28 prior checks untouched + stress-spawns (all 120 agents on open cells), runs-with-metrics, repeatable-seed, seeds-differ, perf-sane, exit-restores. Atomic deploy.
- 2026-08-19: v1.1 — unaggroed patrol behavior, per chat. Three idle modes (radio group, default patrol): FIXED WAYPOINT ROUTES — each monster gets a deterministic route seeded from its spawn coords (mulberry(x*7919+y*104729+12345)), 2-3 extra waypoints within ±18, wall-rejected and A*-reachability-checked at generation; patrol walks the loop at HALF speed ((tick+i)%2) with budgeted replans (8/tick), waypoints drawn as plum dots. RANDOM WANDER — 60%-per-tick 4-dir stroll using sim.rng (mulberry(9001) reseeded every resetRun, so whole-sim runs are DETERMINISTIC — probe proves two identical 30-tick wander runs end identically). STAND STILL — the old behavior. Aggro/disengage preserved end-to-end and re-probed: sight-aggro drops the patrol path, disengage drops both paths and the monster resumes its route from wherever it ended up. Idle monsters politely refuse to step onto the player's cell (only chasers tag). Routes ride the JSON map format (export carries them, import honors hand-edits, rejects wrong-count/wall-sitting waypoints by name); numeric grid + randomMap regenerate them. Suite extended to 28/28 (pf2-probe): all 21 v1.0 checks + routes-fixed-and-clean, patrol-walks-the-route (bounded, off-wall, unaggroed), patrol-resumes-after-disengage, wander determinism, still-mode, routes-in-map-data, idle radio buttons. Atomic deploy via scratch copy.
- 2026-08-19: v1.0 — built per two chat requests landing together. 256×256 grid (65,536 cells) on a 512px pixelated canvas. REAL A*: binary heap of (f<<16|idx)-encoded ints with lazy deletion, generation-stamped typed arrays (no per-call clearing), 4-dir manhattan, 30k expansion cap, honest null on no-route; probe-verified straight lines (len = manhattan+1), forced detours through a one-gap wall, sealed = null. AGGRO is coordinated + obstacle-aware per the follow-up: initial aggro needs Bresenham LINE OF SIGHT (walls block), a chasing monster then alerts idle neighbors within the radius (pack join, no LOS needed — they heard the call), disengage beyond 2R. Player A*-walks to the gold goal; monsters chase with per-tick repath budget 16 (round-robin), monster-monster collision (wait if next cell occupied), tag = kind pause. EDITABLE MAP DATA, two formats, import auto-detects: compact JSON (RLE walls + spawns) and the requested NUMERIC GRID — 256 rows × 256 digits, 0 empty · 1 wall · 2 player · 3 goal · 4 monster; both roundtrip probe-verified byte-identical, rejections are specific and kind ("row 2 has 2 cells — every row needs 256 digits"). TRAILS: Uint8 layers stamped 255 on every agent move, −5/tick decay, cyan/red translucent render, toggleable. Controls: step (N), play/pause (Space, aria-pressed), reset (R), speed 1–30 t/s, aggro radius 4–64, brush 1–8, seeded cellular-cave generator (2 smoothing passes, spawn carve-out, guaranteed route via L-corridor fallback), clear. Canvas keyboard editing: tabindex + role=application, arrows move cursor (shift=8), Enter paints. Probe 21/21 (one probe-side fix: grid import collects monsters row-major, so probe now finds monsters by coordinates, not assumed order).

## issues
- monster identity from grid import is row-major scan order — anything asserting on monster order must match by coordinates.

## todos
- 8-direction toggle, weighted terrain (cost 2 mud digit), flow-field mode comparison, if chat asks.
