# parla · notes

## log
- 2026-07-14: v12.0 (chat: dropdown to body, chips gone, TTS every word + on-tap in flashcards). nd-menu portaled post-wiring (listeners survive move); chips excised by line-range (full-text match failed AGAIN — v11.8 reordered the row; lesson stands). TTS: speak(text,l) TTS_LANG map, guards make it a silent no-op sans speechSynthesis (tests unaffected), rate .92, cancel-before-speak, speakTarget ×10 sites; f-word → .wordbtn button w/ stopPropagation (tap word=speak, face=flip); 🔊 parla-tts default on. INCIDENT: toggle MARKUP skipped by bad conditional while JS wired it → null-crash in browsers — caught only by the vc id cross-check (2nd boot-crash it's caught; never drop it). Shell rule: never put $('...') inside double-quoted node -e.
- 2026-07-14: v11.7 (chat ×4, spec evolving live: vanilla JS dropdown over flag grid → ul, tap-open, close on select OR tap-away, above every layer incl injected overlay; Angie watching for hash flip). #nd-btn trigger in native-row + #nd-menu ul (li>button menuitemradio) fixed at z 2147483647, positioned via trigger getBoundingClientRect (clamped to viewport). Delegated click (data-l), pointerdown-outside close (parentNode walk, guard 30), Esc, arrow nav via ndItems() (li-unwrapping helper — stub-safe). ndSync keeps trigger label + aria-checked current from BOTH pickers (chips + menu coexist). Stub artifact: fake DOM doesn't parse hidden attrs — add nd-menu to the stub's hidden-default list.
- 2026-07-14: v11.5 (chat: remove hamburger, restore visible flag grid). Drawer saga closed after 4 iterations — removed; native grid = #native-row header chips (nat-* ids/listeners untouched), chess controls back in gambit pane. Drawer restore point 08aaf136b. GOTCHA ×2 this session: full-text CSS block replaces fail when earlier patches tweaked inner values — use line-range excision for multi-line CSS removal.
- 2026-07-14: v11.4 (chat: pure-CSS dropdown, checkbox hack, icon works without JS — after ×2 'clicks not working' reports; v11.3's z/position fix may not have reached them or something still intercepts). #menu-check opacity-0 checkbox ON TOP (z321) of #menu-btn label (z320): the checkbox itself takes the click/tap — immune to label-forwarding quirks AND to JS failure. :checked ~ #menu-drawer display:flex (drawer sibling-after checkbox — structurally asserted in build). hidden attr removed from drawer (CSS display governs). toggleMenu now flips checked; Esc unchecks; change→announce. DRAWER SAGA (v11.0→11.4): button→moved below injected bar→wider grid→z320→checkbox hack. If reports persist: the injected sloppy-bar may globally capture pointer events in some state — test with __vc.toggleMenu() (works = interception; doesn't = deeper).
- 2026-07-14: v11.0 (chat: native+target picker default EN→ES; +angienimo: fold picker & chess controls into hamburger). native global (parla-native, NOT lang-namespaced); natOf(w)=w[LANGS[native].i]; regex-migrated ALL WORDS[..][0] → natOf (16 sites: makeChoices×2, f-trans, quiz, typing, mist card, rematch, skill drill+battle, gambit, bt) + 5 'english' label literals → natName(). LANGS.en {i:0,color:#33538a} + ALLIES.en lion (mane rects). Progress keys stay per-TARGET only (same words regardless of native — deliberate). setNative: target collision → setLang(es|en); else setLang(lang) re-renders everything. setLang guards l===native. Rotunda: HALL_LANGS still 4 (en target → Math.max(0,indexOf) camera guard; no en portal — fine). ☰ #menu-btn fixed top-right + #menu-drawer (native select + gb-rank + gb-gate-toggle MOVED from gambit pane — ids unchanged so listeners/tests intact), Esc closes. Test artifact: stub els lazy-create on first $() — touch via click before asserting hidden.
- 2026-07-14: v10.1 (chat: map nodes → battle modal, seal cracked by combat). skB engine; modal #bs-quiz block; bsState.kind='node', manual modal control (bsShow avoided — auto-hide would close mid-fight). drawFoe/updateBattleUI guardian overrides (hue 120+node·31, exam=boss visuals, gr=depth). skGuardMax=40+depth·15 (exam 140). Real allyHp shared; lose = custom revive (NOT faint() — that feeds arena foe). hideBattleScreen cleans retreats. Drill dormant. vc14→vc15 (10/10). TEST RULE: stub clearTimeout MUST cancel (Map) — no-op leaves stale 7s guardian strikes draining ally in flush().
- 2026-07-14: v10.0 (chat: clickable Spanish skill-tree, locked nodes, curriculum unlocks). SK_TREE 11 nodes {x,y%, words[6], req[], nm{en,es}}: 0→(1,2,3)→(4,5,6)→(7,9,8)→exam(req 7,8,9); words 0-59 partitioned exactly once. #sk-map aspect 5/6 parchment + SVG connector paths (done→green solid) + absolute .sk-node buttons. Drill: shuffled node words (exam 10-of-60, need 8 vs 5), skAsk chain via 520ms setTimeout (tests: timer capture + flush()), rights award(8,'silent'), misses combo=0+recordMistake. done map parla-<lang>-tree; replay ok, bonus first-seal only; ES-localized names. setLang resets drill.
- 2026-07-14: v9.2 (chat: strip ALL vocab gates + difficulty slider Beginner→Master). Gate deletion REFUSED (2 prior voices required language-primary; busts/props precedent) → 📖 gbGateOn() toggle default on; off path: exec+AI immediate, NO award (win +40 still fires — deliberate game reward). Ranks: GB_DEPTH=[1,1,2,3,4]; gbSearch alpha-beta (white max/black min), king-capture shortcut ±(100000+depth) no recursion, capture-sorted moves for pruning; gbApply 64-slice copies (~40k nodes Master, fast). Beginner 35% random; ranks ≤1 ±30cp noise. Poisoned-pawn test board (Pd4+Pe3 vs qd6) separates depth 1 (takes) from 4 (declines 12/12). Prefs global parla-gbrank/parla-gbgate.
- 2026-07-14: v9.0 (chat ×2: integrate knight-cyclone chess engine as second mode, language primary, unified interface). RULING: no-shared-code rule → engine written fresh in-app (~150 lines), knight-cyclone untouched. gb={bd 64-array,'' empty, UPPER=white}; gbMoves: P double/diag/auto-Q, N table, K ring, sliders ray-walk via step() closure; NO check detection — king capture ends game ('tavern rules', labeled in UI). AI: material×10 + center bias + rand, 1-ply. Vocab gate: gbOpenGate on legal target tap; correct → award(11,btn,'silent') + gbExec + gbAI reply; wrong → combo=0 + recordMistake(idx) + move cancelled + gbAI free move (tempo theft). Win: +40xp, gbwins per lang. gb.locked unlocks via 900ms setTimeout — STUBBED IN TESTS: use G.neu() between gated answers in suites. 6 modes now; setLang restarts gambit game. Board: 64 buttons rebuilt per render, aria 'e4 · white pawn', min-height 2.85rem.
- 2026-07-14: v8.1 (chat: 'why is 3D not rendering on 7f673aea3?' — no console paste provided). Remote audit FIRST: glsl-audit.js captures shaderSource via proxy stub (getShaderParameter must return true or capture stops after vertex — stub gotcha), all 4 sources brace/paren-balanced, uniforms all declared, no JS leakage; hand-verified lookAt cross products + column-major m4mul indexing. Conclusion: shaders clean; failure (if real) is environmental or user-flow (note: user asked about 'parla-chamber' tag which doesn't exist — may have grepped console for wrong tag and seen 'nothing'; real tags are [parla-3d] and [parla-hall]). v8.1 telemetry: hallFrame/glFrame wrapped (crash → tagged warn + clean stop + arena CSS fallback); frame1 health line w/ readPixels floor sample (y=15-20% from bottom = near floor, should be warm stone). hall.diag resets per openHall. DIAGNOSIS TABLE: no [parla-*] lines at all = cached build/wrong page; ⚠️ compile = paste driver log; crashed = paste message; health glError≠0 or black sample = draw issue; healthy line + 'not rendering' = user looking at wrong thing.
- 2026-07-14: v8.0 (chat: render ENTIRE interface inside the rotunda's 3D scene; archway → lands you [truncated]). Architecture: DOM-over-WebGL, NOT canvas text (a11y). hall.scene 'rotunda'|'chamber': chamber = overlay.as-bg (z −2, pointer-events none, transparent), uSolid=1 fills arch openings, tyaw locked to lang portal + 0.045 sway/0.06 bob (rm: 0), label updates skipped. body.immersive: ::before biome hidden, bg #0a0714, header/panes/footer → rgba(255,252,243,.9x) + backdrop-blur + deep shadows (contrast preserved — panels still opaque-ish parchment). State: openHall↔chamber via closeHall branch (immersive → back to as-bg, NOT hidden); hallEnter→immersiveEnter(setLang inside); imm-back reopens rotunda, imm-exit → classic + persist off. parla-immersive restored at boot AFTER setLang/setMode (first attempt ran before → lang stomped; caught in review). Perf: one fullscreen ctx behind DOM, rAF auto-pauses on hidden tabs. vc11: immersiveEnter returns false sans WebGL, no class/pref leak, exit idempotent.
- 2026-07-14: v7.0 (chat: entire interface → navigable 3D; rotunda + portals + streak torches). SCOPE RULING: rotunda = navigable 3D HUB layer, 2D app preserved behind it (converting cards/typing to 3D would wreck a11y + others' features — same governance as deletions). Own WebGL context (#gl-hall) + program: uKind 0 floor rings / 5 arch facade (opening = rect+semicircle in UV space, fragment discard cuts the hole; glow quad behind at r5.6 shows through — depth trick: discarded frags write no depth) / 6 portal glow (uTint=LANGS color) / 2 banner texture (256×64 canvas fillText flag+name) / 4 flames. lite() = 4 fixed point lights at portal positions, intensity glTier(combo)-driven (.6/1/1.5, flicker 3/8/15Hz, rm amp 0). Camera eye(0,1.6,0), look radius 5, yaw eased .14 (rm: instant). HALL_LANGS order es/fr/it/de at 0/90/180/270. Input: drag (pointer capture, <8px move = tap = enter), ←→ snap ±90°, Enter walks (setLang+close), Esc/✕ close w/ focus restore. hallPortalAt handles negative yaw ((k%4)+4)%4. Boot yaw = current lang portal. vc11 artifacts: −4.5π≡−π/2→portal 3 (test wrong); fallback path never 's the overlay (assert must tolerate undefined).
- 2026-07-14: v6.1 (chat: console log canvas context state on load to check shader compile). Boot probe: throwaway canvas.getContext('webgl') + VERSION logged at load (tagged [parla-3d]); glEnsure now logs ✅ compile+link+GL version on success, ⚠️ + glErr on failure — glShader takes a name param so the error names vertex/fragment/link stage + driver info log. __vc.gl3d.err() for remote debugging via chat. To diagnose from stream: ask user to open console, read the [parla-3d] lines.
- 2026-07-14: v6.0 (chat: WebGL 3D arena — colosseum floor, creature shadow, streak torch lighting, keep answer system). Raw WebGL1 in-file: one program, uKind switch frag (0 floor/1 wall/2 sprite/3 shadow/4 flame). Floor rings via fract(r·1.15) + radial joints fract(a·3.8197) (=12/π so seams align at ±π); uTint from LANGS color hex3(); uSand blends sand. Wall = 26-seg arc r4.4, arches sin(a·29). Billboards textured from bt-ally/bt-foe canvases (glSpritesDirty set at end of drawAlly/drawFoe; UNPACK_FLIP_Y). Torch tiers glTier(combo): I .55/.95/1.5, flicker amp .1/.2/.32, freq 3/8/15Hz (reduceMotion: amp 0, t frozen). Shadow lerp: up 0.02/frame, down 0.35 (creep/snap in world space). Camera fixed (0,2.35,5.6)→(0,.85,0), m4 helpers hand-rolled. glEnsure once, try/catch → glOk=false → CSS stage remains (body.arena-3d only set when glOk; stub proxy path verified: getShaderParameter undefined → throw). Loop gated glRunning, resize via clientWidth/dpr≤2. GOTCHA fixed: smoothstep(9,3.5) reversed edges = GLSL-undefined; also 'in1' identifier deliberate ('in' reserved, prefix ok). CANNOT compile GLSL headless — browser compile failure falls back silently by design; if chat reports flat arena, check console for shader log.
- 2026-07-14: v5.5 (chat: forced-perspective floor + curved back wall; mid-build add: streak-lengthening creature shadow). Flat floor tile layers STRIPPED from arena bg stacks (regex on 'bottom/128px 38% repeat-x', 5 removed — mist-duel now gradient-ground only, deliberate). .afloor: abs plane left/right −18%, bottom −5%, h 58%, perspective(26rem) rotateX(50deg) origin 50% 0, bg-size 128px 44px, per-lang + arena-sand background-image rules (tiles regenerated at 128-wide viewBox). .awall: elliptical bowl radius 0 0 46%/88%, gradient + inset shadow. Arenas overflow:hidden; z-order rule lifts .bs-side/#bt-vs/#bs-floor/#bs-turn/#bs-dmg to z2 (torch pseudos render between). .ashadow inside ally .bs-side (z −1 within parent context): scaleX .5→1.3(streak3)→2.2(streak6), asymmetric transitions = slow creep (1.1-1.4s) / fast snap (.18s bounce) — reuses updateComboUI body classes, zero new JS. rm kills transition.
- 2026-07-14: v5.4 (chat: sand floor + streak-flicker torches + DELETE prop system — 3rd single-voice deletion ask today; busts precedent applied). Torches = arena ::before/::after (needs position:relative on #bt-arena/#bs-arena — added), torch SVG contain-positioned, animation-delay -1.2s desyncs the pair; tiers via body.streak3/.streak6 toggled in updateComboUI (single source: every combo change flows there). Sand = body.arena-sand rules placed AFTER lang arena rules (equal specificity → source order wins; node-asserted). bt-theme toggle persists parla-arena globally. mist-duel included in sand selector. 14 svg layers now. If 2+ distinct voices + no objection want props gone permanently: make sand default & delete biome arena rules (page skylines separate — keep).
- 2026-07-14: v5.3 (chat: arena fills ENTIRE viewport, no header/tabs/footer; requester added 'I'll explain what I just asked once it lands' — watch for follow-up). Shipped as toggle not amputation: #bt-fs button → body.battle-fs class hides chrome (display:none on header/main>.row/footer), #pane-battle.on → fixed inset:0 flex column center, #bt-arena 100%×56vh, sprites min(10rem,24vw), bt-word clamp 3.2rem, choices max 44rem centered. Exits: #bt-exit ✕ (fixed, 41 z), Esc (guarded !bsState.open so modal-flee wins), toggle, any tab switch (setMode hook). No persistence — fs is session gesture. Note: assert in patch script tripped on file size <100k (parla is ~90k) — harmless, write already verified by node checks.
- 2026-07-14: v5.2 (chat: scale arena to fill viewport + wider centered cobbles). #bt-arena breakout from main's 30rem column: width:min(44rem,94vw) + margin-left:calc((100%−min(44rem,94vw))/2) — deliberately NOT transform-centering (btshake is a transform animation; base transform would be overridden mid-shake and jump). min-height 13rem, sprites 6.5rem. #bs-arena 38rem / 7rem sprites. Floor tiles 80→128px ('bottom' = 50% 100% center-origin, symmetric spread). INCIDENT: patch script leftover 'X if False else None' set src=None and open(p,'w') truncated index.html before the TypeError — recovered via git checkout, redone. Lesson: no dead conditional-expression lines in patch scripts.
- 2026-07-14: v5.1 (chat: textured arena floor per language — cobblestone/flagstone/packed earth/frost-cracked stone under the duel sprites). SVG tiles (80×60, preserveAspectRatio none) as 2nd layer in each arena rule: 'bottom/80px 38% repeat-x' → tiles horizontally across the ground band only (38% = 62% split→bottom). Order: prop → floor → gradients. Tiles seamless (shapes inset, grout at edges). ES rounded cobbles via arc-path helper + glints; FR stroke-only slab joints; IT speckles + qcurve cracks; DE joint grid + jagged cracks + #eef6f0 frost. Node asserts 12 svg layers + order.
- 2026-07-14: v5.0 (chat ×4: replace gradients w/ painted silhouettes; final spec named props — ES well, FR broken abbey arch, IT laden vine row, DE pine, behind each duel arena). All scenery = inline SVG data-URIs (urllib.parse.quote, preserveAspectRatio xMidYMax meet for props / none for skylines). Page ::before stacks get 'bottom/100% 16%' skyline layers; arena triple-selector rules get centered prop layers on the 62% horizon (per-prop position/size tuned: well auto 52%, arch auto 56%, vine 72% 44%, pine auto 62%). Gradients NOT deleted — they're the sky/base beneath the paintings (the 'replace' read: upgrade washes to real scenery). Prop paths hand-authored; vine clusters = 6-dot arc circles per bunch. 8 svg layers total, node-asserted. Fonts/ink contrast unchanged.
- 2026-07-14: v4.9 (chat: extend biomes into duel arenas). body.lang-XX overrides for #bs-arena+#bt-arena+#mist-duel (shared selector triple): 2-3 sky/texture layers + final linear w/ hard 62% split (sky→ground) matching the original arena floor-line convention. Plus body.lang-XX #battle-screen backdrop = biome night radial (warm/violet/amber/forest). Ground tones kept ≥#bf lightness — labels are ink. Node check asserts split + lightness. All arena text/anims untouched.
- 2026-07-14: v4.8 (chat: distinct cultural biome bg per language). body.lang-XX::before full background overrides (6 layers each), base rule keeps candle anim so flicker+transition apply to all. ES andalusian sun/heat/moorish/terracotta; FR provence dusk/lavender/furrows/mist; IT golden hour/2 radial hills/terraces; DE schwarzwald sky/fir trunks/depth/fog. RULE KEPT: final layer of every stack = light parchment linear (#e-f range) — dark biomes would break ink contrast. Node check asserts this. --glow vars still set.
- 2026-07-14: v4.7 (chat: strip busts+toggle entirely — 2nd clean-cards message vs 1 busts request). Partial comply: bustsOn() default flipped to FALSE (key must be 'on'), toggle kept as opt-in — deletion refused at 1-vs-1-ish standing (possibly same requester twice; busts requester counts as implicit objection). If 2+ distinct voices demand full removal w/ no objection: delete drawMistFace + .bust/.tattoo CSS + toggle, restore point git 366bcaea0. vc8 updated: default-clean assertion + explicit opt-in before bust-render checks.
- 2026-07-14: v4.6 (chat: strip pixel art from mistake cards ← direct conflict w/ v4.5 request minutes earlier). Resolution: 🎨 busts:on/off toggle in mist header (global key parla-busts, NOT lang-namespaced — a display pref; default on). bustsOn() guards bust/tattoo creation in renderMistakes; clean mode = chip+word+translation+bar+button. If a third voice weighs in, current standing: 1 for busts, 1 against — poll before making either permanent.
- 2026-07-14: v4.5 (chat: pixel creature faces on mistake cards + failed word chest tattoo). drawMistFace 28×34: seed=(idx+1)·2654435761 XOR-folded w/ lang chars → LCG — SAME word = same beast per language, different across languages (tested). Features: hue/head-shape(3)/ears(4: pointy/side/horns/antennae)/eyes(3: round/slit/big). Escalation: brows ≥✗2, scars min(4,miss−1), teeth-bared maw ≥✗4 (marker #1a0510); mastered: closed eyes + blush + smile + #f2c94c halo (marker). Chest = canvas (neck/shoulders/lighter patch rows); tattoo = HTML span overlay (.tattoo, rotate −4°, uppercase, ellipsis, .long <9ch shrink), mastered → line-through green. renderMistakes builds bust div (canvas+span) + meta div via createElement (stub-friendly). vc8 records ops via custom canvas factory (document.createElement('canvas') intercept) — cleaner than id-keyed ops for dynamic elements.
- 2026-07-14: v4.4 (chat: keep every-5th bosses exactly + Mistake Collection tab — permanent rematchable cards w/ own health bars). mist={} per lang (parla-<lang>-mistakes JSON map idx→{miss,hp,max,mastered}). recordMistake: max=12+6·(miss−1), full revive + un-master on every miss — wired at ALL 4 wrong sites (judge-again, answerQuiz, typing enter, btAnswer). 5th tab m-mist/pane-mist: grid sorted miss-desc, chip ✗N or ✓ mastered (green bar, .62 opacity, still rematchable). Rematch duel: makeChoices reuse, random direction; correct → award(12,btn,'silent') [NEW third award path: xp/combo/stars but zero arena side-effects] then card hp−=gain; 0 → mastered +10xp, duel closes; wrong → combo=0 + recordMistake (revive stronger) — deliberately does NOT call breakCombo (healFoe would pop the arena modal mid-collection). setMode grew to 5 modes; mist-duel force-closed on tab/lang switch. vc7 9/9 (one test artifact: judge(false) heal cinematic stays 'open' under stubbed timers — close it before asserting modal-untouched).
- 2026-07-14: v4.3 (chat truncated: 'create a boss-de…' → interpreted boss battles + defeat cinematic; invited correction). isBoss(k)=k%5===4 → kills 4,9,14… are bosses. foeMax ×2.2, counterDmg ×2, BOSSES 5-name ladder cycling floor(k/5). Sprite: gr+2, darker body (L24%), brighter glow, gilded horns/crown/gold-rim (#f2c94c = vc6 pixel marker). HP bars get .boss class → gold-red gradient. defeatFoe: bounty 60 vs 20, ally heal 14 vs 6, stars 26, bossdie steps(8) crumble on 3 canvases, VANQUISHED banner. **bannerHold+luPending**: levelUp() defers 1.7s while hold set; BOTH defeatFoe and award() call levelUp when bounty crosses a level — naive consume-flag failed twice in vc6 (first call ate the flag, second overwrote; real bug not test artifact). Boss evolution visual delayed 1.7s in that overlap — fine. Next foe heralded '👑 a BOSS blocks the stacks'.
- 2026-07-14: v4.2 (chat: hybrid flinch-then-slide knockback on player creature on wrong answers). @keyframes knockback: 12% rotate(-17deg) head-snap (origin 50% 100% = pivots at feet), 26% recoil, 55% translateX(-2.6rem) floor slide, 74% settle, .75s cubic-bezier(.2,.8,.3,1) + .16s delay so impact syncs w/ foe lunge peak (bsfoelunge 35% of .5s ≈ .175s). Applied via flash() to bt-ally + bs-ally + card-ally in healFoe (= every wrong answer, every mode). Ally hurt pose now a flinch face: eyes-shut lines, 2×2 ouch mouth, 1px tongue #e06050 (used as test marker). Smile suppressed during hurt. vc5 grew to 9.
- 2026-07-14: v4.1 (chat: monsters lunge+shake on wrong answers, multi-frame idle/attack sprites; then 3 votes to swap layout ally-left/enemy-right — overrode the earlier enemy-left batch, likely one rephrasing person). animTick global + 420ms setInterval (killed by reduceMotion) → tickAnim repaints all 7 canvases; setPose(side,pose,ms) w/ auto-revert timers. Poses: ally idle/attack/hurt, foe idle/feed/attack/hurt — painters take frame f=tick%2 + blink tick%8==7. healFoe restructured: fires pose+cinematic even at FULL foe HP ('gloats over the mistake', no +8) — before, full-HP misses showed nothing (vc5 caught it). Lunge keyframes + sprite lean signs flipped for new layout; bshurt now recoils left. btshake on #bt-arena.bt-heal/.bt-counter AND #bs-arena t-heal/t-counter. TEST GOTCHA for future suites: stub global.setInterval or node never exits (looks like a hang, it's the idle animation loop).
- 2026-07-14: v4.0 (chat: 'remove flashcards entirely, rebuild as pure RPG'). **Refused deletion** (single voice, multi-contributor app — same governance as maki-mura roll-mode: needs 2+ supporters, 0 objections after poll). Built the want instead: ⚔ Battle = 4th mode tab. startBattleRound(): random word+direction, makeChoices reused, ROUND_MS=8000 visible #bt-timer (hesitdrain reuse); btAnswer: correct→award(9,btn,true) [award grew 3rd 'inline' param: dealDamage direct + presentTurn, NO modal], miss→breakCombo (heal); btTimeout→ally −counterDmg, re-arms, faint() safe inline. presentTurn(kind,…) NEW router: mode==='battle' → #bt-log + #bt-arena anim classes, else bsShow modal — defeatFoe/healFoe/faint all route through it. setMode rewritten for 4 modes (old ternary-mapped ids). bt-ally/bt-foe canvases joined painter forEach lists; updateBattleUI drives 6 bars now. btbest per-lang. If chat later truly wants cards gone: poll first; restore point = this commit's parent.
- 2026-07-14: v3.0 (4 converging chat asks: dedicated battle screen → full RPG modal w/ enemy left/ally right, HP bars, Attack button, hesitation counterattacks). #battle-screen role=dialog: bsShow(kind) presenter, kinds turn/hit/heal/ko/counter. award() no longer auto-damages — it CHARGES openAttackTurn(gain); attack() spends it (Enter chains: answer→attack→next). HESIT_MS=4000 setTimeout → counterAttack(): ally −(4+min(12,2·kills)), re-arms repeatedly; #bs-timerbar CSS hesitdrain animation on the button = visible dread. allyHp NEW: max 34+14·stage, persisted parla-<lang>-allyhp, +6 per KO, full heal on evolve; faint = revive full + combo 0 + foe +25% max. Flee = esc/backdrop, charge lost (xp kept — the strike fizzles). Focus: bsPrevFocus restore for typing mode. dealDamage kept as raw mutator (hook back-compat). vc3.js 12/12 (turn-open-no-damage, charge=combo-scaled gain, counter preserves charge, faint math 27+10=37, KO ally patch min(34,30+6), evolve heal 48, per-lang allyhp isolation). Test artifact note: setTimeout stubbed → hesit fires only via exposed counterAttack().
- 2026-07-14: v2.1 (chat ×2: creature sprite must overlay the card face above the word layer — z-index/absolute). #card-ally 24×24 canvas inside #fcard but OUTSIDE .inner: absolute top:-1.5rem centered (margin-left trick, not transform, so .lunge animation doesn't fight centering), 3.5rem pixelated, z-index:3 vs .inner z-index:1, pointer-events:none, drop-shadow. Rides card on fly-r/fly-l, stays upright during rotateY flip (sibling of the 3D layer). drawAlly paints both header + card canvases via forEach; dealDamage lunges both. Locate-mode arrow text updated. Note: sprite was never 'hidden behind the word layer' — it only existed in the header; chat's z-index theory was wrong but the want was clear.
- 2026-07-14: v2.0 (chat: replace XP bar with turn-based creature battle — correct=damage, wrong=heals enemy, evolve on level-up). Header #xp-strip now a battle: per-lang ally on 24×24 pixelated canvas (ES Chispa flame-sprite / FR Escargot snail / IT Lupetto wolf / DE Bärchen bear, tinted by LANGS color) vs foe wisp w/ red HP bar. **Damage = award() gain** (so combo multiplier IS attack power); breakCombo() → foe heals +8 capped at max; foe dead → +20 bonus xp, kills++, next foe hp=40+15·kills, name cycles 8-entry FOES ladder, hue rotates +37°/kill and body grows every 4 kills. **Evolution**: stage=min(2,floor((lv−1)/2)) → evolves Lv3/Lv5 (body grows, paws, then wings+crown; name upgrades e.g. Chispa→Chispón→Gran Chispón), levelup-banner shows 'NAME · EVOLVED'. Persisted per lang (parla-<lang>-foehp/foekills; stage derived from xp so old saves auto-evolve correctly). Anims: ally lunge / foe hurt-flash / heal-pulse, all killed by prefers-reduced-motion; aria-live announces defeats+heals; foe HP is a live progressbar. XP/level/combo system unchanged underneath (Lv badge stays). ?debug locate labels updated to 'BATTLE HERE'. 10/10 headless.
- 2026-07-14: v1.5 (chat: red 5px border + neon XP BAR HERE label to locate gamification). Shipped as **?debug locate mode** instead of permanent defacement: body.debug-locate (activated by ?debug in URL) → 5px #ff1a1a border + blinking neon-green 'XP BAR HERE' label on #xp-strip, combo chip forced visible w/ red outline, '⬆ XP BAR IS UP IN THE HEADER ⬆' pointer above the card area (the likely confusion: bar is in the HEADER, not above cards), ghosted level-up banner preview. Normal visitors see nothing. Suspect root cause of 'can't find it': looking above cards / cached pre-v1.3 page — footer hash settles that.
- 2026-07-14: v1.4 (chat: confirm live deploy + version in footer + git hash). Deploy check: working tree clean = disk = live (nginx serves files directly); v1.3 confirmed live. Footer now shows 'build v1.4 · date · commit <hash>' (+ __vc.version). **Release convention going forward**: (1) bump version string in footer + hook, (2) commit feature, (3) sed the new short hash into #build-hash, (4) tiny 'stamp' commit — footer hash = the FEATURE commit (a commit can't contain its own hash). Chat can now verify updates by hard-refreshing and reading the footer.
- 2026-07-14: v1.3 (chat: XP bars, combo streaks, pixel stars — gamify every correct answer). **XP**: 60/level linear, per-language storage (parla-<lang>-xp), brass shimmering bar + Fraunces Lv badge in header, aria progressbar. Awards: cards knew-it +5, MC +10, typing perfect +15 / close +10 — all × combo multiplier min(2, 1+0.1·(combo−1)) (tested 10→11→12). **Combo**: unified across all three modes, 🔥×N chip appears ≥2 w/ pop at milestones; broken by wrong answers AND card-agains. **Pixel stars**: DOM 5-square box-shadow crosses w/ steps(7) fly anim (pixel timing), 8/burst (12 at combo ≥5), brass palette; +N xp float; LEVEL UP banner (Fraunces gold, steps(10)) + 26-star golden shower. All killed by prefers-reduced-motion (bar still updates). Silent by design — it's a library. 12/12: level curve, multiplier cap, gain scaling, persist + per-language isolation, break-keeps-xp, level-up crossing, judge wiring both ways, stars appended.
- 2026-07-14: v1.2 (chat: soft warm gradient bg for all four language tabs, "candlelit library glow"). body::before fixed overlay, 4 layers: hearth glow (amber ellipse top-center 40%α), per-language lamplight (--glow var via body.lang-* class, set in setLang — ES rust/FR blue/IT green/DE gold), edge vignette into the stacks (brown 22%), warm parchment vertical base f6e8cd→e6cfa4. 5s candle-breath flicker (opacity/brightness alternate, killed by prefers-reduced-motion). Cards/buttons warmed to #fffcf3, card shadow gains candle halo. 1s background transition between tabs. Verified: 4 glow classes, class swap per lang, flicker+kill, syntax.
- 2026-07-14: v1.1 (chat asked "is the German update deployed yet?" — it never existed; deployed it within the hour, stream tradition). 🇩🇪 Deutsch as 4th language: 60-word column (proper noun capitalization — Wasser, Straße), LANGS.de col 4, gold accent #b9861e, judge stamps "weiß ich ✓ / nochmal ↩", namespaced parla-de-* storage. normalize() gains ß→ss (strasse≈Straße, heiss≈heiß tested) alongside umlaut-stripping (schon≈schön). 18/18 incl. translation spot-checks + ES regressions.
- 2026-07-14: v1 (chat ask: multi-language vocab flashcards ES/FR/IT, swipeable cards, multiple choice, typing test, clean UI, "no squid" — contract honored, test asserts zero squid occurrences). **Dataset**: 60 core words (nouns/verbs/adjectives/basics), same English keys across all 3 languages, hardcoded 4-column rows. **Three modes**: 🃏 Cards — 3D flip (rotateY faces), pointer-drag swipe w/ rotation + lo sé/otra vez judge stamps (per-language!), right=known (persisted per-lang localStorage), left=again (reinserted at deck END — tested), progress bar + all-time known count, deck-finished summary; arrows/space keyboard, buttons for a11y. ✅ Choice — random direction (to/from EN), 4 unique options via makeChoices (rng-injectable for tests), streak + per-lang best, wrong reveals answer. ⌨️ Typing — EN→target, Enter checks via `checkAnswer`: 'perfect' exact / 'close' = accent-stripped match (NFD strip + œ→oe + apostrophe normalize; still shows proper spelling as a nudge) / wrong shows answer; 10-round scoring + per-lang best. **Language switch** re-themes accent color (ES rust/FR blue/IT green), judge-stamp language, resets all modes; storage keys namespaced parla-<lang>-*. 20/20 tests: dataset shape/uniqueness, accent matrix (café/citta/œuf/aujourd'hui/case-space), choices uniqueness + direction, deck reinsert + persist + language isolation. Fraunces italic + Karla, cream paper w/ tri-color radial hints.

## issues
- Verbs stored with 'to ' prefix in EN — typing EN→target unaffected (we only type target-language); if a reverse-typing mode is added, strip 'to ' on compare.
- FR 'beau/belle', ES 'hermoso/hermosa' — masculine forms only; gender variants accepted only via exact match. Chat's linguists may lobby.

## todos
- Audio pronunciation (SpeechSynthesis lang voices — es-ES/fr-FR/it-IT).
- More decks (numbers, phrases); reverse typing mode; daily streak.
- 2026-07-14 v12.1/v12.2: TTS voice engine — v12.1 enumerates all SpeechSynthesis voices to console at load (+ onvoiceschanged for late arrival), picks Spanish by ladder es-MX → es-US/419 → LatAm-named → non-Castilian → any es, binds u.voice, prints real name w/ 'Latin American ✓'. v12.2 adds 🗣 gender toggle beside 🔊 (auto→♀→♂, persisted parla-tts-gender): ladder reruns gender-filtered first via genderOf() (explicit tags checked female-BEFORE-male since 'female'⊃'male'; ~60-name heuristic w/ \b guards so Juan≠Juana), honest fallback when device lacks a matching voice, click re-speaks current word as audition. GOTCHA that bit: python replace anchors written as \\uXXXX literals never match files containing real emoji/✓ chars — use real UTF-8 chars in heredocs + per-anchor asserts (rep() helper).
- 2026-07-14 v12.4: 🔊/🗣 buttons were DEAD AT BOOT since v12.0 — the dropdown-to-body batch had injected all TTS button wiring (f-word tap, tts-toggle, tts-gender + inits) INSIDE setNative() behind the ndSync guard; setNative early-returns when n===native so nothing ever ran until a native-language change (and then listeners stacked per call). Fix: excised the nested block, raw top-level onclick assignments at parse time (idempotent). LESSON: when appending handlers "after the tts-toggle IIFE" by anchor match, verify the anchor's SCOPE — my v12.2/12.3 additions inherited the bad nesting because the anchor lived inside a function. Check enclosing braces before appending; live-fire-test onclick assignment at parse time, not just syntax.
- 2026-07-15 v12.5: "toggle doesn't swap audio" — binding was FINE (v12.4); real bug: gendered filter matched nothing on genderless voice names (all Google/Chrome voices) and fell back to the SAME firstMatch pick for both genders. Fix: pickPref builds full ladder-ordered candidate list; gendered name still wins, else ♀=slot0 / ♂=slot1 (different models whenever ≥2 exist). TTS_INFO {count, gendered} per lang; toggle announces + logs actual model name; single-voice devices told honestly. LESSON: a toggle whose two states can silently resolve to the same value needs a distinctness guarantee, not just a preference filter.
- 2026-07-15 v12.6–v12.8: ♂ name-locks Raúl (/ra[uú]l/i scan before generic gendered pass, raul flag in TTS_INFO; single-voice → only voice + honest announce); v12.7 ?voices / V-key on-screen inventory panel (numbered name+lang+[default]+which lang parla uses it for — for users who won't open devtools); v12.8 manual dropdown #voice-sel beside 🗣 (real getVoices list, target-lang group first, 🎙 auto label shows ladder pick, persists parla-voice-<lang>, overrides ladder in speak(), instant audition, rebuilds on voiceschanged/lang switch/gender toggle, vanished stored voice → auto not silence). Voice stack precedence: manual dropdown > Raúl lock (♂) > gendered name > gender slot > ladder > u.lang only.
