# idle-slots

## log
- 2026-05-02: **Adjacent 2-of-3 pair-rows added at every N — fixes "missing rewards on cells 1,2 / 4,5" reports.** Per chat audit: at N≥4 a near-miss like 🍀🍀🪐 on the top row paid zero because the only horizontal line was the full `[0,1,2]` — partial 2-of-3 sublines weren't emitted. The N=3 special-case had pair-rows `[0,1]` + `[1,2]` for the early game, but no other N got the same treatment. **Fix**: in `getLines(N)`, every emitted 3-cell row now ALSO appends its two adjacent pair sublines. Removed the now-redundant N=3 branch — the generic loop produces the same 3-line set for N=3 (`[0,1,2]` + `[0,1]` + `[1,2]`). Coverage by N: 3→3 lines, 4→4, 5→6, 6→9, 7→9, 8→10, 9→14. Concretely at N=6 the bottom row now emits `[3,4,5]` + `[3,4]` + `[4,5]`, so 2-of-3 matches on cells 4,5 (1-indexed) pay; at N=9 all three rows get pair-sublines so cells (1,2)/(4,5)/(7,8) all reward partial wins. Pure additive — full-row + col + diag detection unchanged, evalLine's len² scoring keeps the new 2-cell lines well below full-row payouts.
- 2026-05-02: **Unified procedural payline generator — drop the per-N hand-written tables.** Per chat: "remove the specific 6 and 9 reel calculation rules and apply the 4, 5, 7, and 9 logic globally". Replaced the `LINES_BY_N` table + `autoHorizontalLines` helper + `getLines(N)` dispatch (which special-cased multiples of 3 to use the canonical hand-written table and only auto-generated horizontals for irregular grids) with one procedural `getLines(N)` that walks the real `cols × rows` grid for every N. **Logic**: emit every row + every column whose cell-set has `len ≥ 2`; emit both diagonals only when `cols === rows && cols * rows === N` (still gates diagonals to the strictly square N=9 case). **N=3 special-case kept**: 1×3 boards add the two adjacent 2-of-3 pair rows so early game still pays on near-misses (evalLine's len² scoring keeps these well below the full-line). **Behavioral changes vs prior split logic**: N=5 now picks up partial row `[3,4]` (was missing before — silent gap). N=7 now picks up partial cols `[1,4]` and `[2,5]` (was getting an antidiag `[2,4,6]` before, which was geometrically suspect on a 3×3-with-bottom-row-incomplete board — removed). N=8 picks up the partial col `[2,5]`. N=3, N=6, N=9 produce identical line sets to the old hand-written tables. Eliminates the per-N branching in `getLines`, the hand-written `LINES_BY_N` map, and the `autoHorizontalLines` helper — single procedural source of truth.
- 2026-05-01: **`evalLine()` extraction (clean independent per-line scan) + dynamic horizontals for irregular grids.** Two coupled chat asks: (a) "perform a clean, independent pass on each payline, ensuring symbols are reused for multiple wins" and (b) "generate horizontal paylines dynamically when cell count isn't 3, 6, or 9, ensuring irregular rows are fully covered". (a) Extracted the per-line wild-aware match into a PURE function `evalLine(ln, result)` that returns `{idx, len, kind, dominantSym}` or `null` — no shared state with the outer scope, no early bailout based on prior line results, callable in any order. PASS 1 of the multi-pass scan now just calls `evalLine` on each entry in `getLines(N)` and pushes the verdict. Cells are reused across all lines that include them — `cellHits[i]` increments per winning line independently, and PASS 3 sums each winning line's `len² × val` separately (no dedupe). The "reuse" of a single cell across multiple wins is the explicit point — center cell of a full-grid match contributes to 4 different line bonuses simultaneously. (b) New `autoHorizontalLines(N)` helper walks the grid layout (`cols × rows` from `colCount(N) × ceil(N/cols)`) and emits `{idx, len, kind:'row'}` for every row with ≥2 cells, INCLUDING partial trailing rows. `getLines(N)` branches: if `N % 3 === 0` (3/6/9), returns the canonical hand-written `LINES_BY_N` table verbatim; else (4/5/7/8), returns auto-generated horizontals merged with stored cols+diagonals. Net behavior change vs before: N=5 now picks up `row [3,4]` (the 2-cell partial bottom row), and N=8 picks up `row [6,7]` (also 2-cell partial bottom row). N=4 and N=7 don't gain new lines because their trailing rows are 1-cell (cell 3 alone, cell 6 alone). N=3, 6, 9 unchanged. Verified by node fixture: line set per N matches expected coverage at every step, multiples of 3 still get diagonals only at the square N=9, irregular Ns now have full row coverage including partial-row 2-of-a-kind opportunities.
- 2026-05-01: **Multi-pass payline scan + cell-overlap tally surfaced to HUD.** Per chat: "rewrite the win logic to perform a multi-pass scan across all eight canonical paylines, ensuring overlapping symbols are counted for every valid line match". Restructured the scoring as three explicit passes: **PASS 1 (match)** iterates `LINES_BY_N[N]` (up to 8 entries at N=9: 3 rows + 3 cols + 2 diagonals), checks each for a wild-aware match, and pushes successful matches to `winningLines` with the dominant symbol stashed inline so PASS 3 doesn't redo the work. **PASS 2 (overlap tally)** initializes `cellHits = new Array(N).fill(0)` then increments `cellHits[i]++` for every cell `i` that appears in any winning line. Cells at intersections accumulate higher counts — the 3×3 center cell can reach a max of 4 (row 1 + col 1 + main diag + anti-diag all converging). PASS 2 also computes `maxOverlap` as the highest single-cell hit count. **PASS 3 (score)** iterates winningLines and sums `len² × val` independently per line — overlap NEVER deduplicates; a cell on 4 winning lines contributes its symbol's value to all 4 line bonuses (the existing behavior, just made explicit). Multi-line + L-win multipliers stack on the sum afterwards as before. **HUD**: `showLineSummary` extended with a `maxOverlap` parameter — when ≥ 3 it appends "${maxOverlap}× OVERLAP" to the line summary text and applies the gold-tier highlight. Threshold of 3 (not 2) keeps it as a discovery: 2 is already implied by the L-WIN tag, but 3+ overlap is rare and earns its own callout. **Verified by node fixture** across 6 canonical 3×3 patterns: 9-of-a-kind shows hits `[3,2,3,2,4,2,3,2,3]` with maxOverlap=4 (center), L-corner row 0 + col 0 shows hits[0]=2 with maxOv=2, both-diagonals + center shows hits[4]=2, wild-substitution row+col shows hits[1]=2 (the wild cell participating in two winning lines). Bonus totals match independently-summed line scoring (e.g., 9-of-a-kind = 8 × 9 × val, no dedupe). Pure refactor — payout values unchanged.
- 2026-05-01: **🌌 nebula → wild card with LUCK-scaling spawn rate.** Per chat: "make the galaxy a wild that matches any icon, reduce its base spawn rate but scale up with the LUCK upgrade". Single-symbol change with three implications: scoring, count-bonus, and jackpot detection all become wild-aware. **Symbol redef**: 🌌 nebula now has `wild: true`, base weight reduced 3 → 2 (rarer at L0), val unchanged at 80. The 🚀 rocket entry's misleading "wild" comment dropped (it was never wired as a wild — kept as a normal high-tier). **Spawn rate**: new `symbolWeight(sym, luckBoost)` helper splits the existing rare-tilt formula into two paths: wilds get `Math.max(1, weight + luck × 0.40)` (UP-scales with luck) while non-wilds keep `Math.max(1, weight - luck × 0.25)` (the existing common-symbol compression). Pool share at L0 = 2.02%, L5 = 4.34%, L10 = 7.02%, L15 = 10.06%, L20 = 13.51%, L25 = 17.20%. **Line match (wild-aware)**: a line matches if every cell is either the same non-wild "dominant" symbol OR a wild. Pure-wild lines score at the wild's val (80). Mixed lines with two different non-wild symbols never match. Implementation: `nonWilds = syms.filter(s => s.nm !== WILD_NM)`; if empty → dominantSym = wild itself; else verify every nonWild matches the first; on mismatch `continue`. **Count bonus (wild substitution)**: wilds add to the BEST non-wild's count (highest base count). All-wild grids score at wild's val. Wilds cannot stack onto multiple non-wild symbols simultaneously — best substitution wins. Computed by finding `bestNonWild` first, then `eff = bestNonWildC + wildCount`, scoring at `bestNonWild.val`; if pure-wild gives a higher bonus (`wildCount² × wild.val × 0.40`), use that instead. **Jackpot (wild-aware)**: now fires when `distinct.size === 1` (every cell same) OR `distinct.size === 2 && distinct.has(WILD_NM)` (one non-wild + wilds). The non-wild becomes the jackpot symbol and gets the full `N² × val × 5 × jp` payout. Pure-wild jackpot scores at wild's val. **LUCK upgrade desc** updated from "+10% payout & better odds" to "+10% payout · more 🌌 wilds" so the new scaling is visible to the player. Verified by node fixture: line substitutions, count substitutions, jackpot detection, and luck-scaling pool share all behave correctly across canonical test cases.
- 2026-05-01: **Intersecting L-win bonus — 2× payout when winning lines share a cell.** Per chat: "implement logic for intersecting L-wins, recognizing when they overlap for double payouts". An L-win = 2+ winning lines that share at least one cell, forming an L (row+col at a corner), T (row+col mid-edge), or + (row+col+diag all crossing the center). On the 3×3 grid, MOST cross-direction pairs (row+col, row+diag, col+diag) share a cell — the bonus fires whenever the player lands a non-parallel multi-line combo. **Detection**: build a `Set(line.idx)` per winning line, scan all pairs for any shared cell, set `hasIntersection = true` on first hit. Bail early via labeled break. **Scoring**: stacks on top of the existing multi-line multiplier — `totalLineMult = multiLineMult × (hasIntersection ? 2.0 : 1.0)`. So 2 parallel rows = ×1.25 (existing), 2 intersecting (L/T) = ×2.5, 3 intersecting = ×3.0, full 9-of-a-kind jackpot path = ×5.5 (8 lines × 2.75 × 2.0 = 5.5x effective on the line bonus alone). Single hasIntersection flag (not per-pair count) so the doubling can only apply once even if 3+ lines all cross at the center cell — keeps the curve readable. **Parallel multi-line wins** (2 rows or 2 cols, no shared cells) keep the standard ×1.25 / ×1.50 etc curve without doubling. **HUD**: `showLineSummary` extended to take an `isLWin` flag — line summary now reads e.g. "2 WINS · ×2.50 · L-WIN" with the same gold-tier highlight that jackpot uses. The L-WIN suffix only appears when an intersection actually fires. Verified by node fixture across canonical 3×3 patterns: parallel rows row0+row2 → 22.5 (no L), L-shape row0+col0 → 45 (L doubled), T-shape row0+col1 → 45, jackpot 9-of-a-kind → 396 (all 8 lines × 2.75 × 2.0), no-wins still 0.
- 2026-05-01: **5-of-a-kind count bonus added (3×3 grid kept exactly as inisso ordered).** Per chat: "stop, do not change the layout, keep it 3x3 as inisso ordered, just fix the logic within that frame" + "ensure 5-reel matches are detected without changing the layout". With the strict line-only scoring rewrite, getting 5+ identical symbols across the 3×3 grid paid out NOTHING unless they happened to align on a 3-cell payline — missing the rare-frequency win every slot machine is expected to recognize. **Fix** (purely additive): new count-bonus tier sits alongside the line bonus. For each symbol with count ≥ 5 across the grid, add `count² × val × 0.40` to the payout. Quadratic so the bigger combos scale satisfyingly: 5-of-a-kind = 10·val, 6 = 14.4·val, 7 = 19.6·val, 8 = 25.6·val, 9 = 32.4·val. The 0.40× scale prevents this from drowning out true line wins (a 3-line full-row still beats a scattered 5-of-a-kind: 9·val × 1.0 vs 10·val with 0 lines, but 2-line stacked beats it). Threshold of 5 (not 3) keeps the existing line tier doing what it does; only "rare" frequencies (≥55% of grid same) trigger the new bonus. **HUD**: combo badge gained an "${kindCount} OF A KIND ${sym.em}" variant that shows when count ≥ 5 AND not jackpot. Jackpot keeps higher precedence (every-reel-same already replaces line+count payouts entirely). Layout untouched: `colCount(n) = Math.min(3, n)` unchanged, `LINES_BY_N[9]` still the canonical 3-row + 3-col + 2-diag set, EXTRA REEL upgrade caps unchanged, no DOM changes. Verified by node fixture across 8 canonical 3×3 patterns: horizontal/vertical/diagonal lines all fire identically to before, jackpot path unchanged, 5-of-a-kind scattered case (5 same w/ no line) now pays 10·val instead of 0, 9-of-a-kind path fires both jackpot AND count bonus tiers with ×2.75 multi-line multiplier intact.
- 2026-05-01: **Audit + fix: lines drawn over mismatched symbols when worker stall-detection force-resolves a spin.** Chat reported "lines on mismatched symbols at 50% spin speed". Root cause traced: when the rAF ticker is paused (tab hidden, browser throttled to 30fps for unfocused windows, system load), the auto-spin worker's stall-detection at `now - lastRafTime > 200ms` force-resolves the in-flight spin by clearing `reelAnims` and firing the resolve callback — but it **never wrote `translateY(endY)` to each strip's CSS transform**. Strips remained at `translateY(0)`, so when the tab regained focus the visible cell in each reel-window was cell 0 (a random filler from `pickSymbol(luck)` painted in `startReelTween`), NOT cell `LANDING_IDX = 20` (the computed result). The colored win-line overlay was drawn assuming the LANDING cell is visible, so the polylines connected reels showing random fillers — the "lines on mismatched symbols" the player saw. **Fix**: in the stall-detection branch (`setupAuto` worker callback), iterate `reelAnims` and snap each `a.r.strip.style.transform = 'translateY(' + a.endY + 'px)'` (with `transition: 'none'` first to avoid CSS easing) BEFORE clearing the array and calling the resolve callback. Now even during background catch-up the strips end up at their landing position so the visible symbol always matches what the line draws over. Wrapped in try/catch since some entries might point to detached DOM (mid-prestige). Audit also confirmed: single keydown listener path, no other strip-transform writes outside `startReelTween` + `reelTicker`, the `runEpoch` epoch guard correctly bails the resolve on prestige, `buildReels` centralized cleanup correctly clears `reelAnims + reelOnComplete` so EXTRA-REEL-mid-spin doesn't leak old tween references, and the per-N `LINES_BY_N` table guarantees no wrap-around or non-square diagonal phantom wins.
- 2026-05-01: **Strict per-N paylines + line-only scoring + colored win-line overlay + total-win badge.** Three coupled chat asks landed: (a) "color-code winning lines + show multiplier", (b) "rewrite to strictly check adjacent reels per row, diagonals only if board is square, no phantom wins", (c) "rewrite win logic to only match identical adjacent symbols". **Per-N line tables**: replaced the procedural `getLines()` generator with explicit `LINES_BY_N` config — N=3 (1 row), N=4 (1 row + 1 col), N=5 (1 row + 2 cols), N=6 (2 rows + 3 cols), N=7 (2 rows + 1 col), N=8 (2 rows + 2 cols), N=9 (3 rows + 3 cols + 2 diagonals). **Diagonals only at N=9** because the strict 3-col cap means N=9 is the ONLY configuration where the board is fully square (3×3, all 9 cells filled). Eliminates the false-positive diagonal at N=7/8 where the procedural generator would emit `[2,4,6]` even though the bottom row was incomplete (cells 7+8 missing). Verified by node fixture: every line in every N=3..9 table passes a geometric `isStraightLine` check. **Phantom-win removal**: stripped the legacy global `count² × val` formula that paid out for ANY symbol frequency across the grid regardless of cell adjacency (e.g. 3 nebulas at cells [0,5,8] used to pay despite not being on any line). Scoring now ONLY recognizes wins where identical symbols sit on adjacent cells along an explicitly-defined payline. Also removed the 2-of-3 partial-line consolation (also off-line in spirit). Two reward tiers remain: full-line `len² × val` + multi-line `× (1 + 0.25 × (lineCount−1))` mult on the sum. Jackpot bonus (every reel same symbol) replaces the line-bonus formula. **Color-coded line overlay**: single persistent SVG element (`#lineOverlay`) absolutely positioned over `.reel-deck`, re-appended at end of every `buildReels` since `innerHTML=''` would wipe it. Each winning line drawn as a polyline through cell centers (computed from `wrap.offsetLeft + wrap.offsetWidth/2` etc) in a unique neon color from an 8-color palette (cyan / magenta / gold / lime / pink / amber / violet / white — covers max 8 lines at N=9). Lines fade in (10% of 2.4s anim), hold, fade out via `lineFlash` keyframe; staggered 80ms per line for a sequential reveal. Drop-shadow uses `currentColor` for per-line glow. **Win-count badge**: new `#lineSummary` span next to the combo badge in the payout readout — shows "3 WINS · ×1.50" when payouts include a multi-line bonus, plain "1 WIN" for single-line, gold variant when jackpot or multiplier ≥ 1.5×. Empty when no line wins.
- 2026-05-01: **Background catch-up summary popup — replaces dribbled individual pops with one big Bungee-gold summary.** Per chat: now that the Web-Worker auto-spin keeps firing while the tab is hidden, returning to the tab dribbles the queued FX pops out at one every 90ms (queue cap = 12 → ~1.1s of stale "+12c +8c +24c +96c…" pops for spins the player didn't watch). **Fix**: on `visibilitychange → hidden`, snapshot `{ totalChipsEver, spins, t }`. On `→ visible`, compute the delta. If the player was away > 1s AND ≥1 spin happened, clear the FX queue (`FX.queue.length = 0` suppresses the individual pops) and spawn ONE big centered summary popup: `WHILE YOU WERE AWAY · +N 💫 · M spins · Xs away`. Single DOM element, animates in via 0.45s opacity + scale transition (from 0.85→1), holds 3500ms, fades out, removes. Single jackpot-tier sfx instead of N individual win chimes. **Format**: amount in 42px Bungee gold w/ glow + tracking, title in 11px Space Mono 0.36em-tracked cream-grey upper, stats line in 13px Space Mono showing spin count + duration via a `formatElapsed()` helper (Ns / Mm / X.Yh). Popup layered z-index 200 above the regular toast. Brief tab-flips (< 1s) don't trigger; fast hide/show/hide cycles deduplicate by removing any prior popup before appending a new one. Save state + auto-spin progression are unchanged — this is purely a notification-consolidation pass.
- 2026-05-01: **Background-friendly gameloop — Web Worker auto-spin tick + rAF stall detection.** Per chat: stardust must accumulate even when the tab is backgrounded. Both rAF (paused or 1Hz throttled in hidden tabs) and main-thread `setInterval` (clamped to ~1s) are throttled when the page isn't visible, which silently stopped auto-spin progression. **Fix**: added `startBackgroundTimer(callback, intervalMs)` helper that spins up an inline Web Worker (Blob URL → `URL.createObjectURL`) running `setInterval` on its own thread. Worker `setInterval` is NOT subject to the same throttles as the main thread, so the tick keeps firing at ~100ms accuracy even with the tab hidden. The worker posts `'tick'` messages back; the main thread's handler runs the existing auto-spin logic (gate on `nextSpinAt`, call `spinOnce()`, etc). Returns a `{ kind, stop() }` handle replacing the bare `setInterval` ID. Falls back to plain `setInterval` if Worker creation throws (CSP / sandbox edge cases) — that path is throttled in background but better than nothing. **Stall detection**: added `lastRafTime` stamped each frame inside `fxLoop` (which runs continuously when foreground). The worker tick checks `now - lastRafTime > 200` ms while `spinning === true` to detect "rAF is paused (tab hidden) but a spin is mid-flight" — the reel-tween ticker is halted, `reelOnComplete` will never fire on its own, and `spinning` would stay true forever, gating all future auto-spins. When detected, force-resolves: invokes the stashed `reelOnComplete` callback (does HUD readouts + win-glow), clears `reelAnims`, hard-clears `spinning = false`. Player still gets full payout credit (math was applied at click time inside `spinOnce`); they just don't see the visual catch up until they switch back to the tab. **Updated all `clearInterval(autoTimer)` call sites** (setupAuto cleanup, prestige reset) to `autoTimer.stop()`. Existing inisso spec preserved: still a single live tick clock, still 100ms cadence, still re-reads `effectOf(speed)` per tick.
- 2026-05-01: **Strict 3-reel-per-row cap + ghost-tween cleanup centralized.** Two coupled chat asks: (a) "strict 3-reel maximum per row" and (b) "ghost reels left behind on unlock + clear out legacy code". (a) Replaced the legacy `cols = N <= 4 ? N : 3` heuristic — which let N=4 sprawl into a 4-wide single row — with `colCount(n) = min(3, n)` and a `COLS_MAX = 3` constant as the single source of truth. Both `buildReels` (DOM grid layout) and `getLines` (payline generator) now call `colCount()` so they always agree on grid shape. **Layout change for N=4**: was 1 row × 4 cells (single payline `[0,1,2,3]` len 4); now 2 rows × 3 cols with row 0 = `[0,1,2]` full + cell 3 alone in row 1 col 0, paylines = row `[0,1,2]` (len 3) + col `[0,3]` (len 2). Verified by node fixture: all N=3..9 produce strict `cols ≤ 3`, every generated line passes a `isStraightLine` check (no wraps). (b) Ghost-reel fix: previously `reelAnims.length = 0` and `reelOnComplete = null` were only cleared in the prestige handler. When a player bought EXTRA REEL mid-spin, `buildReels()` rebuilt the DOM but in-flight tweens kept holding refs to the now-detached old wrap/strip elements — the rAF ticker would write `transform` styles to dead DOM until each tween elapsed (no visible bug but stale state lingering). Moved the cleanup INTO `buildReels()` itself so every rebuild path (extra-reel buy, prestige, init) shares one cleanup. Removed the duplicate cleanup from the prestige handler (now just a comment pointing to the centralized version). The `runEpoch` guard on resolve closures still serves as a separate safety net for the case where prestige fires AFTER a tween started without rebuilding reels.
- 2026-05-01: **Win-condition audit — replaced hardcoded LINESETS table with a grid-walking generator that structurally cannot emit wrap-around lines.** Audit findings on the prior implementation: (a) **silent coverage gap** — `LINESETS = {6: [...], 9: [...]}` only had entries for N=6 and N=9, so at every other reel-count level (N=3, 4, 5, 7, 8) the line-detection loop ran over `[]` and awarded zero full-line, partial-line, or multi-line bonuses despite N=3 being the canonical 3-in-a-row slot machine layout AND the starting state of the game. (b) **No structural wrap-protection** — the hand-written tables happened to be correct, but a future edit adding e.g. `[2,3,4]` at N=6 (which crosses a row boundary from row 0 col 2 → row 1 col 0/1) would have looked like a horizontal line to the scorer and silently inflated payouts on impossible geometry. (c) **Asymmetric diagonals at non-square N** — N=7 and N=8 have a valid anti-diagonal `[2,4,6]` but the main diagonal `[0,4,8]` requires cell 8, which doesn't exist at N=8; naive generators would emit it. **Fix**: introduced `getLines(N)` that builds the same 2D grid `buildReels` paints (`cols = N≤4 ? N : 3`, `rows = ceil(N/cols)`), then walks rows/cols/diagonals only emitting lines whose every cell maps to a real reel index (`grid[r][c] !== null`). Wrap-around is impossible because the generator never crosses a row/col/diagonal boundary — it iterates the *physical* grid, not consecutive linear indices. Diagonals only fire on square grids (rows === cols && rows ≥ 3). Cached per-N. **Verified by fixture**: all 27 generated lines across N=3..9 pass a `isStraightLine` check (every cell on same row OR same col OR same +1/−1 slope) — no wraps. **Coverage gained**: N=3 → 1 line `[0,1,2]`; N=4 → 1 line of len 4 `[0,1,2,3]`; N=5 → 3 lines (1 full row + 2 cols); N=7 → 4 lines (2 rows + 1 col + 1 anti-diag); N=8 → 5 lines (2 rows + 2 cols + 1 anti-diag). Existing N=6 and N=9 sets match the old hardcoded ones byte-for-byte so established players see no regression — only the gap-fill and the structural guarantee.
- 2026-05-01: **Inisso recalibration — base prestige reward capped at 1 stardust, Ascent Power goes exponential.** Two coupled changes per inisso's new spec: (1) `prestigeGain()` returns 1 if `runChips ≥ prestigeTarget()` else 0 — no more log-scaled overshoot bonuses (old: `floor(log2(runChips/10000) - stardust)`, which could grant +3 or +4 stardust on a single deep run). The target itself still doubles each prestige (`10000 × 2^(✦+1)`) so the curve still demands exponentially more chips per ascension; you just always get exactly 1 stardust for clearing it. (2) Ascent Power's per-stardust slope went exponential — `slope = 0.5 × 1.5^ap_lvl` (was: linear `0.5 + ap_lvl × 0.1`). At L0 = 0.5× per stardust (matches the original baseline), L3 = 1.69× (~3.4× the baseline), L5 = 3.80×, L10 = 28.83×. Cumulative stardust cost to max AP at the existing grow:2.5 curve is ~6,362 (1+3+7+16+40+98+245+611+1526+3815) — long endgame grind, but L1–L5 (cumulative 67) is meaningfully cheap. The mechanical shift: meta progression is now driven by *stacking* Ascent Power across many small prestiges rather than farming one 80,000-chip overshoot for +3 ✦. UI reflects this — REROLL panel reads "gain +1 🌟 on reroll (capped at 1)" and shows a live per-🌟 slope readout that updates with AP level (`+50%` → `+76%` at L1 → `+288%` at L5). Confirm dialog also surfaces the current AP-buffed slope so the player sees exactly what the next stardust is worth. Backward-compatible: all save schemas untouched, `stardustEarned` lifetime tracker still drives the multiplier, prestige doesn't reset Ascent Power (it's stardust-tab meta).
- 2026-05-01: **Reel animation rewritten as a single rAF ticker — three latent bugs fixed in one pass.** (1) **Mobile landing desync**: old code hard-coded `SLOT_H = 100` to compute `translateY(-20 * SLOT_H)`, but the mobile breakpoint at ≤560px drops `.reel{height:80px}` and `.slot{height:80px}`, so phone players were translating the strip 100px (one full cell) past the window — the symbol they saw at rest was off by one row. New `startReelTween` reads `r.wrap.clientHeight` at spin-start so endY is always layout-correct. (2) **Off-by-one painted vs. landed**: the previous painter wrote `result[i]` at strip index 21, but `translateY(-20 * SLOT_H)` centered cell 20 in the window — meaning the visible "landed" symbol on every spin was a random filler from the `pickSymbol(luck)` write, not the computed result. Payout math was right; the visual just lied. New code uses a single `LANDING_IDX = 20` constant for both the painter and the translation magnitude, so what you see is always what was rolled. (3) **Resolve race**: the prior `setTimeout(longest)` could fire before or after the actual CSS transition under tab-throttling or high-frequency auto-spin, occasionally flashing readouts a frame ahead of (or behind) the reel stop. Resolve now fires from the rAF ticker's "all reels landed" callback (`reelOnComplete`), guaranteeing visual + state sync. Also dropped `Math.random() * 100` per-reel jitter in favor of deterministic `BASE_DUR + i*STAGGER + SETTLE` so `longest` exactly matches last-reel finish time — FX buffer's `readyAt` now syncs the float-chip pop to the precise moment the last reel lands instead of arriving 140ms late. Prestige path clears `reelAnims` + `reelOnComplete` so detached-DOM strips don't get pointless transform writes after a reroll. Easing swapped from CSS cubic-bezier `(.16,.84,.36,1)` to JS `easeOutCubic` (visually almost identical, but driven by the same time clock as `spinning` lock for race-free coupling).
- 2026-05-01: **Galaxy reskin — 2D pixel-art space theme, all spec math intact.** Path stays `/idle-slots/` so existing localStorage saves carry over. **Symbol swap** (same weights + values, only the emoji + name change so the audited payout math is byte-for-byte identical): cherry→🪐 planet (val 1, weight 28), lemon→👽 alien (2, 22), bell→🛸 ufo (4, 16), star→⭐ keep (8, 12), clover→☄️ comet (12, 9), gem→💎 crystal (24, 6), seven→🌌 nebula (80 jackpot, 3), wild→🚀 rocket (50, 4). **Palette**: cherry-red cabinet → deep space-indigo gradient (`#1c1042 → #0c0626`), gold trim → chrome-cyan (`#9be7ff` border + `#3a89c9` rivets), reels' cream paper → near-black `#1a0d3a → #0a0420` with cyan inner glow. Sharp pixel-art corners (border-radius:0 on cabinet + reel-deck + reels). `image-rendering: pixelated` on every element so emoji edges stay crisp at any zoom. **Starfield bg** via two layered `body::before` and `body::after` pseudo-elements, each with 10 radial-gradient star sprites at 300/500px tile sizes panning vertically over 80s/160s with reverse direction on the second layer for parallax depth — pure CSS, zero JS overhead. **Symbol glow**: every reel slot gets a `drop-shadow(0 0 8px rgba(58,255,255,0.45))` to make the emoji feel lit from behind by the reel. **Brand**: rebranded to `GALAXY · SLOTS` with `stardust · upgrades · ascend` subtitle. Switched the title from Bungee Shade (carnival) to Press Start 2P (chunky pixel-art bitmap) with a 4-layer text-shadow stack — cyan glow + magenta glow + pixel-step black shadow + cabinet-deep extrusion. Title meta + favicon also flip to 🌌 galaxy emoji and `idle slots · galaxy`. **OG image** repointed to a galaxy slot machine prompt. All previously verified inisso specs still pass: ✓ 1.20^lvl payout, ✓ 1+✦×0.5 prestige, ✓ luck multiplier, ✓ setInterval auto-loop, ✓ localStorage, ✓ runEpoch race guard. The reskin is purely cosmetic — chip balances + upgrade levels + stardust persist seamlessly.
- 2026-05-01: **Code review pass — 3 hiccups found and fixed beyond the spec audit.** (1) **Chips counter `.bump` class never cleared** — adding `.bump` triggers `transform: scale(1.08)` via the `transition: transform 0.12s` rule, but nothing was removing the class, so after the very first payout the counter stayed permanently 8% larger. Fix: after `add('bump')` schedule a `setTimeout` to remove it 140ms later (one frame past the transition end), with a stored `_bumpT` to debounce overlapping bumps. (2) **Prestige `target` and `gain` formulas didn't agree on "target met"** — old target was `10000 × 2.5^✦` and gain was `floor(log2(runChips/10000) − ✦)`. At stardust=1 the target said 25,000 but gain stayed at 0 until 40,000. Fix: synced target to the smallest runChips where gain is ≥ 1, i.e. `target = 10000 × 2^(✦+1)` so at stardust=0 target=20,000 (yields +1 ✦), at ✦=1 target=40,000 (yields +1 ✦), etc — progress bar now hits 100% exactly when REROLL enables. (3) **In-flight spin-resolve race after prestige** — the `setTimeout(longest)` resolve closure inside `spinOnce` captured `state` by reference, so if the player prestiged while the reels were spinning, the post-resolve `award()` would write payout chips into the freshly-zeroed run. Fix: added a `runEpoch` counter incremented on prestige; the spin captures `epoch = runEpoch` at start and the resolve guards `if (epoch !== runEpoch) return`. Also flipped `spinning = false` immediately on prestige so the next spin isn't blocked. Headless syntax check + 9/9 grep checks all pass.
- 2026-05-01: **Audit pass — all 13 of inisso's specs verified, two deviations fixed.** Found via grep: (1) auto-spin was a chained `setTimeout` chain not the `setInterval` loop inisso explicitly asked for; (2) LUCK only tilted symbol weights and was missing from the payout multiplier chain (inisso: "spin logic must follow luck and prestige multipliers"). **Fix 1 — setInterval auto-loop**: replaced the chained `setTimeout(tick, dt)` pattern with a single `setInterval(..., 100)` that checks `performance.now() >= nextSpinAt` every tick and updates `nextSpinAt += effectOf(speed)*1000` after each spin. Single live timer, still adapts to mid-run SPEED upgrades because the interval body re-reads `effectOf(speed)` fresh. Cleared via `clearInterval(autoTimer)` everywhere (prestige reset path, AUTO toggle off). **Fix 2 — luck as payout multiplier**: extended `multiplier()` from `m * star` to `m * luck * star` where `luck = 1 + LUCK_lvl × 0.10` (each LUCK upgrade now adds +10% payout AND tilts symbol weights toward rares — dual purpose). Updated LUCK upgrade description to "+10% payout & better odds" so the dual effect is visible. **Spec checklist** (all green): ✓ single-page (one HTML file), ✓ localStorage save (`setItem(STATE_KEY)`), ✓ localStorage load (`getItem(STATE_KEY)`), ✓ exponential cost scaling (`base × grow^lvl`), ✓ PAYOUT compounds at `1.20^lvl`, ✓ JACKPOT at `1.5^lvl`, ✓ SPEED at `0.92^lvl`, ✓ prestige bonus = `1 + ✦ × 0.5` (inisso's exact formula), ✓ LUCK is a payout multiplier (`1 + lvl × 0.10`), ✓ setInterval auto-loop, ✓ chips currency (`state.chips`), ✓ luck upgrade exists, ✓ speed upgrade exists, ✓ prestige system (REROLL → +stardust → permanent multiplier). Headless verified via grep + JS parse.
- 2026-05-01: Created. **Single-page idle slot machine with chips currency, exponential upgrade scaling, and a prestige system.** Single-file vanilla JS. **Symbols** (8 total): cherry/lemon/bell/star/clover/gem/seven/wild — each with weight + base value. **Spin payout**: counts each symbol across the N reels, payout per symbol = `count² × value`. All-N match (single symbol on every reel) triggers JACKPOT bonus = `N² × value × 5 × jackpotMultiplier`. Final payout × `multiplier()`. **Multiplier scaling** (the chat-requested core progression driver): PAYOUT upgrade compounds at `1.20^lvl` (exponential — L20 ≈ 38×, L40 ≈ 1470×, L60 ≈ 56k×); JACKPOT at `1.5^lvl` (full-match bonus, exponential — L10 ≈ 58×, cap L18). Prestige stardust: per inisso's spec, **`1 + stardust × 0.5`** (linear, +50% per ✦). Total payout multiplier = `1.20^multi × (1 + ✦ × 0.5)`. **Upgrades** (6 total, exponential cost `base × grow^lvl`): PAYOUT (×1.20/lvl, base 25c grow 1.65, cap 60), SPEED (−8% auto interval/lvl, 0.92^lvl down to 0.18s floor, base 50 grow 1.50, cap 25), EXTRA REEL (+1 reel, base 600 grow 6.0, cap 3 → max 6 reels), LUCK (better odds on rares — reduces weight gap of common symbols, base 120 grow 1.55, cap 25), JACKPOT (×1.5/lvl full-match bonus, base 800 grow 1.85, cap 18), AUTO-SPIN (one-time 200c unlock). Buying a reel re-renders the deck. Affordable upgrades highlight in gold; maxed upgrades show MAX badge in green. **Prestige**: target = `max(10000, 10000 × 2.5^stardust)` (exponential, so the second prestige needs 25k chips, third 62.5k, etc). Stardust gain = `floor(log2(runChips / 10000) - stardust)`. ★ REROLL button activates when gain > 0; confirm dialog → wipes chips + all upgrade levels but adds stardust permanently. Pretty progress bar + "+N ✦" preview before reroll. **Auto-spin**: chained setTimeout that reads `effectOf(speed)` fresh each tick, so buying SPEED while AUTO is running adapts immediately. Disabled until AUTO-SPIN upgrade is purchased. **Persistence**: full state via `localStorage['idle-slots-v1']` JSON — chips, runChips, totalChipsEver, stardust, spins, autoOn, all upgrade levels. Saves on every spin + every upgrade buy. **Reels**: each reel is a 24-row strip with the landing symbol placed at index 21; spin animates `transform: translateY(-20 * SLOT_H px)` over 700+220×i ms with cubic-bezier `(.16, .84, .36, 1)` easing — staggered reveal for each reel. Win-glow keyframe pulses gold around reels showing the matched symbol. **HUD**: massive Bungee chips counter top-right with bump animation on increase + live `+N/sec` rate (computed from expected payout × inverse speed × auto-on). Stardust pill in upgrades panel header. Toast for purchases / prestige / errors. **Audio**: Web Audio synth — spin click 360Hz square, win bell 440+v/5 Hz two-note triangle, jackpot 5-note arpeggio (523/659/784/1047/1318), prestige rising 6-note chord, buy two-tone confirmation. **Controls**: SPIN button, AUTO toggle, prestige REROLL, click any upgrade row to buy. Keyboard: `Space` spin, `A` toggle auto. Pollinations OG.

## issues
- Idle progression rate is intentionally moderate at L0 (avg payout ~5c × ~0.4 spins/sec ≈ 2c/sec). PAYOUT L10 already pushes that to ~12c/sec; PAYOUT L20 lifts off into idle-game pacing.
- Wild symbols (🎰) currently count as their own symbol class for matching, not as multi-substitution wilds. Could be reworked to be true wilds for more strategy.
- The `prestigeGain()` formula uses `log2(runChips / 10000) - stardust`, so each successive prestige requires roughly 4× more runChips for the same stardust. If a player overshoots they only get a fraction of "fair" stardust — could be smoothed to log2 without subtraction.
- Floating chip pop is positioned in `reel-deck` coords using `bottom:50% + left:random(40-clientWidth-80)`; on very wide decks this concentrates pops near the center. Cosmetic.
- localStorage save fires on every upgrade buy + spin — fine on desktop, mild battery cost on mobile. Could debounce 250ms.
- No reset/wipe button beyond prestige; users who want a clean slate need devtools `localStorage.clear()`.
- Auto-spin doesn't fire while the tab is hidden (browser throttles setTimeout/setInterval). True idle progress while tabbed-out would need a "catch-up" calc on visibility resume.

## todos
- Catch-up bonus on tab resume (compute spins missed × avg payout, award with a "while you were away…" toast).
- Achievement system (first jackpot, first prestige, 1M chips earned, etc).
- Multiple machines (unlock new themed machines as upgrades; each with own symbol pool).
- Click-to-spin animation showing the lever pull.
- Stats panel (lifetime spins, jackpots hit, biggest single payout).
- Online leaderboard via Supabase (highest stardust, biggest single payout).
- Skill tree alternative to flat upgrades — branching choices instead of pure number-go-up.
- Dark/light theme toggle for the casino.
