# Jinix Jupiter — fantasy console + two carts

## log
- 2026-08-28: v1.2 — **spiral waves + crunch** for BELT.CART, per a public request ("Jupiter scrolling asteroid shooter with spiral waves, edge wrapping, crunchy explosions, Z fire, and Space smart bomb — validate against the documented runtime"; v1.1 already had everything but the spirals and the crunch). SPIRAL WAVES: `B.spiralT` counts down (240f after launch, then `max(240, 480−20·sector)` after each wave); `spawnSpiral()` drops a storm eye at y=−6 (`B.spiral = {cx, cy, vx, vy:.3, omega ±(0.024+0.0015·sector), inward, total, broken, arms}`) and spawns `min(12, 4+sector)` rocks (40% large) carrying `r.sp={theta,rad}`; arms = 1/1/2/2/3…; odd-numbered waves spiral IN (rad 66−6k → 9), even ones OUT (rad 3+2.5k → 64). `updateSpiral()` moves formation rocks on polar coords around the descending eye every frame (they collide + can be shot the whole time), and `releaseRock()` flings each one along the arm tangent at `0.45+0.04·sector` with `vy ≥ 0.2` (rocks only despawn off the BOTTOM — a released rock flying up would otherwise leave forever). The field spawner rests while a formation is riding in. Shooting every formation rock before it unwinds = **SPIRAL BROKEN +100·sector** (`B.msgS/msgT` banner under the sector strip, `spiral` chime). Eye drawn as 6 rotating red/orange dots. CRUNCH: `crunch(x,y,size,cols,stop)` = 2/4/7 chunky 2×2 debris squares (`B.chunks`, fade 6→3→2), an expanding pset ring (`B.rings`, max r 7/13/22, greys past 60%), shake 1/3/6 and **hit-stop** `B.stop` 1/2/4 frames for SHOT rocks only — `beltUpdate` returns early while `B.stop>0` (B.t frozen, shake still decays) and remembers Z/Space taps made during the freeze (`fireReq/bombReq`). Bomb kills and ship death add NO hit-stop (the smart-bomb + respawn probes pin their timings); ship death instead paints one red frame (`B.flash=1, flashC=14`), shake 8 and ship-coloured debris. Synth: new `noise()` = band-passed white-noise burst; `rock`/`rock_s`/`bomb`/`hit` gained a noise layer, new `rock_l` (large-rock crunch) and `spiral` (rising trill) voices — `SFX_NAMES` + the cart's `sounds` list updated or the validator would flag the cart BAD. Boot line 2 `SPIRAL WAVES · HIT-STOP CRUNCH`, title tag `A SPIRAL-WAVE ASTEROID SHOOTER`. Seam adds `spawnSpiral`, `killRock`. Probes: desktop 82/82 (v1.1's 70 + spiral-forms/rides/releases/outward/broken/clears, hit-stop-medium/large, stop-buffers-bomb, bomb-no-stop, death-crunch, fx-expire; `spawner` now allows ≤12 rocks and requires ≥1 spiral in the first 600f), mobile 11/11.
- 2026-08-28: v1.1 — **BELT.CART (Belt Runner) + a cartridge system** per a public request ("scrolling asteroid-field shooter cart for Jinix Jupiter with edge wrapping, eight-way movement, Z fire, and Space smart bomb; use the existing documented runtime and validate the cartridge"). RUNTIME: carts are objects `{id, file, name, tag, icon, keys, pad, labels, help, aria, sprites, boot, sounds, insert(), tick(), render(), restart()}` passed to `registerCart()`, which runs `validateCart()` (slug id, `NAME.CART` file, name/tag/boot lines in font chars that fit the screen, all four hooks, keys 0-7 with something on button 4, pad L/R/U/D/A/B, labels ≤6, help pairs, 8×8 hex sprites with NO name collisions across carts, icon names a cart sprite, sounds known to the synth) and only compiles the sprites of valid carts. The BIOS boot screen lists every slot (`JUPITER.CART 20 SPR OK`, a `BAD` line + first error in red for invalid carts) and is followed by a CARTRIDGE SELECT screen (up/down or left/right, Z/A/Enter inserts; bad carts show `BAD CART:` and refuse). Key + pad maps are per cart and swapped on insert: the runtime's SELECT map (arrows/WASD 0-3, Z/X/K/Space 4, Enter 5), Jupiter (Up/Z/Space/X/K = 4 jump, pad U = jump), Belt (arrows/WASD 0-3, Z/X/K = 4 fire, **Space**/C/Shift = 6 bomb, Enter 5). Buttons are logical (`data-p="U|L|R|D|A|B"`) and resolve through `PAD` at press time; the d-pad is now a 4-way cross; A/B `<small>` labels, aria-labels, the slot readout, the keys list and the canvas aria-label are rebuilt by `setChrome(cart)`. New chrome: eject pill + Esc key (→ select screen), `?cart=belt` deep link (`cartFromSearch`), last cart remembered in `jinix-jupiter-cart` (cursor defaults to it). Jupiter's tick/render moved into `JUP.tick/render` unchanged — physics, level, demo bake and completability search still pass. BELT.CART: ship 8×8 (idle / thrust / bank-left, bank-right is the flipped left) with 8-way movement (`SHIP_ACC .26 · FR .86 · MAX 1.7`, diagonals normalised by 0.7071 so the speed cap is a circle), **toroidal wrapping** of ship, bullets (all four edges) and rocks (x only; they scroll off the bottom), Z fire (btnp instant, hold auto-fires every 9f, ≤5 live bullets, 44f lifetime so they wrap top→bottom once), **Space smart bomb** (3 per run, cap 5: 3-frame white flash, shockwave ring +5px/f that destroys rocks as it passes with no splitting, 45f invulnerability, bullets cleared; empty → `empty` blip + status), rocks in three sizes (L 16×16 from four quadrant sprites / M 8×8 two tumble frames / S ~5px; radii 7/4/2.5; 20/50/100 pts; L→2M→2S on bullet hits), pickups from 20% of shot large rocks (72% bomb, 28% extra ship, cap 5 each), sectors every 900f (scroll 0.6→≤1.6, spawn interval 46−4·sector, cap 5+1.5·sector ≤14, side drifters from sector 3), lives 3 (respawn after 60f at bottom centre with 120f blink shield + rocks within 40px cleared), game over panel (score/best/sector/rocks/bombs used, NEW BEST! → `jinix-belt-best`), title with drifting rocks + BEST. Backdrop: 3-layer scrolling starfield + a wobbling Jupiter limb in the bottom 14 rows. Deterministic LCG `brand()` (seedable via `__X.seed`) for reproducible probes. New synth voices: select/bad/shoot/rock/rock_s/bomb/hit/pick/life/wave/over/empty. Build stamp v1.1. Probes: desktop 70/70 (BIOS validation lines, select nav, insert/eject/Esc/pill, Jupiter regression 22 incl. demo bake + completability search 227ms, belt: launch, normalised 8-way, speed cap, wrap on all four edges + rock wrap, fire/hold cap/bullet wrap, L→M→S splits + scores, smart bomb clears 6 rocks for 340 with no bombs → refused, death/respawn/game over/best/fly again, pickups + caps, sector ramp, 600f spawner bounds ≤cap, key map Z/Space/Up/Down, pad U/A/B via pointer events, 8 validator negative cases, deep link, paints 16 colours, 576×384, hit-tests, 44px targets, sound, a11y, no hscroll), mobile 11/11 (375px: 4 pad buttons ≥44px in view, U hit-test, touch up/A/B, no overlap, deck bottom 504px).
- 2026-08-28: v1.0 — built per a public request ("small vanilla JS canvas fantasy-console prototype inspired by Jinix Jupiter, keyboard controls, pixel-art visuals, one playable level"). Interpreted "Jinix Jupiter" as the console name + cart name, since no such product is documented. CONSOLE (JX-192): 192×128 software framebuffer (`Uint8Array` palette indices → `Uint32Array` ABGR → 192×128 offscreen canvas → nearest-neighbour `drawImage` onto a dpr-sized display canvas; integer CSS scale ≥2 on desktop, fractional fill on phones), 16-colour Jupiter palette, API `cls/pset/rectfill/spr/print/printc/camera/btn/btnp/sfx`, sprites as 8 hex-digit rows ('.' transparent), 3×5 bitmap font (A-Z 0-9 .:!-/,?>×♥·'()), tiny Web Audio synth (jump/gem/stomp/death/flag/win/start/boot), sound off by default (persisted `jinix-jupiter-sound`). CART (JUPITER): 108×16 ASCII map (`#` ground autotiled top/fill, `-` one-way band w/ surface at tile-top+2, `^` spike, `o` gem ×8, `w` wisp ×5, `F` checkpoint ×2, `c` cloud deco, `P`, `E` pod). Physics per 60Hz frame: ACC .18 / MAX 1.15 / friction .82 / G .22 / JV −3.5 (≈26px rise; 3-tile band climbs need ≥24) / max fall 4, coyote 6f, jump buffer 6f, variable height (release → vy clamped to −1.2). Edge sampling uses `ceil(x+w)-1` (floor(x+w-1) caused sub-pixel wall jitter 66→66.9→66). Wisps patrol 0.45px/f, turn at walls/ledges/spikes, stompable (bounce −2.4), else lethal. Death → 50f fall anim → respawn at last flag, wisps reset, gems kept. Exit → clear panel (time, gems, deaths, PERFECT RUN if 8/8 + 0). Boot screen → title (parallax bands, blinking prompt) → after 12s idle an ATTRACT DEMO plays: `DEMO_RLE` is a machine-found perfect run (885 frames, wisps hidden = "ghost run"); any real button aborts. Chrome: cream handheld shell (Handjet + Chivo Mono), d-pad + A/B as real pointer/keyboard buttons, sound + reset pills, aria-live status line, buildstamp.

## issues
- **The first spiral wave arrives 240f after launch and rides in at the TOP** — any legacy probe that parks the ship near y=0 after that (the wrap-top test) gets it killed and every later assertion in that flow fails. The desktop suite sets `B.spiralT=99999` right after `belt-launch`; new probes that want a spiral call `X.spawnSpiral()` explicitly (it obeys `B.spirals` parity for in/out).
- **Hit-stop freezes `B.t` and every timer** — probes that count frames to a respawn/shockwave must either avoid shot-rock kills in that window or add `B.stopTotal`. Bombs and deaths deliberately add none.
- **Carts must not share sprite names** — `validateCart` rejects a cart whose sprite name already exists in the runtime table (`SPR` is one namespace). Prefix new carts' sprites (`rock_`, `ship`…). The validator is exercised by the desktop probe `validator` (8 negative cases) — keep it passing when the cart contract changes.
- **The belt spawner runs during play tests**: any probe that counts `B.rocks` after several frames must set `B.spawnT=99999` first (it bit `split-large` and `death-respawn` on the first run). `beltStart()` resets `spawnT=30`.
- **Key maps are swapped on insert/eject and `btnState` is cleared** — a key held across a swap is dropped rather than stuck; `blur` also clears. Pad buttons remember which button index they pressed (`held`) so a map swap mid-press releases the right one.
- **Completability is machine-checked** (probe `completable`, Jupiter): a best-first search over the REAL physics (seam `__X.step/press/clearPrev/clearFx`, `noRender`), waypointed through every one-way band + gem in x-order, then the exit. Any Jupiter physics/level change → rerun; the baked demo (`DEMO_RLE`) MUST be regenerated afterwards or `demo-bake` fails (it replays the script and requires clear + 8/8 + 0 deaths).
- Headless: run the desktop suite WITHOUT `--virtual-time-budget` — everything is stepped synchronously through `__X.freeze` + `step()`.
- No emoji in the 3×5 font; HUD uses sprites/glyphs only. Font chars: A-Z 0-9 . : ! - / , ? > × ♥ · ' ( ) space — cart names/tags/boot lines are validated against this set.

## todos
- Belt: a boss rock every 5 sectors, a second weapon (spread) pickup, gamepad API (poll in loop → btnState — would work for both carts through `PAD`).
- Chat ideas: leaderboard of clear times / belt scores (Supabase table), palette swaps ("night Jupiter"), a moon-hop low-gravity variant, sound on by default once the gesture question is settled.
- A third cart is now ~200 lines: sprites + a `{keys,pad,labels,help,aria,boot}` header + four hooks; the BIOS validates it and the select screen lists it automatically (three rows fit at 38px pitch; a fourth needs scrolling).
