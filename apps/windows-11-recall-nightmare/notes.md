# Windows 11 · Recall is Recording — notes

## log
- 2026-09-13: STAY-A-WHILE.EXE — reversible satire mini-game overlay per three chat requests (opening quote as harmless overlay; the quote verbatim + "Stay a while" repeated mechanically + points for correct tasks; visible top-left score counter, preserve routes). One self-contained block (style + markup + IIFE, saw- namespaced, outer try/catch) appended before the FINAL </body>, following this app's own existing self-contained-block convention. Diff proof: 144 insertions, ZERO deletions — ?bsod= routes and the 5 referencing apps (bluescreen-roulette redirect, showcase, inventory, sloppy-shell) untouched by construction. REVERT = delete the one block.
- Game: 🎮 chip (top-right) opens a Win11-style dialog; the quote types out VERBATIM ("You think you can work while your machine runs on windows operating system"), then "Stay a while." stacks on a fixed 900ms clock (mechanical, capped, reduced-motion instant); START TASKS = 45s shift of satirical popups where the CORRECT answer is always the resistant one and the machine deliberately styles the WRONG buttons shiny; +10 with streak bonuses (cap +50), −5 wrong (floor 0), score counter fixed TOP-LEFT of the window, aria-live. Esc/✕ close and restore the desktop exactly.
- Context: chat first asked to convert this app into a SondeHub game — declined the in-place conversion (5 apps + the BLUESCRN.EXE convention reference it); the SondeHub game shipped separately as rs41-rain, and the three follow-ups defined this overlay instead.
- 2026-09-26: refresh pass (helper session), three commits, no features removed, no storage keys touched:
  - FOUND why the page was never headless-probeable: the renderer CRASHED during parse (Inspector.targetCrashed at ~0.5 s). Bisected over the 1549 top-level rules of the main <style>: the `.sat-window` rule alone crashes it — 35 closed windows sat at opacity 0 but still painted, each with backdrop-filter blur(60px). Fix: closed `.sat-window` is `visibility:hidden` (delayed 0.28 s so the close fade still plays; `.open` flips it back instantly). Also removes hidden windows' close buttons/iframes from the Tab order. Every opener uses the `.open` class, so nothing else changed.
  - Pixl Pal "playing in background · RESTORE" chip showed on EVERY visit: its inline `display:flex` beat the `hidden` attribute. Added `#pixlpal-minibar[hidden]{display:none!important}`; the restore/kill flow still works.
  - Phones / small screens: zones ran off the right edge (only 3 of 6 zones reachable at 390 px, also at 800×600). `arrange()` now switches to a STACKED layout (zones top to bottom, row-major, icon layer scrolls; `.dz-stacked` + a `.dz-spacer` for taskbar clearance) only when the side-by-side grid can't fit even at the 72 px minimum, so every layout that fit before is byte-for-byte the same path. Swipes that start on an icon scroll the layer (pointercancel, no click); bare-wallpaper taps still open the Bixon diagnostic. Taskbar (~925 px) is a swipeable strip ≤960 px (Start was cut off). Closed Bing sidebar translateX(calc(-100% - 24px)) on ≤720 px (it left an 18 px sliver). ≤560 px: rec pill left-aligned so the STAY-A-WHILE chip no longer covers it.
  - Favicon was U+1F5DF (not an emoji, emojicdn has no image) → 👁️.
  - Tested live via CDP (see verification note below): phone 390×844 24/24 (also against the live file), desktop 1280×800 22/23 (the one miss is the headless-only diary label overlap; with the REAL sibling apps loaded in two iframes the headless desktop renderer stalls, so desktop window flows were checked against 404 iframes), tablet 768×1024 and 800×600 pass the layout checks; all 16 script blocks pass node --check.
- VERIFICATION (+, 2026-09-26, replaces the caveat below): the page IS probeable now, but not with --dump-dom/--virtual-time-budget. Launch headless_shell with --remote-debugging-port and drive it over CDP (a small Python websocket client in the helper scratch, cdp.py + t_func.py): navigate, evaluate, click, touch events, screenshots. Blackhole DNS (`--host-resolver-rules="MAP * ~NOTFOUND, EXCLUDE 127.0.0.1"`) so fonts/7tv don't stall. KNOWN FLAKE: at 1280 px the renderer sometimes crashes/hangs ~16 s after load when the boot overlay reaches the privacy screen (also seen with the fix reverted, not reproducible in isolation, likely software-compositor load); suppress with `runBootSequence=function(){}` at 2 s when testing other flows. The diary icon label overlaps Sosumi.wav in headless only because Caveat (Google Fonts) can't load there.
- VERIFICATION CAVEAT (+, important for future sessions): this page has NEVER been headless-dumpable — dump-dom produces nothing even with --timeout and --virtual-time-budget on the UNMODIFIED file (renderer never settles; pre-existing, cause unknown). Verified instead: (a) module probe 17/17 ×2 widths against the VERBATIM block bytes extracted from the live file, running in a shim host (quote verbatim, mechanical repetition, top-left score geometry, +10/streak/−5/floor, close/Esc restore, 44px + dialog aria); (b) append-only git diff (144+/0−); (c) node --check on the extracted script. Any future edit to this app must reuse the shim-probe pattern.

## issues
- (fixed 2026-09-26) Headless-unprobeable page: it was a renderer crash from 35 hidden blur(60px) windows, see log. Live CDP probing works now.
- Several other closed panels (copilot, recall window, start menu, search, bing) still keep backdrop-filter while off-screen/transparent. If performance complaints come in, give them the same visibility:hidden treatment.
- og:image is a pollinations URL (not a .png file). Left alone: a headless screenshot shows tofu boxes for the emoji icons.

## todos
- Icon drag on touch never worked (the browser takes the pan and fires pointercancel). Fine now that the stacked layer scrolls; a long-press-to-drag would be the way if chat asks.
