# SondeQuest — notes

## log
- v1.0 (2026-09-11): Built fresh per chat ("safe radiosonde tracking and recovery game, referencing radiosondy.info without scraping or copying it, include claimed-flight markers") after the read-only check confirmed nothing existed. radiosondy.info is credited BY NAME on the intro card ("named with respect, not linked, not scraped") per the named-not-linked rule; zero anchors on the page, probe-asserted.
- Safety is structural (glimwalk pattern): app scripts contain no location APIs, no camera APIs, no fetch/XHR/WebSocket — the probe scans script text with split needles. LESSON (+, again): the engine's own honesty COMMENT contained the API word and tripped the scan — a promise-comment must never spell the needle its enforcement greps for (sister of the apply.py guard-keying lesson). Comment now says so explicitly.
- Engine (pure `<script id="eng">`, seeded, dual-export): value-noise terrain (field/forest/lake + sinuous roads; SPEED road>field>forest>lake), altitude-layered wind (veers + strengthens aloft), flight lifecycle asc→burst→desc→down with per-step world clamping, predictLanding integrating the remaining flight through the same wind model (exact under deterministic wind; uncertainty radius shrinks on descent — the gameplay ellipse), claim rules (only downed, first claim wins), two named fictional rivals that retarget nearest predicted landing, drive terrain-aware, linger 2.5s, then claim. Real sonde model names (RS41-SGP/DFM-09/M20) + 400–406MHz band used factually; callsigns fictional S46xxxxx.
- CLAIMED-FLIGHT MARKERS (the specific ask): on-map flag planted at the sonde (amber = yours, gray = rival, claimer name beside it) + tracker rows flip to "CLAIMED · name" (amber when you). Recover: within 30u of a downed sonde → RECOVER button/E → your claim, logbook persisted (sondequest-log-v1).
- UI: B612/B612 Mono (the aviation typeface — thematic), topo-paper map vs dark telemetry panel, focus-prediction toggle per flight, signal meter + beeper that quickens with proximity (default OFF; toggle click is the WebAudio gesture), touch pad for phones.
- SCREENSHOT PASS caught a real design bug: winds were ~5× too strong for the 2400×1600 map — flights drifted to pos −3774,−3139 (off-world) making chases impossible. Fix: windSpd 5.5–9, launch site near map centre, per-step clamps in flight + predictor. Also ⌖/⚑/⛏ tofu in B612 Mono → plain text. (Balance lesson: scale wind drift to WORLD SIZE, target total drift ≈ ⅓ of the map.)
- Verified: engine 21/21 node (determinism, terrain mix + speed order, wind layers, lifecycle, predictor exactness + shrinking uncertainty, claim precedence, rival chase-and-claim, road-vs-forest movement, 3-active cap, no NaN at 50 sim-minutes); browser 22/22 ×3 widths (honesty note + named reference, zero anchors, split-needle API scan, tracker rows, focus toggle, recover→CLAIMED·you + amber flag pixels on the map, rival claim row + tally, no re-claim, terrain driving via live step, lazy AudioContext on beeper toggle, a11y, overflow); screenshots.

## issues
- One python edit batch aborted on an indent-mismatched needle and silently left the file untouched while a stale suite run looked green — re-ran the WHOLE batch after fixing the needle; always re-run the engine suite after the write, not before.
- Two "Vector-Karl" labels can appear at once (rival dot + his planted flag) — honest but slightly noisy; could suffix the flag label with "(claim)".

## todos
- Wind forecast panel (tomorrow's drift preview); day/night; a rare ozone-sonde high flight worth double.
