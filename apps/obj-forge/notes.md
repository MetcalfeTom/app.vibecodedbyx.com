# obj-forge · notes

## log
- 2026-05-18: v1.6 — **proper toggle buttons for boolean params + silhouette-only alpha mask for PNG sprites**. Two stacked chat asks: (1) "check if we can flip the heightmap logic or add an Invert button to OBJ Forge", (2) "fix the OBJ Forge to ignore transparent pixels in PNGs so it only extrudes the silhouette".
  - **Toggle buttons** · The `Invert` param was technically there as a 0..1 step-1 range slider that showed "on/off" text — easy to miss. Boolean params (any param with min=0, max=1, step=1) are now rendered as iOS-style toggle buttons with an on-state badge: orange border + filled orange track + dot slides right + light orange tint on the button. `aria-pressed` toggles for screen readers. Single click flips state and triggers a debounced rebuild. Other range sliders stay as-is.
  - **Silhouette-only mode** · New `alphaMask` param (toggle) added to the heightmap shape. When ON, the heightmap builder captures per-vertex alpha into a parallel array, then during triangle generation skips both triangles of any quad whose four corners all have alpha < 0.5. Quads on the silhouette edge (mixed alpha) survive so anti-aliased PNG borders stay crisp. Drop a sprite with a transparent background and the mesh now extrudes only the visible silhouette, with transparent regions becoming proper holes in the geometry. Verified with sim: 8×8 grid with a circular alpha mask kept 25 quads (the disc) and skipped 24 (the corners).
  - **Diagnostics** · console.info logs `[obj-forge] silhouette mask · skipped N / M quads` when the toggle is on, so chat can see how much got cut.
  - **Unused vertices** · skipped quads leave their vertices in the position buffer (no faces reference them) — wastes a bit of OBJ/FBX bytes but keeps the index logic dead simple. Could remap to compact later if needed.
- 2026-05-18: v1.5 — **ASCII FBX exporter for Blender + Unreal**. Chat: "can we add a basic FBX exporter to OBJ Forge for easier importing into Unreal and Blender?". Yes — hand-rolled an ASCII FBX 7.4 writer rather than pulling Three.js examples/exporters. ASCII FBX is the simpler half of the format (the binary half is proprietary and nasty); both Blender (File → Import → FBX) and Unreal (Content Browser → Import) accept it directly.
  - **Layout strategy** · convert geometry to non-indexed via `geo.toNonIndexed()` so every triangle carries its own position/normal/UV. Lets us emit normals + UVs as `MappingInformationType: "ByPolygonVertex"` + `ReferenceInformationType: "Direct"` — the simplest valid form with no IndexToDirect lookup tables.
  - **PolygonVertexIndex encoding** · each triangle written as `[a, b, ~c]` where `~c` is JS bitwise NOT (= `-(c+1)`). FBX uses negative-last-index as the polygon-end marker — verified with single-tri test: vertices `[0,1,2]` → `0,1,-3`.
  - **Structure** · FBXHeaderExtension (with timestamp + Creator) → GlobalSettings (UpAxis=1=Y-up matching Three.js, Blender/Unreal will rotate on import) → Definitions (counts) → Objects (Geometry + Model blocks with Vertices, PolygonVertexIndex, LayerElementNormal, LayerElementUV, Layer) → Connections (`C: "OO", geoId, modelId` + `C: "OO", modelId, 0` to attach to RootNode).
  - **`fbxPushArray(lines, indent, key, arr, isFloat)` helper** chunks 12 values per line with `,\n<indent>` continuation — produces readable + spec-compliant `Key: *N { a: ... }` blocks.
  - **UI** · panel export button is now a 2-button row (.obj orange primary + .fbx cyan alt). Viewport floating button is the same pair — both bottom-right of viewport. Keyboard: `e` = .obj, `f` = .fbx. `data-format` attribute on each button + single delegated listener so adding more formats later is one element + one entry.
  - **Bumped file 52→61 KB.**
- 2026-05-18: v1.4 — **floating export button + demo heightmap placeholder + OBJ writer UV support + camera retarget for heightmap + stronger default amp**. Three stacked chat asks: (1) "add an OBJ export button to the forge, and ensure the heightmap extrusion is actually visible", (2) "check the OBJ export logic and increase the default heightmap intensity, it should not be flat".
  - **Floating export button** · Big orange `↓ .obj` button now floats at the bottom-right of the viewport with a 20px shadow, always in view. Same `doExport` handler as the existing panel button. HUD moved bottom-right → bottom-left to make room. Fixes the mobile case where the panel scrolls off the bottom and chat thinks there's no export.
  - **Demo heightmap placeholder** · Picking the Heightmap shape before uploading used to render a flat PlaneGeometry → looked like the extrusion was broken. New `buildDemoHeightmap(p)` generates a radial-ripple surface (`cos(r*4)*exp(-r*0.55) + sin(x*1.8)*cos(z*1.8)*0.18`, normalised to [0,1]) using the live amp/size/invert params, with proper UVs + computed normals. Verified: with defaults, height spread is 0.17→1.98 over a 3×3 footprint — clearly visible relief instead of a flat sheet.
  - **OBJ writer UV support** · `meshToOBJ` now emits `vt u v` lines when geometry has a `uv` attribute and uses the correct `f a/ua/na b/ub/nb c/uc/nc` face format. New `faceRef(idx, hasUV, hasN)` helper picks the right per-vertex reference shape (`a`, `a/u`, `a//n`, or `a/u/n`). Added vertex/triangle counts to the header comment too. Audit confirmed previous writer was correct for `f a//na` (normals-only) format but silently dropping UVs that Three.js was generating for sphere/box/plane/cylinder/extrude/tube primitives.
  - **Real heightmap now writes UVs** · the image-based builder also generates a proper u,v ∈ [0,1] grid, so exported OBJ files can be textured in Blender with the original image directly.
  - **Default amp 1.2 → 2.0, max 3.0 → 4.0** · gentler defaults made meaningful peaks look almost flat from the default camera angle. New defaults give 67% aspect ratio (height vs size) which reads clearly as terrain.
  - **Camera retarget on heightmap select** · `selectShape('heightmap')` now sets `controls.target` to `(0, amp/2, 0)` instead of the origin, so the orbit centre is at the mesh centroid and the camera actually frames the extruded surface. Other shape selections reset target to origin.
- 2026-05-18: v1.3 — **heightmap extrusion convention fix**. Chat: "heightmap looks inverted or zero-offset, making models look like holes". Root cause: the v1.2 formula `(lum - 0.5) * amp * 2 * inv` mapped black to **-amp** and white to **+amp** (centred range [-amp, +amp]) — so any image that's mostly dark or mid-grey sat below the y=0 baseline, and from the default camera angle looked like a crater/hole in the ground plane. New formula `(invert ? (1 - lum) : lum) * amp` follows the standard displacement-map convention used by Blender's displace modifier, Unity's terrain, etc: black = ground (y=0), white = peak (y=amp), mesh always sits on or above the baseline. Verified by sim: lum=0→h=0, lum=0.5→h=0.6, lum=1→h=1.2. Invert toggle now flips the bright/dark interpretation cleanly while keeping the mesh above ground (so a "mountain" heightmap becomes an upside-down range of basins still sitting on the plane, not a mirror plunge below it). Default amplitude bumped 0.8→1.2 to compensate for the halved vertical range (old spread was 2×amp around zero, new spread is 1×amp upward) — visual impact stays similar. Max amp 2.5→3.0. Dropzone hint now reads "black = ground · white = peak" so the convention is documented in the UI.
- 2026-05-18: v1.2 — **heightmap pipeline rewrite for big files + broader format support + alpha handling + diagnostics**. Three chat asks converged on the same code path: (1) handle larger files + common formats; (2) debug pixel-processing stalls on certain formats; (3) PNG upload failing for deadbydaddyttv — likely the FileReader→dataURL stage choking. Replaced the whole loader.
  - **Fast decoder** · `decodeImage(file)` races `createImageBitmap(file, { imageOrientation: 'from-image' })` against a 15s timeout, with two graceful fallbacks: plain `createImageBitmap(file)` if the orientation option throws (older Safari), then a Blob-URL `<img>` element if even that fails. Avoids the FileReader→base64→`img.src=` round-trip entirely — base64 inflates a 25MB PNG to ~33MB and forces a second decode, which was the most likely cause of the deadbydaddyttv failure.
  - **Pre-downsample cache** · After decode, the source is downsampled once to a 1024-edge max canvas (`state.heightmapSource`) with a hard `MAX_INTERMEDIATE = 8192` ceiling (below the Chrome 16384, Firefox 11180, Safari 4096-or-16384 canvas-size limits). Every subsequent slider rebuild draws from this small cached canvas instead of re-sampling the original — so a 6000×4000 source still rebuilds in milliseconds when you drag the resolution slider.
  - **Defensive `getImageData`** · Both the downsample step and the per-rebuild sampling wrap `getImageData(0,0,1,1)` in try/catch with a placeholder-plane fallback + toast on tainted-canvas errors. `willReadFrequently: true` hint on both contexts so Chrome optimises for repeated CPU pixel reads.
  - **Format support** · accept attribute now includes png/jpeg/webp/gif/bmp/avif/svg+xml + image/*. Explicit upfront rejection for HEIC/HEIF/TIFF/RAW/PSD via `KNOWN_BAD_EXT` + `KNOWN_BAD_MIME` regexes with a helpful "re-save as PNG or JPG" toast — these would silently fail in `createImageBitmap` on Chrome/FF anyway.
  - **Alpha-aware luminance** · pixel conversion now composites RGB over black using the alpha channel (`r * (a/255)` then standard ITU-R BT.601 weights), so transparent PNG pixels read as low elevation instead of arbitrary subject-RGB.
  - **Size cap bumped** · 12 MB → 32 MB.
  - **Diagnostics** · 12 `console.info`/`console.warn` checkpoints across the load path so future bug reports show exactly which stage failed (file metadata, decoder choice, dimensions, downsample, taint check, thumb). Visible in DevTools console.
  - **Clear button now closes ImageBitmap** · explicitly calls `.close()` on the bitmap reference before nulling, releasing GPU memory immediately instead of waiting for GC.
  - Dropzone metadata line shows `OOOxOOO → CCCxCCC cached` when downsampled, just `OOOxOOO` otherwise. Bumped ~40→~48KB.
- 2026-05-18: v1.1 — **clear-image button + hardened drag-drop**. Per chat ask. Image dropzone now wraps in `.dz-wrap` so the loaded state shows a `× clear image` ghost-button beneath the label (its own click stops propagation so it can't accidentally re-open the file picker through the label). Drag handling rewritten with a depth counter (dragenter/leave increment/decrement, only remove the `drag-pulse` highlight at depth 0) — fixes flicker when the cursor crossed the thumbnail img inside the label. New `pickFirstImage(dataTransfer)` walks `files` then `items` and prefers the first image-typed entry, so multi-file drops or weird drag sources still find the image. `isImageFile()` accepts `image/*` mime OR extension match (png/jpe?g/webp/gif/bmp/tiff). 12MB cap + min 4×4 dimension check with toast on failure. File-input gets `value = ''` after change so re-picking the same file fires the listener. Window-level dragover/drop preventDefault (skipping targets inside `.dropzone`) stops accidental drops outside the zone from navigating the tab to the file. Dropzone meta line now also shows file size. Added `.drag-pulse` keyframe for visible drag feedback. Bumped file ~37→~40KB.
- 2026-05-18: v1 — **procedural 3D mesh generator with .obj export + image-to-heightmap**. Single file ~37KB, three.js r128 + OrbitControls from unpkg, inline OBJ writer.
  - **9 shape builders**: Crystal (jagged icosahedron) · Gear (extruded toothed shape with hole) · Rock (fbm-displaced sphere) · Helix (TubeGeometry on CatmullRom helix) · Twist (twisted+tapered box) · Terrain (fbm-displaced plane) · Shroom (capped half-sphere + cylinder via mergeGeometries) · Pod (superellipsoid via parametric sweep with `sgn(x)*|x|^(2/n)` exponent) · **Heightmap** (image upload → canvas-sampled luminance → vertex grid).
  - **Per-shape params** as range sliders (3-4 each) that live-rebuild on input with 30ms debounce. Each shape has its own param state dict so switching back preserves your tweaks.
  - **Heightmap dropzone**: file input + drag-drop, FileReader → Image → drawn to offscreen canvas at chosen resolution (16–256), luminance `(0.299R + 0.587G + 0.114B)/255` becomes Y, X/Z spread across `size` units. Invert toggle as a 0/1 slider. Preserves loaded image in state; shows file thumbnail in the dropzone once loaded. Without image, renders a flat placeholder plane.
  - **Seeded value noise** (`hash3` → trilinear-smooth-lerped `noise3` → 4-5 octave `fbm`) drives rock + terrain so reseed button gives new variants.
  - **OBJ writer**: emits `o name`, vertices (`v x y z`), normals (`vn x y z`), faces (`f a//na b//nb c//nc` with normals if present, else `f a b c`). Handles both indexed and non-indexed BufferGeometry. Drops trailing zeros via `parseFloat(n.toFixed(6))`. Filename = `<shape>-<ISO-timestamp>.obj`.
  - **Scene**: dark navy radial bg, orange key light + cyan fill + faint white rim, 20-unit grid floor at y=-1.5, axis helper. MeshStandardMaterial flatShading=true so faceted shapes read crisp. Wireframe overlay (toggle) adds cyan LineSegments child on the mesh.
  - **Controls**: OrbitControls (damping 0.08), auto-rotate toggle (Y axis 0.005/frame). Keyboard shortcuts: `e` export, `w` wireframe, `r` reset camera, `Space` toggle auto-rotate.
  - **HUD** shows live `verts · tris · ~KB` (file-size estimate `verts*50 + tris*30` bytes). Toast for export/reseed/heightmap-load events.
  - **Aesthetic**: CAD blueprint vibe — JetBrains Mono everywhere, Major Mono Display for the `obj·forge` title, dark navy bg with faint cyan 48px grid lines, orange (#ff7a3d) action accent + cyan (#5ee3ff) data accent. Brutalist sliders with monospace value readouts. 3-col shape picker grid.
  - **WCAG**: semantic main/aside/header/h1-2, role="radiogroup" on shape grid + aria-checked, role="application"+aria-label on canvas, role="status"+aria-live on toast, focus-visible outline 2px cyan, prefers-reduced-motion kills transitions, ≥2.75rem buttons. 760px breakpoint reflows to viewport-on-top + panel-below.
  - **OG image** via Pollinations flux (no `referrer` param per project notes).

## issues
- Image upload stays in memory — bigger images at 256-res take ~50ms to rebuild on each slider drag (acceptable but noticeable).
- Heightmap normals from `computeVertexNormals()` look soft on grayscale gradients; consider edge-aware shading later.
- No texture coordinates emitted in OBJ; Blender will fall back to default UV. Add `vt` + `f a/t/n` triplets later if anyone wants textured exports.
- Smooth-shading flag (vs the current flat) would be nice as a toggle.
- File-size estimate is rough — actual OBJ tends to be smaller after decimal-stripping.

## todos
- Material/MTL companion file with the current color/roughness baked in.
- Cube · Sphere · Torus · Capsule as basic primitives in a separate "basics" row.
- Multi-mesh export (place several shapes, export combined .obj with `o` groups).
- Decimation slider (mesh simplification before export).
- glTF export alongside OBJ.
- Per-vertex color export for terrain (height-based gradient → vertex colors in OBJ extension).
- Save/load preset combos (shape + params) to localStorage.
- Drag-rotate handle inside the viewport so touch users can orbit without OrbitControls touch quirks.
