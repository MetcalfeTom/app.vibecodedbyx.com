# stockroom · notes

## log
- 2026-07-12: v1.3 (chat: user management — admin add/edit/delete dummy-email users, per-user profile settings page, role assignment on the panel). **Users moved into DB** (persisted, `USERS` rebound to `DB.users` on load/reset; pre-v1.3 saves migrate to defaults + field patches). **👥 User Management** (admin): table w/ avatar/name/email/username, live role select + branch select per row, edit modal (name/email/username/password), delete w/ confirm, + user modal (role+branch at creation). **Guards** (all in pure fns addUser/updateUser/setRole/deleteUser): username regex + uniqueness, email format, pw ≥4, can't delete self, can't delete/demote last admin, admins get branch=null, non-admins can only update THEMSELVES (profile path), role changes re-branch sensibly. **New users can genuinely log in at the gate** and route by role (tested: created cashier lands on POS; promoted to manager → lands dash w/ requests page). **⚙ My Profile** (every role): display name, dummy email, password change (old pw invalidated — tested), avatar radio (8 emoji), landing-page preference (validated against role's pages, overrides HOME on login; falls back if invalid), compact-tables toggle (body.compact CSS, applied on login + save). Sidebar whoami shows avatar. Node 25/25 incl. permission matrix both directions, full CRUD lifecycle, pref-home override + isolation between users, migration.
- 2026-07-12: v1.2 (chat: "on Selkom" — no such app exists, clearly meant Stockroom — restock request page for cashiers + HQ approval page for admins). **DB**: `requests[]` {id,at,pid,name,branch,qty,note,by,status pending|approved|denied,decidedBy,decidedAt,reason}; seed adds 2 pending + 1 approved; `load()` migrates pre-v1.2 saves (requests=[]). **📮 Restock page** (cashier+manager, own branch): low-stock quick chips (click → preselects product), product select w/ live branch stock, qty+note, guards (role gate, qty≥1, one pending per product+branch), branch request table w/ status badges + HQ reason column. **✅ HQ Approvals** (admin only): pending queue (oldest first) showing branch/current-stock-with-low-badge/requester/note, approve = stock added to that branch + toast, deny = modal reason (shown back to branch); decision history (last 30). Pending-count badge on the nav item (updates on create/decide). PERM: `request` (manager+cashier) / `approve` (admin) capabilities; admin deliberately can't file requests. Node 17/17 (one test failure was my own ordering assumption — deny targeted the exact id, remaining pending was seed2 not seed1): seed shape, nav gating, create/dup/qty guards, stock untouched until approval, +18 on approve, deny keeps stock + carries reason, double-decide rejected, role rejections both directions, badge count, old-save migration.
- 2026-07-12: v1.1 (chat: login gate — username/password fields routing to role dashboards + logout button; truncated "logout but…" read as button). Landing form (username/password, submit on Enter, wrong creds → red error + box shake, reduced-motion safe). Demo credentials shown ON the cards (ada/stock123, mia/harbor123, cash/till123 — client-side auth can't be secret, honesty > fake lock); clicking a card FILLS the form (no longer logs in directly), then sign-in routes by role via `HOME` map: admin→dash (full control), manager→dash (branch-locked view), cashier→pos. `authenticate()` trims + lowercases username, password case-sensitive. Session persists in `stockroom-session` (resume on refresh, validated against USERS), sidebar button renamed "log out" → clears session + resets form + focuses username. Node 12/12: cred matrix (valid/case-tolerance/wrong/unknown/empty/pw-case), per-role landing pages, persistence write, logout clear, fresh-boot resume-as-manager sim.
- 2026-07-12: v1 (chat ask: inventory management site, 3 roles admin/manager/cashier, branch system, sales charts, clean UI, no CodeIgniter, vanilla frontend + "whatever backend"). **Backend decision**: our Supabase RLS is own-rows-only for writes (see root notes) → a genuinely shared multi-user inventory can't be enforced server-side. Chose honest architecture: fully client-side demo suite, localStorage (`stockroom-v1`), seeded deterministic data (mulberry 4242: 3 branches, 18 products × per-branch stock, ~90 sales across 14 days), reset button reseeds. **Roles**: login screen = 3 demo user cards (Ada admin / Mia manager·Harbor / Cash cashier·Harbor); `PERM` matrix gates nav pages + capabilities (admin: all 6 pages + delete + all-branches selector; manager: dash/inv/pos/sales locked to own branch, restock+edit but no delete; cashier: inv read-only, POS, own receipts only). Switch role anytime. **Pages**: Dashboard (4 stat tiles incl. low-stock warn, 14-day revenue line chart + revenue-by-branch bars — both hand-drawn canvas, DPR-aware, no chart libs), Inventory (search + category filter, per-branch or summed stock, low badges, add/edit/restock/delete modals by permission), New Sale (POS product grid w/ live remaining count, cart with per-line remove, oversell blocked at add AND revalidated at checkout, stock decremented + sale recorded), Sales Log (sorted, cashier sees own only), Branches (admin CRUD; new branch zero-stocks all products), Users (admin; live manager/cashier branch reassignment). Toasts, Esc-closing modals, sticky cart. Node 22/22: seed shape, permission matrix (per role), aggregate math (day buckets vs history, branch revenue sums exactly, top-products sort), POS flow (2-stock: third add blocked, checkout zeroes stock + records, empty-cart rejected), branch-scoped stockOf + low-stock count. Sora + IBM Plex Mono, cream/teal SaaS light theme (deliberate contrast to the week's dark games).

## issues
- Client-side roles are demo-grade by design — stated on the login screen. If chat wants true multi-user, needs DB-side role enforcement we can't create with current tools (would need RPC/views).
- Sales log caps at latest 80 rows for render speed.

## todos
- CSV export of inventory + sales; date-range picker for charts.
- Category donut; per-product sales sparkline in inventory rows.
- Barcode-style quick-add (type code → cart) for cashier keyboard flow.
